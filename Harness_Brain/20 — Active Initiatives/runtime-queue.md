---
type: queue
program: mission-control
status: open
tags: [queue, program/mission-control, runtime]
---

# Runtime — the hermes half of the Mission Control queue

Mission Control is one surface across two repos. **This file holds the rows whose fix lives in this repository**; the launcher's half is `EterniaLauncher/Launcher_Brain/20 — Active Initiatives/mission-control-queue.md`, and a finding that needs both sides is filed on the side that must move first, with the other side named in the row. Ruling: [[0012 — The hermes half of Mission Control is queued here, split by ownership]] (amends [[0011 — One brain per repo, one Mission Control queue in the launcher]]).

Rows are one line and a pointer; claim with `**TAKEN <date> <who>**` before starting; the landing commit deletes the row; a ` · VERDICT <date>:` suffix is an owner decision, not lane work. Repo-relative paths are this repo's; another repo's file is cited with its prefix in backticks (`EterniaLauncher/…`).

**The three headings are the [[Fork Boundary Map]]'s three kinds of file**, and they decide what a lane may do:

| heading | what it means | what a lane does |
|---|---|---|
| Fork-owned | the file is absent from `upstream/main` (`agent_runtime/`, `hermes_cli/harness*`, fork-only modules and tests) | refactor freely; never conflicts on merge |
| Seams | an upstream file the fork already edits (`gateway/hosted_rooms.py`, `agent/credential_pool.py`, `hermes_cli/auth.py`, `hermes_cli/codex_models.py`, …) | ADDITIVE edits only, one import and one call at a stable point; every sync conflicts here |
| Upstream-owned | an upstream file the fork does not touch | never edited here: a caller-side memo or seam on our side, a marker, or an upstream issue/PR |

Rows below were moved verbatim from the launcher queue on 2026-09-22 (their provenance headings are kept as sub-headings). Fork-hygiene findings the launcher queue carried (CI, the suite, the mutation gate, upstream reds) went to [[fork-hygiene-queue]] in the same commit.

## Fork-owned



### Filed on arrival — 2026-09-24 (lane LLAMA-H)

- [ ] **The launcher still speaks the fork's local-llama contract: the persona provider rename to upstream's `llamacpp` and the pinned-tag setup wizard are a launcher re-vendor** · `fork / local llama` · cross-repo, hermes moves first: accept `llamacpp` as a persona provider (set-model, then the turn through `agent_runtime/local_llama_adapter/provider.py::turn_scope`'s lease), then `EterniaLauncher/lib/features/mission_control/` switches the model-menu id (`kHermesLocalLlamaProviderId`), the visibility `provider_id` and the QA keys, and its wizard drops `destination_parent` and asset sizes (`installation.plan` now names upstream's `runtimes_root()` install dir, `download_bytes` is 0) · evidence: the module docstrings of `agent_runtime/local_llama_adapter/provider.py` and `setup.py` · filed by lane LLAMA-H 2026-09-24 **UNCLAIMED**
- [ ] **GAP-PR-1 (`local_runtime.executable_path`) and GAP-PR-2 (`local_runtime.model_dirs`) are held while upstream PRs are paused; the adapter carries both, and the row-9 knob preset, as recorded parallels** · `fork / upstream` · when PRs resume, file each as a generic upstream PR, then delete `Engine.start`'s executable path and `engine.scan` for upstream's; the knob preset retires only on a per-model override upstream has refused in principle · evidence: `docs/agent-runtime-harness/planned/upstream-footprint-ledger.md` § Fork modules that shadow an upstream symbol · filed by lane LLAMA-H 2026-09-24 **UNCLAIMED**

### Filed on arrival — 2026-09-24 (lane MOVE-A)

- [ ] **The harness config keys (`remote_gateway.*`, `charsheet.*`) are top-level keys nobody declares; the ruled home is the plugin manifest `config_schema` (`plugins.entries.eternia-harness.settings.*`)** · `fork / config` · cross-repo, hermes moves first: readers `hermes_cli/harness_parts/serve.py::gateway_listen_config` + `agent/charsheet/pipeline.py::provider_timeout_seconds` switch to the plugin settings path with a one-chokepoint delete-and-report of the old keys, then the launcher's `gateway_listen_policy.dart` / `gateway_port_owner.dart` write the new path · evidence: ledger row `hermes_cli/config_defaults.py` · filed by lane MOVE-A 2026-09-24 **TAKEN 2026-09-24 DOORS-A** · VERDICT 2026-09-24 (lane DOORS-A): DESIGN — hermes-first delete-and-report switches off every live LAN listener: the launcher writes `remote_gateway.listen` through `hermes config set` on each toggle and respawns serve (`serve.py` R-L1 note), so the chokepoint would delete the value the launcher just wrote until the launcher row lands. Owner call: land both halves in one coordinated wave (hermes readers + chokepoint behind the launcher's new-path writer), or accept the off-window; settings names proposed `remote_gateway_listen`, `remote_gateway_port`, `charsheet_provider_timeout_seconds` under `plugins.entries.eternia-harness.settings`

### Filed on arrival — 2026-09-24 (seam lane S2)

### Filed on arrival — 2026-09-24 (lanes UPREV, DISP-M)

### Filed on arrival — 2026-09-24 (seam lane S5)


### Filed on arrival — 2026-09-24 (seam lane S4)

### Owner asks — 2026-09-22, filed on arrival


- [ ] **`test_serve_rpc_office_subscribe_live_hub` waits 5 s with no `@pytest.mark.timeout`, so it flakes under a full-suite batch and passes alone** · `fork / suite` · a wait bound over 30 s must declare the marker and this one is under it, so the marker is not the fix — the fix is a wait that observes the hub rather than sleeping toward it. Seen once in lane HM's 221-file touched run, green when run alone · filed by lane HM at its landing 2026-09-22 **UNCLAIMED** **TAKEN 2026-09-24 FORK-CODE** · VERDICT 2026-09-24 (lane FORK-CODE): the tree already has the row's fix — every wait in the file is `_wait_for`, a 10 ms poll on a hub CONDITION (`_boot_hydrate_delivered` observes produced + delivered frames) under one 5 s `_DEADLINE_SECONDS`; no sleep-toward-it exists, and the file is unchanged since 51ee01d954 (09-15), before the row. What stays open is a load question the row does not carry evidence for: WHICH case timed out under the 221-file batch and on what condition. Owner call: raise `_DEADLINE_SECONDS` (4 × 7 s still fits the 30 s cap) or require the failing case id + `what=` string before any change. File alone: 25 passed, slowest call 1.59 s. · EVIDENCE 2026-09-24 (lane H4): `::test_a_real_office_write_reaches_a_subscriber_as_an_office_patch` fails only in combined runs and passes alone, identically at H4's MOVE commit where the code is byte-identical to base — a leak through the process-global `OFFICE_SUBSCRIPTIONS`, not load; the failing case the row lacked.
- [ ] **`runtime.level.get` is `console`, so a `read`-tier device cannot load the environment its own office draws** · `fork / runtime` · the level family landed with all three verbs at `TIER_CONSOLE` (plan §3's header, implemented as ruled). The two writes are level mutations and the one-line tier rule decides them; the READ follows `runtime.media.get` instead — it hands back up to 1 MB of raw document bytes and the `read` tier is open to `unknown`, a caller the transport authenticated and could not place. The cost is that a paired VIEWER device renders an office with no environment under it, silently, because `SceneLoadNotFound` and "scope denied" are not the same answer and the adapter has to tell them apart. Decide: keep `console` and have Stage L's adapter surface the refusal as a distinct state, or split the read to `read` and accept the egress. · `agent_runtime/serve_rpc.py::_runtime_level_get`, `tests/agent_runtime/test_serve_rpc_method_tiers.py` · filed by lane H at its landing **UNCLAIMED** **TAKEN 2026-09-24 FORK-CODE** · VERDICT 2026-09-24 (lane FORK-CODE): tree still as filed (`serve_rpc.py` level get/set/clear and `runtime.media.get` all `TIER_CONSOLE`). The row is an owner decision between two named options, not a code defect; recommendation: keep `console` (same egress posture as `runtime.media.get`, whose 1 MB-of-bytes argument it inherits) and file the distinct scope-denied state on Stage L's adapter in the launcher queue — widening a read open to `unknown` is the irreversible one.

### Contract questions the launcher is waiting on (filed 2026-09-20/21, routed by the 2026-09-22 verdict sheet)

- **The same wire fact decodes to different values on different lanes — exit codes, and int tolerance generally** · `launcher / mission control` · a tool's `exit_code` of `-1` is ABSENT on the bridge's `tool.finished` lane (`missionBridgeNullableInt`, pinned with a stated reason) and `-1` on the persona-chat trace (`_nullableExitCode`, unpinned); `from_offset`/`cap_bytes` refuse a numeric `String` in `remote/` (I3d) while `status_code`/`turn_seq` accept one in `data/` (I3); `code:` in the visibility model ROUNDS a `num` where every other reader truncates. None of this is a copy defect — each is a per-file policy with no wire contract behind it, which is the "parallel resolvers answering the same question differently" shape. C1 preserves every site's behaviour and names the variants; deciding which tolerance the harness wire actually promises is a contract question for the hermes side · `EterniaLauncher/docs/mission_control/planned/wave3/wire-decode-family.md` §2.2 · VERDICT 2026-09-22: DESIGN, and the row says why: which int tolerance the harness wire PROMISES is a contract question for the hermes side. No launcher lane can pick the answer, and picking one silently would mint exactly the parallel authority the row is complaining about. · the launcher queue carried this twice (its 2026-09-21 landing section and this one); one row here **UNCLAIMED**
- **Two resolvers answer "did `harness realm adopt` succeed?" differently in one file** · `launcher` · `_adoptRealmDefinition` refuses on `exitCode != 0 || payload == null || payload['ok'] == false`; `setUpGrant` refuses on `exitCode != 0 || (payload?['ok'] == false)`, so a null envelope is a FAILURE in one and a SUCCESS in the other for the same CLI verb. Z3 preserves both deliberately; unifying them is a behaviour CHANGE needing its own review and producer-side receipt evidence · `EterniaLauncher/lib/features/mission_control/sync/realm_sync_service.dart`, Z3 note §5, §8.3 · VERDICT 2026-09-22: DESIGN as the row itself states - unifying a null envelope from FAILURE to SUCCESS (or back) is a behaviour change on a CLI verb, and which reading is right is a producer-side question for hermes, not a launcher decision. Confirmed live on the tree, now across TWO libraries: `_adoptRealmDefinition` is in `sync/realm_sync_default_workspace_commands.dart` and `setUpGrant` in `sync/realm_sync_service.dart`, so the row's "in one file" is stale and the two readings are further apart than when it was filed. **UNCLAIMED**

- **`MissionAgentLogStatus.fromWire`'s `whenAbsent` divergence is a live wire-contract question for hermes** · `launcher / mission control` · after B28 one decoder reads eleven harness words for both doors, but an ABSENT value still reads `idle` on a persona-instance row and `unknown` on a snapshot log row. The fold made the divergence a named parameter rather than picking; whether hermes intends the two to differ is unanswered · `EterniaLauncher/lib/features/mission_control/data/snapshot/mission_snapshot_status_and_envelope.dart` · moved from the launcher queue 2026-09-22 (wave 8 sheet) **UNCLAIMED**

### Owner design changes — 2026-09-18 (the generic-desk landing, hermes `f1268bd017`)

- **Five serve-manifest tests are red on hermes `main` (`297d218297`)** · `hermes` · `test_serve_rpc_office.py` ×3, `test_serve_rpc_office_upsert.py` ×2 — hardcoded method lists never gained the `runtime.discussion.*` family from the 09-17 Discussion Tables landing; reproduced on the clean tip · evidence: hermes `f1268bd017` commit message, "PRE-EXISTING RED". **UNCLAIMED**
- **A dropped legacy desk leaves its hermes actor file behind** · `hermes` · the launcher deletes a persona-addressed desk at its load chokepoint and reports the count (owner ruling 5, 2026-09-18), but the drop is a READ: `before` and `after` are both already clean, so `officeVacatedActorKeys` measures nothing and no `runtime.office.remove` is sent. A desk that shared an agent's actor is dropped from that file on the next write to it; a desk that was the only item under its address is never rewritten. The census that would have reported it was deleted in hermes `f1268bd017`. Decide: a one-shot sweep, or leave it. **UNCLAIMED**
- **`OfficeItem.minted_kind` is stamped on every upsert and, since hermes `f1268bd017`, has NO reader** · `hermes` · its only consumer was the retired desk-litter census; it is persisted provenance outside the content hash and the wire, so removing it is a stored-data decision · `docs/agent-runtime-harness/07-observability.md`. **UNCLAIMED**
- **A generic desk write still pays ONE `scan_actors`** · `hermes` · a desk actor names no `persona_instance_id`, so it is a CLASS-KEYED payload and `_guard_class_keyed_write` scans the directory; the retired fence took two scans to one, not to zero — the generic-desk plan's cost claim was half right. Decide with launcher slice 2 whether desk writes should leave the class-key fence · `test_office_store.py::test_a_generic_desk_write_pays_the_CLASS_KEY_scan_and_only_that_one`. **UNCLAIMED**

### Discussion Tables (hermes half) — from the 2026-09-17 reviews of `codex/discussion-tables-*`

- **Discussion Tables (hermes half): a retention pin leaks on a crash between `_retain` and `disband_room`** · `hermes` · `agent_runtime/discussions/service.py:316-318`; a pin on a still-live room is reclaimable by no lane (age/tombstone exempt pins, the byte lane selects `disbanded_at IS NOT NULL`) and `unpin_room_history` has no production caller that could clear it. Strictly narrower than the per-run leak the fix retired. Reviewer plant B, 2026-09-17 review of `codex/discussion-tables-fix` @ `20b148694e` (verdict LAND). **UNCLAIMED**
- **Discussion Tables (hermes half): D3 closes the typed-refusal lane by subclass, leaving three families on the raw-text boundary** · `hermes` · `agent_runtime/discussions/rpc.py:90-105`: `DiscussionPolicyError` (the base), the `DriverStateError` family (`gateway/hosted_room_driver.py:104-111`) and checkpoint `RuntimeError`s (`gateway/hosted_room_policy_checkpoint.py:183,291,298`) still reach `serve_rpc` as `handler_failed` with `f\"handler error: {exc}\"`. Latent — not live-reachable today. Same review @ `20b148694e`. **UNCLAIMED**
- **Discussion Tables (hermes half): `run.end` still refuses on a non-`RoomNotFound` disband failure** (e.g. `AuthorityConflictError`) — decide whether a terminal run may release its claims with the room undisbanded · `hermes` · `agent_runtime/discussions/service.py::_finalize_room`; residual named by the 2026-09-17 D2 fix lane on `codex/discussion-tables-fix` @ `20b148694e`, deliberately not widened there. **UNCLAIMED**
- **Discussion Tables (hermes half): `roster` reloads the full runtime config per instance inside a loop on a polled `read` method** · `hermes` · `agent_runtime/discussions/native.py:80-112`; hoist the load. Same review @ `f4abe884a9`. **UNCLAIMED**

### The ghost agent — filed 2026-09-11 from the office removal-lane fix

- **The snapshot's `unplaced-instance` parity warning is OWED, and the two ways to feed it were both measured too expensive** · `hermes` · the reconcile lane classifies and prunes it (`classify_unplaced_persona_instances`, phase 2b), but `_parity_warnings` cannot: the evidence is "an ARCHIVED office actor names this instance", and the snapshot's `offices` section carries LIVE actors only. Measured on the live store 2026-09-11: **82 archived actor files / 59 KB across 2 workspaces against 4 live actors**, with no retention policy, so carrying the ids on the wire is an unbounded list on every snapshot and decoding them in `_offices_summary` is a 21× increase in files read on the snapshot build path, both growing with every removal forever. Needs a ruling on which cost to pay (or a retention policy first — the row below). Until then an operator learns about a ghost by running `harness persona-instance reconcile --dry-run`, never from the launcher. · `agent_runtime/snapshot.py` `_parity_warnings` / `_offices_summary`; the classifier and its evidence gatherer are in `agent_runtime/persona_instance_identity.py`. **UNCLAIMED**
- **The office archive directory has no retention, and that is what makes the row above expensive** · `hermes` · 82 files / 59 KB after roughly six weeks on a store with 4 live actors; every `remove_actor` adds one and nothing ever removes one. `persona_instances_archive/` has a 20-entry retention (`.ended.json` rows do too); this directory does not. It is cheap while nobody reads it and it is exactly what any new reader has to walk. · `agent_runtime/office_store.py` `_archive_actor_locked` / `paths.office_archive_dir`. **UNCLAIMED**
- **`agent.create` refuses an unmarked placement id; the office write lane accepts one, and that pair is how the ghost was minted** · `hermes` · `perform_agent_create` answers `placement_id_not_discriminable` ("supply the `<persona-token>_agent_<hex8>` shape"), but `PersonaInstanceStore.add_instance` takes any token and `OfficeStore.upsert_actor` will place the result, so `persona instance create` + a scene save produces an instance-backed actor with no marker — `personainst_chara_a2_7b31d0e4`, the 09-11 ghost. Either the second route should mint marked ids or the create door's refusal is protecting a discriminator nothing else relies on any more. Needs a ruling, not a patch; the launcher no longer depends on the marker for its archive lane. · `agent_runtime/agent_create.py`, `agent_runtime/persona_assignments.py` `add_instance`; the pin that the refusal still exists is `test_the_placement_verb_really_does_refuse_this_id_shape` in `tests/agent_runtime/test_persona_instance_unplaced.py`. **UNCLAIMED**

### Runtime observability — filed 2026-09-06 from the post-rebuild field run

- **The Mac did not answer `runtime.persona.instance.open_chat` inside the budget three times while its snapshot stream flowed, and its Neko instance reads `status=running` with no visible activity, which is exactly the cockpit's occupied-chat dialog condition** · `hermes` (Mac read) + `launcher` · 2026-09-07 16:26:51 / 16:27:06 / 16:27:39Z `open_chat lane=method install=912c69ce… outcome=unknown reason=call_timeout retry=same_key`; `fold_applied persona_instance x1` from the Mac 5 s and 24 s after the first two. Needs the Mac's `agent.log` for 16:26:40–16:27:40Z and the instance row's `status`; the launcher's open-chat receipt should name WHICH budget expired (the connector's default or the lane client's 30 s). Unclaimed; `restart-drain-fence.md` §3 names it. **UNCLAIMED**
- **The local chat prep cost is now a measured baseline** · `hermes` · `turn_context_ms` 620 / 829 / 1,233 / 4,730 with `builds_overlapped` 1 / 0 / 2 / 3 on four idle local turns (2026-09-06 15:29–15:30Z), the provider itself 0.9–1.5 s — `planned/chat-turn-prep-cost.md` (hermes canon) is re-armable as a stage against these numbers. Unclaimed; after O-h so RO-7 measures the result. · **PLAN RE-ARMED 2026-09-07 hermes `c670168049` (Fable, read-only against the live store): the slow half is hermes’ pre-admit span (`request_received → write_ahead`) — 906 ms uncontended, 2,796–3,172 ms under a led build or a chat-open prewarm (12 of 15 turns); ≥ 60 % of the floor is three skill-directory walkers that repeat each other; the provider leg reads the same on both machines; the launcher’s own share is 102–137 ms. Rulings CP-1..CP-9, Stages 6–10 (instrument first, then the admitted-turn deferral, one skill walk per turn, the observability row post-turn, the resolve memo refreshed off the turn). Plan `docs/agent-runtime-harness/planned/chat-turn-prep-cost.md`. **BUILD CLAIMED 2026-09-07 (Fable, this session): Stage 6 (the instrument, both halves) dispatched to an Opus builder — hermes worktree `prep-stage6` (under the hermes worktrees root), launcher worktree `_worktrees/prep-stage6`; Stages 7/8/10 after the Stage 6 operator read per CP-9.** · **STAGE 6 LANDED 2026-09-07: hermes `a319021acc` (+ ledger `7bad0aee54`), launcher `a4b8e390c` — the `timing` block carries `write_ahead_ms` and five siblings, `profile_timing` carries the seven pre-admit sub-spans, a bundle rebuild names its moved component, the admitted-turn counter records `prewarm_overlapped`/`admitted_at_exit`; the launcher line appends `rt_write_ahead_ms=` and `rt_bundle_builds=`; both byte-pinned fixture families match with no byte regenerated. OWED = one agent-chat turn per machine read off the line and the record (CP-9), then Stages 7/8/10.** · **CP-9 PC READ TAKEN + STAGE 7 LANDED 2026-09-08 (Opus builder, dispatched by Fable's prep-cost implementation brief).** The operator rebuilt the launcher and restarted; serve pid 33460 came up on hermes `68b8361de0` (Stage 6 in, Stage 7 out) and **nine Windows agent-chat turns** carried all seven pre-admit sub-spans, `visibility_bundle_rebuild_component_registry_epoch` on 9 of 9, and `rt_write_ahead_ms=` on the launcher line — the PC half of CP-9 is CLOSED. Numbers: `write_ahead` p50 **844 ms**, uncontended p50 **516** (438/593), contended p50 891, against §0.1's pre-Stage-6 906 uncontended and 2,796–3,172 contended — but NO build storm was present (one bundle build per turn), so that gain is NOT Stage 7's and is not claimed as such. CP-1's 300 ms is NOT met; Stage 7's acceptance ratio reads **1.64×** against its 1.30× target, which is the pre-Stage-7 baseline it must move. **Skill work is 74–89 % of `write_ahead`, median 88.2 %** (`observability_skill_rows_ms` p50 374, range 157–547, against a 30 ms target) — **Stage 8 is now billed on live operator turns, not the §0.3 sandbox profile.** **STAGE 7 LANDED hermes `483bcf6fab`** (ff-only; the admitted-turn deferral and both prewarm yields read `chat_turns_admitted() or agent_runs_in_flight()`, bound 1,000 → 3,500 ms; five behavioural reds pre-fix, 53 passed after, four mutations each red a named test, gate list 111 passed, affected seam 443 passed) and **hermes main is PUSHED** `42a07c5dfa..483bcf6fab`, so the Mac can now pull Stages 6 and 7. **Mac half of CP-9 still OWED**: its three method-lane turns to install `912c69ce…` carried `rt_write_ahead_ms=-`, which proves its runtime predates Stage 6 (and proves the launcher's three-absences rule fires correctly against a genuinely older remote runtime). **OWED = the Mac pulls and re-reads; Stage 7's ten-turn number; and `EterniaLauncher/test/fixtures/hermes_serve_frames/ready.json` re-captured against landed hermes main, because `build.code_tree` hashes `agent_runtime` and Stage 7 moved it.** Stage 8 (CP-4/CP-5, one skill walk per turn) is IN PROGRESS on a hermes worktree `prep-stage8` (under the worktrees root), branch `codex/prep-cost-stage8-one-walk`.** · no branch of that name exists on 2026-09-22 — re-verify Stage 8's state before claiming

### Snapshot build latency on Windows — filed 2026-09-08

- **The live serve's `agents_readiness` walk costs 5-10x the same walk in a quiet process, and nothing explains the difference** · `hermes` · `snapshot_agents_readiness walk_ms=` reads 651-1595 ms across 664 samples in the operator's `agent.log`, against 126-155 ms for the identical five-persona walk measured read-only against the same live store from an isolated process; the 2026-08-22 note in the walk itself records 146 ms and matches the quiet figure, so the algorithm is not what moved — the serve process is · `docs/agent-runtime-harness/planned/windows-path-syscall-cost-2026-09-08.md` "What the measurement did NOT explain" item 1 · found while profiling the build for the memo landed at hermes `6d79b6d724`. **UNCLAIMED**
- **The core cache writes on every build and has never once been served on this install** · `hermes` · 2,094 `snapshot_core_cache_write` lines against ZERO `core_source=cache` serves in one `agent.log`; the same code DOES serve on a quiescent copy of the same store (226-241 ms against a 1.0-2.2 s cold build), so convergence is the whole blocker — and "just stop writing" is dead as a remedy because disabling the write-back entirely bought 4 ms · `docs/agent-runtime-harness/planned/core-cache-input-closure.md` 2026-09-08 addendum, which is the field half of its own IC-4 "measure first" gate · the per-family reader audit that plan demands is still owed. **UNCLAIMED**
- **`venv_integrity_issues` re-enumerates site-packages once per persona per build** · `hermes` · 243 `os.stat` calls answering a question about the INTERPRETER'S venv, which cannot change between two builds two seconds apart and is identical for all five personas (`site_package_dirs()` reads `sysconfig`, process-global, not profile-scoped); measured at 7 ms after the root memo landed, so this is filed on its structure — a per-persona loop around a process-global fact — and explicitly NOT as a perf row · `hermes_cli/venv_integrity.py` `_metadata_dirs_by_distribution` reached from `hermes_cli/runtime_environment.py` `runtime_environment_status`, same note item 4. **UNCLAIMED**
- **`sections_top=` prints three of the build's seven sections, and every remedy plan that reads attribution off `agent.log` is reading a truncated list** · `hermes` · this is how `prompt_observability` — which ties `agents_readiness` for the top slot on this install (180.8 ms vs 170.2 ms warm) — went unnamed by the whole boot-window programme; the full `sections_ms` map exists and the receipt shows a third of it · `agent_runtime/snapshot.py` `_log_snapshot_build_core` / `build_receipt_facts`, same note item 5. **UNCLAIMED**

- **Full `PersonaAssignmentStore` retirement is gated on a residual-row census that came back NONZERO (81, unchanged at the 2026-09-04 re-census)** · `hermes` · blocked on an OPERATOR settle of the 15 non-terminal rows, not on implementation; the launcher half is closed (§AX2 / Z1, `EterniaLauncher` `bf33487b2`). Routed out of the launcher Mission Control queue 2026-09-22 (wave 8, lane docs-2) per `EterniaLauncher` `9de7b2bfd`. **UNCLAIMED**

### Harness CLI and the doctor — 2026-08-30 / 2026-09-02 hand-backs, 2026-09-12 skill sync

- **`root_config_misplacement` has a cure and a denominator; the 9-vs-2 comparison needs re-reading** · `hermes` · **OPEN, operator** — the finding half landed 2026-09-04 (w12/m5, hermes `42d54af562`): `remediation` (present even when `ok`) and `scope` (`profiles_examined` / `root_only_key_patterns`) from the rows' own walk; doctor `schema_version` 9. A row is (profile × concrete key) and two of four patterns are per-persona, so 9 vs 2 across two machines compares inventories. What remains is operator-only: re-read the two stores against `scope` and fix the genuinely inert rows by hand — there is deliberately no automated repair, because rewriting an operator's `config.yaml` is not a write the doctor takes.
- **Nineteen `--json` harness verbs reach the root-observability gate only through a helper, and none of them states its root** · `hermes` · found 2026-09-06 (w18/hf, hermes `f940e99215`) — `test_harness_json_root_observability._handlers()` counted DIRECT `emit_json` calls only until w18/hf taught it to follow a handler's local `_emit_*` seam (three handlers; `_cmd_usage` was routed through `attach_root_observability` in the same commit). Following EVERY local callee instead surfaces THIRTY handlers, of which nineteen are neither classified nor attaching: `_cmd_characters_add_state`, `_cmd_characters_approve_direction`, `_cmd_characters_backfill_home`, `_cmd_characters_base`, `_cmd_characters_compose`, `_cmd_characters_list`, `_cmd_characters_migrate_home`, `_cmd_characters_payload_contract`, `_cmd_characters_reopen`, `_cmd_characters_reroll_direction`, `_cmd_characters_reroll_row`, `_cmd_characters_rows`, `_cmd_characters_sprite`, `_cmd_characters_start`, `_cmd_characters_status`, `_cmd_characters_thumb`, `_cmd_characters_turnaround`, `_cmd_mission_chat_message`, `_cmd_persona_instance_archive`. Each is a verb whose empty answer cannot be told from a wrong-root answer, the 2026-08-12 defect class the gate exists for. The fix is per verb: route the payload through `attach_root_observability`, or write a reason that is actually true for it; `_BACKLOG_REASON` forbids adding them to the existing list. Widen `_handlers()` to the full local call graph in the SAME change that empties the list, so the gate cannot go blind again. Agent work. **UNCLAIMED**
- **No CLI-level test pins the `realm sync resolve --key skill::<slug>` envelope the launcher's held-skill buttons call — only the python seam (`skill_sync.resolve_held_skill`) and the argparse dump are covered, nothing drives `main()` end-to-end** · `hermes` · handed over by the hermes lane 2026-09-12; evidence `docs/agent-runtime-harness/planned/skill-three-way-sync-mutation-record-2026-09-12.md`, contract §4.6 of `EterniaLauncher/docs/mission_control/planned/held-skill-publish-direction.md` · `tests/hermes_cli/` **UNCLAIMED**

- [ ] **`agent_runtime` reaches UP into the CLI namespace: lazy `from hermes_cli import harness` in `agent_runtime/discussions/native.py`, `dispatch_delivery.py`, `peer_directory.py`, `persona_open_chat.py`** · `fork / runtime` · runtime → CLI is an upward import that W0-G6 cannot see until those modules declare `__layer__`; the god-file lanes R1–R4 declare layers and invert the dependency (the CLI registers what the runtime needs) · evidence: lane W0 2026-09-24, `docs/agent-runtime-harness/planned/downstream-god-file-refactor-reach-census.md` **UNCLAIMED**


- [ ] **H2 sheet leftovers** · `fork / refactor` · `runtime_commands` → `runtime/{work,verify}` + `WorkCancelOutcome`; `PromotionSourceKind` probes; realm `_SELECTION_MODES`; `_cmd_skills_promote` "refused" constant (R4, the last W0-G5 harness row); `_pet_sheet_revision` fold (C1); `_usage_iso`/`_parse_usage_iso` → `clock` (R3); `dispatch_store`/`running_work` `_pid_exists` → doors (R1/R2) · evidence: `docs/agent-runtime-harness/planned/god-file-layout-sheets/harness.md` §1–§3, lane H2 2026-09-24 **UNCLAIMED**

### Filed on arrival — 2026-09-25 (lane R4)

- [ ] **`test_hermetic_env_blanking[HERMES_HEAD_HOME]` is red on main: the witness no longer finds the reader in `profile_home.py`** · `fork / test env` · red in lane R4's touched-test run on `09d722bb30` against files the lane never touched, so either the reader moved out of `agent_runtime/profile_home.py` in a Wave 1/2 MOVE and the witness still greps the old module, or the variable lost its reader — find which and re-point or delete the parameter · evidence: lane R4 report (holding file, 2026-09-25) · filed by lane R4 2026-09-25 **UNCLAIMED**


## Seams — fork edits inside upstream files (additive only)

### Filed on arrival — 2026-09-24 (seam lane S2)

- [ ] **Nine Stage 2 rows the ledger said the surface "has" wait on five widening PRs nobody owns, and none is on §Stage 3's P-list** · `seams / upstream` · `register_command` with gateway context + `busy_policy` (`/queue-status`, 4 rows); a terminal command-guard hook consulted by `_check_all_guards` with a typed result (the envelope gate); a per-call usage ledger in the turn result (`usage_ledger`, 3 rows); a tool-schema transform hook (wire briefs); a register-toolset PR (bundle membership, `harness_core`). Each needs a P-number, an owner and a fallback in `docs/agent-runtime-harness/planned/harness-plugin-and-upstream-seams.md` §Stage 3 · evidence: `docs/agent-runtime-harness/planned/upstream-footprint-ledger.md` rows marked "DESIGN (lane S2)" / "waits on" · filed by lane S2 2026-09-24 **UNCLAIMED**
- [ ] **The fork's `_load_tools` edit breaks upstream's own test: `_emit_tool_defs_receipt(agent.status_callback, …)` reads an attribute upstream's `_load_tools` never needed** · `seams / agent` · `tests/agent/test_system_prompt.py::test_kanban_guidance_requires_worker_task_at_agent_init` (both params) is red on main (base `37ca422c25`) with `AttributeError: … 'status_callback'` at `agent/agent_init.py:1078`; a seam edit must tolerate the agent shapes upstream's tests build (`getattr(agent, "status_callback", None)`) · evidence: lane S2 base run · filed by lane S2 2026-09-24 **UNCLAIMED**

### Filed on arrival — 2026-09-24 (disposition wave, lane DISP-C)


### Filed on arrival — 2026-09-23 (seam lane S45, Stage 4/5/6 inventories)

- [ ] **177 of the 252 upstream test files the fork edits hold in-place edits, not fork tests. Stage 5's MOVE lanes cannot retire them, and its `[up-fp] files −242` target is not reachable (MOVE ceiling 28)** · `seams / tests` · these need a disposition pass (a batched portability/hermeticity PR upstream, or carry with a reason) before Stage 5's gate means anything. Evidence: the same note §1.1–§1.3 · filed by lane S45 2026-09-23 **TAKEN 2026-09-23 lane S5 (X:/wt/h-s5)** · VERDICT 2026-09-23 (owner): upstream, separate PRs per fix class; never carried

### Standing

- **Discussion Tables (hermes half): pin exempts rooms from the gateway byte budget while its own comment says it does not, and no test touches any new gateway symbol** (`pin_room_history`, `max_active_events`, `DiscussionLimits`, `request_reconciliation`, `stop_unresolved`) · `hermes` · `gateway/hosted_rooms.py:556-577`; degrades stock Group Chat retention, not only discussions. Same review @ `f4abe884a9`. · `d33dff5bbd` covered the three symbols; the exemption-versus-comment question is still open **UNCLAIMED**
- **A `manual:device_code` Codex pool entry can never be rescued by a re-auth, so a stale exhausted mark only ever clears on the TTL** · `hermes` · `CredentialPool._sync_codex_entry_from_auth_store` is gated on `entry.source == "device_code"` and the field row's source is `manual:device_code`, so fresh tokens sitting on disk never reach it; the naive widening is the #39236 regression, because `_sync_codex_pool_entries` syncs a `manual:device_code` entry ONLY when its access token matched the PREVIOUS singleton (the same source string is also produced by independent-account additions), so the fix needs that legacy-alias test — deliberately NOT bundled into the readiness fix, which is why the false amber was narrowed rather than its cause removed · `agent/credential_pool.py` `_sync_codex_entry_from_auth_store` against `hermes_cli/auth.py` `_sync_codex_pool_entries`, evidence in hermes `3c3a5631c8`. **UNCLAIMED**
- **`resolve_codex_runtime_credentials`' pool fallback defeats the cooldown the pool selection just enforced** · `hermes` · when the singleton is unusable `_pool_codex_access_token` accepts any pool row with an access token whose `last_error_reset_at` is not in the future — it never consults `last_status`/`last_status_at`, the cooldown `_available_entries` used to refuse that very row seconds earlier — so a turn can be served with a credential the pool declared exhausted; the readiness mirror deliberately does not copy this lane and says so at its definition · `hermes_cli/auth.py` `_pool_codex_access_token` against `agent/credential_pool.py` `_available_entries`, evidence in hermes `3c3a5631c8`. **UNCLAIMED**
- **Spark has a Pro usage bucket but Hermes's ChatGPT-account Codex route rejects it** · observed 2026-09-16: the live model catalog omitted Spark, the same account's usage response named the separate `codex_bengalfox` bucket, and a minimal `gpt-5.3-codex-spark` request returned HTTP 400 "not supported when using Codex with a ChatGPT account." The picker now shows it disabled. Investigate a supported runtime route before enabling it · `EterniaLauncher/docs/mission_control/planned/model-picker-account-catalog-contract-20260916.md`, `hermes_cli/codex_models.py`. **UNCLAIMED**


- [ ] **RULED 2026-09-24 (owner): the 16 FIRST-DOOR rows of `docs/agent-runtime-harness/planned/second-doors-2026-09-24.md` are done IN the next upstream merge lane — upstream door adopted, fork second door deleted, hook row retired — with four rulings: (1) lazy installs go through upstream `security.allow_lazy_installs` / `HERMES_LAZY_INSTALL_TARGET`, installs outside a turn are NOT kept; (2) BG "on" means BOTH: notify on by default AND a background agent turn on completion, per-spawn opt-out is upstream's parameter; (3) profile delete adopts upstream's tombstone and the plugin clears persona placements on delete; (4) the `auth` spelling moves to the plugin's `harness` command, launcher argv row filed in `Launcher_Brain/20 — Active Initiatives/mission-control-queue.md`** · `hermes / seams` · order: top-5 of the table first (process_registry notify, claim TTL env, PM watcher → on_kanban_dispatch_tick, /queue-status → pre_gateway_dispatch, dispatch timing → llm_execution), then the rest by lines deleted; NEAR-DOOR rows stay held PRs · evidence: the table, `X:/wt/_holds/seam-s1-green-2026-09-23.md` § DOORS **TAKEN 2026-09-24 DOORS-A** · VERDICT 2026-09-24 (lane DOORS-A): 13 of 16 DONE on `seam/doors-a-2026-09-24` (process_registry, conversation_loop + G1, transports/codex, chat_completion_helpers, config_defaults + G9, skill_commands, turn_api_call, desktop registry, commands, profiles, run_busy, run_startup, slash-commands.md; rulings (1)-(3) applied). LEFT: `hermes_cli/subcommands/auth.py` + `hermes_cli/auth_commands.py` wait on the launcher argv row (ruling (4); the launcher moves first). DESIGN `agent/turn_api_request.py`: the only door that sees the attempt start (`started_at`) is `pre_api_request`, and registering it makes `has_hook` true for every agent's every call, which sanitizes a full copy of the request body (`_api_request_payload_for_hook`) — a per-call whole-prompt cost to keep an 11-line timing hunk. Owner call: drop the request_build / pre_api_hook / request_dump stamps (the table's own advice for `turn_response_check.py`), or keep them until PHASE-PR

- [ ] **Upstream `_apply_request_chain` hands every `llm_request` callback the ORIGINAL request and keeps only the LAST result, so a second callback silently drops the first's rewrite; the eternia-harness plugin therefore composes the wire-brief rewrite and the Codex cache-key rewrite in ONE callback** · `hermes / seams` · widening PR candidate (HELD while PRs are paused): chain the callbacks (each sees the previous result) — a one-line upstream change; until then every fork `llm_request` rewrite must join the single composed callback in `plugins/eternia-harness/` · evidence: lane DOORS-A 2026-09-24 (`seam/doors-a-2026-09-24`, the cache-key commit) **UNCLAIMED**

## Upstream-owned — never edited here

- [ ] **`hermes_state_repair.py::state_db_has_structural_damage` reads an FTS-only stomp as structural damage (recover instead of FTS rebuild)** · `upstream` · on SQLite 3.45.3 `PRAGMA integrity_check` RAISES `vtable constructor failed: messages_fts` and any raise reads as structural; file and test byte-identical to `upstream/main`; the test is an `_up_red` id marker (`tests/_downstream/id_markers.py`, ec714fa150) · PR candidate: a `DatabaseError` naming a `messages_fts*` vtable is FTS-only · moved from fork-hygiene by lane FORK-CODE 2026-09-24 (lane GREEN verdict 2026-09-23)
- [ ] **Native Windows ACP qualification has five baseline failures: resource-link file paths, ping pipe handles and three cwd symlink/lexical cases.** · Reproduced unchanged at `2455c606b4`; exact cases and comparison in `docs/downstream/acp-named-provider-identity-2026-09-24.md`. Review upstream-owned behavior/tests without broadening the named-provider correction. **UNCLAIMED**

- [ ] **Test runs leave `hermes_cli.main gateway run --replace` processes orphaned** · `upstream` · upstream test `tests/hermes_cli/test_update_shim_parent_wait.py` spawns the detached watcher with pid 4; upstream issue #121622, fix PR #121630; closes when that PR lands in a merge · moved from `fork-hygiene-queue.md` 2026-09-24 (owner ruling) **UNCLAIMED**
- [ ] **`hermes_cli/cli_init_mixin.py:347` reads the `except … as e` name inside a deferred closure (`_present_store_warning`), after Python has unbound it** · `upstream` · ruff F821 under the repo config — the one error keeping `ruff check .` red, on `main` at `37ca422c25` and on `upstream/main`; if `render_notification` defers the callback the warning path raises NameError. Upstream PR candidate (bind `err = e` before the closure) · evidence: [[upstream-release-2026-09-24]] § Gates · filed by lane MERGE 2026-09-24 **UNCLAIMED**
- [ ] **`scripts/check_subprocess_stdin.py` fails on Windows over `plugins\security-guidance\patterns.py`, which is example text, not real calls** · `hermes` · its exclusion appears not to match backslash paths; upstream-owned script — PR candidate. Evidence: `up/win-text-encoding` commit body (48a7527049) · filed by lane UPPR 2026-09-24 **UNCLAIMED**
- [ ] **`tests/hermes_state/test_clean_close_residual_poison.py:132` still calls `monkeypatch.undo()` in a `finally`, dropping the autouse HERMES_HOME redirect before `db.close()`** · `hermes` · upstream-owned test; PR candidate alongside `up/monkeypatch-undo-scoped`. Evidence: that branch's commit body (7031a0fdc3) · filed by lane UPPR 2026-09-24 **UNCLAIMED**
- [ ] **hermes advertises no `mcpCapabilities`, so every ACP client's HTTP tool-server offer is refused by default — the registration path it would use already exists.** · `hermes` — `acp_adapter/server.py`'s `_register_session_mcp_servers` already builds `{url, headers}` for `McpServerHttp`/`McpServerSse`, but its `initialize` returns `AgentCapabilities(...)` with the field unset and the ACP schema defaults `http`/`sse` to `False`. Upstream-owned file (it is in `upstream/main`'s tree) — boundary ledger + a `contributor` PR, not a fork edit. Evidence: the status line of `EterniaCompanion/docs/decisions/0153-neutral-host-tool-capability.md`. · moved from the launcher queue 2026-09-22 (wave 8 sheet) **UNCLAIMED**
- **`agent/skill_utils` spends 480 `realpath` + 698 `os.stat` calls per snapshot build** · `hermes` · `_resolved_path` (three call sites under `resolve_skills`) and `is_skill_support_path` (under `_skill_root_registry`); on Windows each `realpath` is an `nt._getfinalpathname` handle open at ~71 us, so this is the second-largest path-syscall term after the one just retired. Upstream-owned file — the fork's leave-upstream-alone rule means the fix is a caller-side memo or a resolver seam, not an edit there · `docs/agent-runtime-harness/planned/windows-path-syscall-cost-2026-09-08.md` item 3. **UNCLAIMED**
- **The provider verdict's join to its failure is a STRING equality on `_summarize_api_error` output** · `hermes` · found 2026-09-11 (launcher lane QUOTA), routed here 2026-09-22 from the launcher's wave-8 cockpit-2 lane — `agent_runtime/profile_runner.py`'s `_ProviderErrorCapture.block_for` hands its typed block over only when the terminal result's `error` string equals the summary it captured, because upstream `agent/conversation_loop.py` collapses the SDK exception into that string and returns a dict. Mutation-proven, but a reword of `_summarize_api_error` upstream makes a live 429 fall back to `outcome_unknown` SILENTLY. Structural answer: the loop carrying the typed error on the failed result — an upstream change, so either an upstreamed patch or a fork-owned result wrapper around the loop's return (a seam, not an edit). The launcher side has nothing to change.
