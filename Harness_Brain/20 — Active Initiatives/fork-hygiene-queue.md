---
type: queue
program: fork-hygiene
status: open
tags: [queue, program/fork-hygiene]
---

# Fork hygiene — open queue

The repository AS A FORK: upstream sync and the boundary, CI, the suite and its gates, the god-file refactor, this vault. Runtime and Mission Control rows do NOT go here — the hermes half is [[runtime-queue]], the launcher half is `EterniaLauncher/Launcher_Brain/20 — Active Initiatives/mission-control-queue.md`. Rows: one line + a pointer, never a restatement; claim with `**TAKEN <date> <who>**` before starting; a landing deletes its row.

## Release validation — 2026-09-23

- [ ] **Upstream `tests/agent/test_prompt_builder.py` calls `os.geteuid()` inside two `skipif` at import, so the module and its `_downstream` sibling cannot be collected on Windows** · `fork / upstream` · same shape the open `up/win-posix-only-apis` PR fixes elsewhere (`os.name != "nt" and os.geteuid() == 0`); add these two sites to that PR · evidence: lane CARRY `.lane-logs/c2_run1.log` (collection error), red on `origin/main` too · filed by lane CARRY 2026-09-24 **UNCLAIMED**
- [ ] **`tests/test_claude_code_credentials_file_gate.py::test_gate_opt_in_requires_a_redirected_home` takes 29.5 s cold against the suite's 30 s `--timeout`** · `fork / suite` · measured on `origin/main` (`6e25c6753c`), 8.5 s warm; the census `read_text`s every `tests/**/test_*.py` before its token pre-filter — pre-filter on a cheaper read or cache the census; never widen the timeout · evidence: lane CARRY `.lane-logs/base_c5_gate.log` · filed by lane CARRY 2026-09-24 **UNCLAIMED**
- [ ] **ruff's F821 per-file-ignores (`tests/**`, `tools/**`, `agent/**`, `gateway/**`) hide merge-introduced NameErrors** · `fork / tooling` · the 2026-09-24 merge shipped a production NameError (`heartbeat` in `tools/terminal_tool.py`, fixed `77cd9c6a6f`) and six test files with dropped imports (`3ea8d47e10`) with the repo ruff gate green; only the test run saw them. Structural answer: narrow the ignores to the exec-assembled part files that need them, or add a merge-lane step `ruff check --isolated --select F821` on the conflicted/merged files diffed against the pre-merge tree · evidence: [[upstream-release-2026-09-24]] · filed by lane MERGE 2026-09-24 **UNCLAIMED**
- [ ] **Six new upstream v0.21.4 tests fail on Windows** · `fork / upstream` · three `test_container_boot.py` cases call POSIX-only `os.chown`; three `test_update_host_obligation.py` cases assume POSIX chmod/path spelling; upstream implementation/test parity verified, marker-only pending upstream fix · evidence: [[upstream-release-2026-09-23]] **UNCLAIMED**
- [ ] **Sidebar cache concurrency and serve socket disconnect tests are flaky** · `fork / suite` · `test_projects_tree_coalesces_concurrent_scans_and_returns_copies` and `test_a_disconnect_unsubscribes_and_does_nothing_else` fail then pass bounded retries on the exact release merge; do not count retry success as stable proof · evidence: [[upstream-release-2026-09-23]] **UNCLAIMED**

## Filed on the move — 2026-09-22 (fork rows the launcher queue had carried since 2026-09-02)

Moved verbatim from `EterniaLauncher/Launcher_Brain/20 — Active Initiatives/mission-control-queue.md`; they are about the repository as a fork, not about the surface. Upstream-owned ones are tagged so: never patched here, a marker or an upstream issue.

- **hermes CI on `main`: the runner crash and the 43 lint errors and the mutation job are fixed; what is left is hb's slices 5–8 and the Issues switch** · `hermes` · **NARROWED 2026-09-06 (w18/ha `57b6964b01`, w18/hc `2b1f6e849c`)** — run 34046863701 (hc's push) is the first with `ruff enforcement (blocking)` and `Changed-line mutation claims` GREEN. **ha, slices 1–4, CLOSED:** all 16 tests across 11 files, one commit per file (`48a690da4c` doc-cite report needs a 4-commit repo under a depth-1 checkout; `188c511bae` + `9765564afa` stale `patch()` targets from `96cfc09a34`; `9463780a78` tool-inventory mutations wrote CRLF; `caffc04c1c` a hand-typed Windows byte count; `43cbb598ef` a real duplicate helper (`realm_sync._ledger_time` == `store._stamp`); `30f1ec6635` a BOM on `install.ps1` from the 2026-07-31 upstream merge; `23a90c6063` `test_env_gap_registry` was a DESIGN fault — a green host was read as "every row rotted"; `cee66b50fd` a witness that had not followed its reader; `d2137867b7` a directory-only git exclude that misses POSIX's symlink, which is also why the worktree GC never reaped on Linux; `ce3dc7f465` `os.path.normcase` on Windows-registry PATH data). Six of the eleven reproduced on Windows and were never platform facts — they live outside the four validated test directories and had simply never been run. **hc, CLOSED except Playwright:** ruff's 43 were all genuine `F821` (38 undeclared names in exec-assembled part-file import headers, 5 a missing `typing.Any`), 0 noqa (`fa0fb62788`, `f29ad6ca6c`, `3af990fca7`); `test_harness_parts_namespace.py` never checked header PRESENCE, ruff is the real gate, said so in `6c629b9122`; the mutation job's exit was right and four registered needles had stopped anchoring — re-derived, each proven KILLED, plus `tests/scripts/test_mutation_claims_still_anchor.py` (`abaace6a87`, `bbc1caafb0`), and a fifth needle went ambiguous on landing over hd and was fixed forward (`2b1f6e849c`). The notifier `notify-main-red` LANDED (`028aceb9c6`) and FIRED on run 34046863701 — and failed on its own step, because **Issues are disabled on `nekwo/hermes-agent`** (register row). **hb, slices 5–8, CLOSED (hermes `962a10d0fb`, 13 commits):** all nine reds and the uncollected `test_tombstone_registry.py` (1,155 tests lost at collection to a `check=True` diff under `fetch-depth: 1`); two were the CI checkout, not the code — `ci.yml` now fetches full history with `filter: blob:none`, and the docket gate had been FALSELY accusing seven backed stages; three were product defects (`_ABS_PATH_RE`'s POSIX branch truncated at the first space, `entrypoint_name` used the host separator, `note_peer_seen` read the clock twice); the eight `stream_frames` goldens were regenerated for a path-tail redaction (`262a165b47`), which the launcher re-vendored 2026-09-06 (w18/lf). **What remains here:** (1) the next main-push run after hb lands must show the eight slices green; (3) the Issues switch, then one red push to prove the notifier end to end; then delete this row. **UNCLAIMED**
- **Three hermes tests the CI runner reports FLAKY, which AGENTS.md calls bugs** · `hermes` · found 2026-09-06 (w18/ha) in run 33969282189's slice logs — `tests/agent_runtime/test_serve_stream_lane_parity.py`, `tests/run_agent/test_run_agent.py::TestExecuteToolCalls::test_sequential_tool_calls_run_without_delay`, `tests/tools/test_browser_eval_supervisor_path.py::TestEvaluateRuntimeDomNodeCrashRetry::test_reference_chain_crash_retries_without_by_value`. All three read as timing-sensitive: convert to controlled time at the mechanism, never a longer deadline; `tests/agent_runtime/test_realm_sync_eol.py` also hit the 30 s per-test cap under contention on this box (36 s alone). Agent work. **UNCLAIMED**
- **`tests/test_coverage_claims_resolve.py` sits on a 180 s wall its own fixture now overruns** · `hermes` · found 2026-09-06 (w18/hd), measured not asserted — the file declares `pytestmark = pytest.mark.timeout(180)` and its comment records `collect_claims()` at 27–31 s over three runs on 2026-09-02; called directly today with nothing else running it took 211.5 s in the primary checkout on unmodified `main` and 279.5 s in a worktree under `X:/wt`, identical corpus census in both (241 md / 4,097 py / 3,089 test / 725 claims). It passed once mid-lane at 176.4 s. The marker was NOT raised. Two questions, one row: did the walk regress (bisect `collect_claims` between 2026-09-02 and today) or did this box's IO, and why is a worktree ~30% slower than the primary (the Defender exclusion covers the primary's drive, not `X:/wt` — check `Get-MpPreference` ELEVATED, it is blind otherwise). Fix the walk or gate the fixture on a cached census; never widen the wall. Agent work. **UNCLAIMED**
- **Desktop E2E on Linux: the sentinel background command is stopped by a terminal-command approval prompt, so no background process ever spawns** · `hermes` (desktop app + E2E harness) · found 2026-09-06 (w18/hg) while closing the "polls the background dot as a turn-is-running signal" row (fixed in hermes `b4c9dfbd56`: `sessionDotState` is right, all three E2E call sites now poll the `Session running` label, plus a unit case pinning working-over-background with its recorded killing mutation). That fix does NOT make the job green. Evidence is the failing run's own page snapshot (`playwright-test-results` artifact of run 34051553815, `error-context.md` of the sidebar-states spec): at the 30 s timeout the sidebar row shows `status "Session running"`, and the tool row shows `Running echo "long bg output" + 6 commands` with 27 s elapsed on a `background=true` call plus a `Run` / `More approval options` / `Reject` approval bar. A background terminal call returns immediately, so the tool never executed: no `running` item, no subagent row, the composer still "Waking up". The gated command is `sidebarCrossBgCommand(releasePath)` in `apps/desktop/e2e/mock-server.ts` (an `echo` then a `for`/`seq`/`sleep 0.1` poll loop on a release file), introduced by `3a3bc41c7e`; the `sleep 5` fallback the other two specs use is never gated. Ruled out from Windows: `tools.approval.detect_dangerous_command` returns `(False, None, None)` for BOTH forms. NOT ruled out: `tools.tirith_security.check_command_security` returns `allow` for both locally, but tirith is not installed on that box, so it is a fail-open, not a verdict; on a CI runner tirith auto-installs from GitHub releases and is enabled by default, and `check_all_command_guards` sends any tirith `warn`/`block` to the same approval gate. Two things to settle, in order: (1) read the approval's `description`/`pattern_key` from the backend on a Linux run (it sits in a collapsed disclosure and is NOT in the Playwright trace DOM) and name the flagging rule; (2) decide whether the fix is reshaping the sentinel command or making the E2E sandbox hermetic (a harness that auto-downloads a third-party scanner binary at test time is a network-dependent input to every terminal-using spec). Do NOT guess a command reshape without (1). The next CI run after `b4c9dfbd56` is the proof, and it will fail at `waitForFunction(finalText)` rather than at the dot poll. Agent work. **UNCLAIMED**
- **hermes pack maintenance is a SILENT no-op on git < 2.32** · `hermes` · `_run_bounded_repack` issues `git repack -d --geometric=2 --write-midx`, which exits 129 on older git; the runner sends both streams to DEVNULL and never reads the return code, then `_claim_repack_slot` suppresses retries for 6 hours. Measured on git 2.31.1.windows.1 — this box. Upstream-owned; decide: warn, fall back to `repack -a -d`, or require 2.32. Ruling 1. · `hermes_cli/web_routers/git.py`, upstream-owned **UNCLAIMED**
- **hermes `gh_auth_status_route` caches a stale logged-out answer on a coarse clock — upstream race, report upstream** · `hermes` · the stale-probe guard compares two `time.monotonic()` reads with `>=`; Windows resolution is 15.625 ms (10000/10000 back-to-back reads identical, deterministic failure of upstream's own `test_gh_auth_refresh_waits_out_a_probe_started_before_it`), Linux still ties 1009/10000. Operator symptom: dashboard says "not signed in to GitHub" for the 5-minute TTL right after signing in. File is byte-identical to upstream — the fork does not patch it. Ruling 3. **UNCLAIMED**
- **hermes kanban worktree teardown on Windows: does cleanup move out of the directory before removing it?** · `hermes` · `test_cleanup_proceeds_when_cwd_was_deleted` is `linux_only` as of hermes `3de9a499d3` because its premise is POSIX (Windows answers WinError 32) — the Windows behaviour of that path is untested, not proven safe. Ruling 2. Related vocabulary gap: only three OS markers exist, so POSIX-generic tests are spelled `linux_only` and drop out of macOS; a `posix_only` marker is the honest fix. · `hermes_cli/worktree_ops.py` is upstream-owned; the `posix_only` marker is fork tooling **UNCLAIMED**

- The unrun-gate class on hermes `main`: nothing on any push lane runs the full suites, so reds accumulate silently. Two instances measured and FIXED 2026-08-30 (hermes `00fa94dd75`): `tests/scripts/test_run_tests_script.py` (pins now follow `$RUNNER_PATH` and the import-pytest venv probe) and `tests/hermes_cli/test_error_exit_code_producers.py` (`runtime_unavailable` left `_KEPT_UNSPENDABLE` — the gateway lane mints it at five sites). The CLASS is NARROWED 2026-09-02: hermes' push gate now exists (`fc2e16349e` — Lane B runs the validated 4-dir lane on `main` pushes), so what stays open is only the dirs outside that lane, e.g. `tests/scripts`, home of the first instance. Evidence: the misc-batch landing report (hermes `5fa9000b2`), both fixes `00fa94dd75`; hermes `AGENTS.md` §Testing. · moved from the launcher queue 2026-09-22 (wave 8 sheet) **UNCLAIMED**
- Hermes's focused suites cannot be trusted under multi-agent load: the named instance is RETIRED by suite-perf Stage 6 — `test_realm_sync.py` now drives `run_harness_in_process` (`tests/agent_runtime/_harness_cli`) and carries no `timeout` at all (verified 2026-09-01). The CLASS survives: an in-test subprocess cap `--timeout` cannot raise is invisible to the runner. Original filing: it timed out at load average ~400 on a command the diff never touched; fifteen failures across two suites on 2026-08-30 were all this class. Either the in-test cap becomes environment-scalable or the wave serializes pytest across agents. Evidence: the misc-batch landing report. **RULED 2026-09-04 evening (operator, recommended answer adopted):** Option 1. There is no scaling helper anywhere today — a repo-wide grep for `HERMES_TEST_SUBPROCESS_TIMEOUT` / `timeout_scale` finds only an unrelated audio test — while 474 test files carry a `timeout=`, so the class is broad and nothing currently absorbs load. Option 2 needs exactly the cross-agent orchestrator the shared-index row above says does not exist, and serializing test runs is a direct tax on the multi-agent workflow this house runs by default. Next: hermes: add the scaled-timeout helper plus its env knob, convert the subprocess caps in the test tree to use it, and add a gate refusing a bare numeric subprocess `timeout=` in `tests/`. · moved from the launcher queue 2026-09-22 (wave 8 sheet) **UNCLAIMED**

## Filed on arrival — 2026-09-24 (lane CARRY2A)


## Filed on arrival — 2026-09-24 (lanes UPPR2, REDS)

- [ ] **kanban --claim-ttl override** · hermes_cli/kanban_parser.py + kanban_ops.py pass kb.DEFAULT_CLAIM_TTL_SECONDS as an explicit ttl, and run_daemon maps None→DEFAULT; _resolve_claim_ttl_seconds puts explicit ahead of env, so upstream's HERMES_KANBAN_CLAIM_TTL_SECONDS is silently ignored by every dispatch/daemon · fix: default None, let the resolver pick · filed by lane UPPR2 2026-09-24 **UNCLAIMED**
- [ ] **updater history guard vs upstream rescue anchor** · hermes_cli/update_history.guard_fork_history parallels upstream 21cc8bfef3/db1ff59937 (anchor discarded commits before the diverged reset); after the next merge two authorities own the diverged path; supersession pass · filed by lane UPPR2 2026-09-24 **UNCLAIMED** **TAKEN 2026-09-24 LEDGER-DOCS** · VERDICT 2026-09-24 (owner ruling, recorded by lane LEDGER-DOCS): KEEP the fork guard — `guard_fork_history` refuses with exit 2 before any diverged reset, which upstream's updaters (scripts/desktop-update, lane ADOPT group 3) already treat as a non-retryable refusal, so the fork never reaches upstream's rescue-anchor path; it is a RECORDED parallel of 21cc8bfef3/db1ff59937 (ledger row `hermes_cli/update_cmd.py`, G17), re-read in the supersession pass of the next merge — retire the guard there if upstream's anchor covers the fork's no-reset guarantee
- [ ] **Plugin discovery runs INSIDE a mission-chat turn, after the chat-lane bundle cursor, so the first turn in a home reports `visibility_bundle_rebuild_component_registry_epoch` and rebuilds its bundle** · `fork / runtime` · since `skill_search` became a plugin tool, plugin tools register per home scope on first discovery; decide whether turn setup discovers before sampling the cursor or the flag is the accepted cost · evidence: lane REDS commit "a blind runner's block admits the handler's CP-7 rebuild flags by family" (discovering before the turn removes the flag) · filed by lane REDS 2026-09-24 **UNCLAIMED**
- [ ] **`hermes_cli/model_picker_policy.py::_picker_cache_path` re-derives the root from `HERMES_HOME` / `~/.hermes`** · `fork / runtime` · a second opinion beside `hermes_constants.get_hermes_home()` (profile override blind); the env gate does not scan `hermes_cli/` outside `harness_parts`, so nothing reds · fix: take the home from `get_hermes_home()` · filed by lane REDS 2026-09-24 **UNCLAIMED**

## Filed on arrival — 2026-09-24 (disposition wave)

- [ ] **Five new unlifted upstream-PR classes from the tests rows** — linux-only markers (7), stale doc pointers (6), held fixes (4), path-spelling extras (3), default-root pin (2) · `fork / upstream` · one clean `up/*` branch per class, UPREV-style review before any PR · evidence: `docs/agent-runtime-harness/planned/disposition-tests-2026-09-24.md` § PR candidates not yet lifted · filed by lane DISP-T 2026-09-24 **UNCLAIMED**
- [ ] **DISP-C's 19 generic PR-candidate classes (52 rows)** — top 5: G2 Windows paths and identity (18), G9 kanban crash evidence / claim TTL (5), G10 doctor call-time home (5), G1 turn-safe lazy installs (4), G8 read-only config (4); suggested order G16, G7, G2 in slices, G8, G4, G1 · `fork / upstream` · evidence: `docs/agent-runtime-harness/planned/disposition-core-2026-09-24.md` §3 · filed by lane DISP-C 2026-09-24 **UNCLAIMED**

## Filed on arrival — 2026-09-23 (seam Stage 0 lanes)

- [ ] **The canonical test venv has no `acp` package, so 13 `tests/acp_adapter` files fail to collect (and 4 more need `snowballstemmer`)** · `fork / suite` · install the two optional deps into `C:/Users/beast/.venvs/hermes-test` or mark the files importorskip; 18 collection errors on every collect-only run · evidence: lane LAND2 gate (d), lane S5 collect logs · filed by lane LAND2 2026-09-24 **UNCLAIMED**
- [ ] **Under `scripts/run_tests.sh`, `pwsh` cannot find `git` (`test_install_ps1_repository_stage_recovers_from_autostash_conflict`)** · `fork / suite` · the runner's clean env drops Git from the PATH that pwsh sees; the test passes under direct pytest · evidence: lane GREEN `.lane-logs/ps1_runner.log` · filed by lane GREEN 2026-09-23 **UNCLAIMED**
- [ ] **`test_check_no_tmp_literals.py::test_repo_tree_has_no_hits_outside_the_baseline` is red on main twice over: the scan takes 63 s against a 30 s timeout, and 9 unbaselined tmp-path hits** · `fork / suite` · commits.json (3), w18-hf-field-notes.md (2), 13-upstream-mcp-stdio-env-overrides.md (2), website use-mcp-with-hermes.md (2) · filed by lane LAND 2026-09-23 **UNCLAIMED** **TAKEN 2026-09-23 lane GREEN (X:/wt/h-green)** · VERDICT 2026-09-23 (lane GREEN): the seven hits in fork-owned files are cleared (`cc83825c6d`). What is left touches only upstream-manifest files, so it is out of a fork lane's scope: the two hits in `website/docs/guides/use-mcp-with-hermes.md` (fork-added lines in an upstream file) and the scan cost — measured 30.6 s of 33.8 s is `tokenize`+`ast` over 2,271 `.py` files of which 34 carry a token; the repair is an early return in `_iter_lines_with_hits` when `_TMP_TOKEN.search(text)` is None (`scripts/check_no_tmp_literals.py`). Recommend: upstream PR for the early return; owner rules whether the fork carries it and edits the website doc meanwhile.

## Filed on arrival — 2026-09-21 (upstream sync assessment)

- [ ] **PR candidate: run_tests_parallel amortised start-up** · `fork / upstream` · after `76e6fb0dc8` the per-file floor is upstream's own (~1.1 s to session start, ~2.6 s to collected, × every file); the five would-be upstream commits (phase timings, LPT order, small-file batching, POSIX fork workers, lazy root-conftest hooks) are outlined one per bullet with saving and isolation trade; the lane that builds it re-takes the upstream full-suite wall IDLE first (lane SPEED's was loaded) · evidence: `docs/agent-runtime-harness/planned/suite-cost-centres-2026-09-24.md` §8–§9 **UNCLAIMED** · fork after-fix baseline 40.7 min idle (2026-09-24) · VERDICT 2026-09-24 (lane SUITE2): the measured PROTOTYPE of the batching bullet is the fork-only `scripts/run_tests_bundled.py` (+ `.sh`, plugin in `scripts/_bundle_plugin/`), core free of fork paths; first idle validated-scope run 1,802 s (30.0 min) vs 2,442.7 s per-file, 91 bundles, reds unchanged — the wall is held up by known-red files whose pytest-timeout kills a whole bundle (see the row below and the cost note §10). Owner decides when the upstream PR series is cut; the lane that cuts it takes the upstream idle wall first. · HOLD: PART 2 starts only on the owner's explicit go (ruling 2026-09-24); measurements allowed
- [ ] **An upstream-authored venv test leaves a python from its throwaway venv orphaned** · `fork / suite` · four `python.exe` processes, parents dead, were live on the operator box, started 07:02, 07:10, 07:25 and 07:31 on 2026-09-24 from `%TEMP%/hermes-pytest/r-*/pytest-of-unknown/pytest-0/test_venv_sweep_stops_ma…`. The test is `tests/scripts/install/test_install_ps1_venv_process_tree.py` (teknium1, `d10bb2ab6f`). Upstream deleted the file in `524c38a98a`, so it is absent from fork `main` and outside the fork gate. It still ships on `automation/upstream-sync*` and older fork branches, so any run of those trees leaks one python per run. Same class as the `gateway run --replace` row above. If a tree still carrying the file is run again, reap the tree in a finalizer; otherwise close this when the sync branches are rebased past `524c38a98a` · filed by lane POLISH 2026-09-24 **UNCLAIMED** · same class as #121622
- [ ] **ORDER 0 ([[0013 — Seams before the god-file refactor, and main's reds classified first]]) — classify, never baseline: `main` is red on 194 tests in 72 validated-scope files** · `fork / suite` · measured 2026-09-21 by re-running the merge candidate's red files on `main` @ `bd3c22215d` (log `X:/Eternia/worktrees/main-reds-full-20260921.log`); office RPC contract/method-set drift, tool-inventory drift, snapshot-contract literal gate, a collection error in `test_stream_stale_first_routing.py`, plus upstream tests that were already Windows-red; the CI row above is why nobody saw it · evidence: [[upstream-merge-2026-09-21]] § Suite verdict · the launcher queue's 2026-09-18 row (~250 failures / 67 files at `229b1d43a4`, accepted untriaged by owner ruling 6, "most look Windows-environmental — a marker sweep is the likely first cut") folded here 2026-09-22 **UNCLAIMED** **TAKEN 2026-09-24 LEDGER-DOCS**
- [ ] **Serve boot tests depend on the checkout path containing `hermes`** · `fork / suite` · `agent_runtime/serve_registry.py::_looks_like_serve` wants `hermes` AND `serve` in the process command line; under pytest that string comes from the test file path, so `tests/agent_runtime/test_harness_serve.py` (7) and `test_serve_boot_skill_install.py` (2) fail in any worktree not named `hermes-*` and with a relative path on `main` (a `serve_registry_pruned … refused … cmdline_not_serve_like` stderr frame lands before `ready`); the boot's own row should never be classified by its command line, or the tests pin the probe · evidence: [[upstream-merge-2026-09-21]] § worktree path artifact **TAKEN 2026-09-23 lane ORDER0 (X:/wt/h-reds)** · VERDICT 2026-09-23: (c) environmental — all 9 go green (41 passed) with `hermes` added to argv; the design question stands, table in `docs/agent-runtime-harness/planned/main-reds-classification-2026-09-23.md` **TAKEN 2026-09-23 lane GREEN (X:/wt/h-green)** · VERDICT 2026-09-23 (lane GREEN): recommend the boot never classify its OWN row by argv — `agent_runtime/serve_registry.py::classify_serve_record` returns LIVE for `pid == os.getpid()` (with a matching `started_at_ticks`) before the cmdline probe, and `_looks_like_serve` stays only as the recycled-pid fallback for FOREIGN rows with no identity baseline. Fork-owned; one lane, red-first by running the two serve files from a path without `hermes`.
- [ ] **Upstream tests and code red on this Windows box after the merge (13 tests + 6 new files remain)** · `fork / upstream` · the `pwd` import (10 tests) and the kanban `poll()` fakes (3 tests) are UPSTREAM PRs #119069 / #119071, carried on `main` as `743277a3d5` / `801c24abcd` (the `poll()` carry left 2026-09-24, lane CARRY3: the tests/hermes_cli conftest empties `_live_worker_procs` after each test) (delete the carry when the weekly merge brings the PR back); still red: kanban `write_text` without `encoding=` (3, `test_dead_worker_reap_*`), `test_infrastructure_spawn_refusal_never_charges_the_card`, and the rest, which are POSIX-only assumptions, the fork's gateway fence on a live schtasks test, this box's bash, a network-timed picker · evidence: [[upstream-merge-2026-09-21]] § not ours · folded 2026-09-22 from the launcher queue: `tests/hermes_cli/test_plugin_manifest_v2.py::TestDirectoryPluginKeepsIdentityOverEntryPoint::test_loader_and_listing_prefer_the_installed_directory` asserts `endswith("plugins/twin")` against a `WindowsPath` — a separator spelling; marker-only or report upstream, never patch **UNCLAIMED** · VERDICT 2026-09-24 (lane MERGE): #119069 / #119071 still OPEN upstream and not in `upstream/main` at f24a1d7f92, so both carries stay; see [[upstream-release-2026-09-24]]
- [ ] **One upstream doctor test stays red after F4: the `HERMES_HOME` patch never reaches `doctor_config._check_config_file`** · `fork / upstream` · `test_run_doctor_vendor_slug_policy_for_openai_api_endpoint[https://api.openai.com/v1-True]` patches `doctor.HERMES_HOME`; the fork's config check resolves the home with its own call-time `get_hermes_home()` (three sites); routing those reads through the module attribute re-opens the stale-attribute class — owner decision, not a lane call · evidence: lane A report **UNCLAIMED**
- [ ] **`state_db_has_structural_damage` classifies an FTS-only stomp as structural damage** · `fork / state` · `tests/hermes_cli/test_doctor_structural_corruption.py::test_doctor_routes_structural_damage_to_recover_not_fts_rebuild` red on `main` and on the candidate; a real routing defect (recover instead of FTS rebuild), not environment · evidence: lane A report **UNCLAIMED** **TAKEN 2026-09-23 lane GREEN (X:/wt/h-green)** · VERDICT 2026-09-23 (lane GREEN): (d) upstream-owned — `hermes_state_repair.py` and the test are byte-identical to `upstream/main`, so out of a fork lane's scope. Cause measured: on this venv's SQLite 3.45.3 `PRAGMA integrity_check` RAISES `DatabaseError: vtable constructor failed: messages_fts` for an FTS-only stomp, and `state_db_has_structural_damage` reads any raise as structural. Recommend an upstream PR treating a `DatabaseError` that names a `messages_fts*` vtable as FTS-only.
- [ ] **Retarget the scheduled Codex sync job** · `fork / upstream` · it must produce a tested merge candidate weekly (merge, resolve by the rules in [[Upstream Sync]], `scripts/run_tests.sh` on the validated scope, push `merge/upstream-<date>`, report conflicts + results), never per-file copies; its old branches are deleted and `docs/agent-runtime-harness/planned/upstream-sync-automation.md` on `main` is a stale copy of the retired method — delete it in the commit that lands the retargeted job description · evidence: [[0006 — Upstream sync is a real merge, per-file reconciliation retired]] **UNCLAIMED** **TAKEN 2026-09-24 LEDGER-DOCS** · VERDICT 2026-09-24 (lane LEDGER-DOCS): the retargeted job description is written — [[Merging upstream]] § "The scheduled job" — and the stale `upstream-sync-automation.md` is deleted with it; what remains is OPERATOR-only, outside the repo: paste that block as the Codex cloud job's prompt (weekly) and delete `origin/automation/upstream-sync` (still live at `666f46eb51`)

## Filed on arrival — 2026-09-21 (god-file refactor plan)

- [ ] **ORDER 2 ([[0013 — Seams before the god-file refactor, and main's reds classified first]]), after seam Stage 1 lands — Dispatch Wave 0 + S1** · `fork / refactor` · one sitting: the four gates + the reach census, then the `scripts/changed_line_mutation_check.py` split (S1) because every later landing runs it · evidence: plan §2 Wave 0, §3 **UNCLAIMED** **TAKEN 2026-09-24 LEDGER-DOCS**
- [ ] **H1 alone: the seven exec'd parts become modules** · `fork / refactor` · the only lane with a trap (263 `harness.<name>` patches; W0-G4 flips from xfail); `scripts/retarget_harness_patches.py` output pasted into the commit · evidence: plan §2 Wave 1 **UNCLAIMED**
- [ ] **12 production functions only tests call** · `fork / refactor` · plan §4.1 rows (`backfill_instance_profile_ids` 138 lines is the one real deletion; the rest are test seams to move under `tests/`); each lane applies its row · evidence: plan §4.1 **UNCLAIMED**

## Filed on arrival — 2026-09-21 (brain)

- [ ] **Session memory facts that belong here** · `fork / brain` · the operator's Claude session memory (`~/.claude/projects/X--Eternia-hermes-agent/memory/`) holds ~50 project entries; the standing rulings are now ADRs 0001–0011 and the cursors are on the program notes; the remaining per-program detail (queue waves, delete audit, skills realm) graduates on the next touch of each program, not as a sweep · evidence: [[Brain Index]] § source-of-truth routing **UNCLAIMED** **TAKEN 2026-09-24 LEDGER-DOCS**

## Filed on arrival — 2026-09-24 (lane CARRY2B)

- [ ] **The fork's Windows arm of `tools/environments/local.py::_kill_process` is unpinned: its own test (terminate_pid on an exited wrapper's tree) was red on `origin/main`** · `fork / tests` · the upstream file is upstream's bytes again and its two POSIX tests are id-table skips; decide whether the arm or the test was wrong, then pin it in a `_downstream` sibling · evidence: lane CARRY2B `.lane-logs/tools_fork.log` ("got []") · filed by lane CARRY2B 2026-09-24 **UNCLAIMED**
- [ ] **`hermes_cli.plugins_discovery` reads the enable lists, so upstream's `_enable` patch on `hermes_cli.plugins` is dead: `test_entry_point_not_enabled_is_skipped` / `test_entry_point_disabled_wins_over_enabled` pass vacuously on the fork** · `fork / upstream` · the two enable-needing tests are strict xfails with a sibling; either keep the seam live on `hermes_cli.plugins` or accept the vacuous pair · evidence: `tests/providers/test_entry_point_discovery_downstream.py` · filed by lane CARRY2B 2026-09-24 **UNCLAIMED**
- [ ] **`_live_system_guard`'s backend-spawn classifier (`tests/conftest.py`) reads `gateway` inside the comments of an `sh -c` script and refuses it as a backend start** · `fork / suite` · stage2 keygen's 8 tests ride `spawns_gateway_lookalike` by id to get past it; classify the executed command, not the script text · evidence: lane CARRY2B `.lane-logs/tools_up.log` (test_stage2_hook_api_server_keygen) · filed by lane CARRY2B 2026-09-24 **UNCLAIMED**

## Filed on arrival — 2026-09-24 (lane REDS2)

- [ ] **`tests/test_env_gap_registry.py` is red on `origin/main` (4): three registries name tests the upstream purge deleted (agent, hermes_cli, tools), and the hermes_cli EOL-churn probe no longer fires** · `fork / suite` · outside `tests/agent_runtime tests/hermes_cli`, so no fork-scope gate saw it; delete the dead rows, re-measure the probe · evidence: measured on `049a5dd601` by lane REDS2 (pytest tests/test_env_gap_registry.py: 4 failed, 8 passed) · UPDATE 2026-09-24 lane CLOSE: now GREEN BY BLINDNESS — 6 passed, 8 skipped, every skip "tests/<dir> carries no probe-backed registry": the gate loads `tests/<dir>/conftest.py`, and the registries moved to `tests/_downstream/<dir>_conftest.py` (lane CARRY3); point the gate at the downstream modules before the dead rows can be measured **UNCLAIMED**
- [ ] **Upstream test files install a fake `dotenv` (no `dotenv.main`) into `sys.modules` at import — `test_gmi_provider.py`, `test_fireworks_provider.py`, `test_upstage_provider.py` — and a bundle imports every member before running any, so the fake reaches every co-member** · `fork / suite` · two files moved to `scripts/test_bundles_unbundled.txt` for it; the structural answer is a collection-time restore of `sys.modules["dotenv"]` in the bundle plugin or `tests/_downstream/hermes_cli_conftest.py`, then take the two lines back out · evidence: lane REDS2 job-4 commit (pairwise repros) **UNCLAIMED**

## Filed on arrival — 2026-09-24 (lane CARRY3)

- [ ] **CARRY2B's `_MIDTEST_UNDO` skip rows copy upstream test bodies into scoped-context siblings; the `scoped_monkeypatch_undo` id mark runs upstream's bytes instead** · `fork / tests` · `tests/_downstream/conftest_plugin.py::pytest_pyfunc_call` narrows `monkeypatch.undo()` to the body's own stack tail for a marked test (proven on four tests/hermes_cli files, lane CARRY3 commit "tests/ — eleven upstream test files byte-identical"); convert every `_MIDTEST_UNDO` row in `tests/_downstream/id_markers.py` to that mark and delete the sibling copies whose only reason is the scoped rewrite · filed by lane CARRY3 2026-09-24 **UNCLAIMED**
- [ ] **`test_readiness_pass_leaves_credential_store_byte_identical`'s digest assertion cannot red: the fork's rotation cursor is a sidecar, so a PERSISTING readiness selection leaves `auth.json` byte-identical** · `fork / tests` · with `pool_rotation_scope` mutated to always persist, the digest assertion PASSED and only the counter reddened (lane CARRY3); the docstring's killing mutation claims "the digest moves". Point the digest at the file `write_pool_rotation_cursor` writes (or add it), and re-record the red · filed by lane CARRY3 2026-09-24 **UNCLAIMED**

## Filed on arrival — 2026-09-24 (lane REDS3)

- [ ] **`test_tui_gateway_server.py::test_notification_poller_live_loop_requeues_foreign_completion_for_owner` is order/timing-flaky: a stray `session.info` frame (this worktree's cwd and branch) reaches its captured writes** · `fork / suite` · red once in a five-file run on `6251144d09`, green alone and green on the next four-file run; find the emitter that outlives its test (a session-info publish from an earlier test's thread) and join or drain it in teardown · evidence: lane REDS3 `.lane-logs/base.log` (`assert [('session.info', 'msg-id-trunc-sid', {...'branch': 'fork/main-reds-2026-09-24'...})] == []`) **UNCLAIMED**
- [ ] **`test_t6b_brief_descriptions.py::test_wire_ships_brief_shorter_than_full_docs` stops at its first offending tool, so one red hid three defects (read_file, skill_search, vision_analyze) for three days** · `fork / tests` · collect every `wb > fb` / missing-entry offender and assert the list empty, so the red names them all at once · evidence: lane REDS3 commit "fix(t6b): three stale wire briefs behind one red" **UNCLAIMED**
- [ ] **`test_gateway_foreign_xdg_runtime.py::TestPreflightForeignRuntimeNoLeak::test_foreign_xdg_runtime_dir_raises_unavailable_not_permission_error` is red in its bundle (`module 'os' has no attribute 'getuid'`) and green alone** · `fork / suite` · an earlier bundle member leaves the gateway preflight on its POSIX branch; bisect the bundle (the REDS2 pairwise method, `016a6cd71e`), then fix the leak or move the file to `scripts/test_bundles_unbundled.txt` with the observed pair · evidence: wave-close gate on `6251144d09` (gate-wave2.log, now removed with `X:/wt/h-gate2`), lane REDS3 run alone: XPASS(strict) **UNCLAIMED**

## Filed on arrival — 2026-09-24 (lane CLOSE)

- [ ] **`tests/test_path_identity_gate.py` is red on two UPSTREAM-owned sites the f24a1d7f92 merge brought in — `tools/terminal_tool.py::_sanitize_cwd_for_live_env` (line 300, upstream `b911b9794e`) and `tools/tts_tool_speaker.py::_synthesize_to_tmp` (line 135, upstream `77fd944c57`)** · `fork / tests` · the fork gate scans upstream code it may not edit and has only an exemption table ("genuinely the right question") to answer with, so an upstream-arrived finding cannot be recorded honestly; give the gate an upstream-owned lane (site + PR-candidate reason, rotted when upstream fixes it) and register these two · evidence: lane CLOSE, `pytest tests/test_path_identity_gate.py` on `6251144d09` (1 failed) **UNCLAIMED**
- [ ] **Mutation claims `cxr1-codex-readiness-asks-the-pool-alone` and `cxr3-codex-readiness-asks-the-store-before-the-pool` no longer anchor (mutation source not found in `agent_runtime/profile_readiness.py::_codex_provider_issue` / `agent_runtime/provider_probes.py::codex_credentials_resolvable_read_only`)** · `fork / tests` · they guard nothing until re-derived; re-derive each needle and prove it red against its own test · evidence: lane CLOSE, `pytest tests/scripts` on `6251144d09`+job 1 (`test_mutation_claims_still_anchor.py`: 1 failed) **UNCLAIMED**
- [ ] **`tests/gateway/test_background_process_notifications.py::test_agent_notify_receipt_only_while_launching_turn_is_busy[True|False]` is red: `_enqueue_process_completion_notification` is awaited 0 times under `notify_on_complete`** · `fork / gateway` · upstream test at upstream bytes, red on `6251144d09` before lane CLOSE touched anything; suspect the fork's `background_process_agent_turns: false` direct-status lane (BG-PR) skipping the enqueue — needs an id row naming the fork symbol or a fix · evidence: lane CLOSE job 1 (`pytest` of the file alone at `6251144d09`: 2 failed) **UNCLAIMED**

## Filed on arrival — 2026-09-24 (program-end gate on `c7652475cd`)

- [ ] **`tests/hermes_cli/test_custom_provider_model_switch.py::test_custom_endpoint_switch_prunes_stale_model_config_pool_entry` hangs to the 30 s thread timeout under the bundled gate (rerun alone in the same run: timeout again)** · `fork / suite` · the trace ends in `self._condition.wait(timeout)` at line 101; find what the test waits on that never arrives when the box is loaded, or mark it with the real reason; not reproduced outside a gate run · evidence: `X:/wt/_holds/gate-final-2026-09-24.log` (program-end gate, 8 workers, 1231 s) **UNCLAIMED**
- [ ] **`tests/hermes_cli/test_web_server.py` (already solo in `scripts/test_bundles_unbundled.txt`) still dies at the 30 s timeout in two tests under the gate — `test_ticker_runs_when_desktop` (line 4979) and `test_telegram_onboarding_apply_reports_restart_failure_after_save` (line 1575); both green in lane REDS2's alone-run (177 passed / 5 skipped)** · `fork / suite` · load-sensitive waits; measure the wall of each alone vs under 8 workers, then either raise the fork's per-test timeout for the file (`tests/_downstream/conftest_plugin.py`) or drain the wait; the solo entry does not save it because the timeout is per test, not per process · evidence: `X:/wt/_holds/gate-final-2026-09-24.log`, lines 1945–2051 **UNCLAIMED**
- [ ] **`tests/hermes_cli/test_minimax_oauth.py::test_refresh_error_body_bounded_and_readable_with_real_client` is red in its bundle and green on the rerun alone** · `fork / suite` · a co-member leaves a client or transport patched; bisect the bundle pairwise (REDS2 method, `016a6cd71e`) and fix the leak or add the observed pair to `scripts/test_bundles_unbundled.txt` · evidence: `X:/wt/_holds/gate-final-2026-09-24.log` ("red in bundle" line) **UNCLAIMED**

## Filed on arrival — 2026-09-24 (lane LEDGER-DOCS)

- [ ] **Fork CI on `main` runs on every push again but has not been green once since it resumed** · `fork / ci` · `gh run list --branch main --workflow ci.yaml`: 68 runs since 2026-09-15, 38 failure · 28 cancelled · 0 success (red jobs on run 35992190948: Python tests, ruff enforcement, Windows footguns `/tmp` baseline, windows_only tests); `fork-gates.yml` red on every push, `Changed-line mutation claims` → `mutation-check configuration error: cxr1-codex-readiness-asks-the-pool-alone: mutation source not found in agent_runtime/profile_readiness` (run 36062192326) · classify each red job as fork-caused / upstream-caused / runner-environmental before fixing · filed by lane LEDGER-DOCS 2026-09-24 **UNCLAIMED**
