---
type: queue
program: fork-hygiene
status: open
tags: [queue, program/fork-hygiene]
---

# Fork hygiene — open queue

The repository AS A FORK: upstream sync and the boundary, CI, the suite and its gates, the god-file refactor, this vault. Runtime and Mission Control rows do NOT go here — the hermes half is [[runtime-queue]], the launcher half is `EterniaLauncher/Launcher_Brain/20 — Active Initiatives/mission-control-queue.md`. Rows: one line + a pointer, never a restatement; claim with `**TAKEN <date> <who>**` before starting; a landing deletes its row.

## Release validation — 2026-09-23



- [ ] **Sidebar cache concurrency and serve socket disconnect tests are flaky** · `fork / suite` · `test_projects_tree_coalesces_concurrent_scans_and_returns_copies` and `test_a_disconnect_unsubscribes_and_does_nothing_else` fail then pass bounded retries on the exact release merge; do not count retry success as stable proof · evidence: [[upstream-release-2026-09-23]] **UNCLAIMED** **TAKEN 2026-09-24 TESTS** · VERDICT 2026-09-24 (lane TESTS): sidebar half REFUTED — the test was purged in `4b67380698`. Disconnect half is DESIGN: the recorded red (`subscribers` 1 == 0 after `count` == 1) is NOT at the socket seam — a 0.5 s sleep before `_on_connection_closed`'s `_release_subscription`, and deleting that call outright, both stayed green (first poll already `count 1, subscribers 0`), so the hub drops the subscriber on another path; find that path, then poll for both halves before asserting (a both-halves wait was drafted and withdrawn because no mutation could red it)

## Filed on the move — 2026-09-22 (fork rows the launcher queue had carried since 2026-09-02)

Moved verbatim from `EterniaLauncher/Launcher_Brain/20 — Active Initiatives/mission-control-queue.md`; they are about the repository as a fork, not about the surface. Upstream-owned ones are tagged so: never patched here, a marker or an upstream issue.

- **hermes CI on `main`: the runner crash and the 43 lint errors and the mutation job are fixed; what is left is hb's slices 5–8 and the Issues switch** · `hermes` · **NARROWED 2026-09-06 (w18/ha `57b6964b01`, w18/hc `2b1f6e849c`)** — run 34046863701 (hc's push) is the first with `ruff enforcement (blocking)` and `Changed-line mutation claims` GREEN. **ha, slices 1–4, CLOSED:** all 16 tests across 11 files, one commit per file (`48a690da4c` doc-cite report needs a 4-commit repo under a depth-1 checkout; `188c511bae` + `9765564afa` stale `patch()` targets from `96cfc09a34`; `9463780a78` tool-inventory mutations wrote CRLF; `caffc04c1c` a hand-typed Windows byte count; `43cbb598ef` a real duplicate helper (`realm_sync._ledger_time` == `store._stamp`); `30f1ec6635` a BOM on `install.ps1` from the 2026-07-31 upstream merge; `23a90c6063` `test_env_gap_registry` was a DESIGN fault — a green host was read as "every row rotted"; `cee66b50fd` a witness that had not followed its reader; `d2137867b7` a directory-only git exclude that misses POSIX's symlink, which is also why the worktree GC never reaped on Linux; `ce3dc7f465` `os.path.normcase` on Windows-registry PATH data). Six of the eleven reproduced on Windows and were never platform facts — they live outside the four validated test directories and had simply never been run. **hc, CLOSED except Playwright:** ruff's 43 were all genuine `F821` (38 undeclared names in exec-assembled part-file import headers, 5 a missing `typing.Any`), 0 noqa (`fa0fb62788`, `f29ad6ca6c`, `3af990fca7`); `test_harness_parts_namespace.py` never checked header PRESENCE, ruff is the real gate, said so in `6c629b9122`; the mutation job's exit was right and four registered needles had stopped anchoring — re-derived, each proven KILLED, plus `tests/scripts/test_mutation_claims_still_anchor.py` (`abaace6a87`, `bbc1caafb0`), and a fifth needle went ambiguous on landing over hd and was fixed forward (`2b1f6e849c`). The notifier `notify-main-red` LANDED (`028aceb9c6`) and FIRED on run 34046863701 — and failed on its own step, because **Issues are disabled on `nekwo/hermes-agent`** (register row). **hb, slices 5–8, CLOSED (hermes `962a10d0fb`, 13 commits):** all nine reds and the uncollected `test_tombstone_registry.py` (1,155 tests lost at collection to a `check=True` diff under `fetch-depth: 1`); two were the CI checkout, not the code — `ci.yml` now fetches full history with `filter: blob:none`, and the docket gate had been FALSELY accusing seven backed stages; three were product defects (`_ABS_PATH_RE`'s POSIX branch truncated at the first space, `entrypoint_name` used the host separator, `note_peer_seen` read the clock twice); the eight `stream_frames` goldens were regenerated for a path-tail redaction (`262a165b47`), which the launcher re-vendored 2026-09-06 (w18/lf). **What remains here:** (1) the next main-push run after hb lands must show the eight slices green; (3) the Issues switch, then one red push to prove the notifier end to end; then delete this row. **UNCLAIMED**
- **Three hermes tests the CI runner reports FLAKY, which AGENTS.md calls bugs** · `hermes` · found 2026-09-06 (w18/ha) in run 33969282189's slice logs — `tests/agent_runtime/test_serve_stream_lane_parity.py`, `tests/run_agent/test_run_agent.py::TestExecuteToolCalls::test_sequential_tool_calls_run_without_delay`, `tests/tools/test_browser_eval_supervisor_path.py::TestEvaluateRuntimeDomNodeCrashRetry::test_reference_chain_crash_retries_without_by_value`. All three read as timing-sensitive: convert to controlled time at the mechanism, never a longer deadline; `tests/agent_runtime/test_realm_sync_eol.py` also hit the 30 s per-test cap under contention on this box (36 s alone). Agent work. **UNCLAIMED**
- **`tests/test_coverage_claims_resolve.py` sits on a 180 s wall its own fixture now overruns** · `hermes` · found 2026-09-06 (w18/hd), measured not asserted — the file declares `pytestmark = pytest.mark.timeout(180)` and its comment records `collect_claims()` at 27–31 s over three runs on 2026-09-02; called directly today with nothing else running it took 211.5 s in the primary checkout on unmodified `main` and 279.5 s in a worktree under `X:/wt`, identical corpus census in both (241 md / 4,097 py / 3,089 test / 725 claims). It passed once mid-lane at 176.4 s. The marker was NOT raised. Two questions, one row: did the walk regress (bisect `collect_claims` between 2026-09-02 and today) or did this box's IO, and why is a worktree ~30% slower than the primary (the Defender exclusion covers the primary's drive, not `X:/wt` — check `Get-MpPreference` ELEVATED, it is blind otherwise). Fix the walk or gate the fixture on a cached census; never widen the wall. Agent work. **UNCLAIMED**
- **Desktop E2E on Linux: the sentinel background command is stopped by a terminal-command approval prompt, so no background process ever spawns** · `hermes` (desktop app + E2E harness) · found 2026-09-06 (w18/hg) while closing the "polls the background dot as a turn-is-running signal" row (fixed in hermes `b4c9dfbd56`: `sessionDotState` is right, all three E2E call sites now poll the `Session running` label, plus a unit case pinning working-over-background with its recorded killing mutation). That fix does NOT make the job green. Evidence is the failing run's own page snapshot (`playwright-test-results` artifact of run 34051553815, `error-context.md` of the sidebar-states spec): at the 30 s timeout the sidebar row shows `status "Session running"`, and the tool row shows `Running echo "long bg output" + 6 commands` with 27 s elapsed on a `background=true` call plus a `Run` / `More approval options` / `Reject` approval bar. A background terminal call returns immediately, so the tool never executed: no `running` item, no subagent row, the composer still "Waking up". The gated command is `sidebarCrossBgCommand(releasePath)` in `apps/desktop/e2e/mock-server.ts` (an `echo` then a `for`/`seq`/`sleep 0.1` poll loop on a release file), introduced by `3a3bc41c7e`; the `sleep 5` fallback the other two specs use is never gated. Ruled out from Windows: `tools.approval.detect_dangerous_command` returns `(False, None, None)` for BOTH forms. NOT ruled out: `tools.tirith_security.check_command_security` returns `allow` for both locally, but tirith is not installed on that box, so it is a fail-open, not a verdict; on a CI runner tirith auto-installs from GitHub releases and is enabled by default, and `check_all_command_guards` sends any tirith `warn`/`block` to the same approval gate. Two things to settle, in order: (1) read the approval's `description`/`pattern_key` from the backend on a Linux run (it sits in a collapsed disclosure and is NOT in the Playwright trace DOM) and name the flagging rule; (2) decide whether the fix is reshaping the sentinel command or making the E2E sandbox hermetic (a harness that auto-downloads a third-party scanner binary at test time is a network-dependent input to every terminal-using spec). Do NOT guess a command reshape without (1). The next CI run after `b4c9dfbd56` is the proof, and it will fail at `waitForFunction(finalText)` rather than at the dot poll. Agent work. **UNCLAIMED**
- **hermes pack maintenance is a SILENT no-op on git < 2.32** · `hermes` · `_run_bounded_repack` issues `git repack -d --geometric=2 --write-midx`, which exits 129 on older git; the runner sends both streams to DEVNULL and never reads the return code, then `_claim_repack_slot` suppresses retries for 6 hours. Measured on git 2.31.1.windows.1 — this box. Upstream-owned; decide: warn, fall back to `repack -a -d`, or require 2.32. Ruling 1. · `hermes_cli/web_routers/git.py`, upstream-owned **UNCLAIMED**
- **hermes `gh_auth_status_route` caches a stale logged-out answer on a coarse clock — upstream race, report upstream** · `hermes` · the stale-probe guard compares two `time.monotonic()` reads with `>=`; Windows resolution is 15.625 ms (10000/10000 back-to-back reads identical, deterministic failure of upstream's own `test_gh_auth_refresh_waits_out_a_probe_started_before_it`), Linux still ties 1009/10000. Operator symptom: dashboard says "not signed in to GitHub" for the 5-minute TTL right after signing in. File is byte-identical to upstream — the fork does not patch it. Ruling 3. **UNCLAIMED**
- **hermes kanban worktree teardown on Windows: does cleanup move out of the directory before removing it?** · `hermes` · `test_cleanup_proceeds_when_cwd_was_deleted` is `linux_only` as of hermes `3de9a499d3` because its premise is POSIX (Windows answers WinError 32) — the Windows behaviour of that path is untested, not proven safe. Ruling 2. Related vocabulary gap: only three OS markers exist, so POSIX-generic tests are spelled `linux_only` and drop out of macOS; a `posix_only` marker is the honest fix. · `hermes_cli/worktree_ops.py` is upstream-owned; the `posix_only` marker is fork tooling **UNCLAIMED**

- The unrun-gate class on hermes `main`: nothing on any push lane runs the full suites, so reds accumulate silently. Two instances measured and FIXED 2026-08-30 (hermes `00fa94dd75`): `tests/scripts/test_run_tests_script.py` (pins now follow `$RUNNER_PATH` and the import-pytest venv probe) and `tests/hermes_cli/test_error_exit_code_producers.py` (`runtime_unavailable` left `_KEPT_UNSPENDABLE` — the gateway lane mints it at five sites). The CLASS is NARROWED 2026-09-02: hermes' push gate now exists (`fc2e16349e` — Lane B runs the validated 4-dir lane on `main` pushes), so what stays open is only the dirs outside that lane, e.g. `tests/scripts`, home of the first instance. Evidence: the misc-batch landing report (hermes `5fa9000b2`), both fixes `00fa94dd75`; hermes `AGENTS.md` §Testing. · moved from the launcher queue 2026-09-22 (wave 8 sheet) **UNCLAIMED**
- Hermes's focused suites cannot be trusted under multi-agent load: the named instance is RETIRED by suite-perf Stage 6 — `test_realm_sync.py` now drives `run_harness_in_process` (`tests/agent_runtime/_harness_cli`) and carries no `timeout` at all (verified 2026-09-01). The CLASS survives: an in-test subprocess cap `--timeout` cannot raise is invisible to the runner. Original filing: it timed out at load average ~400 on a command the diff never touched; fifteen failures across two suites on 2026-08-30 were all this class. Either the in-test cap becomes environment-scalable or the wave serializes pytest across agents. Evidence: the misc-batch landing report. **RULED 2026-09-04 evening (operator, recommended answer adopted):** Option 1. There is no scaling helper anywhere today — a repo-wide grep for `HERMES_TEST_SUBPROCESS_TIMEOUT` / `timeout_scale` finds only an unrelated audio test — while 474 test files carry a `timeout=`, so the class is broad and nothing currently absorbs load. Option 2 needs exactly the cross-agent orchestrator the shared-index row above says does not exist, and serializing test runs is a direct tax on the multi-agent workflow this house runs by default. Next: hermes: add the scaled-timeout helper plus its env knob, convert the subprocess caps in the test tree to use it, and add a gate refusing a bare numeric subprocess `timeout=` in `tests/`. · moved from the launcher queue 2026-09-22 (wave 8 sheet) **UNCLAIMED**

## Filed on arrival — 2026-09-24 (lane CARRY2A)


## Filed on arrival — 2026-09-24 (lanes UPPR2, REDS)

- [ ] **updater history guard vs upstream rescue anchor** · hermes_cli/update_history.guard_fork_history parallels upstream 21cc8bfef3/db1ff59937 (anchor discarded commits before the diverged reset); after the next merge two authorities own the diverged path; supersession pass · filed by lane UPPR2 2026-09-24 **UNCLAIMED** **TAKEN 2026-09-24 LEDGER-DOCS** · VERDICT 2026-09-24 (owner ruling, recorded by lane LEDGER-DOCS): KEEP the fork guard — `guard_fork_history` refuses with exit 2 before any diverged reset, which upstream's updaters (scripts/desktop-update, lane ADOPT group 3) already treat as a non-retryable refusal, so the fork never reaches upstream's rescue-anchor path; it is a RECORDED parallel of 21cc8bfef3/db1ff59937 (ledger row `hermes_cli/update_cmd.py`, G17), re-read in the supersession pass of the next merge — retire the guard there if upstream's anchor covers the fork's no-reset guarantee
- [ ] **Plugin discovery runs INSIDE a mission-chat turn, after the chat-lane bundle cursor, so the first turn in a home reports `visibility_bundle_rebuild_component_registry_epoch` and rebuilds its bundle** · `fork / runtime` · since `skill_search` became a plugin tool, plugin tools register per home scope on first discovery; decide whether turn setup discovers before sampling the cursor or the flag is the accepted cost · evidence: lane REDS commit "a blind runner's block admits the handler's CP-7 rebuild flags by family" (discovering before the turn removes the flag) · filed by lane REDS 2026-09-24 **UNCLAIMED** **TAKEN 2026-09-24 FORK-CODE** · VERDICT 2026-09-24 (lane FORK-CODE): owner call, recommendation DISCOVER FIRST — `agent_runtime/tool_visibility.py::_ensure_plugin_tools_registered` is idempotent (no `force`) and imports `hermes_cli.plugins` only, and the same turn pays that discovery a few steps later anyway, so calling it in turn setup before the chat-lane bundle cursor is sampled moves a cost rather than adding one and retires a flag that reports a rebuild nobody caused. Accepting the flag leaves CP-7's `visibility_bundle_rebuild_*` family unable to tell a real epoch move from first-turn discovery in any home. Red-first: the REDS commit's own first-turn case must stop reporting `visibility_bundle_rebuild_component_registry_epoch`.

## Filed on arrival — 2026-09-24 (disposition wave)

- [ ] **Five new unlifted upstream-PR classes from the tests rows** — linux-only markers (7), stale doc pointers (6), held fixes (4), path-spelling extras (3), default-root pin (2) · `fork / upstream` · one clean `up/*` branch per class, UPREV-style review before any PR · evidence: `docs/agent-runtime-harness/planned/disposition-tests-2026-09-24.md` § PR candidates not yet lifted · filed by lane DISP-T 2026-09-24 **UNCLAIMED**
- [ ] **DISP-C's 19 generic PR-candidate classes (52 rows)** — top 5: G2 Windows paths and identity (18), G9 kanban crash evidence / claim TTL (5), G10 doctor call-time home (5), G1 turn-safe lazy installs (4), G8 read-only config (4); suggested order G16, G7, G2 in slices, G8, G4, G1 · `fork / upstream` · evidence: `docs/agent-runtime-harness/planned/disposition-core-2026-09-24.md` §3 · filed by lane DISP-C 2026-09-24 **UNCLAIMED**

## Filed on arrival — 2026-09-23 (seam Stage 0 lanes)


## Filed on arrival — 2026-09-21 (upstream sync assessment)

- [ ] **PR candidate: run_tests_parallel amortised start-up** · `fork / upstream` · after `76e6fb0dc8` the per-file floor is upstream's own (~1.1 s to session start, ~2.6 s to collected, × every file); the five would-be upstream commits (phase timings, LPT order, small-file batching, POSIX fork workers, lazy root-conftest hooks) are outlined one per bullet with saving and isolation trade; the lane that builds it re-takes the upstream full-suite wall IDLE first (lane SPEED's was loaded) · evidence: `docs/agent-runtime-harness/planned/suite-cost-centres-2026-09-24.md` §8–§9 **UNCLAIMED** · fork after-fix baseline 40.7 min idle (2026-09-24) · VERDICT 2026-09-24 (lane SUITE2): the measured PROTOTYPE of the batching bullet is the fork-only `scripts/run_tests_bundled.py` (+ `.sh`, plugin in `scripts/_bundle_plugin/`), core free of fork paths; first idle validated-scope run 1,802 s (30.0 min) vs 2,442.7 s per-file, 91 bundles, reds unchanged — the wall is held up by known-red files whose pytest-timeout kills a whole bundle (see the row below and the cost note §10). Owner decides when the upstream PR series is cut; the lane that cuts it takes the upstream idle wall first. · HOLD: PART 2 starts only on the owner's explicit go (ruling 2026-09-24); measurements allowed
- [ ] **An upstream-authored venv test leaves a python from its throwaway venv orphaned** · `fork / suite` · four `python.exe` processes, parents dead, were live on the operator box, started 07:02, 07:10, 07:25 and 07:31 on 2026-09-24 from `%TEMP%/hermes-pytest/r-*/pytest-of-unknown/pytest-0/test_venv_sweep_stops_ma…`. The test is `tests/scripts/install/test_install_ps1_venv_process_tree.py` (teknium1, `d10bb2ab6f`). Upstream deleted the file in `524c38a98a`, so it is absent from fork `main` and outside the fork gate. It still ships on `automation/upstream-sync*` and older fork branches, so any run of those trees leaks one python per run. Same class as the `gateway run --replace` row above. If a tree still carrying the file is run again, reap the tree in a finalizer; otherwise close this when the sync branches are rebased past `524c38a98a` · filed by lane POLISH 2026-09-24 **UNCLAIMED** · same class as #121622 **TAKEN 2026-09-24 TESTS** · VERDICT 2026-09-24 (lane TESTS): DESIGN — `main` is clean (the file is absent), but `origin/automation/upstream-sync` still carries `tests/scripts/install/test_install_ps1_venv_process_tree.py`; the fork-side answer is deleting that retired-method branch (the "Retarget the scheduled Codex sync job" row already says its old branches go), an owner call, not a lane push
- [ ] **ORDER 0 ([[0013 — Seams before the god-file refactor, and main's reds classified first]]) — classify, never baseline: `main` is red on 194 tests in 72 validated-scope files** · `fork / suite` · measured 2026-09-21 by re-running the merge candidate's red files on `main` @ `bd3c22215d` (log `X:/Eternia/worktrees/main-reds-full-20260921.log`); office RPC contract/method-set drift, tool-inventory drift, snapshot-contract literal gate, a collection error in `test_stream_stale_first_routing.py`, plus upstream tests that were already Windows-red; the CI row above is why nobody saw it · evidence: [[upstream-merge-2026-09-21]] § Suite verdict · the launcher queue's 2026-09-18 row (~250 failures / 67 files at `229b1d43a4`, accepted untriaged by owner ruling 6, "most look Windows-environmental — a marker sweep is the likely first cut") folded here 2026-09-22 **UNCLAIMED** **TAKEN 2026-09-24 LEDGER-DOCS** · VERDICT 2026-09-24 (lane LEDGER-DOCS, DESIGN — owner closes): DONE by the 2026-09-24 seam program — the classification this row asked for exists: lanes REDS, REDS2, REDS3 put every inherited red in the fork scope on an id row in `tests/_downstream/id_markers.py` (strict xfail naming the fork symbol, `linux_only`-shaped skip, or probe-backed env-gap skip) or on its own queue row, never a baseline; the program-end gate on `c7652475cd` (fork scope plus the whole inherited set via the root conftest) read 20,435 passed · 2 failed · 451 skipped · 224 xfailed against this row's 194 red in 72 files, and both failures carry rows; the ratchet went 459/2834/24 (S0) → 215/1036/4 at `fa12c6c5a3` (214/1027/4 on `fork/ledger-rows-2026-09-24`), ledger carry rows → 1 (+4 carry-permanent). REMAINS, each already its own row: the program-end gate's three (two 30 s hangs, one bundle-only red), REDS2's env-gap registry and fake-`dotenv` leak, REDS3's three, CLOSE's three (path-identity gate on two upstream sites, the unanchored cxr1/cxr3 claims, the bg-notification receipt), and `main`'s CI red on every run (LEDGER-DOCS row). Nothing in this row is left that those rows do not own — delete it on the owner's word
- [ ] **Retarget the scheduled Codex sync job** · `fork / upstream` · it must produce a tested merge candidate weekly (merge, resolve by the rules in [[Upstream Sync]], `scripts/run_tests.sh` on the validated scope, push `merge/upstream-<date>`, report conflicts + results), never per-file copies; its old branches are deleted and `docs/agent-runtime-harness/planned/upstream-sync-automation.md` on `main` is a stale copy of the retired method — delete it in the commit that lands the retargeted job description · evidence: [[0006 — Upstream sync is a real merge, per-file reconciliation retired]] **UNCLAIMED** **TAKEN 2026-09-24 LEDGER-DOCS** · VERDICT 2026-09-24 (lane LEDGER-DOCS): the retargeted job description is written — [[Merging upstream]] § "The scheduled job" — and the stale `upstream-sync-automation.md` is deleted with it; what remains is OPERATOR-only, outside the repo: paste that block as the Codex cloud job's prompt (weekly) and delete `origin/automation/upstream-sync` (still live at `666f46eb51`)

## Filed on arrival — 2026-09-21 (god-file refactor plan)

- [ ] **ORDER 2 ([[0013 — Seams before the god-file refactor, and main's reds classified first]]), after seam Stage 1 lands — Dispatch Wave 0 + S1** · `fork / refactor` · one sitting: the four gates + the reach census, then the `scripts/changed_line_mutation_check.py` split (S1) because every later landing runs it · evidence: plan §2 Wave 0, §3 **UNCLAIMED** **TAKEN 2026-09-24 LEDGER-DOCS** · VERDICT 2026-09-24 (lane LEDGER-DOCS, DESIGN): the gate this row waited on is OPEN — seam Stage 1 is on `main` (merged ahead of the S4+S5 landing, per the ratchet fixture's reasons; its +70 ms startup debt waived 2026-09-24), and seam Stages 0/2/4/5 plus the carry passes landed with it. NOTHING of the row itself is done: [[Downstream Refactor]]'s cursor is still "plan LANDED, not dispatched", no EXEC_CARD exists, and `scripts/changed_line_mutation_check.py` is still one 1,150-line file. REMAINS: the one Wave 0 sitting (the four gates + the reach census + the EXEC_CARD), then S1 — re-measure the 41-file census first, since the seam program moved code out of the god files it names
- [ ] **12 production functions only tests call** · `fork / refactor` · plan §4.1 rows (`backfill_instance_profile_ids` 138 lines is the one real deletion; the rest are test seams to move under `tests/`); each lane applies its row · evidence: plan §4.1 **UNCLAIMED** **TAKEN 2026-09-24 FORK-CODE** · VERDICT 2026-09-24 (lane FORK-CODE): `backfill_instance_profile_ids` DELETED with its six tests (the one §4.1 **delete** row). The other rows stay: §4.1 names each a TEST SEAM to move under `tests/<pkg>/_seams.py` (or keep / NOT dead: `ORPHAN_ACTOR_REASONS`, `_runtime_office_get`), and a named seam is a move for the god-file refactor lane that owns its file, not a delete — per this lane's decision rule they are DESIGN, claim kept

## Filed on arrival — 2026-09-21 (brain)


## Filed on arrival — 2026-09-24 (lane CARRY2B)


## Filed on arrival — 2026-09-24 (lane REDS2)


## Filed on arrival — 2026-09-24 (lane CARRY3)


## Filed on arrival — 2026-09-24 (lane TESTS)

- [ ] **`tests/test_live_system_guard_self_test.py::test_os_kill_blocks_foreign_pid` / `::test_pty_spawn_systemctl_blocked` and `tests/test_live_system_guard_self_test_downstream.py::test_gateway_lookalike_marker_allows_only_gateway_shape` are red on `fa12c6c5a3`** · `fork / suite` · the fence that refuses killing foreign pids may not hold; red with and without lane TESTS' classifier change · evidence: lane TESTS `.lane-logs/r11_base.log` (DID NOT RAISE RuntimeError) · filed by lane TESTS 2026-09-24 **UNCLAIMED** **TAKEN 2026-09-25 lane Q-GATES** · VERDICT 2026-09-25 lane Q-GATES: DESIGN — the downstream half is DONE (the lookalike + pass-through tests spawned `python -c pass hermes …`, inert since 10aa344964; they now spawn an absent `hermes` and prove pass-through by FileNotFoundError, with a refusal control). The two upstream tests are Windows-premise reds, not a fence failure: FOREIGN_PID=1 does not exist on Windows so `_is_own_subtree` allows it as stale and real kill raises WinError 87; `import pty` needs `termios`. Remaining: id-mark both in `tests/_downstream/id_markers.py` as `_up_red` (POSIX premise).
- [ ] **`tests/hermes_cli/test_update_launchd_restart_verification.py::TestInvokingProfileIsVerifiedLikeItsSiblings::test_restart_that_leaves_the_old_pid_supervised_is_not_a_restart` is red when run in one process with 12 other hermes_cli files (fake clock slept 768413 s against a 20 s budget) and green alone** · `fork / suite` · a co-member leaves the fake clock or `LAUNCHD_SUPERVISION_VERIFY_TIMEOUT` patched; bisect pairwise (REDS2 method, `016a6cd71e`) · evidence: lane TESTS `.lane-logs/r10b.log` · filed by lane TESTS 2026-09-24 **UNCLAIMED**

## Filed on arrival — 2026-09-24 (lane REDS3)

- [ ] **`test_tui_gateway_server.py::test_notification_poller_live_loop_requeues_foreign_completion_for_owner` is order/timing-flaky: a stray `session.info` frame (this worktree's cwd and branch) reaches its captured writes** · `fork / suite` · red once in a five-file run on `6251144d09`, green alone and green on the next four-file run; find the emitter that outlives its test (a session-info publish from an earlier test's thread) and join or drain it in teardown · evidence: lane REDS3 `.lane-logs/base.log` (`assert [('session.info', 'msg-id-trunc-sid', {...'branch': 'fork/main-reds-2026-09-24'...})] == []`) **UNCLAIMED** **TAKEN 2026-09-24 TESTS** · VERDICT 2026-09-24 (lane TESTS): DESIGN — the only test naming `msg-id-trunc-sid` is `test_prompt_submit_truncates_by_message_id` (upstream bytes), which starts a daemon `_run_after_agent_ready` thread it never joins; but run alone it calls `server._session_info` zero times (traced) and the pair emitter→victim stayed green under a 0.1–1.5 s delay mutation in `_session_info`, so the stray needs state an earlier co-member leaves (the frame's `branch` key points at a git-meta/cwd publish). Next: capture the stray's stack in the failing five-file run (`traceback` in a patched `_emit`), then join or drain that emitter in a fork teardown; no mechanism landed without a red
- [ ] **`test_gateway_foreign_xdg_runtime.py::TestPreflightForeignRuntimeNoLeak::test_foreign_xdg_runtime_dir_raises_unavailable_not_permission_error` is red in its bundle (`module 'os' has no attribute 'getuid'`) and green alone** · `fork / suite` · an earlier bundle member leaves the gateway preflight on its POSIX branch; bisect the bundle (the REDS2 pairwise method, `016a6cd71e`), then fix the leak or move the file to `scripts/test_bundles_unbundled.txt` with the observed pair · evidence: wave-close gate on `6251144d09` (gate-wave2.log, now removed with `X:/wt/h-gate2`), lane REDS3 run alone: XPASS(strict) **UNCLAIMED** **TAKEN 2026-09-24 TESTS** · VERDICT 2026-09-24 (lane TESTS): DESIGN — not reproduced: alone 4 passed / 6 xfailed; its full-scope bundle (20 files, `test_flow_commands`..`test_gateway_multiplex_lifecycle`, xdg at position 7 under the gate-time unbundled list) run as one process: the test green (1 unrelated red, `test_gateway_home_receipt.py::test_sticky_marker_answers_when_the_env_is_not_profile_shaped`); the 25 files sorted before it: green. No observed pair, so no unbundled entry; the next gate that reds it must log its bundle's member list

## Filed on arrival — 2026-09-24 (lane CLOSE)


## Filed on arrival — 2026-09-24 (program-end gate on `c7652475cd`)

- [ ] **`tests/hermes_cli/test_minimax_oauth.py::test_refresh_error_body_bounded_and_readable_with_real_client` is red in its bundle and green on the rerun alone** · `fork / suite` · a co-member leaves a client or transport patched; bisect the bundle pairwise (REDS2 method, `016a6cd71e`) and fix the leak or add the observed pair to `scripts/test_bundles_unbundled.txt` · evidence: `X:/wt/_holds/gate-final-2026-09-24.log` ("red in bundle" line) **UNCLAIMED** **TAKEN 2026-09-24 TESTS** · VERDICT 2026-09-24 (lane TESTS): DESIGN — not a co-member leak: the gate's exact bundle (#56 recomputed from `c7652475cd`'s unbundled list, minimax at position 5 = "after 5 earlier members") run as one process: 236 passed, 1 skipped. The test streams a 64 KB 400 body over a REAL loopback socket and reads it bounded while the client is open; its one red was under 8 workers — treat as load-timing on the real socket (read the stream to EOF or shrink the body below one socket buffer), not an unbundled pair

## Filed on arrival — 2026-09-24 (lane LEDGER-DOCS)

- [ ] **The empty-WAL core-cache fixture unlinks a file SQLite 3.53.1 already removed during setup.** · `fork / tests` · `tests/agent_runtime/test_core_fingerprint_cache.py::test_a_key_written_with_an_empty_wal_matches_once_it_is_gone` fails on unchanged `2455c606b4`; establish the intended present-WAL precondition without relying on SQLite retaining an unused file · evidence: `docs/downstream/acp-named-provider-identity-2026-09-24.md` § Installation qualification. **UNCLAIMED**
- [ ] **Stage 6 desktop's owner-ruling DONE heading fails the landed-commit citation gate.** · `fork / docs` · `tests/test_docket_stage_claims.py` fails unchanged on `2455c606b4`; distinguish owner disposition from a shipped-code claim in `docs/agent-runtime-harness/planned/harness-plugin-and-upstream-seams.md` without inventing a landing · evidence: `docs/downstream/acp-named-provider-identity-2026-09-24.md` § Installation qualification. **UNCLAIMED**

- [ ] **Fork CI on `main` runs on every push again but has not been green once since it resumed** · `fork / ci` · `gh run list --branch main --workflow ci.yaml`: 68 runs since 2026-09-15, 38 failure · 28 cancelled · 0 success (red jobs on run 35992190948: Python tests, ruff enforcement, Windows footguns `/tmp` baseline, windows_only tests); `fork-gates.yml` red on every push, `Changed-line mutation claims` → `mutation-check configuration error: cxr1-codex-readiness-asks-the-pool-alone: mutation source not found in agent_runtime/profile_readiness` (run 36062192326) · classify each red job as fork-caused / upstream-caused / runner-environmental before fixing · filed by lane LEDGER-DOCS 2026-09-24 **UNCLAIMED**

## Filed on arrival — 2026-09-24 (lane DOORS-A)

- [ ] **`tests/tools/test_tirith_config_call_sites.py::TestApprovalCronLane::test_fail_open_override_reaches_the_cron_lane` passes alone but fails in a combined gateway/tui run, identically on `origin/main`: order-dependent** · `fork / tests` · bisect pairwise (REDS2 method, `016a6cd71e`); fix the leak or add the observed pair to `scripts/test_bundles_unbundled.txt` · evidence: lane DOORS-A report, `X:/wt/_holds/seam-s1-green-2026-09-23.md` § DOORS-A **UNCLAIMED**

## Filed on arrival — 2026-09-24 (lane W0)

- [ ] **`tests/scripts/install/*`: 16 reds on the Windows test host (install.sh stage tests — commit-pin, lockfile churn, unmerged index, termux bounds…), seen by lanes CLOSE and W0, untouched by either, not id-marked** · `fork / tests` · classify on base (host `charmap` decode vs a missing `encoding="utf-8"` = the check-windows-footguns class) and mark by id or fix · evidence: lane CLOSE + W0 reports, `X:/wt/_holds/seam-s1-green-2026-09-23.md` **UNCLAIMED**

## Filed on arrival — 2026-09-24 (lane MERGE)

- [ ] **Next weekly merge: upstream `27df3b8847` ("bundles & unified installer", 3,390 files, first on `upstream/main` after `749220ef00`) conflicts in 54 files / 73 hunks and deletes `agent/ssl_guard.py` (truststore) and `hermes_cli/dep_ensure.py` (PM), which `hermes_cli/harness_parts/serve.py` (`verify_ca_bundle`) and `hermes_cli/_downstream_cli.py` (postinstall) import** · `fork / upstream-sync` · design lane first: re-seat serve's CA check on `agent/ssl_verify` and postinstall on upstream's PM (adopt, rule 9), retire the `ssl_guard` row and PR #121647 (`up/ssl-ca-memo`), then merge; sized by `git merge-tree --write-tree` on `merge/upstream-2026-09-24`.
- [ ] **Four fork gates red on `origin/main` `f6cb75f2de`, byte-identical inputs after the merge: `test_commands.py::TestSlackNativeSlashes::test_telegram_parity` XPASS(strict) (DOORS-A put `hermes_cli/commands.py` at upstream bytes, '/platform' fits Slack's cap again), `test_no_kanban_dependency` (DOORS-A `kanban_blocked_pm_tick.py` imports `hermes_cli.kanban_blocked_pm`), `test_dispatch_delivery_observability::test_an_event_upstream_rejects_is_still_named` (`considered` 1 != 0), `tests/scripts/test_doc_cite_adjacency.py` (2 unwaived cites: 04-boot-and-lifecycle.md:112, 05-chat-turn-lane.md:466)** · `fork / tests` · per test: retire the telegram fence + banner, rule the kanban import (owner), re-read the ghost-drop tally, re-anchor the two cites.
- [ ] **`tests/hermes_cli/test_process_dock.py::test_monitor_controls_stop_processes_and_never_steer_them` hangs to the pytest-timeout in `process_registry._release_finished_handles` → `stream.close()` under `scripts/run_tests_bundled.sh` (alone too, 108 s, identically on `f6cb75f2de`) but passes in 2 s under plain pytest, with or without PYTHONUTF8=1** · `fork / tests` · bisect the runner's `env -i` environment for the variable that makes the pipe close block.

## Filed on arrival — 2026-09-24 (lane H1)
## Filed on arrival — 2026-09-25 (lane R4)

- [ ] **`[up-fp]` ROSE on main and the fixture was not raised with a reason** · `fork / footprint` · fixture 195 files / 990 deleted lines; 196 / 994 at `627f5ea4fa`, 200 / 999 at `09d722bb30` — the provider/auth commits `98f8a8caf9`, `38f9345633`, `09d722bb30` edit eight upstream files (`hermes_cli/auth*.py`, `provider_catalog.py`, `subcommands/auth.py`), so `tests/scripts/test_upstream_footprint.py` is red on main for every lane behind them. Owner call: raise `tests/fixtures/upstream_footprint.json` with a `reasons` row per file, or move the edits behind the plugin surface · evidence: `X:/wt/_holds/main-reds-check-2026-09-25.log` · filed by orchestrator 2026-09-25 **UNCLAIMED**
- [ ] **`tests/tooling/test_fork_import_layers.py::test_declared_layers_are_bound_at_runtime_and_point_down` is red on main because the test venv has no `acp`** · `fork / test env` · `agent_runtime/acp_skills.py:6` imports `acp` (`agent-client-protocol==0.9.0`, the `acp` extra in `pyproject.toml`), the gate imports every `__layer__` module, and `C:/Users/beast/.venvs/hermes-test` was resolved before that extra existed; the fix is the owed venv dependency re-sync (`pip install -e .[acp]`), and the gate should name the missing extra rather than a bare `ModuleNotFoundError` · evidence: `X:/wt/_holds/main-reds-check-2026-09-25.log` · filed by orchestrator 2026-09-25 **UNCLAIMED** **TAKEN 2026-09-25 lane Q-GATES** · VERDICT 2026-09-25 lane Q-GATES: DESIGN (owner: venv) — the gate half is DONE in this commit: the test now collects ModuleNotFoundError per declared module, still checks every importable module's layers, then reds NAMING the extra (`agent_runtime.acp_skills: No module named 'acp' (the `acp` extra: re-sync with `pip install -e .[acp]`)`). Owner runs: `C:/Users/beast/.venvs/hermes-test/Scripts/python.exe -m pip install -e ".[acp]"` in the hermes checkout; the row closes when the gate is green.

## Filed on arrival — 2026-09-25 (lane S2B)

- [ ] **dead-code census rows carry no OWN-FILE count — 6 of 13 rows on the batch-2b files are refuted by an in-file call or a production caller** · `fork / refactor` · `_rebalance_column`@847, `_guard_turn_state_vocabulary`@351, `_get_executor`@1088, `_kill_child`@1008, `dispatch_detached_turn` (agent_chat_tool), `mission_chat_compaction_threshold_tokens` (profile_runner/execute:507) · evidence: sheets §5 · filed by lane S2B 2026-09-25 **UNCLAIMED**

## Filed on arrival — 2026-09-25 (lane S2A)


## Filed on arrival — 2026-09-25 (lane 2B-A)

- [ ] **`test_pruning_never_deletes_an_undelivered_answer` passes whether or not its `_MAX_RETAINED_TERMINAL` seam binds** · `fork / suite` · reverting its `setattr` to the package (a no-op) stays green: the negative assertion ("nothing pending was deleted") holds at the real cap of 200 because the fall-through is never reached — its docstring says the fixture is "shaped so the fall-through is REACHED" but nothing checks it; wants a positive control (the delivered rows ARE pruned at cap 1) · evidence: lane 2B-A dispatch_store MOVE body · filed by lane 2B-A 2026-09-25 **UNCLAIMED**

## Filed on arrival — 2026-09-25 (lane Q-DEAD-A)

- [ ] **The reach census chooses its traced suite by SUBSTRING, so a test spelling `from scripts import <module>` is never traced — `tests/scripts/` calls `_claims_for` nine times and the census still read it 0 hits** · `fork / refactor` · select the suite from resolved imports (the AST import graph `god_file_probe.imports_of` already walks), not tokens; until then a script's tested functions read cold · evidence: `docs/agent-runtime-harness/planned/downstream-god-file-refactor-reach-census.md` ("Re-run 2026-09-25") · filed by lane Q-DEAD-B 2026-09-25 **UNCLAIMED**
## Filed on arrival — 2026-09-25 (lane B2)

- [ ] **`test_every_stage42_global_flag_is_honored` is a source walk keyed on the name `args`: a flag read through `self.args` or a renamed parameter reads as unhonoured, so phase objects must read flags in the handler** · `fork / gates` · a source walk certifying a POSITIVE guarantee ("this flag is honoured") by spelling — the fork CLAUDE.md gate rule says ask the runtime or the element model; re-key it on the parser's `set_defaults` targets and the attribute reads they reach · evidence: B2 gateway_commands CHANGE body · filed by lane B2 2026-09-25 **UNCLAIMED**

## Filed on arrival — 2026-09-25 (lane Q-GATES)

- [ ] **The live-system guard's refusal tests spawn the REAL command when the refusal list regresses** · `fork / tests` · dropping `serve` from `_BACKEND_SUBCOMMANDS` (tests/conftest.py) made `test_subprocess_run_hermes_serve_blocked` start a live `hermes serve --port 8090` from the operator runtime; point every blocked-spawn case in `tests/test_live_system_guard_self_test_downstream.py` at an absent `hermes` path (the pattern its pass-through cases now use) so a regression reds with FileNotFoundError instead of booting a backend · evidence: lane Q-GATES commit `test(guard): lookalike and pass-through …` body · filed by lane Q-GATES 2026-09-25
## Filed on arrival — 2026-09-25 (lane S3)

- [ ] **tools_conftest.py's known-defect lane runs the pre-ML-16 classifier (`failed` only, no owner-prefix filter) — a strict xfail in tests/tools would retire its own banner** · `fork / suite` · fold onto `_env_gap_fence.KnownDefectTracker` (lane B5 creates it) with the xfail control · evidence: god-file-layout-sheets/hermes_cli_conftest.md §2 row 3 · filed by lane S3 2026-09-25 **UNCLAIMED**
- [ ] **tests/test_env_gap_registry.py::_registry_module_path reads a fence only as a flat FILE; a packaged fence falls back to the upstream conftest and every check SKIPS (green by blindness)** · `fork / suite` · the `__init__.py` arm + the "directory is found" control land in lane B5's MOVE, before the conftest package exists · evidence: hermes_cli_conftest.md §6.2 · filed by lane S3 2026-09-25 **UNCLAIMED**

## Filed on arrival — 2026-09-25 (lane B3)

- [ ] **`tests/agent_runtime/test_snapshot_contract_version_authority.py` is red on `origin/main` (2 tests): its lane exemption is keyed on the BASENAME `contract.py`, and `agent_runtime/discussions/contract.py` + `agent_runtime/persona_instance_sync/contract.py` (lane 2B-C) are two** · `fork / gates` · key the exemption on a path (the test's own advice) — a basename key exempts every future `contract.py` the god-file lanes create; lane B3 named its module `conversation.py` to stay out of it · evidence: lane B3 operator_channels CHANGE body · filed by lane B3 2026-09-25 **UNCLAIMED**

## Filed on arrival — 2026-09-25 (lane W3-A)

- [ ] **22 grandfathered silent package-attribute monkeypatches (10 on `agent_runtime.config`, 7 on `agent_runtime.snapshot`, 2 on `persona_assignments`, 1 each on `agent_create` / `gateway_peers` / `persona_chat_continuity`) — re-point each to the module that looks the name up and delete its row** · `fork / suite` · evidence: `tests/fixtures/silent_package_patches_grandfathered.json`, lane W3-A commit 33d3ca26d5 · filed by lane W3-A 2026-09-25 **UNCLAIMED**
- [ ] **The shared test venv lacks the `acp` extra, so W0-G6's runtime test reds on its LAST assertion (`agent_runtime.acp_skills: No module named 'acp'`) with the layer assertion green — `tests/tooling` is 1 failed / 46 passed on that alone** · `fork / suite` · re-sync the extra the gate names (`pip install -e .[acp]`), or make the venv recipe carry it · evidence: lane W3-A commit f3a1b7b418 body · filed by lane W3-A 2026-09-25 **UNCLAIMED**
- [ ] **`prompt_observability`'s skills-catalog concept stays split across `catalog_store` (48 code lines) and `catalog_lookup` (73) because `context_store` sits between them — it writes through one and is read by the other; passing the catalog writer into `context_store`'s persist would let the two fold into one module** · `fork / refactor` · evidence: lane W3-A fold-review commit 14726ae2a8 · filed by lane W3-A 2026-09-25 **UNCLAIMED**
