---
type: program
program: <slug>
status: active
cursor: "<YYYY-MM-DD — one-line current state: what last landed (sha), what is owed>"
tags: [program/<slug>, program]
---

# <Program Name>

<One paragraph: the capability or substrate this program owns, in the fork's terms.>

> [!info] Cursor
> `cursor::` see frontmatter.

## Where the truth lives

- Canon: [`docs/agent-runtime-harness/<NN>-<domain>.md`](../../docs/agent-runtime-harness/<NN>-<domain>.md) — read first.
- Plans: `docs/agent-runtime-harness/planned/<plan>.md` + its field notes.
- Launcher half (if any): `EterniaLauncher/docs/mission_control/<doc>.md`.
- Queue: the launcher's `mission-control-queue.md` (runtime) or [[fork-hygiene-queue]] (repo).

## Source

- `agent_runtime/<modules>` · `hermes_cli/harness_parts/<part>` · tests under `tests/agent_runtime/<…>`.

## Open programs / what is owed

| item | plan | state |
|---|---|---|
| | | |

## Facts an agent forgets

- <the one thing every session re-learns about this program>
