# In-turn tools — the full inventory

GENERATED by `scripts/emit_harness_tool_inventory.py` from the live tool registry. Do not edit by hand; run the emitter with `--write`.

`harness_core` = 15 member toolsets, **44 callable tools**, ~1190 model tool tokens (`tool_name_envelope_v1` heuristic, not a provider bill). This is what every Eternia persona's harness lane resolves unless its profile declares something else — see canon `05-chat-turn-lane.md` §4c.

`gated` means the tool carries a `check_fn` (desktop/service availability), so it may be absent on a given box. `mutating` means it crosses the mutation boundary (`tool_permissions.READ_ONLY_BLOCKS`).

| tool | toolset | mutating | gated | description |
|---|---|---|---|---|
| `agent_chat_dispatches` | `agent_chat` | — | — | List your in-flight and recent background dispatches (read-only). |
| `agent_chat_installs` | `agent_chat` | — | — | List the other installs (machines) paired with this one, or one install's agent roster (read-only, no mint). |
| `agent_chat_log_path` | `agent_chat` | — | — | Get the file path of a teammate thread's live transcript log to grep/tail the full history (read-only, no mint). |
| `agent_chat_open` | `agent_chat` | — | — | Review the recent message tail of your shared thread with a teammate (read-only, no mint). |
| `agent_chat_send` | `agent_chat` | — | — | Send a chat message to another Harness persona and return their reply, or dispatch it in the background with wait=false (agent-to-agent chat). |
| `agent_chat_threads` | `agent_chat` | — | — | List your agent-to-agent chat threads with reachable teammates (read-only, no mint). |
| `board_card_add` | `board` | — | — | Add a planning card to the Mission Board (advisory; never mutates a goal). |
| `board_cards` | `board` | — | — | List active Mission Board cards for the current workspace. |
| `browser_back` | `browser` | — | yes | Navigate back to the previous page in browser history. Requires browser_navigate to be called first. |
| `browser_cdp` | `browser-cdp` | — | yes | Send a raw Chrome DevTools Protocol (CDP) command. Escape hatch for browser operations not covered by browser_navigate, browser_click, browser_console, etc.... |
| `browser_click` | `browser` | — | yes | Click on an element identified by its ref ID from the snapshot (e.g., '@e5'). The ref IDs are shown in square brackets in the snapshot output. Requires brows... |
| `browser_console` | `browser` | — | yes | Get browser console output and JavaScript errors from the current page. Returns console.log/warn/error/info messages and uncaught JS exceptions. Use this to... |
| `browser_dialog` | `browser-cdp` | — | yes | Respond to a native JavaScript dialog (alert / confirm / prompt / beforeunload) that is currently blocking the page. **Workflow:** call ``browser_snapshot``... |
| `browser_get_images` | `browser` | — | yes | Get a list of all images on the current page with their URLs and alt text. Useful for finding images to analyze with the vision tool. Requires browser_naviga... |
| `browser_navigate` | `browser` | — | yes | Navigate to a URL in the browser. Initializes the session and loads the page. Must be called before other browser tools. For simple information retrieval, pr... |
| `browser_press` | `browser` | — | yes | Press a keyboard key. Useful for submitting forms (Enter), navigating (Tab), or keyboard shortcuts. Requires browser_navigate to be called first. |
| `browser_scroll` | `browser` | — | yes | Scroll the page in a direction. Use this to reveal more content that may be below or above the current viewport. Requires browser_navigate to be called first. |
| `browser_snapshot` | `browser` | — | yes | Get a text-based snapshot of the current page's accessibility tree. Returns interactive elements with ref IDs (like @e1, @e2) for browser_click and browser_t... |
| `browser_type` | `browser` | — | yes | Type text into an input field identified by its ref ID. Clears the field first, then types the new text. Requires browser_navigate and browser_snapshot to be... |
| `browser_vault_enter_code` | `browser` | — | yes | The page asks for a one-time / verification / 2FA code after the password: call this. If the saved login has an authenticator key the code is generated and e... |
| `browser_vault_fill` | `browser` | — | yes | Fill the CURRENT browser page from a vault handle (see browser_vault_list): a login item fills ONLY the password field (type the identifier/username yourself... |
| `browser_vault_list` | `browser` | — | yes | ALWAYS call this first when a page asks for a password, card or address. Lists saved website logins, payment cards and addresses as handles with metadata (ki... |
| `browser_vault_save_login` | `browser` | — | yes | The current page is a login form and browser_vault_list has no item for its origin: ask the user, through a masked prompt in their UI, to save the login for... |
| `browser_vault_unlock` | `browser` | — | yes | Ask the user to unlock a password manager (1Password or Bitwarden) for this session. The master password is typed into a masked prompt owned by the UI and ne... |
| `browser_vision` | `browser` | — | yes | Take a screenshot of the current page so you can inspect it visually. Use this when you need to understand what the page looks like - especially for CAPTCHAs... |
| `clarify` | `clarify` | — | yes | Ask the user one or more questions when you need a decision, clarification, or feedback before proceeding. Pass every question in `questions` (1-5 entries) —... |
| `delegate_task` | `delegation` | — | yes | Spawn one or more subagents in isolated contexts. Description is rebuilt at every get_definitions() call to reflect the user's current delegation limits. |
| `execute_code` | `code_execution` | — | yes | Run Python that calls Hermes tools programmatically. Use when you need 3+ tool calls with logic between them: filtering/reducing large outputs before they en... |
| `memory` | `memory` | — | yes | Save durable facts to persistent memory that survive across sessions. Memory is injected into every future turn, so keep entries compact and high-signal. HOW... |
| `patch` | `file` | yes | yes | Targeted find-and-replace edits in files. Use this instead of sed/awk in terminal. Uses fuzzy matching (9 strategies) so minor whitespace/indentation differe... |
| `process_manage` | `terminal` | — | — | Poll, wait on, or kill background terminal processes (from terminal(background=true)). Completed results remain retrievable by session_id when resuming their... |
| `read_file` | `file` | — | yes | Read a text file with line numbers and pagination. Use this instead of cat/head/tail in terminal. Output format: 'LINE_NUM\|CONTENT'. Suggests similar filenam... |
| `search_files` | `file` | — | yes | Search file contents or find files by name. Use this instead of grep/rg/find/ls in terminal. Ripgrep-backed, faster than shell equivalents. On macOS, broad s... |
| `session_search` | `session_search` | — | yes | Recall past conversations: search or read old Hermes sessions (FTS5), or scroll inside one. Four shapes, picked by args: `query` = discovery (top-N matching... |
| `skill_manage` | `skills` | — | — | Create, update, or delete skills — your procedural memory for recurring task types. The call is an operations array (a single edit is a list of one); it appl... |
| `skill_search` | `skills` | — | yes | Search installed skills + the Hermes Skills Hub by query without loading SKILL.md bodies (compact ids/descriptions). Disambiguator: skill_view loads an insta... |
| `skill_view` | `skills` | — | yes | Skills allow for loading information about specific tasks and workflows, as well as scripts and templates. Load a skill's full content or access its linked f... |
| `skills_list` | `skills` | — | yes | List available skills (name + description). Use skill_view(name) to load full content. |
| `terminal` | `terminal` | yes | yes | Run shell commands; cwd/exported env persist. Use background=true with notify=true for long tasks; pty=true for interactive CLIs. Prefer file tools for file... |
| `todo_list` | `todo` | — | yes | Track a task list for multi-step work (3+ steps). Use for complex tasks with 3+ steps or when the user provides multiple tasks. For 'all N items' tasks, enum... |
| `vision_analyze` | `vision` | — | yes | Load an image into the conversation so you can see it. Call it any time the user references an image — then answer from what you see. |
| `web_extract` | `web` | — | yes | Extract content from web page URLs. Returns clean page content in markdown/text (no LLM summarization — fast). Also works with PDF URLs (arxiv papers, docume... |
| `web_search` | `web` | — | yes | Search the web for information. Returns up to 5 results by default with titles, URLs, and descriptions. The query is passed through to the configured backend... |
| `write_file` | `file` | yes | yes | Write content to a file, completely replacing existing content. Use this instead of echo/cat heredoc in terminal. Creates parent directories automatically. O... |

## CLI-only verbs

Operate-table rows with no in-turn tool. Reaching for the terminal for anything NOT on this list is a navigation failure worth reporting.

- `hermes harness persona instance open-chat`
- `hermes harness mission-chat steer`
- `hermes harness mission-chat turn-resolve`
- `hermes harness mission-chat queue-skill`
- `hermes harness persona instance return-summary`
- `hermes harness persona instance steer`
- `hermes harness flow set`
