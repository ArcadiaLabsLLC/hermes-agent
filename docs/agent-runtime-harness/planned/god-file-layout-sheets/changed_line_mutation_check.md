# Layout sheet — `scripts/changed_line_mutation_check.py` (lane S1 · exec lane B5)

Base: `main` @ `8411d26a01` · 1,150 raw / 831 code / 31 top-level defs · longest `run` 173 (depth 4 — the W0-G7 row) · chains 2/1 (the `_qualified_definitions.walk` `isinstance` ladder, 4 arms — the W0-G5 row `scripts/changed_line_mutation_check.py|_qualified_definitions.walk|isinstance|child`) · `str==` 4 · `isinstance` 18 · sha256 `5f1a9227586b6756421e93ccca87043bb6130f1a2ce120c88ec420f7c5aa5139` · owner doc `tool/test_quality/README.md` and `docs/downstream-development.md` § the mutation gate. **Run by every landing** and by CI: `.github/workflows/fork-gates.yml:41` (`--base "$BASE" --list`, then `grep -q '^mutation candidates: 0 '` to decide whether to install the environment at all) and `:103` (`--base … --wall-budget-seconds 600`); `scripts/unattended_suite_run.ps1:188` (`--list --base origin/main`); `CLAUDE.md:108`; `docs/downstream-development.md:158`. Reads `tests/mutation_claims.json` (`DEFAULT_CLAIMS`) and `tool/test_quality/mutation_exemptions.yaml`. Imported as `from scripts import changed_line_mutation_check as gate` by nine test files (`tests/scripts/{conftest,test_changed_line_mutation_check,test_mutation_claim_anchoring,test_mutation_claim_platforms,test_mutation_claim_preflight,test_mutation_claims_still_anchor,test_mutation_selection_follows_the_symbol,test_mutation_cap_matches_workflow}.py`, `tests/test_mutation_gate_worktree_lock.py`) — 30 `monkeypatch.setattr` sites on the module (§6.4 lists them by destination). **Six claims anchor in the file itself** (`--claims-for scripts/changed_line_mutation_check.py`, `.lane-logs/claims_for_gate.log`): `s4b-…` and `hh14-the-mutant-is-spliced-at-the-first-occurrence-again` on `run`, `hh14-the-anchor-escapes-…` and `hh14-a-stale-symbol-…` on `_symbol_span`, `hh14-a-re-indented-block-…` on `_candidate_offsets`, `w7-the-definition-walk-…` on `_qualified_definitions` — the gate mutates its own source, and their `path` re-points in the MOVE.

**The CLI contract is byte-fixed, and this is the dump that proves it.** The lines CI and the tests read: `mutation candidates: N (reported, not capped; wall budget Ns)` as LINE 1 with the trailing space after `N` (`fork-gates.yml:44`; `test_changed_line_mutation_check.py:118, 149, 213, 271`; `test_mutation_claim_anchoring.py:482`; `test_mutation_claim_platforms.py:98, 167`; `test_mutation_selection_follows_the_symbol.py:133, 207, 238`); `  <id>: <path>::<symbol> [<operator>]` with ` (selected by symbol)`; `changed production sources: N (M carry no registered claim)` + `  NO CLAIM ANCHORS HERE: <path>`; `RE-ANCHORED: …`; `WARNING: stale derivation: …`; `UNSELECTED (0 changed lines): <id>` (only under `--list`); `SKIPPED (platform): …`; `BASELINE: …`; `MUTATE: <id>` / `KILLED: <id>`; `SURVIVED: a, b` on stderr with exit 1; `baseline failed; mutation result would be meaningless`, `wall budget exhausted: …`, the lock refusal block and `mutation-check configuration error: …` on stderr with exit 2; `claims anchored at <q>: N` + rows or `(nothing anchored there — …)`; `--base` required unless `--claims-for` (argparse, exit 2); `--wall-budget-seconds` read off the workflow by `test_mutation_cap_matches_workflow.py`. **The dump:** before the MOVE the lane captures three commands to `.lane-logs/cli_contract_before.log` — `--list --base <B>`, `--claims-for scripts/changed_line_mutation_check.py`, `--help` — choosing `<B>` so the list shows EVERY line class (a candidate selected by lines, one by symbol, an `UNSELECTED` row, a `NO CLAIM ANCHORS HERE` row, a `SKIPPED (platform)` row; the body names `<B>` and which lines it exercised — a dump that shows no `UNSELECTED` line proves nothing about it); after the MOVE the same three, `cmp` exit 0, pasted in the MOVE body. The nine test files are the standing pins.

**Package `scripts/mutation_check/`** (§3.2 said `{claims,partition,run,report}`; refolded under the floor: `report` would be ~40 code lines of print blocks that only make sense beside `run`, and `claims` without the schema constants is ~50). `scripts/` is a namespace package (no `__init__.py`), so `from scripts import changed_line_mutation_check` and `from scripts.mutation_check import run` both resolve; the package gets its own `__init__` (the map). The ENTRY file keeps its path and name — CI, the ps1, `CLAUDE.md` and the nine tests name it.

## 1. Skeleton (owner rulings 2026-09-25) — this text IS the package `__init__` map

```
scripts/changed_line_mutation_check.py   wiring   the ENTRY (path unchanged): argparse `main` (1073–1146) + `if __name__`; re-exports every name the tests READ (run, main, _anchor_claim, _claims_for, _qualified_definitions, _acquire_gate_lock, REPO_ROOT, DEFAULT_CLAIMS, HUNK, DERIVED_AT_KEY, LOCK_PATH) — names the tests PATCH are retargeted to the module that reads them (§6.4)
scripts/mutation_check/
  __init__.py    wiring   the map; the relocated measurements (the S5 zero, the W1-H3 counts, the H-H2 miss, the locks.py platform fork) at its foot
  schema.py      models   VOCABULARY/TABLE module (floor-exempt): REPO_ROOT, DEFAULT_CLAIMS, DEFAULT_EXEMPTIONS, LOCK_PATH, HUNK, DEFAULT_WALL_BUDGET_SECONDS, WHOLE_MODULE_SYMBOLS, BLOCK_STATEMENTS, PLATFORMS, MAX_REINDENT_COLUMNS, DERIVED_AT_KEY, ANCHOR_KEY, SELECTION_KEY, NON_PRODUCTION_PREFIXES, ClaimAnchor; after the CHANGE, SELECTED_BY_LINES / SELECTED_BY_SYMBOL and EXIT_SURVIVED / EXIT_REFUSED
  anchor.py      policy   pure text + AST: _qualified_definitions, _line_offsets, _is_whole_module, _symbol_span, _reindent, _candidate_offsets, _anchor_claim, _reanchor_hint, _read_source, _anchor_or_raise; after the CHANGE, the _WALK table
  selection.py   stores   the claims file and git: _load_json, _validate_exemptions, _current_platform, _declared_platforms, _changed_sources, _changed_lines, _partition_claims, _commits_since_derivation, _symbol_head, _path_matches, _symbol_matches; after the CHANGE, _git
  run.py         lanes    the two verbs: _claims_for (the inventory lane) and run (the mutating lane) with _command, _run_command, _acquire_gate_lock, _refuse_because_locked, _over_budget, _refuse_over_budget; after the CHANGE, run = _report + _runnable + _mutate
```
Entry points and the modules an agent opens to follow each: a landing's `--list --base` → the entry → `run.py` → `selection.py` (→ `anchor.py` for the resolution; `schema` is the table both read) — **3**; `--claims-for` → the entry → `run.py` → `selection.py` (the matchers) — 2; a mutating run → the entry → `run.py` → `anchor.py` (`_read_source`, the splice) — 3; CI's `^mutation candidates: 0 ` → `run.py` — 1; a test reaching `_anchor_claim` → `anchor.py` — 1. Layers go DOWN: entry → `run` → `selection` → `anchor` → `schema`. No cycle, no lazy import anywhere. W0-G6 does not walk `scripts/` today (Q31); `__layer__` is declared anyway. The entry is ~90 raw lines, not §3.2's "≤ 60": `main`'s own help text (1078–1102) is thirty of them and a `cli.py` would be a sub-100 wiring module with one function — the floor outranks the entry target, and the entry IS the wiring module.

### 1.1 Section map → target modules (line ranges are the base file's; sizes raw / est. code — docstrings count, `#` lines do not)

| lines | what is there | → module | raw / code | layer |
|---|---|---|---|---|
| 1–43 | the module docstring (the in-place-rewrite warning, the census, the budget doctrine) | `__init__.py` (verbatim — it is the map's preamble) | 43 / 40 | wiring |
| 45–58 | imports | regenerated per module (each imports what it uses; `argparse` only in the entry) | — | — |
| 61–170, 182–187, 492–512 | `REPO_ROOT` … `SELECTION_KEY`, `NON_PRODUCTION_PREFIXES`, `ClaimAnchor` | `schema.py` (imports `ast`, `re`, `pathlib`, `dataclasses`) | ~150 / 60 | models |
| 172–180, 190–297, 624–757 | `_load_json`; `_changed_sources`, `_changed_lines`, `_validate_exemptions`, `_current_platform`, `_declared_platforms`; `_partition_claims`, `_commits_since_derivation`, `_symbol_head`, `_path_matches`, `_symbol_matches` | `selection.py` (imports `anchor._anchor_or_raise`, `schema`, `subprocess`, `json`, `datetime.date`) | ~290 / 170 | stores |
| 300–490, 515–621 | `_qualified_definitions`, `_line_offsets`, `_is_whole_module`, `_symbol_span`, `_reindent`, `_candidate_offsets`; `_anchor_claim`, `_reanchor_hint`, `_read_source`, `_anchor_or_raise` | `anchor.py` (imports `schema`, `ast`) | ~300 / 210 | policy |
| 760–1070 | `_claims_for`; `_command`, `_run_command`, `_acquire_gate_lock`, `_refuse_because_locked`, `_over_budget`, `_refuse_over_budget`, `run` | `run.py` (imports `selection`, `anchor._read_source`, `schema`, `subprocess`, `time`, `datetime`) | ~310 / 230 | lanes |
| 1073–1150 | `main`, `if __name__` | the entry file (imports `run.run`, `run._claims_for`, `schema` defaults) | ~90 / 75 | wiring |

Result: 4 modules + the map + the entry; every non-table module 170–230 code lines. The three modules a landing traverses (`run` → `selection` → `anchor`) are the flow bound.

## 2. Routing sites (rule 12)

| site (base line) | shape today | replacement | killing mutation |
|---|---|---|---|
| `_qualified_definitions.walk` 329–347 (the W0-G5 row: `isinstance` ×4 on `child`) | def-kinds → record + descend; `Assign` → record targets; `AnnAssign` → record; `BLOCK_STATEMENTS` → descend with the same prefix | `_WALK: Mapping[type[ast.AST], Callable[[ast.AST, str, Recorder, Walker], None]]` in `anchor.py` — `{ast.FunctionDef: _definition, ast.AsyncFunctionDef: _definition, ast.ClassDef: _definition, ast.Assign: _assign, ast.AnnAssign: _ann_assign, **{block: _descend for block in BLOCK_STATEMENTS}}`, looked up on `type(child)` (a parsed tree never holds a subclass of a node class, so `type()` equals `isinstance` here); a missing key is today's fall-through. `BLOCK_STATEMENTS` (`schema`) stays the vocabulary of block kinds and the table its only reader | delete the block rows → the registered claim `w7-the-definition-walk-stops-descending-into-blocks` (`[skip-block-bodies]`, anchored on this function) reds through its own test; swap `_assign` / `_ann_assign` → §6.1's annotated-constant case reds |
| `run` 898–1070 (the W0-G7 row: 173 lines, depth 4) | one function: the seven print blocks, the platform filter, the budget check, the lock, the baselines, the mutate loop | `run` becomes ≤ 40 lines of orchestration over `_report(claims, unselected, base, wall_budget_seconds, list_only)` (913–991 — the print blocks, in TODAY'S ORDER: the candidates line is line 1 and stays it), `_runnable(claims)` (992–1002), and `_mutate(runnable, started, wall_budget_seconds)` (1020–1061, the loop that runs under the lock; `run` keeps the `try / finally: LOCK_PATH.unlink`) | move the census print above the candidates line → `test_changed_line_mutation_check.py:149` (`lines[0].startswith("mutation candidates: 0 ")`) reds; the byte-equal dump is the second proof |
| `_partition_claims` 678–685 and `run` 918 | the selection reason written as the free strings `"lines"` / `"symbol"` in one module and compared as `== "symbol"` in another function | `SELECTED_BY_LINES = "lines"`, `SELECTED_BY_SYMBOL = "symbol"` in `schema.py` beside `SELECTION_KEY`; `run._report` reads the constant (single-reader rule; no Enum — the word rides the report line CI and humans read) | respell `SELECTED_BY_SYMBOL`'s value → `test_mutation_selection_follows_the_symbol.py:133`'s ` (selected by symbol)` pin reds |
| exit codes 866, 895, 1029, 1069, 1133, 1146 | `return 2` × 4, `return 1`, spelled as literals | `EXIT_SURVIVED = 1`, `EXIT_REFUSED = 2` in `schema.py`; the entry's `main` and `run.py` read them | — (`test_mutation_gate_worktree_lock.py` pins `== 2` on the lock refusal; the constants are a spelling, the pins stay literal on purpose) |
| `_candidate_offsets` 447–489 | verbatim at shift 0, else line-start at the smallest explaining shift | a two-lane SEARCH, not a ladder; guarded by `hh14-a-re-indented-block-goes-unanchored`; moved, not touched | — |

`isinstance` 18 → 14: the four in the walk go; the rest are BOUNDARY validations of the claim schema (`_partition_claims`, `_declared_platforms`, `_command`, `_load_json`) and stay.

## 3. Helper folds

| here | owner / duplicate | verdict |
|---|---|---|
| `_load_json` 172 | `agent_runtime/serde.read_json` | **NOT a fold** — the selector step runs in CI BEFORE the repository environment is installed (`fork-gates.yml:38–41`; the comment at 253–254 says so for the exemptions), so nothing in this package may import `agent_runtime`. Named so no lane folds it |
| `_acquire_gate_lock` 821 (`open(..., "x")`, no liveness probe) | `agent_runtime/file_locks` | NOT a fold — the same dependency-free rule, and this lock's contract is deliberately "no `os.kill(pid, 0)`" (Windows kills), which the runtime's locks do not share |
| `_read_source` 590 (CRLF → LF normal form, raw bytes kept for the restore) | `scripts/god_file_scope.source()` (`errors="replace"`, no newline fold) | NOT a fold — a different contract (the probe measures, this one splices and restores bytes) |
| `subprocess.run(["git", …], cwd=REPO_ROOT, capture_output=True, text=True, encoding="utf-8", check=False)` × 3 (198, 216, 709) | `scripts/god_file_probe._git` — but the probe is a SUBJECT of this gate and this package must not import it | FOLD in-package: `selection._git(*args) -> CompletedProcess`; the tests' `monkeypatch.setattr(gate.subprocess, "run", …)` (325, 245) patch the `subprocess` MODULE itself, so they still land |
| `HUNK` 71 | `scripts/check_profile_scope_patterns.py:34` `_HUNK_RE` — the same regex, byte for byte | a two-spellings row for the S2 lane (a `scripts/lib/` text helper does not exist; creating one for one regex is fragmentation); not this lane's edit |
| `_line_offsets` 353, `_is_whole_module` 360 | no owner | stay beside their readers in `anchor.py` |
| `clock` / `paths` / `redaction` / `god_file_scope` | — | no candidate (`datetime.now(timezone.utc).isoformat()` at 845 writes the lock's courtesy text — not the fork's wire clock, and `clock` is `agent_runtime`) |

## 4. Doors

None upstream: stdlib only. The contract's readers instead: `.github/workflows/fork-gates.yml` (two invocations — unchanged by construction; `test_mutation_cap_matches_workflow.py` reads it), `scripts/unattended_suite_run.ps1:188`, `CLAUDE.md:108`, `docs/downstream-development.md:158`. `tests/mutation_claims.json`: the six self-claims' `path` re-points to the module holding their symbol (`run` → `scripts/mutation_check/run.py`; `_symbol_span`, `_candidate_offsets`, `_qualified_definitions` → `scripts/mutation_check/anchor.py`), `find` / `replace` unchanged, `derived_at` re-stamped to the MOVE (their new file is 100 % changed lines, so the MOVE's own CI run selects and mutates all six — the MOVE's free positive control: `6 KILLED`, pasted).

## 5. Dead code found while reading

| symbol | evidence | verdict |
|---|---|---|
| `_path_matches`, `_symbol_matches`, `_claims_for`, `_refuse_because_locked`, `main` | reach-census rows (`downstream-god-file-refactor-reach-census.md:95–99`, 0 hits) — the census ran without `tests/scripts/test_mutation_claim_preflight.py` (9 + 1 reads of `_claims_for` / `main(["--claims-for", …])`) and `tests/test_mutation_gate_worktree_lock.py:205` (`_acquire_gate_lock() is False` → the refusal) | LIVE — an instrument artefact, not dead code; no row |
| every other def | `git grep -nw` per name → ≥ 1 in-file or test caller (pasted in the MOVE body) | live |
| comments 86–102, 139–156, 234–245, 908–934, 946–991 | dated MEASUREMENTS inside contract comments (the locks.py fork, the S5 zero, the Z1 deletion-only hunk, the W1-H3 counts, the H-H2 miss) | RELOCATE the measurements to the `__init__` history block (rule 7); the contract sentences beside the lines they bind STAY — a judgment, stated: "measurements move, contracts stay" |

Nothing to delete.

## 6. Positive controls (ruling Q6) — land in the MOVE, before the table replaces the walk

1. Before `_WALK`: `tests/scripts/` has no `AnnAssign`-anchored case (`git grep -n "AnnAssign\|: int = " tests/scripts` → 0) — add to `test_mutation_claim_anchoring.py` a module with `LIMIT: int = 3` at top level and a claim `symbol: "LIMIT"` that anchors, plus a class-level `Assign` (`class C: LIMIT = 3`, `symbol: "C.LIMIT"`). The swap mutation in §2 row 1 is green without them.
2. **The dump** (header): three commands before and after, `cmp` exit 0, with the base named and the line classes it exercised listed.
3. Before splitting `run`: the pins that exist — the lock released on a `RuntimeError` (`test_mutation_gate_worktree_lock.py:160`), the original bytes restored after a run (`:143`), the budget refused BEFORE the lock (`test_changed_line_mutation_check.py:501, 539`), the baseline-failed exit 2 — are named in the CHANGE body as `_mutate`'s and `run`'s controls; none is added.
4. **The monkeypatch retarget IS the MOVE's killing mutation** (the `state_patches` precedent). Thirty `setattr` sites patch the FILE's namespace; after the MOVE each name is read in one module: → `scripts.mutation_check.run` — `LOCK_PATH` (`tests/scripts/conftest.py:27`, `test_mutation_gate_worktree_lock.py:73, 250`), `REPO_ROOT` (`:72`), `_validate_exemptions` (`:74`), `_partition_claims` (`:75`), `_changed_sources` (`conftest.py:42`, `test_changed_line_mutation_check.py:261, 297`, `worktree_lock:80`), `_command` (`:81`), `_run_command` (`:82, 100, 159, 179`), `_read_source` (`:160`), `_commits_since_derivation` (`test_changed_line_mutation_check.py:387, 418`), `_current_platform` (`test_mutation_claim_platforms.py:67`); → `scripts.mutation_check.selection` — `_changed_lines` (`test_changed_line_mutation_check.py:88`, `anchoring:89`, `platforms:75`, `selection:127, 155, 201, 234`); → `scripts.mutation_check.anchor` — the `ast` swap (`test_mutation_claims_still_anchor.py:107–108, 136`). `run.py` therefore binds each of its seams in its OWN globals (`from .selection import _partition_claims, …`), never through a module reference. Control: revert `conftest.py:42` alone → the census asks real git for base `"BASE"` → `RuntimeError: git diff --name-only failed` in every `run` test; paste it.

## 7. Commits and the MOVE hash proof (one MOVE, one CHANGE — program §3.1e)

1. **MOVE** `refactor(scripts): changed_line_mutation_check.py → scripts/mutation_check/ (4 modules) + the entry` — spans byte-identical; per range in §1.1 `git show 8411d26a01:scripts/changed_line_mutation_check.py | sed -n 'A,Bp' | sha256sum` vs the new module's span, pasted; the entry re-exports the header's read names; the 30 patch sites retargeted (§6.4); the six claims re-pointed and re-stamped (§4); the W0-G5 row re-keyed to `scripts/mutation_check/anchor.py|_qualified_definitions.walk|isinstance|child`, the W0-G7 row to `scripts/mutation_check/run.py|run`, the size row deleted (hand-edited: `--write-fixtures` refuses a re-key as NEW — the `c43fc8c68b` precedent); `__layer__` per §1; measurements relocated; controls §6.1–6.2; the dump byte-equal. Killing mutation: §6.4.
2. **CHANGE** `refactor(mutation_check): _WALK; run → _report/_runnable/_mutate; SELECTED_BY_*; EXIT_*; selection._git` — §2 + §3 with each red pasted; both grandfather rows leave their fixtures; the dump re-taken byte-equal (the CHANGE moves no output); `[ds-size]` −1 (831 → 0 units over).

## 8. Lane and what it must not touch

Exec lane B5, FIRST of three: every later landing — this lane's own four commits included — runs it, and the six self-claims make its MOVE the one MOVE in the program that the gate mutates for free. Must not edit in parallel: `tests/mutation_claims.json` beyond the six `path` / `derived_at` fields; `.github/workflows/fork-gates.yml` (unchanged by construction — `test_mutation_cap_matches_workflow.py` pins it); `scripts/god_file_probe.py` and `scripts/god_file_scope.py` (the instruments that measure this lane); `scripts/check_profile_scope_patterns.py` (the `HUNK` twin is S2's row).
