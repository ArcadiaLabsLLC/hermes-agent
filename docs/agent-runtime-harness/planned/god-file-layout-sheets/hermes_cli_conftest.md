# Layout sheet — `tests/_downstream/hermes_cli_conftest.py` (lane T2 · exec lane B5)

Base: `main` @ `8411d26a01` · 1,347 raw / 893 code / 29 top-level defs · longest `_sys_modules_identity_is_restored` 78 (depth 5 — the W0-G7 row) · chains 0/0 · `str==` 5 · `isinstance` 4 · sha256 `0acfe569532eafedb3981d538524d51c39da6db16c27d6259a7c1406316c1ebe` · owner doc `docs/downstream-development.md` (the test runner and its validated scope) and `tests/_env_gap_fence.py`'s module docstring (the fence contract). Test infrastructure: the fork half of `tests/hermes_cli/conftest.py`, loaded for every one of the ~1,260 per-file `tests/hermes_cli` processes.

**The import path is a contract, and these are its readers** (every one takes the module by the string `tests._downstream.hermes_cli_conftest`, so the package is named after the file and nothing retargets): the root `conftest.py:36` (`_DIRECTORY_PLUGINS["hermes_cli"]`, imported with `importlib.import_module` and registered under the plugin name `tests/hermes_cli/_downstream_conftest.py` — pytest scopes a plugin's fixtures to the directory of a name ending in `conftest.py`, so the registered OBJECT must carry every autouse fixture and every `pytest_*` hook as an attribute); `tests/hermes_cli/test_downstream_conftest_loader_downstream.py` (asserts `_no_live_process_table` in `request.fixturenames` and the plugin name); `tests/hermes_cli/test_conftest_host_probes_are_lazy.py:14` (`_LOCAL_MODEL_PROBE_NODE_IDS`, `_WEB_BUILD_PREREQ_FILES`, and `.cache_info()` on `_local_model_probe_reason` / `_web_build_prereq_reason` — the SAME function objects, not copies); `tests/hermes_cli/test_commands_downstream.py:9` (`TELEGRAM_PARITY_DEFECT_REASON`, `pytest_runtest_logreport`, and a `setattr` on `_KNOWN_DEFECT_FAILURES`); `tests/test_env_gap_registry.py:64` (execs the FILE by path through `spec_from_file_location` and reads `_ENV_GAP_SKIPS` and `_ENV_GAPS` — see §6.2, this is the reader the MOVE breaks unless the arm lands first). Fixture rows: `size_ceiling_grandfathered.json` 893; `legibility_grandfathered.json` `_sys_modules_identity_is_restored`. Mutation claims anchored here: 0 (`--claims-for tests/_downstream/hermes_cli_conftest.py`).

**Package `tests/_downstream/hermes_cli_conftest/`** — the first package under `tests/_downstream/` (a package directory there is inside the fork's own tree, per the scope rule). `__init__` is the object pytest registers, so it BINDS every fixture and hook by name — the same shape the pre-CARRY3 star-import gave, now explicit. Its imports are ABSOLUTE (`from tests._downstream.hermes_cli_conftest import fences`), because `tests/test_env_gap_registry.py` execs the file under a synthetic module name where a relative import has no parent.

## 1. Skeleton (owner rulings 2026-09-25) — this text IS the package `__init__` map

```
tests/_downstream/hermes_cli_conftest/
  __init__.py     wiring   the map; `import dotenv` on line 1 (the real python-dotenv before any tests/hermes_cli module); binds the 10 autouse fixtures, the 5 hooks and every name a reader pins; the relocated history block at its foot
  fences.py       lanes    "no test reaches this machine": _gateway_fence.install() at import, _gateway_fence_is_armed_for_this_test, _no_windows_gateway_pause_token, _no_live_process_table (+ _empty_process_iter), _agent_browser_probe_never_spawns (+ _AGENT_BROWSER_PROBE_BINDINGS)
  isolation.py    lanes    "no test's state outlives it": _kanban_live_worker_registry_is_per_test, _web_server_app_is_pristine (+ _APP_BASELINE), _pairing_dir_follows_the_test_home, _sys_modules_identity_is_restored
  probes.py       stores   every host probe, each answering once per process: _node_version, _web_build_prereq_failure / _reason, _local_model_probe_failure / _reason, _no_module, _no_posix_mode_bits, _no_os_chown, _no_posix_wait_status, _no_posix_privilege_api, _posix_only_branch, _no_shebang_script_execution, _test_python_outside_project_venv, _unelevated_windows_shell
  registry.py     stores   TABLE module (floor-exempt): _ENV_GAP_SKIPS, _WEB_BUILD_PREREQ_FILES, _LOCAL_MODEL_PROBE_NODE_IDS, _KNOWN_DEFECTS — the file a lane edits to add a row; imports probes for the probe callables and id_markers for the one shared reason string
  hooks.py        wiring   _OWNER_DIR / _OWNER_NODEID_PREFIX, the five pytest hooks, the _KNOWN_DEFECT_FAILURES accumulator; reads registry, never probes directly
```
Entry points and the modules an agent opens to follow each: the root `conftest.py` registration → `__init__` — **1** (the map names the owner of every fixture and hook); "why was this test skipped?" (an env-gap, web-build or local-model skip) → `hooks.py` → `registry.py` → `probes.py` — 3; "why is psutil / the pause token / agent-browser patched under me?" → `fences.py` — 1; "why did my `sys.modules` swap not stick, why did `app.state` reset?" → `isolation.py` — 1; the KNOWN DEFECTS banner → `hooks.py` → `registry.py` (the string itself is `id_markers/reasons.py`) — 2; the session-finish latch → `hooks.py` → `tests/hermes_cli/_gateway_fence.py` — 2; `tests/test_env_gap_registry.py` → `registry.py` — 1. Layers go DOWN: `__init__` → `hooks` → `registry` → `probes`; `fences` / `isolation` reach only `tests/hermes_cli/_gateway_fence`, `_module_identity`, `tests._env_gap_fence` and upstream modules through `sys.modules`. `registry` is `stores` because the layer is what a module imports (it holds callables from `probes`, which spawn `node` and open a socket — I/O, so `stores`, not `policy`). W0-G6's `layered_roots` does not walk `tests/_downstream/` today, so the `__layer__` each module declares is bound by import but judged by nobody until Q31 rules.

### 1.1 Section map → target modules (line ranges are the base file's; sizes raw / est. code — docstrings count as code, `#` lines do not)

| lines | what is there | → module | raw / code | layer |
|---|---|---|---|---|
| 1–30 | docstring; imports; `import dotenv` (21–27); `_env_gap_fence` and `tests.hermes_cli` imports | `__init__.py` (the docstring, `dotenv` FIRST, then the module imports in today's order 27 → 29 → 30 → 40) | ~40 / 25 | wiring |
| 32–40, 51–96, 117–170, 367–440 | the fence install comment + `install()`; `_gateway_fence_is_armed_for_this_test`; `_AGENT_BROWSER_PROBE_BINDINGS` + `_agent_browser_probe_never_spawns`; `_no_windows_gateway_pause_token`; `_empty_process_iter` + `_no_live_process_table` | `fences.py` | ~180 / 150 | lanes |
| 42–48 | `_OWNER_DIR`, `_OWNER_NODEID_PREFIX` | `hooks.py` | 7 / 4 | wiring |
| 99–114, 173–248, 251–283, 286–364 | `_kanban_live_worker_registry_is_per_test`; `_APP_BASELINE` + `_web_server_app_is_pristine`; `_pairing_dir_follows_the_test_home`; `_sys_modules_identity_is_restored` | `isolation.py` | ~210 / 185 | lanes |
| 443–532, 668–752, 1167–1183 | three dated audit narratives (the 2026-07-30 fence, the 07-31 sweep, the 08-10 audit, the retired hooks banner) | relocated as a `#` block at the foot of `__init__.py` (rule 7 / Q3 — comments, so the counter does not read prose as code) | ~190 / 0 | — |
| 533–534, 753, 1164 | `_WINDOWS`, `_HOST`, `_ENV_GAPS = {}`, `_STALE_ENV_GAP_ENTRIES` | `hooks.py` in the MOVE; deleted in the CHANGE (§5, Q30) | 4 / 4 | — |
| 537–558, 620–638 | `_WEB_BUILD_PREREQ_FILES`, `_VITE8_NODE_FLOOR`, `_LOCAL_MODEL_PROBE_NODE_IDS` | `registry.py` (`_VITE8_NODE_FLOOR` → `probes.py`, its only reader is `_web_build_prereq_failure`) | ~40 / 12 | stores |
| 561–617, 641–666, 755–902 | the two lazy probes and their `functools.cache` wrappers; `_POSIX_MODE_BITS_PROBE`, `_SHEBANG_EXEC_PROBE`; the nine registry probes | `probes.py` | ~260 / 200 | stores |
| 904–1101, 1184–1222 | `_ENV_GAP_SKIPS`; the `TELEGRAM_PARITY_DEFECT_REASON` import; `_KNOWN_DEFECTS`; `_KNOWN_DEFECT_FAILURES` (→ `hooks.py`) | `registry.py` (imports `tests._downstream.id_markers` for the one string, `probes` for the callables) | ~240 / 225 | stores |
| 1103–1161, 1225–1300 | `pytest_configure`, `pytest_collection_modifyitems`, `pytest_runtest_logreport`, `pytest_sessionfinish`, `pytest_terminal_summary` | `hooks.py` | ~140 / 115 | wiring |
| 1303–1347 | `__all__` (45 private names) | not re-created — it is the `__init__` import list's source (§5) | — | — |

Result: 5 modules + the map; every non-table module 115–200 code lines; nothing near 300. No cycle: `registry` → `id_markers` (a sibling package that imports nothing from here), `hooks` → `registry` → `probes`; `fences` and `isolation` import neither. The one cross-package edge (`registry` → `id_markers.reasons`) is the reason `id_markers` lands before this file in the lane.

## 2. Routing sites (rule 12) — none on the W0-G5 fixture; what the CHANGE converts

`str==` 5 are `report.when != "call"`, `report.outcome == "failed"` / `"passed"`, `sys.platform == "win32"` ×2 — pytest and stdlib vocabulary, boundaries not routing. The CHANGE's rule-12 items:

| site (base line) | shape today | replacement | killing mutation |
|---|---|---|---|
| `pytest_collection_modifyitems` 1138–1161 | per item, three hand-written lookups — the web-build file guard, the local-model id guard, the (empty) `_ENV_GAPS` group loop — then `apply_skips` | the two prerequisite guards are one shape, `(files-or-ids, reason_probe)`: a `_PREREQ_GUARDS: tuple[PrereqGuard, ...]` in `registry.py` (two rows; `PrereqGuard(files=frozenset, node_ids=Mapping, reason=Callable[[], str \| None])`), one loop in the hook. **NOT a fold into `_ENV_GAP_SKIPS`** — a prerequisite guard is meant to go inert on a host that meets the floor, while `tests/test_env_gap_registry.py` reads a registry row whose probe answers "no gap" as ROTTED and fails the run (`stale_skip_rows`, scoped by `firing_skip_rows`); folding them would red the registry gate on every box with Node ≥ 20.19. Named so nobody folds it | drop the local-model row → `test_conftest_host_probes_are_lazy.py::test_each_probe_ran_only_if_a_file_that_needs_it_was_collected` reds when run in one session with `tests/hermes_cli/test_timeouts.py` (`misses == 0` against `local_needed == True`) — §6.3 says which session |
| `pytest_configure` 1112–1125 | two `addinivalue_line` calls spelling the env-gap marks by hand | `_env_gap_fence.register_marks(config)` — the two sibling conftests (`agent_conftest.py:150`, `tools_conftest.py:350`) already call it; behaviour-identical for the mark NAMES, the description text changes (this file's still names croniter / pywinpty / pathspec, retired 2026-08-10). Under Q30's default the call goes too | — (a description string; no test reads it) |
| `pytest_runtest_logreport` 1244–1257 + `pytest_terminal_summary` 1279–1300 | the known-defect lane (failed-or-`wasxfail`, owner-prefix filtered) and the stale-env-gap lane, hand-rolled | `_env_gap_fence.KnownDefectTracker(known, registry_location)` — NEW owner this lane creates: `record(report)` with THIS file's semantics (`failed` OR `wasxfail`, owner prefix from the location as `StaleEntryTracker` derives it) and `report(terminalreporter, title)`; `hooks.py` keeps two lines. `tools_conftest.py:363` carries the pre-ML-16 copy (`failed` only, no owner filter) — folding it there CHANGES behaviour (a strict xfail in `tests/tools` would today retire its own banner), so tools folds in its own lane with the xfail control; fork-hygiene row filed with this sheet | drop the `wasxfail` arm from the tracker → `test_commands_downstream.py::test_an_xfailed_known_defect_still_reaches_the_banner` reds (both arms are pinned there today; its `setattr` on `_KNOWN_DEFECT_FAILURES` retargets to the tracker's list) |
| `_sys_modules_identity_is_restored` 337–364 | depth 5 (the W0-G7 row): `for` → `if not str` → `if is not` → `if parent` → `try` | the loop body becomes `_restore_binding(name, module)` (the `sys.modules` row and the parent-attribute half, each with its measurement comment) — depth 3 in the fixture, 2 in the helper; the row leaves the fixture | drop the parent-attribute half → §6.5's control (`from hermes_cli import main` and `import hermes_cli.main` answer one object after the fixture's teardown) reds — the docstring's own measurement ("the update tests kept failing until this line existed") turned into a test |

`isinstance` 4 (`registry is dict`, two `ConnectionRefusedError` / `TimeoutError` checks, `name is str`) — boundary checks; no density goal.

## 3. Helper folds

| here | owner / duplicate | verdict |
|---|---|---|
| `_no_module(name)` 758 — a closure over a one-element list as a once-cache | `functools.cache` (the file's own two lazy probes at 615 / 663 use it) | FOLD: `@functools.cache def _module_missing(name) -> bool`; `_no_module(name)` returns `functools.partial(_module_missing, name)` so the registry rows read unchanged. Behaviour-identical: one import attempt per name per process |
| `_POSIX_MODE_BITS_PROBE` 755 / `_SHEBANG_EXEC_PROBE` 836 — module globals as once-caches, `global` statements in the probes | same | FOLD: `@functools.cache` on `_no_posix_mode_bits` and `_no_shebang_script_execution`; the two globals go (in `__all__` only; `git grep -nw` → 0 readers) |
| the known-defect lane (§2 row 3) | `tools_conftest.py` 327–380 — same shape, older classifier | the owner is NEW (`_env_gap_fence.KnownDefectTracker`); this file folds onto it identically; tools does NOT fold here (behaviour differs — named) |
| `file_name = nodeid.split("::", 1)[0].rsplit("/", 1)[-1]` 1242, 1285 (and `_env_gap_fence.StaleEntryTracker.record`) | three spellings of one line | inside the tracker after the fold; no separate helper |
| `import sys` / `from pathlib import Path` inside `_test_python_outside_project_venv` 879–880, `import sys` inside `_unelevated_windows_shell` 892 | the module imports both at 15–17 | delete the inner imports (CHANGE) |
| `is_owned`, `apply_skips`, `EnvGapSkipRegistry` | already `tests._env_gap_fence` | the shared owner; nothing to do |
| `serde` / `clock` / `paths` / `redaction` / `file_locks` / `god_file_scope` | — | no candidate: nothing here parses, coerces, tells time or locks. And a conftest that runs for UPSTREAM tests must not import `agent_runtime` (it does not today; the rule is stated so a fold never introduces it) |

## 4. Doors — where the fixtures reach upstream, and what a merge can silently break

All by attribute on a module already in `sys.modules`, never by importing a `_private` name (W0-G6's arm would have nothing to say even if it walked here). The table is the "what a rename breaks" list; §6.6 is the control that turns a silent no-op into a red.

| reach | kind | note |
|---|---|---|
| `hermes_cli.main._pause_windows_gateways_for_update` 397–405 | PRIVATE, patched with `raising=False` | the one SILENT seam: a rename upstream leaves the fixture patching nothing and the real pause token reachable again (L2/L3 of the fence still stand behind it, per its docstring) |
| `hermes_cli.kanban_db_dispatch._live_worker_procs` 111 | private attr, read through `sys.modules.get` | silent by design ("only a module already imported is touched") |
| `hermes_cli.web_server.app.state._state`, `app.router.lifespan_context` 233–248 | Starlette private | the fixture returns early if the shape changes ("the reds come back honestly") — stated, and the control pins the shape |
| `gateway.pairing.PAIRING_DIR` 276–283 | public constant | `hasattr` guard, silent |
| `hermes_constants.agent_browser_runnable` + `hermes_cli.dep_ensure`, `hermes_cli.doctor_tools`, `tools.browser_tool_lifecycle` 122–166 | public name bound by value in four modules | the `assert` at 170 is the only live control today |
| `hermes_constants.get_hermes_home` 279 | public | — |
| `psutil.process_iter` 440 | third party, `raising=True` | fails loud by design |
| `tests/hermes_cli/__init__.py`, `_gateway_fence.py`, `_module_identity.py` | FORK-ONLY files inside the upstream directory `tests/hermes_cli/` (the C1/T1 class of §0.3) | not moved by this lane: 9 fork test files import them by that path and the fence's own docs name it. Q32 asks whether they belong under this package |

No widening rows.

## 5. Dead code found while reading

| symbol | evidence | verdict |
|---|---|---|
| `__all__` 1303–1347 | `git grep -n "hermes_cli_conftest import \*" -- '*.py'` → 0; the root `conftest.py` registers the module object (lane CARRY3), so no star-import exists to read it. `docs/agent-runtime-harness/planned/seam-s4-s5-s6-inventory-2026-09-23.md:239` still says "star-imported" — stale doc | DELETE in the MOVE: the `__init__` import list is written FROM it (every name it lists is bound in `__init__`), and the list is not re-created. Named in the commit body |
| the mark-only env-gap lane: `_ENV_GAPS = {}` 753, `_WINDOWS` / `_HOST` 533–534 and their registration 1112–1125, the group loop 1154–1160, `_STALE_ENV_GAP_ENTRIES` 1164, the stale arm 1250–1257, the stale section 1292–1300 | `_ENV_GAPS` is empty and `tests/test_env_gap_registry.py::test_no_directory_still_uses_the_mark_only_registry` asserts it STAYS empty (absent is also accepted: `getattr(module, "_ENV_GAPS", None) or {}`); reach census rows `pytest_terminal_summary [if @1279]` 11 lines 0 hits, `_sys_modules_identity_is_restored [if @340]` (the non-str-key arm, kept — it is a measured case) | DELETE in the CHANGE (Q30's default). The two sibling conftests carry the same empty dict plus `StaleEntryTracker` / `apply_marks` / `register_marks`; those are a class row in the dead-code queue, not this lane's edit |
| `_POSIX_MODE_BITS_PROBE`, `_SHEBANG_EXEC_PROBE` | in-file only | retired by the `functools.cache` fold |
| the `_HOST` marker text 1120–1125 (croniter / pywinpty / pathspec, git ≥ 2.31) | the rows it describes were retired 2026-08-10 (484–488) | goes with the lane |
| `"registered in _ENV_GAPS (tests/hermes_cli/conftest.py)"` 1296 | the file has been `tests/_downstream/hermes_cli_conftest.py` since CARRY3 | goes with the lane; `id_markers`' twin (`TELEGRAM_PARITY_DEFECT_REASON` 100–101) is that sheet's CHANGE |
| comments 443–532, 668–752, 1167–1183 | dated history; still the only record of the 08-10 audit's fifty stale rows | RELOCATE (rule 7), not delete |
| the ten autouse fixtures, `_empty_process_iter`, the twelve probes | autouse by construction; every probe is a `_ENV_GAP_SKIPS` row's callable or a `_PREREQ_GUARDS` reason | live (`git grep -nw` per name, pasted in the MOVE body) |

## 6. Positive controls (ruling Q6) — land in the MOVE, before any table replaces a site

1. **The registration IS the MOVE's killing mutation.** `test_downstream_conftest_loader_downstream.py::test_directory_half_is_registered_with_directory_scope` asserts `_no_live_process_table` in `fixturenames`; `test_conftest_host_probes_are_lazy.py` reads the two cache objects off the package. Drop `from tests._downstream.hermes_cli_conftest.fences import _no_live_process_table` from `__init__` → the first reds (`assert "_no_live_process_table" in request.fixturenames`); paste it.
2. **`tests/test_env_gap_registry.py::_registry_module_path` reads a fence only as a flat FILE** (`downstream.is_file()`, line 65); a packaged fence falls back to `tests/hermes_cli/conftest.py`, `_ENV_GAP_SKIPS` is absent there, `_load_registry` returns `None`, and both parametrised checks SKIP for `hermes_cli` — "green by blindness", the exact failure the function's own docstring records from 2026-09-24. The arm (`…/<dir>_conftest/__init__.py` when the flat file is absent) lands in the MOVE, with `test_every_fenced_directory_is_found` asserting `_load_registry("hermes_cli") is not None` — a skip is the red this control exists to refuse. Revert the arm → that test reds. And because the loader execs the path under `spec_from_file_location("_env_gap_probe_hermes_cli", …)`, the package `__init__` uses ABSOLUTE imports; a relative one raises `ImportError: attempted relative import with no known parent package` there.
3. Before `_PREREQ_GUARDS`: run `tests/hermes_cli/test_conftest_host_probes_are_lazy.py` in ONE session with `tests/hermes_cli/test_timeouts.py` and `tests/hermes_cli/test_cmd_update.py` (the `needed == True` arm of both probes); alone it proves only the negative. Record the session in the CHANGE body.
4. Before the `KnownDefectTracker` fold: `test_commands_downstream.py::test_an_xfailed_known_defect_still_reaches_the_banner` pins BOTH arms (an xfail report recorded; a strict XPASS — `failed`, no `wasxfail` — recorded); retarget its `setattr(package_conftest, "_KNOWN_DEFECT_FAILURES", …)` to the tracker's list and keep both assertions.
5. Before the depth lift of `_sys_modules_identity_is_restored`: no test exercises the three isolation fixtures today (`git grep -l "sys_modules_identity\|_web_server_app_is_pristine\|_pairing_dir_follows" tests/` → only the file itself). Add `tests/hermes_cli/test_conftest_isolation_downstream.py`: (a) a test that deletes `hermes_cli.main` from `sys.modules` and re-imports it, followed by a test asserting `sys.modules["hermes_cli.main"] is hermes_cli.main` (the parent attribute) — the pair the docstring measured; (b) a test that sets `app.state.auth_required = True`, followed by one asserting it is absent again (the setup-time reset). Each is two tests in file order — the positive control for a fixture whose guarantee is "the NEXT test sees clean state".
6. **The seams exist.** `tests/hermes_cli/test_conftest_seams_downstream.py`: one assertion per upstream attribute the fixtures patch with `raising=False` or through `getattr(…, None)` — `hermes_cli.main._pause_windows_gateways_for_update`, `hermes_cli.kanban_db_dispatch._live_worker_procs`, `gateway.pairing.PAIRING_DIR`, `hermes_constants.agent_browser_runnable`, `hermes_cli.web_server.app.state._state`. A merge that renames one turns a fixture into a silent no-op today; the test turns it into a red naming the seam.

## 7. Commits and the MOVE hash proof (one MOVE, one CHANGE — program §3.1e)

1. **MOVE** `refactor(tests): hermes_cli_conftest.py → tests/_downstream/hermes_cli_conftest/ (5 modules)` — spans byte-identical; per range in §1.1 `git show 8411d26a01:tests/_downstream/hermes_cli_conftest.py | sed -n 'A,Bp' | sha256sum` vs the new module's span, one row each, pasted in the body; `__init__` binds every name `__all__` listed (its 45 rows are the import list; `__all__` itself is not re-created) plus `_PREREQ_GUARDS` once the CHANGE lands; `import dotenv` on line 1; absolute imports; `__layer__` per §1; history relocated; the `test_env_gap_registry` arm + control (§6.2); controls §6.1, §6.5, §6.6; `size_ceiling_grandfathered.json` row deleted (every module is under 800), the W0-G7 row re-keyed to `tests/_downstream/hermes_cli_conftest/isolation.py|_sys_modules_identity_is_restored` (hand-edited — `--write-fixtures` refuses a re-key as NEW, the `c43fc8c68b` precedent); the reach-census note's line cites re-anchored. Killing mutation: §6.1.
2. **CHANGE** `refactor(tests): _PREREQ_GUARDS; KnownDefectTracker (env_gap_fence, new owner); functools.cache probes; _restore_binding; the mark-only _ENV_GAPS lane deleted (Q30)` — §2 + §3 with each red pasted; §6.3–6.4 recorded; the W0-G7 row leaves the fixture; `[ds-size]` −1 (893 → 0 units over).

## 8. Lane and what it must not touch

Exec lane B5 (with `changed_line_mutation_check` and `id_markers`), THIRD of three: `registry.py` imports `id_markers.reasons` (through the `id_markers` package), so `id_markers` lands first. Must not edit in parallel: `tests/_downstream/agent_conftest.py` and `tools_conftest.py` (their known-defect and `_ENV_GAPS` copies are their own rows); `tests/_env_gap_fence.py` beyond ADDING `KnownDefectTracker`; `tests/hermes_cli/_gateway_fence.py`, `_module_identity.py`, `__init__.py` (Q32); the root `conftest.py` (the import string is unchanged by construction); `tests/test_env_gap_registry.py` beyond the one arm and its control.
