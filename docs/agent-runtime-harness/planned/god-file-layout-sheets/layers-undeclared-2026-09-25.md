# Layout sheet — the W0-G6-undeclared population (lane LAYERS-DESIGN, 2026-09-25)

Base: `origin/main` @ `478fcc5ba5`. Rules the two `runtime-queue.md` rows marked `TAKEN 2026-09-25 lane LAYERS-DESIGN` — the 66-module row (lane W3-B, `24763b9a8e` / `60c0309dfc` / `cc22bb16b3` / `c37726d912`) and the three-upward-edge row (lane 2B-B). Bar: `god-file-program-2026-09-24.md` §2 rule 16 and gate W0-G6 (§2.4). Design only — no code changes in this lane.

## 1. Population and method

**Population.** 66 modules, read from the probe itself (`scripts/god_file_probe.py`, `layer_census()["undeclared"]` at the base — byte-equal to `tests/fixtures/import_layers_grandfathered.json`'s `undeclared` list; the fixture was not retyped). 30,457 raw lines. Every one is under `agent_runtime/` except `agent/charsheet/fake_draftsman.py`. Plus the three modules of the second row (`persona_chat_history/curation.py`, `persona_chat_history/markers.py`, `harness_parts/persona/chat_history_writes.py`) — see §2.4.

**Method** (the three facts per module, all computed, none copied from W3-B's commit bodies):

- **reach** — the highest layer the module's imports touch, read exactly as the gate reads them (`imports_of`: module-level AND deferred imports both count; a `from agent_runtime.<pkg> import name` resolves to `<pkg>/__init__.py`'s layer, because `layer_violations` looks one `rsplit` up and no further). Reach is taken TRANSITIVELY through undeclared imports, because an undeclared import is invisible to the gate today and becomes an edge the moment that module declares — the planning instrument must see the edge the exec lane will create.
- **cap** — the lowest layer among the module's DECLARED importers; a declaration above the cap is an upward edge from that importer.
- **raw** — `wc -l`.

Three classes account for every gap between reach and cap; the verb per module in §2 names which:

| class | what it is | verb |
|---|---|---|
| **A · package-door reach** | the module imports a name from a package `__init__` declared at the package's TOP layer, while the name is defined in a lower submodule (`from .serve_rpc import ERR_CONFLICT` → `serve_rpc/__init__` is `lanes`, `ERR_CONFLICT` lives in `serve_rpc/protocol.py`, `models`). 15 of the 66 carry one. | MOVE the import to the owning submodule — a respelling, no code moves |
| **B · a low-layer fact stranded in a high module** | a vocabulary constant or a pure helper defined in a stores/lanes module and read by a models/policy module (`EVENT_PAYLOAD_LIMIT_BYTES` in `events.py`, `ProjectionAccountant` in `parity.py`, `stamp_passed` in `store_file_io.py`). | MOVE the symbol down; the high module re-imports it |
| **C · a declared importer whose layer is wrong** | a module declared `policy`/`models` that reads a store or a live process fact (`root_observability` reads the process chat scope; `mcp_admission/resolve` reads readiness and registrations; `serve_rpc/registry` lazily imports three handler modules). | RE-DECLARE the importer, after checking ITS importers admit the new layer (every one listed in §2 was checked) |

After A–C are applied, 33 of the 66 have no upward edge at all and simply DECLARE — their W3-B cap reason was a chain through another undeclared module, not an edge of their own.

**Decision rules applied** (the brief's, plus two the population forced): a module doing store I/O is `stores` whatever its name says (`serve_gateway_auth`, `chat_reply_stamps`, `provider_visibility`, `harness_doctor/model` all re-declare on this rule); a module read by ONE importer folds into that importer's package rather than getting a layer (no instance survived — every single-importer module here is already inside its importer's package, `discussions/native.py`, or is imported by a flat file); a declaration goes at the layer of the module's WORK, bounded by cap and reach — never "the lowest layer the imports allow" (that reading makes an RPC handler `stores` and hides the next upward edge behind it); a cycle between two modules is legal to the gate and is left alone unless a fact in it belongs lower.

**The gate today.** `tests/tooling/test_fork_import_layers.py` runs at the base (11 passed, 12.8 s, the `acp` pin of `478fcc5ba5` removed the runtime arm's blocker the second row names). It reports **no** live violation: the one edge the census surfaced — `serve_rpc/registry.py` (`models`) → `local_llama_adapter/rpc.py` — is not a gate edge because `rpc.py` is undeclared; it BECOMES one in the first exec lane that declares `rpc.py`, `discussions/rpc.py` or `chat_turn.py`, which is why the registry re-declare is pinned to lane L1 in §3.

## 2. The table — one verb and one reason per module

Columns: raw lines · reach (the import that sets it; `(fn)` = deferred) · cap (the declared importer that sets it) · the ruling · exec lane (§3). "Respell" = class A; "→ `<module>`" = class B, the symbol moves there; "RE-DECLARE x → layer" = class C. The bold layer at the end of a ruling is the layer the module DECLARES once the ruling is applied.

### 2.1 MOVE (28) — an import is respelled or a symbol moves down, then the module declares

| module | raw | reach | cap | ruling → layer | lane |
|---|---|---|---|---|---|
| `agent/charsheet/fake_draftsman.py` | 382 | lanes: `pipeline/__init__` (fn) `MAGENTA`, `view_prefix`, `row_prefix`, `PREFIX_TURNAROUND` | stores: `pipeline/provider.py` (fn) `draftsman_from_env` | respell all four to `pipeline.geometry` (models, where every one is defined) → **stores** (it writes the fake PNG bytes the provider hands out) | L4 |
| `agent_runtime/auxiliary_chat.py` | 67 | stores: `persona_assignments/__init__` (fn) `chat_session_owner_instance_id` | policy: `mission_chat_turns/records.py` (fn) `safe_auxiliary_result` | respell to `persona_assignments.identity` (policy, the definer) → **policy** | L4 |
| `agent_runtime/chat_turn.py` | 772 | lanes: `serve_rpc/__init__` (fn) `ERR_CONFLICT`/`ERR_HANDLER_FAILED`/`ERR_INVALID_PARAMS` | models: `serve_rpc/registry.py` (fn) `CHAT_TURN_METHOD_PARAMS`; policy: `chat_reply_stamps.py` (fn) `PEER_REQUESTED_BY_PREFIX` | respell `ERR_*` to `serve_rpc.protocol` (models); `PEER_REQUESTED_BY_PREFIX` → `serve_rpc/protocol.py` (a wire-vocabulary constant; `chat_turn` re-imports it); RE-DECLARE `serve_rpc/registry.py` → lanes (all 15 importers are lanes; it lazily imports three handler modules, which is lanes work — rule 12's registry is the lane's method table, not a model); RE-DECLARE `harness_parts/persona/chat_reply_stamps.py` → stores (it mints reply media through `media_handles`; its 3 importers are lanes) → **lanes** | L1 |
| `agent_runtime/dispatch_session_policy.py` | 290 | stores: `config/__init__` (fn) `mission_chat_dispatch_session_policy` | policy: `config/knobs.py`, `config/sections.py` `normalize_dispatch_session_policy` | respell to `config.knobs` (policy, the definer; the knobs↔policy cycle is already deferred on this side and stays so); the four vocabulary constants `NEW_PER_DISPATCH`/`STICKY`/`DISPATCH_SESSION_POLICIES`/`DEFAULT_DISPATCH_SESSION_POLICY` → `runtime_config.py` (models — see that row) and are re-imported → **policy** | L4 |
| `agent_runtime/events.py` | 680 | stores: `event_rotation`, `locks` (undeclared → stores), `config/__init__` (fn) | models: `harness_doctor/model.py` `EventLog`, `state_patches/models.py` `EVENT_PAYLOAD_LIMIT_BYTES`; policy: `state_patches/payload.py` `EVENT_PAYLOAD_LIMIT_BYTES`, `payload_bytes` | `EVENT_PAYLOAD_LIMIT_BYTES` + `payload_bytes` → `agent_runtime/models.py` (models, beside `Event`; `events.py` re-imports); RE-DECLARE `harness_doctor/model.py` → stores (its `DoctorContext` carries a live `EventLog` handle; its 4 importers are lanes) → **stores** | L1 |
| `agent_runtime/gateway_identity.py` | 429 | stores: `store_file_io` (undeclared → stores) | models: `gateway_peers/models.py` `gateway_dir`; policy: `serve_gateway_auth.py` `gateway_dir` | `gateway_dir` + `install_record_path` (pure path joins) → `agent_runtime/paths.py` (models); RE-DECLARE `serve_gateway_auth.py` → stores (seven `store_file_io` names incl. `store_lock`/`write_secure_json`: it IS store I/O; its 11 importers are stores/lanes) → **stores** | L1 |
| `agent_runtime/gateway_targets.py` | 421 | lanes: `gateway_peers/__init__` (fn) `list_peers`/`usable_peers`/`read_peer_cache`; `profile_home` (fn) | stores: `gateway_peers/cache.py` (fn) `REASON_PEER_EXPIRED`/`_REVOKED`/`_REVOKED_YOU` | respell to `gateway_peers.trust_store` (`list_peers`) and `gateway_peers.cache` (the other two), both stores; the three `REASON_PEER_*` constants → `gateway_peers/models.py` (models; `gateway_targets` re-imports) → **stores** (`peer_store_root` resolves the install root and reads the peer cache) | L1 |
| `agent_runtime/mcp_lane.py` | 342 | stores: `profile_readiness` (fn) (undeclared → stores) | models: `mcp_admission/vocabulary.py` `MCP_NOT_REGISTERED_ON_LANE`; policy: `mcp_admission/resolve.py` (fn) | `MCP_NOT_REGISTERED_ON_LANE` → `mcp_admission/vocabulary.py` (models — it already re-exports it as one of the nine codes; `mcp_lane` re-imports); RE-DECLARE `mcp_admission/resolve.py` → stores (it reads readiness, registrations, the profile and the root config through five deferred imports; importers `mcp_admission/__init__` and `profile_runner/execute` are lanes) → **stores** | L4 |
| `agent_runtime/media_handles.py` | 990 | lanes: `chat_live_log/__init__` (fn) `capture_chat_live_log_root`; `dispatch_store/__init__` (fn) `dispatch_store`; `gateway_targets` (fn) | policy: `chat_reply_stamps.py` (fn) `mint_reply_media` | respell to `chat_live_log.files` (stores) and `dispatch_store.db` (stores — the handle the `__init__` door hands out); `chat_reply_stamps` re-declared under the `chat_turn` row → **stores** | L1 |
| `agent_runtime/mission_chat_outcome.py` | 841 | lanes: `profile_runner/__init__` (fn) `RunBudgetExceeded` — its ONLY layered import | models: `dispatch_delivery/vocabulary.py` (fn) `ChatErrorKind`…; stores: `chat_request.py`, `chat_target.py` | respell to `profile_runner.errors` (models, the definer) → **models** (the outcome vocabulary and typed reasons; rule 14 names it as the model) | L4 |
| `agent_runtime/parity.py` | 365 | stores: `event_rotation` | policy: `running_work/rows.py` `ProjectionAccountant` | `ProjectionAccountant` + `DropRecord` + `_MAX_BY_DESIGN_DROP_SAMPLE` (a pure counter; no I/O) → new `agent_runtime/projection_accountant.py` (models); `parity`, `rows`, `status`, the `stream/` importers respell → **stores** (watermarks) | L2 |
| `agent_runtime/persona_chat_durability.py` | 241 | stores: `persona_assignments/__init__`, `persona_chat_continuity/__init__`, `chat_session_scope` (fn) | policy: `persona_assignments/identity.py` (fn) `ensure_durable_persona_chat_root` inside `_durable_chat_root` | `_durable_chat_root` (identity.py L136–~190: the mint that WRITES the chat root) → `persona_assignments/chat_binding.py` (stores); its callers respell (grep `_durable_chat_root`); `identity.py` cannot re-declare — `office_store/normalize`, `operator_channels/instances`, `persona_instance_sync/projection` (policy) import it → **stores** | L4 |
| `agent_runtime/persona_config_sync.py` | 810 | lanes: `sync_admission` (fn) `refuse_entity` (undeclared → lanes today, policy after its row) | policy: `persona_instance_sync/projection.py` (fn) `find_nonportable_values` | `find_nonportable_values` + `NONPORTABLE_HINT` (pure walk over a mapping) → `sync_admission.py` (policy — it already lazily imports them; `persona_config_sync`, `projection`, `persona_artifacts` respell) → **stores** (`load_raw_config`, `project_persona_definitions` read and write files) | L3 |
| `agent_runtime/persona_runtime.py` | 1033 | lanes: `mcp_admission/__init__`, `profile_runner/__init__`, `progress`, `tool_permissions` | policy: `persona_assignments/summary.py` (fn) `apply_chat_lane_tool_scope`; `prompt_observability/context_files.py` (fn) `_mission_chat_soul_overlay`, `MISSION_CHAT_WORKSPACE_AGENTS_PREAMBLE`, `_mission_chat_identity_prompt`, `_mission_chat_operative_rules` | two moves out: (i) the prompt builders `_mission_chat_operative_rules`, `_mission_chat_identity_prompt`, `MISSION_CHAT_WORKSPACE_AGENTS_PREAMBLE`, `_mission_chat_soul_overlay` (+ `_safe_read_soul_overlay`, `_persona_profile_home`, `_is_safe_soul_overlay_path`) → new `agent_runtime/mission_chat_prompts.py` (stores: the overlay is READ from disk); RE-DECLARE `prompt_observability/context_files.py` → stores (importers `mission_chat`, `turn_results` lanes, `workspace_agents` stores); (ii) the chat-lane scope family `_enabled_toolsets_for_chat`, `_blocked_tool_names_for_chat`, `chat_lane_capability_drops`, `_augment_chat_capabilities`, `_CHAT_CAPABILITY_TOOLSETS`, `mission_chat_admission_line`, `mission_chat_operating_skills`, `apply_chat_lane_tool_scope` → `chat_lane_bundle.py` (which already lazily imports four of them; declares stores — see §2.3), with `profile_runner/execute._blocked_tool_names_with_registry_hygiene` (a pure union over `REGISTRY_HYGIENE_BLOCKED_TOOLS`) → `personas.py` (models, beside the set) so the family reaches nothing above stores. Decision rule for the exec lane: if one moved function's own imports reach `profile_runner.runner` or `GPTPersonaRuntime`, it STAYS in `persona_runtime` and `summary.py` takes the scoped `ToolVisibilityOptions` as a parameter from its stores caller instead → **lanes** | L5 |
| `agent_runtime/personas.py` | 597 | stores: `store/__init__` (fn) `AgentStore`, `config/__init__` (fn) ×2, `profile_context` (fn) — all inside `declared_lane_toolsets` (L243) and `promote_profile_to_persona` (L489) | models: `agent_runtime/__init__.py` `AgentRole`/`AutonomyLevel`, `blueprints/resolve.py` `promote_profile_to_persona`, `terminal_envelope/records.py` `canonical_persona_id`/`role_or_attr` | the five profile-resolving functions `declared_lane_toolsets`, `profile_persona_resolution`, `profile_chat_toolset_resolution`, `profile_chat_toolsets`, `promote_profile_to_persona` (they read profile YAML, the roster and the store) → new `agent_runtime/persona_profiles.py` (stores); the enums, `canonical_persona_id`, `persona_id_aliases`, `validate_toolsets`, `PROFILE_ROLE_SENTINEL`, `role_from_persona`, `effective_toolsets`, `blocked_tool_names`, the two blocked-tool sets and `all_registered_toolsets` stay; RE-DECLARE `blueprints/resolve.py` and `blueprints/__init__.py` → stores (they call the promotion; importer `plugin_api.py` is stores) → **models** | L4 |
| `agent_runtime/profile_artifact_sync.py` | 709 | lanes: `realm_sync/__init__` (fn) `_canonicalize_text_bytes` (defined in `realm_sync/models.py`), `_profile_home_for_token` (in `realm_sync/families.py`); `sync_admission` (fn); `profile_context` (fn) | policy: `realm_sync/families.py` (fn) `classify_destination`; stores: `persona_artifacts.py`, `sidecar.py` | respell `_canonicalize_text_bytes` to `realm_sync.models`; the destination vocabulary `PROFILE_FILES_ROOT`, `MEMORY_DESTINATION`, `CORE_CONTEXT_FILENAMES`, `classify_destination`, `entity_key`, `published_relative_path`, `content_hash` (pure) → `realm_sync/families.py` (policy, the destination-kind authority that already lazily imports `classify_destination`); `_profile_home_for_token` (needs the active profile) → `profile_context.py` (stores) as `profile_home_for_token`, which also deletes `families.py`'s one `profile_context` import (L22, used only at L266) → **stores** | L3 |
| `agent_runtime/profile_context.py` | 474 | stores: `profile_home` (undeclared → models after its row), `paths` | policy: `mcp_admission/resolve.py` (fn), `profile_runner/status.py` `PersonaProfileBinding`, `realm_sync/families.py` `active_profile_name` | `PersonaProfileBinding` (a dataclass) → `agent_runtime/models.py` (models; `profile_context`, `status.py` respell); `families.py`'s import leaves under the `profile_artifact_sync` row; `resolve.py` re-declares under the `mcp_lane` row → **stores** (`active_profile_name` falls back to the on-disk `active_profile` marker; `resolve_persona_profile` reads profile homes) | L4 |
| `agent_runtime/profile_home.py` | 314 | stores: `chat_session_scope` `configured_head_home` (undeclared → stores) | models: `agent/charsheet/_support.py` (fn) `get_shared_characters_dir` | `configured_head_home` (an `os.environ` read, the "one reader of `HERMES_HEAD_HOME`") → `profile_home.py`; `chat_session_scope` re-imports it from there, so the ladder still reads the ONE primitive and `profile_home`'s only remaining layered import is `_upstream_doors` (models) → **models** (home authorities: env + `ContextVar` state, no file I/O) | L4 |
| `agent_runtime/realm_membership.py` | 450 | lanes: `realm_sync/__init__` `MembershipDecision`/`RealmMembershipProvider`/`RealmSyncError` | stores: `realm_sync/git.py` (fn) ×2 | respell to `realm_sync.models` (models, the definer) → **stores** (`EventLog` + the membership provider I/O) | L3 |
| `agent_runtime/redaction_mode.py` | 30 | stores: `config/__init__` (fn) `load_root_runtime_config` | policy: `config/loader.py` `normalize_redaction_mode`, `persona_chat_history/text.py` `redaction_observe_enabled` | `STRICT`/`OBSERVE`/`ALLOWED`/`normalize_redaction_mode` (the field's vocabulary + pure normaliser) → `config/schema.py` (models; `loader.py` already imports `schema`); `redaction_mode()`/`redaction_observe_enabled()` (config readers) stay; RE-DECLARE `persona_chat_history/text.py` → stores (it reads the mode from config at two sites; importers `curation`, `history_rows` are stores) → **stores** | L4 |
| `agent_runtime/runtime_config.py` | 473 | policy after the `dispatch_session_policy` row (today: `DEFAULT_DISPATCH_SESSION_POLICY` from it), `permission_modes` (models) | models: `config/schema.py` `RuntimeConfig` | receives the four dispatch-session vocabulary constants (see that row); with them here its imports are `permission_modes` only → **models** | L4 |
| `agent_runtime/serve_auth.py` | 273 | stores: `store_file_io` (undeclared → stores) | models: `core_cache/vocabulary.py` `SERVE_AUTH_TOKEN_FILENAME` | `SERVE_AUTH_TOKEN_FILENAME` → `agent_runtime/paths.py` (models; a filename is the paths module's fact; `serve_auth` and `core_cache/vocabulary` re-import) → **stores** | L1 |
| `agent_runtime/serve_gateway_peers_rpc.py` | 306 | lanes: `gateway_peers/__init__` `unusable_reason`, (fn) `list_peers`/`read_peer_cache`/`usable_peers`/`peer_store_revision`; `gateway_targets` (fn) | stores: `gateway_peers/trust_store.py` (fn) `publish_peer_event` | respell the door imports to `gateway_peers.cache` / `gateway_peers.trust_store`; the store→lane call inverts through a seam: `trust_store.py` keeps a module-level `_peer_event_listeners: list[Callable]` it calls at the SAME site it appends the event today, and `serve_gateway_peers_rpc` registers `publish_peer_event` into it at serve boot (lanes → stores, downward; `trust_store` L568's deferred import is deleted) → **lanes** | L1 |
| `agent_runtime/skill_promotion.py` | 813 | stores: `profile_home`, `skill_publishability` (fn), `skill_sync` (fn) | stores: `realm_sync/drift.py`, `store/realms.py` … | `is_windows_reserved_component` (a pure stem check) → `agent_runtime/paths.py` (models); `skill_promotion` and `sync_admission` respell (this is what lets `sync_admission` declare policy) → **stores** | L3 |
| `agent_runtime/skills_inventory.py` | 329 | lanes: `realm_sync/__init__` (fn) `read_realm_sync_sidecar` | stores: `prompt_observability/skills_context.py`, `skills_resolver.py` | respell to `realm_sync.sidecar` (stores, the definer) → **stores** | L3 |
| `agent_runtime/store_file_io.py` | 344 | stores: `locks` (undeclared → stores) | models: `gateway_peers/models.py` `stamp_passed`; policy: `serve_gateway_auth.py` ×7 | `iso_stamp` + `stamp_passed` (the writer/reader pair the docstring says must live together) → `agent_runtime/clock.py` (models, rule 15's clock module); `store_file_io`, `gateway_peers/models`, `serve_gateway_auth`, `local_llama_adapter/{manager,setup}` respell; `serve_gateway_auth` re-declares under the `gateway_identity` row → **stores** | L1 |
| `agent_runtime/sync_admission.py` | 267 | lanes: `realm_sync/__init__` (fn) `_is_hard_excluded_path`/`_is_secretish_path` (defined in `realm_sync/families.py`, policy); `skill_promotion` (fn); `persona_config_sync` (fn) | policy: `persona_instance_sync/projection.py` (fn) `path_refusal`/`Refusal`/`refuse_entity`; stores: `pull.py` | respell the two predicates to `realm_sync.families`; `is_windows_reserved_component` now comes from `paths`; `find_nonportable_values` now lives HERE (from `persona_config_sync`) → **policy** (the refusal vocabulary and the path predicates; `Refusal` is rule 14's typed reason) | L3 |
| `agent_runtime/tool_visibility.py` | 730 | lanes: `mcp_admission/__init__` `LANE_MISSION_CHAT`, `admission_enabled`, `admission_requirement_failures`, `resolve_mcp_admission` | stores: `persona_assignments/__init__`, `snapshot/summaries.py`; policy: `persona_assignments/summary.py` | respell to `mcp_admission.vocabulary` (`LANE_MISSION_CHAT`, models) and `mcp_admission.resolve` (the other three; stores after its re-declare); `summary.py` re-declares under the `tool_permissions` row → **stores** (`load_tool_turn_history`, readiness, the workdir) | L5 |

### 2.2 RE-DECLARE (5) — the module's own reach is right; a declared importer sits at the wrong layer

| module | raw | reach | cap | ruling → layer | lane |
|---|---|---|---|---|---|
| `agent_runtime/chat_session_scope.py` | 888 | stores: `persona_assignments/__init__` (fn), `profile_home` (fn) | policy: `root_observability.py` (fn) `resolve_process_chat_scope` | RE-DECLARE `root_observability.py` → stores: it stamps the LIVE process resolution and chat scope into a payload (both are store-backed facts); all 18 importers are `harness_parts` lanes → **stores** (opens the session DB) | L4 |
| `agent_runtime/discussions/rpc.py` | 109 | stores: `contract`, `definitions`, `run_store`, `service` (undeclared → lanes after its row) | models: `serve_rpc/registry.py` (fn) `register` | `serve_rpc/registry.py` → lanes (ruled under `chat_turn`, lane L1; L2 lands after L1) → **lanes** (the verb family's RPC surface) | L2 |
| `agent_runtime/local_llama_adapter/provider.py` | 111 | stores: `config`, `rpc` (fn), `config/__init__` (fn) | policy: `harness_parts/provider_visibility.py` (fn) `catalog_visibility` | RE-DECLARE `provider_visibility.py` → stores: it reads the environment, the key files, the auth logins and the local-llama catalog into blocks; its one importer `parser/machine.py` is wiring → **stores** (`catalog_visibility` reads the `ConfigStore`) | L2 |
| `agent_runtime/local_llama_adapter/rpc.py` | 112 | stores: `gateway_identity` (fn), `manager` (fn), `config` | models: `serve_rpc/registry.py` (fn) `register` | as `discussions/rpc.py` → **lanes** | L2 |
| `agent_runtime/tool_permissions.py` | 406 | lanes today only through `tool_visibility` (stores after its row); `config/__init__` (fn) ×2 | policy: `mcp_admission/resolve.py` (fn) `PERMISSION_MODE_READ_ONLY`, `persona_assignments/summary.py` `default_permission_mode`/`permission_options_for_chat` | RE-DECLARE `persona_assignments/summary.py` → stores: it builds the operator preview from `permission_state_for_persona` (tool-turn history), `default_permission_mode` (root config) and the chat-lane scope; its one importer `persona_assignments/__init__` is stores; `resolve.py` re-declares under `mcp_lane` (L4, lands first) → **stores** (`ChatToolPermissionStore` + the config read in `default_permission_mode`) | L5 |

### 2.3 DECLARE (33) — no upward edge of their own once §2.1–2.2 land; the W3-B cap reason was a chain

| module | raw | reach | cap | layer | lane |
|---|---|---|---|---|---|
| `agent_runtime/board_sync.py` | 349 | stores (`events`; `sync_admission` is policy after its row) | stores (`realm_sync/drift`, `snapshot/boards`) | **stores** (reads the board baseline) | L3 |
| `agent_runtime/chat_lane_bundle.py` | 564 | stores after the `persona_runtime` move-in and the `mcp_admission` respell (`mcp_lane`, `tool_permissions`) | lanes (`chat_admission`, `mission_chat_turn_context`, `persona_chat_actor_prewarm`, `persona_runtime`) | **stores** (the bundle reads admission, readiness and the workdir; it is the ONE chat-lane scope authority after the move) | L5 |
| `agent_runtime/chat_turn_presence.py` | 208 | stores (`events` fn, `mission_chat_turns`) | lanes | **stores** | L1 |
| `agent_runtime/chat_turn_reservations.py` | 402 | stores (`locks`) | lanes (`serve/lanes.py`) | **stores** (the reservation file) | L1 |
| `agent_runtime/continuity.py` | 173 | lanes (`chat_live_log` writes) | lanes (`instance_commands`) | **lanes** | L2 |
| `agent_runtime/discussions/native.py` | 390 | stores (`auxiliary_chat`, `profile_home`, `profile_context`, `persona_chat_durability`) | — (only `service.py`, same package) | **stores** | L2 |
| `agent_runtime/discussions/service.py` | 485 | stores (`native`, `attempt_store`) | lanes (`serve/boot_phases`, `serve/session`) | **lanes** (the service that runs discussions for the serve lane) | L2 |
| `agent_runtime/local_llama_adapter/config.py` | 186 | stores (`store_file_io` fn) | — (package-internal) | **stores** (`ConfigStore`, the journal) | L2 |
| `agent_runtime/local_llama_adapter/engine.py` | 300 | stores (`config`) | — | **stores** (model files, the process) | L2 |
| `agent_runtime/local_llama_adapter/manager.py` | 458 | stores (`store_file_io`, `config`, `engine`, `setup` fn) | — | **stores** | L2 |
| `agent_runtime/local_llama_adapter/setup.py` | 447 | stores (`store_file_io`, `config`) | — | **stores** (downloads, the journal) | L2 |
| `agent_runtime/locks.py` | 232 | stores (`config/__init__`) | stores (twelve store modules; `serve_rpc/office_read` lanes) | **stores** | L1 |
| `agent_runtime/media_proxy.py` | 279 | stores after the `media_handles` and `gateway_targets` rows | lanes (`serve_rpc/media`) | **lanes** (the proxy is the serve lane's fetch) | L1 |
| `agent_runtime/mission_chat_steer.py` | 369 | lanes (`chat_live_log`) | lanes | **lanes** | L5 |
| `agent_runtime/mission_chat_turn_context.py` | 1168 | lanes (`prompt_observability`, `chat_lane_bundle`, `skill_resolution`) | lanes (`chat_turn_commit/run`) | **lanes** | L5 |
| `agent_runtime/mission_chat_workdir.py` | 296 | stores (`config/__init__`) | lanes (`inspect_commands`) | **stores** (the workdir ladder reads config and the tree) | L5 |
| `agent_runtime/office_sync.py` | 614 | stores (`events`; `sync_admission` policy after its row) | stores (`drift`, `publish_scans`, `snapshot/offices`, `state_patches/office`) | **stores** | L3 |
| `agent_runtime/patch_coverage.py` | 824 | stores (`state_patches`) | lanes (`stream/*`, `serve/*`) | **stores** (fold tokens over the patch store) | L2 |
| `agent_runtime/persona_chat_actor_prewarm.py` | 825 | lanes (`mcp_admission`, `mission_chat_turn_context`, `chat_lane_bundle`) | lanes (`chat_admission`, `chat_open`, `serve/boot`) | **lanes** | L5 |
| `agent_runtime/persona_open_chat.py` | 314 | lanes (`serve_rpc`) | lanes (`serve_rpc/chat`) | **lanes** | L5 |
| `agent_runtime/persona_prewarm.py` | 441 | lanes (`agent_create`, `persona_runtime` fn) | lanes (`serve_rpc/chat`) | **lanes** | L5 |
| `agent_runtime/profile_readiness.py` | 632 | stores (`provider_probes`, `profile_context`, `skill_install`, `skill_resolution` fn, `local_llama_adapter/provider` fn) | policy today (`mcp_admission/resolve` → stores after its row); lanes (`snapshot/sections`) | **stores** | L4 |
| `agent_runtime/progress.py` | 659 | lanes (`chat_live_log.writes` `record_chat_tool` fn) | — (only `persona_runtime`, a flat file: the fold rule does not apply) | **lanes** (the progress sink writes the mirror through the writes lane) | L5 |
| `agent_runtime/provider_health.py` | 59 | stores (`profile_context`) | lanes (`runtime_commands`) | **stores** | L4 |
| `agent_runtime/scope_activation.py` | 520 | lanes (`serve_rpc`) | lanes (`serve_rpc/scope`, `realm_commands`, `workspace_commands`) | **lanes** | L2 |
| `agent_runtime/serve_office_subscriptions.py` | 1115 | stores (`patch_coverage`) + `serve_rpc` lanes | lanes (`office_read`, `serve/session`, `serve/subscriptions`) | **lanes** | L2 |
| `agent_runtime/skill_install.py` | 489 | stores (`profile_home`, `skill_resolution` fn) | stores (`agent_create/phases`, `core_cache/fingerprint`, `skills_resolver`) | **stores** | L3 |
| `agent_runtime/skill_publishability.py` | 677 | stores (`skill_resolution`, `profile_home` fn, `skill_promotion` fn) | stores (`skills_context`) | **stores** | L3 |
| `agent_runtime/skill_resolution.py` | 535 | stores (`profile_home`) | stores (five store modules) | **stores** | L3 |
| `agent_runtime/skill_sync.py` | 414 | stores (`profile_home` fn, `skill_promotion` fn) | stores (`drift`) | **stores** | L3 |
| `agent_runtime/status.py` | 227 | lanes (`dispatch_delivery`); `events`, `parity`, `profile_readiness`, `provider_health` all stores | lanes (`runtime_commands`) | **lanes** | L2 |
| `agent_runtime/stream_resume.py` | 218 | lanes (`stream/__init__`) | lanes (`serve/handle_message`) | **lanes** | L2 |
| `agent_runtime/terminal_envelope_explain.py` | 190 | lanes today only through `tool_permissions` (stores after its row) | lanes (`inspect_commands`) | **lanes** (an explain verb for the inspect command) | L5 |

### 2.4 The second row — three declared modules reading a lower package's higher layer

| module | layer at base | the edge the row names | finding |
|---|---|---|---|
| `agent_runtime/persona_chat_history/curation.py` | `stores` (re-declared at `a67518abfb`, lane W3-A) | → `mission_chat_turns.reads` (`stores`) | **closed** — stores → stores |
| `agent_runtime/persona_chat_history/markers.py` | `stores` (same commit) | → `mission_chat_turns.reads` (`stores`) | **closed** |
| `hermes_cli/harness_parts/persona/chat_history_writes.py` | `lanes` | (fn) → `agent_runtime.chat_live_log` (`__init__` and `writes.py` both `lanes`) | **closed** — lanes → lanes; the mirror's package landed with its hot lane at `lanes`, so no seam is owed |

The row's fix was applied by W3-A before the row was filed against it; the gate at the base confirms (11 passed, §1). The row is deleted at the first exec landing of §3 with this table as its closing evidence — no lane work remains on it.
