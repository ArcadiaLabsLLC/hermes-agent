# Layout sheet — `tests/_downstream/id_markers.py` (lane T2 · exec lane B5)

Base: `main` @ `8411d26a01` · 1,216 raw / 1,053 code / 13 defs (11 top-level; the two helpers are mark constructors) · longest `pytest_collection_modifyitems` 26 · chains 0/0 · `str==` 1 · `isinstance` 0 · sha256 `5aed29d0fb6ded233bac105e647e2658f9a3b2d0928be490b398031a4ae60145` · owner doc: the module's own docstring is the contract (lane CARRY, 2026-09-24); `tests/fixtures/upstream_footprint.json` rows 48 / 54 / 85 / 152 cite it as the reason upstream test files are back at upstream's bytes. This is the strict-xfail / probe-skip AUTHORITY: **314 ids on win32** (the program's "~205" was a POSIX-shaped count; measured by executing the base file with a recording dict — `X:/wt/h-sheets3/.lane-logs/idmarks_census.log`, 2026-09-25 — 19 `ID_MARKS` blocks, 8 file-level keys, 175 `file::test`, 131 `file::Class::test` / parametrised; mark shapes: `xfail` 196, `skip` 51, `scoped_monkeypatch_undo` 19, `config_reads_through_load_config` 10, `allow_claude_code_credentials_file` 12, `real_windows_gateway_pause` 8, `timeout` 7, `skipif` 5, the rest ≤ 2). **Exactly one key is written twice** — `tests/hermes_cli/test_macos_tcc_anchor.py::TestEnsureTccAnchor::test_alias_failure_leaves_anchor_unmarked`, a MERGE (`_SCOPED_UNDO` from the CARRY3 block + `_TCC_POSIX_VENV` from the REDS3 loop at 845) — and **zero overrides**: the `dict.update` ORDER of the 19 blocks carries no semantics today, which is what lets a split by class assemble the table by concatenation.

**Readers (the import path is a contract):** `tests/_downstream/conftest_plugin.py:26` (`NO_LIVE_GATEWAY_MARK`, `pytest_collection_modifyitems`, `pytest_make_collect_report`, `pytest_runtest_setup` — re-exported so pluggy finds the hooks on the ROOT plugin; this is how the table applies before upstream's own `modifyitems`); `tests/_downstream/hermes_cli_conftest.py:1188` (`TELEGRAM_PARITY_DEFECT_REASON`); `tests/hermes_cli/test_commands_downstream.py:97` (`id_markers.pytest_collection_modifyitems(config, [item])` with a `SimpleNamespace` config); `tests/test_claude_code_credentials_file_gate.py:516, 555` (`ids_marked`); `tests/tooling/test_downstream_size_ceiling.py:25` (the PATH, as the population's positive control — retargeted in the MOVE). Fixture rows: `size_ceiling_grandfathered.json` 1053. Mutation claims anchored here: 0. The marker VOCABULARY the table applies is registered in `conftest_plugin.pytest_configure` (741–779: `tirith_config_value_under_test`, `config_reads_through_load_config`, `claude_home_is_tmp_path`, `requires_no_live_gateway`, `no_ollama_show_probe`, `no_real_orphan_reap`, `scoped_monkeypatch_undo`, `allow_claude_code_credentials_file`) and in `hermes_cli_conftest` (`real_windows_gateway_pause`); pytest runs without `--strict-markers` (`pyproject.toml:642`), so a misspelled mark is applied silently — §2 row 3.

**Package `tests/_downstream/id_markers/`** — named after the file; every reader's string resolves through `__init__`. The split is by CLASS of id, where the class is WHAT RETIRES THE ROW (the docstring: "each names what retires it") — never by size and never by the lane that filed it.

## 1. Skeleton (owner rulings 2026-09-25) — this text IS the package `__init__` map

```
tests/_downstream/id_markers/
  __init__.py        wiring   the map; re-exports ID_MARKS, the four hook names, ids_marked, TELEGRAM_PARITY_DEFECT_REASON, NO_LIVE_GATEWAY_MARK, IMPORT_TIME_POSIX_SHIMS, REQUIRES_DISTRIBUTION; the per-lane history (CARRY, CARRY2B, CARRY3, REDS2, REDS3 banners) at its foot
  reasons.py         models   VOCABULARY module (floor-exempt): _WIN; every reason string; the four mark CONSTRUCTORS (_posix_only, _posix_xfail, _up_red, _up_red_skip, _fork_replaces); the shared mark objects (_CREDENTIALS_FILE, _REAL_PAUSE, _CONFIG_READ_THROUGH, _LOOKALIKE, _SCOPED_UNDO, _CLAUDE_HOME_TMP, _TIRITH_NO_BUILD, _LIFECYCLE_SCAN, _TCC_POSIX_VENV, _NEEDS_ACP); TELEGRAM_PARITY_DEFECT_REASON; NO_LIVE_GATEWAY_MARK; after the CHANGE, the mark-NAME constants conftest_plugin registers
  fork_marks.py      models   TABLE: rows that hold on EVERY host because of FORK behaviour — fixture opt-ins, the rule-4 strict xfails naming the fork symbol and its *_downstream sibling, the timeouts the fork's --timeout=30 cannot hold; retired by a fork change or a fork PR
  posix_marks.py     models   TABLE (_WIN only): upstream tests whose PREMISE is POSIX — _posix_only skips, _posix_xfail, _WSL_FAKE, _TIRITH_NO_BUILD, _TCC_POSIX_VENV, IMPORT_TIME_POSIX_SHIMS; retired by upstream linux_only marks (branch up/win-posix-only-apis)
  upstream_reds.py   models   TABLE (_WIN only): upstream's OWN Windows reds at v2026.9.24 — _up_red / _up_red_skip with their class letters, the path-spelling xfails, the cp1252 e-ENC probe block; retired by an upstream fix (X:/wt/_holds/upstream-reds-v2026.9.24.md, PR class win-path-spelling)
  distributions.py   models   TABLE (floor-exempt, ~30 code): rows gated on an OPTIONAL distribution — REQUIRES_DISTRIBUTION, the acp rows, the defusedxml skipif; retired by installing the extra
  hooks.py           lanes    _merge (the assembler: four ROWS tables → ID_MARKS, marks CONCATENATED per key), pytest_make_collect_report + _skip_if_distribution_missing, pytest_collection_modifyitems + _base_id / _keys_for / _narrowed_files, pytest_runtest_setup, ids_marked
```
Entry points and the modules an agent opens to follow each: a lane adding a row → the class module — **1** (the point of the split: the file a lane edits is data, and its name says what will retire the row); "why is this upstream test xfailed?" → the reason string in the report names its class → `reasons.py` → the class module — 2; the root plugin's hook registration (`conftest_plugin.py:26`) → `__init__` → `hooks.py` — 2; `ids_marked` (the credentials-file gate) → `hooks.py` — 1; `hermes_cli_conftest` reading `TELEGRAM_PARITY_DEFECT_REASON` → `reasons.py` — 1. Layers go DOWN: `__init__` → `hooks` → the four tables → `reasons`; `hooks` reaches `hermes_cli.gateway.find_gateway_pids` lazily (upstream, public). Each table module exposes ONE `ROWS: dict[str, tuple[MarkDecorator, ...]]`, already host-filtered (its `if _WIN:` / `if _WIN and not sys.flags.utf8_mode:` guards stay INSIDE the module that owns the rows), so the assembler has no platform logic. W0-G6 does not walk `tests/_downstream/` today (Q31); `__layer__` is declared anyway.

### 1.1 Section map → target modules (line ranges are the base file's; the 19 blocks INTERLEAVE classes, so the hash proof is per ROW — §7)

| lines | what is there | → module | raw / code | layer |
|---|---|---|---|---|
| 1–26 | docstring, imports, `_WIN` | `__init__.py` (docstring) · `reasons.py` (`_WIN`) | ~30 / 15 | — |
| 28–105, 260–277, 451–459, 585, 626–647, 819–822, 1029–1033 | the reason strings; `_CREDENTIALS_FILE`, `_REAL_PAUSE`; `_POSIX_ONLY` + `_posix_only`; `_CONFIG_READ_THROUGH`, `_LOOKALIKE`, `_TIRITH_NO_BUILD`; `_CLAUDE_HOME_TMP`, `_fork_replaces`; `NO_LIVE_GATEWAY_MARK`; `_UP_RED`, `_up_red`, `_up_red_skip`, `_posix_xfail`, `_LIFECYCLE_SCAN`; `_TCC_POSIX_VENV`; `_NEEDS_ACP` | `reasons.py` | ~140 / 120 | models |
| 107–196 (less 122–125 → also `fork`), 279–310, 404–408, 461–578 (the `_WIN` block 570–578 included: a fork xfail whose premise happens to be win32), 587–624, 778–812 | opt-ins, `_FORK_*` and `_fork_replaces` strict xfails, the telegram strict xfail, the timeouts, the CARRY3 scoped-undo rows | `fork_marks.py` | ~330 / 300 | models |
| 198–257 (the `_POSIX_ONLY_LINUX` skip 208–210 and the three `_WSL_FAKE` skips 249–256), 312–395, 425–448, 649–667, 722–723, 748–749, 764, 823–845 (the TCC rows, as rows), 862–918, 1020–1027, 1081–1089 | every `_posix_only` / `_posix_xfail` / `_WSL_FAKE` row; `IMPORT_TIME_POSIX_SHIMS` | `posix_marks.py` | ~250 / 230 | models |
| 200–248 less the posix rows (`_WIN_REEXEC_BRANCH` → `fork_marks`: it names the fork's live-system guard), 668–775 less the posix rows, 846–857, 919–1018, 1042–1054, 1057–1080 | every `_up_red` / `_up_red_skip` / `_LIFECYCLE_SCAN` row, the spelling-class xfails (`_PATH_SPELLING`, `_SEPARATOR_SPELLING`, `_CONTAINER_SPELLING`, `_TMP_LITERAL`, `_SQLITE_HANDLE_LEFT_OPEN`), the `test_completion` xfail (an upstream red with an open fork PR), the e-ENC probe block | `upstream_reds.py` | ~290 / 270 | models |
| 409–422, 1034–1040, 1093–1102 | the `defusedxml` skipif rows, the acp rows, `REQUIRES_DISTRIBUTION` | `distributions.py` | ~36 / 30 | models |
| 1105–1217 | `pytest_make_collect_report`, `_skip_if_distribution_missing`, `_base_id`, `_keys_for`, `ids_marked`, `_narrowed_files`, `pytest_collection_modifyitems`, `pytest_runtest_setup` (+ `_merge`, ~8 lines, the MOVE's one addition beside the map) | `hooks.py` | ~130 / 105 | lanes |
| the block banners 260–264, 398–401, 451, 581–584, 626–627, 796, 816–818, 859–861 | history by LANE (which lane filed which block) | relocated as a `#` block at the foot of `__init__.py` (rule 7); each class module carries the RETIRING cause per row group instead | ~25 / 0 | — |

Result: 6 modules + the map; the four tables 30–300 code lines (`distributions` is the stated sub-100 table exemption; folding it into `fork_marks` would misfile "install the extra" under "a fork change"). No cycle: `hooks` imports the tables, the tables import `reasons`, nothing imports `hooks` but `__init__`. `hermes_cli_conftest` imports `reasons` through this package — the reason this file lands before it in the lane.

## 2. Routing sites (rule 12) — none on the W0-G5 fixture; what the CHANGE converts

`str==` 1 is `sys.platform == "win32"` (a platform guard). The rule-12 items are about the TABLE's assembly and the vocabulary it spells:

| site (base line) | shape today | replacement | killing mutation |
|---|---|---|---|
| the 19 `ID_MARKS.update({...})` blocks under 9 `if _WIN:` guards + 1 `if _WIN and not sys.flags.utf8_mode:` + the TCC merge loop 824–845 | one dict built by ORDERED mutation; the reader must replay 19 blocks to know a key's marks | four `ROWS` tables and `hooks._merge(*tables)` which CONCATENATES marks per key in table order (fork, posix, upstream_reds, distributions — the order that keeps today's `(_SCOPED_UNDO, _TCC_POSIX_VENV)` tuple); a key in two tables is a MERGE, never an override | make `_merge` a `dict.update` chain → §6.1's control (the TCC id carries `scoped_monkeypatch_undo` AND `xfail`) reds |
| `_POSIX_ONLY_LINUX` 60 and `_POSIX_ONLY` 265 | one string spelled twice (`"POSIX-only; upstream fix = @pytest.mark.linux_only"`) | one constant in `reasons.py` (the vocabulary rule: a word spelled fork-wide gets a named constant in its single reader, not an Enum) | — (a reason text; §6.2 pins the merged table, not the string) |
| `pytest.mark.real_windows_gateway_pause` 105, `pytest.mark.requires_no_live_gateway` 164 (beside `NO_LIVE_GATEWAY_MARK = "requires_no_live_gateway"` 585), `pytest.mark.config_reads_through_load_config` 272, `pytest.mark.spawns_gateway_lookalike` 273, `pytest.mark.scoped_monkeypatch_undo` 402, `pytest.mark.claude_home_is_tmp_path` 452, `pytest.mark.no_ollama_show_probe` 782, `pytest.mark.no_real_orphan_reap` 788, `pytest.mark.tirith_config_value_under_test` 145 / 283 | the marker vocabulary spelled as attribute access here AND as `_*_MARK` constants in `conftest_plugin.py` (the registrar and the consumer fixtures) and `_gateway_fence.REAL_PAUSE_MARK`; with no `--strict-markers`, a typo on either side applies a mark nothing reads | the mark-NAME constants live ONCE in `reasons.py` (`SCOPED_MONKEYPATCH_UNDO_MARK = "scoped_monkeypatch_undo"`, …); the table builds its mark objects with `getattr(pytest.mark, NAME)`; `conftest_plugin` imports the names for `addinivalue_line` and for its consumer fixtures — the precedent is `NO_LIVE_GATEWAY_MARK`, already imported there; `_REAL_PAUSE` reads `_gateway_fence.REAL_PAUSE_MARK` (importing `tests.hermes_cli._gateway_fence` has no side effect — `install()` is called by the conftest, not at import) | respell one constant's VALUE (`"scoped_monkeypatch_undo"` → `"scoped_monkeypatch_undo_"`) → the consumer fixture in `conftest_plugin` no longer matches the applied mark → `tests/agent_runtime/test_no_midtest_monkeypatch_undo.py` (which pins that an id-marked test's `undo` is narrowed) reds |
| `_skip_if_distribution_missing` 1121–1134 | `from _pytest.reports import CollectReport` (a pytest INTERNAL, inside the function) | `pytest.CollectReport` (public since pytest 7) — one line | — (`tests/acp_adapter/` is the only `REQUIRES_DISTRIBUTION` row; the collection skip is exercised on any venv without `acp`, which is the canonical one) |

The four mark constructors (`_posix_only`, `_posix_xfail`, `_up_red`, `_up_red_skip`) are one shape (`mark(reason=f"{PREFIX} ({detail})")`) — NOT folded into one parametrised helper: each constructor IS a retiring class, and the prefix is what a reader greps for in the report.

## 3. Helper folds

| here | owner / duplicate | verdict |
|---|---|---|
| `_POSIX_ONLY_LINUX` / `_POSIX_ONLY` | each other | one constant (§2) |
| `_base_id` / `_keys_for` (nodeid → the enclosing keys) | no fork owner; `tests/_env_gap_fence.is_owned` answers a different question (path inside a directory) | stay in `hooks.py` beside their one reader |
| `_narrowed_files`' `Path(...).resolve().relative_to(config.rootpath)` | `is_owned` has the same two-line idiom for the opposite purpose | not a fold — different question, and `hooks` importing `_env_gap_fence` would be a new edge for three lines |
| `serde` / `clock` / `paths` / `redaction` / `file_locks` / `god_file_scope` | — | no candidate; and this module is imported into the ROOT plugin for upstream's whole tree, so it must never import `agent_runtime` (it does not; stated so no fold introduces it) |

## 4. Doors

`hermes_cli.gateway.find_gateway_pids` (public, imported lazily inside `pytest_runtest_setup`); `pytest.CollectReport` after §2 row 4 (`_pytest.reports` today — the one non-public reach); `os.geteuid` lent to `tests/agent/test_prompt_builder.py` for its import (`IMPORT_TIME_POSIX_SHIMS`, a stdlib attribute set-and-deleted around one collection — named, kept; §6.4 is its control). No widening rows. The upstream TEST files the table names are not edited — that is the file's whole purpose (`upstream_footprint.json` rows 48 / 85).

## 5. Dead code found while reading

| symbol | evidence | verdict |
|---|---|---|
| `ids_marked` 1147 | dead-code queue row (line 25: "6 · TEST SEAM … nothing in the hooks reads it — delete or make a hook read it"); `git grep -n "ids_marked" -- '*.py'` → `tests/test_claude_code_credentials_file_gate.py:516, 518, 555, 557` — two GATES read it (the credentials-file opt-in gate resolves every marked id to a `Path.home()` redirect) | **REFUTED as dead**: it is the table's read seam for the fork's gates, which its docstring says ("read by the fork's gates"); a hook need not read it. KEEP in `hooks.py` (the reader of `ID_MARKS`); the row closes with the CHANGE commit named |
| `_POSIX_ONLY_LINUX` 60 | duplicate of 265 | deleted by §2 |
| `TELEGRAM_PARITY_DEFECT_REASON` 100–101 "Full account: _KNOWN_DEFECTS in tests/hermes_cli/conftest.py" | the banner has lived in `tests/_downstream/hermes_cli_conftest.py` since CARRY3 (and in its `registry.py` after B5) | text fix in the CHANGE — the string is shared with the banner by identity (`test_commands_downstream` asserts `is`), so one edit |
| the lane banners (§1.1 last row) | history by lane, not by class | RELOCATE (rule 7) |
| every reason constant and constructor | each read ≥ 1 in a row (`git grep -nw` per name, pasted in the MOVE body) | live |

## 6. Positive controls (ruling Q6) — land in the MOVE, before the assembler replaces the `update` chain

1. **The cross-class merge.** `tests/test_id_markers_downstream.py::test_a_key_in_two_classes_carries_both_marks`: the TCC id's marks are `{scoped_monkeypatch_undo, xfail}` — the only such key today (the census above), so the test also asserts `hooks._merge` reports exactly that one collision (a set that moves only when a lane adds a cross-class row on purpose, and says so). This is the assembler's killing mutation (§2 row 1).
2. **The MOVE's killing mutation is a re-export.** Drop `ids_marked` from `__init__` → `tests/test_claude_code_credentials_file_gate.py` fails at `from tests._downstream.id_markers import ids_marked` (ImportError, two tests). Dropping a hook name instead reds the whole session at `conftest_plugin.py:26` — louder, but not test-shaped; paste the first.
3. `tests/tooling/test_downstream_size_ceiling.py:25` retargeted to `"tests/_downstream/id_markers/__init__.py" in files` (a population control must name a file that exists; the walk includes package `__init__`s, as its `agent_runtime/store/base.py` assertion shows).
4. **The lent attribute is returned.** `test_import_time_posix_shims_are_taken_back`: after collecting `tests/agent/test_prompt_builder.py` on win32, `hasattr(os, "geteuid")` is False again — pins the `finally` of `pytest_make_collect_report` (no test today).
5. Before the mark-name constants move: `test_every_applied_mark_name_is_registered` — the set of `mark.name` over `ID_MARKS` values ⊆ the names `conftest_plugin.pytest_configure` and `hermes_cli_conftest.pytest_configure` register (read off a `config` built with `pytest.Config`-free `SimpleNamespace(addinivalue_line=…)`), plus pytest's own (`xfail`, `skip`, `skipif`, `timeout`). Without `--strict-markers` this is the only check that a typo in either file is a red.

## 7. Commits and the MOVE hash proof (one MOVE, one CHANGE — program §3.1e)

1. **MOVE** `refactor(tests): id_markers.py → tests/_downstream/id_markers/ (6 modules; rows split by retiring class)` — **the proof is per ROW**, because the base file's 19 blocks interleave classes: for each of the 314 keys the row's source span (a dict entry or a `**{…}` comprehension, as written in the base) is byte-identical in its destination table; the commit body pastes, per module, `rows=N sha256(concat of the rows it took, in base order)` and the total `rows=314 identical=314`; the constants and constructors are span-hashed per §1.1 (`git show 8411d26a01:tests/_downstream/id_markers.py | sed -n 'A,Bp' | sha256sum`). The only non-moved bytes are the `__init__` map, each table's `ROWS = {` … `}` framing, and `_merge` (declared in the body). `__init__` re-exports the names in the header; `test_downstream_size_ceiling` retargeted (§6.3); controls §6.1, §6.2, §6.4; `size_ceiling_grandfathered.json` row deleted (every module ≤ 300); `__layer__` per §1; lane banners relocated; doc cites re-anchored (`conftest_plugin.py`'s seven docstring mentions of the path still resolve — the package directory is that path). Killing mutation: §6.2.
2. **CHANGE** `refactor(id_markers): mark-name constants declared once (conftest_plugin imports them); _POSIX_ONLY once; pytest.CollectReport; the stale conftest path in TELEGRAM_PARITY_DEFECT_REASON; ids_marked row REFUTED` — §2 + §5 with each red pasted; §6.5 recorded; `[ds-size]` −1 (1,053 → 0 units over).

## 8. Lane and what it must not touch

Exec lane B5, SECOND of three: after `changed_line_mutation_check` (which every landing runs), before `hermes_cli_conftest` (which imports `reasons` through this package). Must not edit in parallel: `tests/_downstream/conftest_plugin.py` beyond the import of the mark-name constants (its fixtures and its `pytest_configure` text stay); `tests/test_claude_code_credentials_file_gate.py` (reads through the package, unchanged); `tests/_downstream/hermes_cli_conftest.py` (the third file — its import line is unchanged by construction); any upstream test file the table names.
