# Upstream footprint ledger — one row per upstream file the fork edits

Generated by `scripts/upstream_footprint.py --ledger`; `added` / `deleted` are rewritten on
every run, `disposition` / `reason` / `stage` are hand-edited and survive regeneration (the
merge is on `path`). A new row arrives as `carry` / `unreviewed` / `-`. Dispositions are the
plan's three: `upstream`, `hook`, `carry` (§1 rule 1 of
[`harness-plugin-and-upstream-seams.md`](harness-plugin-and-upstream-seams.md)), plus
`carry-permanent`: a carry the operator has ruled permanent (§1 rule 6), still counted in
`files=`, settled for the detach read.

| path | added | deleted | disposition | reason | stage |
|---|---|---|---|---|---|
| `.gitattributes` | 7 | 0 | carry-permanent | RULED 2026-09-24 (lane META): only the root `* text=auto eol=lf` rule is left (root-only, not movable). PENDING upstream PR on branch `up/gitattributes-eol-lf` (body `X:/wt/_holds/pr-bodies/gitattributes-eol-lf.md`); a merge retires the row. The fixture `-text` pins moved to `tests/fixtures/{office_layout,response_envelopes,stream_frames}/.gitattributes`, the hook rule to `.githooks/.gitattributes` | - |
| `.github/workflows/contributor-check.yml` | 1 | 1 | upstream | PR candidate: accept `contributors/emails/case-variants/` (case-colliding emails break Windows checkouts); with `scripts/audit_pr_attribution.py` + `scripts/release.py` | S3 |
| `AGENTS.md` | 2 | 0 | carry-permanent | RULED 2026-09-24 (lane META): one pointer line at EOF (+ its blank) to `docs/downstream-development.md` and `CLAUDE.md`; AGENTS.md is what non-Claude agents read, so the pointer cannot move. The fork landing-gate bullet moved to `docs/downstream-development.md` "The fork landing gate" | - |
| `README.md` | 7 | 1 | carry-permanent | RULED 2026-09-24 (lane META): licence notice — the License line states the PolyForm contribution terms beside MIT; a legal notice belongs in the README (replaces upstream lines) | - |
| `agent/agent_init.py` | 29 | 4 | hook | `local-llama-hermes` managed-context floor exemption (a sub-64K window the router actually loaded, pinned by the adapter's read projection): configurable floor PR, held (owner 2026-09-24, feature-diff row 22 KEEP; upstream PRs paused) — replaces upstream lines: the MINIMUM_CONTEXT_LENGTH condition. Also here: MC init-phase timing receipts (`agent_runtime.init_observability`, retires only through a widening PR — no init-phase hook in VALID_HOOKS) and the `blocked_tool_names` pass-through (replaces the `_load_tools` signature/call); not movable: call sites inside `init_agent` | S3 |
| `agent/codex_runtime.py` | 2 | 0 | hook | Left: the usage-ledger row only (`agent_runtime.usage_ledger.record_usage`; this app-server path never fires `post_api_request`, which feeds the ledger everywhere else — lane MOVE-A), PLUGIN-AFTER #123978. The MC provider timing moved (lane PF-3): the plugin's `on_stream_start/delta/end` observers write `provider_stream_first_delta` / `provider_stream_consume` (`agent_runtime/codex_observability.py`), the dispatch total is the existing `llm_execution` span; `client_resolve`, `responses_create` and the per-attempt split were dropped (plugin-fit §4 Q4) | S3|
| `agent/conversation_compression.py` | 11 | 2 | hook | `local-llama-hermes` same-model aux floor exemption: configurable floor PR, held (owner 2026-09-24, feature-diff row 23 KEEP; upstream PRs paused) — replaces upstream lines: the floor condition. Also here: `agent_runtime.compression_metadata.child_model_config` (replaces the `model_config=` argument); not movable: inline expressions | S3 |
| `agent/conversation_loop.py` | 16 | 0 | hook | `reuse_current_user_message` threaded to `build_turn_context` (persona native rows) and MC prompt/turn timing marks (`agent_runtime.conversation_observability`). The G1 `run_conversation` barrier wrapper and its rename are GONE (owner ruling 2026-09-24, lane DOORS-A: upstream's lazy-install door, defaulted shut by the plugin). Cannot move: signature threading and in-function timing have no hook. Retires via ROW-PR (`history_includes_current_user` on `run_conversation` + `transform_persisted_message`) and PHASE-PR (an `on_phase_timing(phase, elapsed)` observer) | S3 |
| `agent/credential_pool.py` | 9 | 18 | upstream | RECORDED PARALLEL of upstream `CredentialPool._select_unlocked`'s strategy branch: `agent_runtime.pool_rotation.PoolRotationMixin` leads the MRO and the branch (-18) becomes `_select_with_rotation` (typed cursor sidecar, no row rewrite); whether a selection persists is the mixin's `pool_rotation_scope` ContextVar (lane CARRY3 2026-09-24 moved it out of `hermes_cli/runtime_provider*.py`, which are upstream's bytes again, and dropped `_select_unlocked`'s `persist_rotation` parameter). ADOPT refused (lane ADOPT 2026-09-24): upstream's `round_robin` rewrites every row's `priority` and `_persist()`s on EVERY select with no non-persisting select, so the launcher's readiness pass would move `auth.json` on a quiescent store (MCF-16, `tests/agent_runtime/test_readiness_credential_store_quiescent.py`); upstream `least_used` keeps its counts in memory only (MCF-45). Retires via a PR: a persisted rotation cursor + a non-persisting select | S3 |
| `agent/pet/generate/atlas.py` | 199 | 14 | upstream | PR candidate: context-aware strip-scale long-axis line erase, vertical box merge, lenient-row validation (class G3 pet atlas extraction); `frame_x_bounds`/`_slot_bounds` are ours (charsheet QA crop), movable → `agent/charsheet/pipeline.py` | S3 |
| `agent/process_bootstrap.py` | 5 | 0 | upstream | PR candidate: the copilot direct-client branch in `build_keepalive_http_client`. The memoized SSL context (class G4) RETIRED at the 2026-09-25 merge (`067fa1a257`): upstream `2f20ceb6f7` trusts the OS store through `agent.ssl_verify.install_truststore()` (design note §2.1, Q2); PR #121647 superseded | S3 |
| `agent/prompt_builder.py` | 39 | 7 | hook | moved (lane S2): the T6b guidance constants and the skill-confirm sentence -> the plugin's `eternia-harness.tool-guidance` section; `skill_matches_environment` import restored (upstream's). Left: the skill runtime-compat visibility filter (`skill_surface`/`root_node_mode`, snapshot v3 `runtime`) - waits on a skill-visibility hook PR, with `tools/skills_tool.py`; carry: all-context-files load (upstream PR `context_files.load_all`). Moved (lane PF-2, 2026-09-26): the Safety line -> the plugin's `llm_request` rewrite (`agent_runtime.prompt_guidance.rewrite_request_safety_sentence`, sentinel + one WARNING on miss); `WINDOWS_NATIVE_TOOLING_HINT` -> the plugin's `eternia-harness.windows-tooling` section | S3 |
| `agent/session_persistence.py` | 5 | 2 | hook | RECORDED PARALLEL inside upstream `_db_flush_row`: persona chat-root rows are projected through `agent_runtime.native_persistence.project_native_message` (+`msg_idx`), replacing the flush row's signature and call. Cannot move (lane CARRY3 2026-09-24): no persisted-row hook. Retires via ROW-PR (`transform_persisted_message`), this plugin its consumer | S3 |
| `agent/shell_hooks.py` | 1 | 1 | upstream | `_split_command` alias retired (lane MECH; upstream split_command_line). Left: PR candidate (G2) - `os.sep` in the script-path pick | S3 |
| `agent/skill_utils.py` | 9 | 4 | hook | RECORDED PARALLEL of upstream `get_all_skills_dirs` / `skills.external_dirs`: the fork's shared skills root (`agent_runtime.profile_home.get_shared_skills_dir()`) is appended after the home dir; `.realm_inbox`/`.provenance` are excluded dirs; `normalize_skill_lookup_name` trusts `get_all_skills_dirs`. Lane CARRY3 2026-09-24 moved the 28-name `agent_runtime.skill_resolution` alias block out (every caller imports `agent_runtime.skill_resolution` directly). ADOPT refused (lane ADOPT 2026-09-24): upstream classes every `external_dirs` root as externally owned (curator + sync treat it read-only) while the shared root is the fork's writable canonical root, and no profile config seed exists to write it. Retires via a skill-roots widening PR (a writable, non-external extra root), this plugin its consumer; the two `as_posix()` lines are G2 (upstream PR) | S3 |
| `agent/system_prompt.py` | 1 | 0 | hook | moved (lane S2): the four tool guidance lines -> the plugin section. Left, additive: `tool_names` in `_plugin_session_info` so a section can gate on the session's tool set - hook-pending; waits on that widening PR (harness as consumer) | S3 |
| `agent/turn_api_request.py` | 11 | 0 | hook | MC request_build / pre_api_hook / request_dump stamps: pre_api_request exists (surface has the bracket); per-phase build durations need a widening PR | S3 |
| `agent/turn_context.py` | 18 | 8 | hook | RECORDED PARALLEL inside upstream `_stage_turn_user_message` / `build_turn_context`: under `reuse_current_user_message` the persona native user row (already persisted) is not re-appended or re-stamped. Cannot move (lane CARRY3 2026-09-24): inline in the staging path, no hook. Retires via ROW-PR (`history_includes_current_user`) | S3 |
| `agent/turn_facade.py` | 2 | 0 | hook | additive: the `reuse_current_user_message` forwarder kwarg on the facade. Cannot move (lane CARRY3 2026-09-24): a signature. Retires via ROW-PR with `agent/turn_context.py` | S3 |
| `agent/turn_response_check.py` | 6 | 0 | hook | MC response_validate stamp: no response-validation hook — needs a widening PR | S3 |
| `agent/turn_response_intake.py` | 12 | 1 | upstream | PR candidate: relay NATIVE reasoning (`_extract_reasoning`) as `reasoning.available`, not only think-block content (class G5 reasoning relay) | S3 |
| `apps/desktop/src/app/settings/uninstall-section.test.tsx` | 43 | 1 | upstream | PR candidate with `uninstall-section.tsx` (same git-history warning): upstream shipped its own i18n test at this path (add/add at the 2026-09-24 merge); the fork describe block rides beside it and leaves with that PR | S3 |
| `apps/desktop/src/app/settings/uninstall-section.tsx` | 6 | 0 | upstream | PR candidate: warn that uninstalling the agent deletes the checkout's git history (holds for every upstream user; no SDK slot inside the confirm step) | S6 |
| `cli.py` | 2 | 1 | upstream | PR candidate: the tirith startup check reads `hermes_cli/tirith_config.tirith_enabled` (config + `TIRITH_ENABLED` env) instead of the raw config key; ships with that resolver | S3 |
| `contributors/emails/uperLu@users.noreply.github.com` | 2 | 2 | upstream | PR candidate: normalize the one CRLF contributor file upstream (fork LF rule, `tests/test_line_endings.py`); fallback: accept upstream's bytes via a `-text` keeper | S3 |
| `evals/postmortem/live_ab/cache_concurrency_probe.py` | 12 | 0 | upstream | PR candidate: fixes a NameError — `m`, `sys_sha`, `tools_sha`, `msg_shas` are used but never defined in upstream's `patched_stream` | S3 |
| `gateway/hosted_room_discussion.py` | 85 | 33 | hook | seam: widen the Group Chat host surface — host-declared `DiscussionLimits` + `active_member_ids`, consumer `agent_runtime/discussions/service.py`; a PR widening the generic surface with this host as consumer | S3 |
| `gateway/hosted_room_policy_checkpoint.py` | 6 | 3 | hook | seam: same Group Chat host-surface PR — host-declared `max_active_events` budget | S3 |
| `gateway/hosted_rooms.py` | 72 | 4 | hook | seam: same Group Chat host-surface PR — `pin_room_history`/`unpin_room_history` retention opt-in (written as generic, stock behaviour unchanged with nothing pinned) | S3 |
| `gateway/lifecycle_ledger.py` | 12 | 4 | upstream | PR candidate: `_process_hermes_home` must not fall back to the override-honouring `get_hermes_home()` when `HERMES_HOME` is unset (issue #56986 class); upstream/main still falls back | S3 |
| `gateway/platforms/base.py` | 4 | 2 | upstream | PR candidate: media hardening — `$HOME` for the delivery denylist, backslash as a MEDIA-path terminator, drop NUL paths in `_add` (upstream's `~\x00` catch covers only the expanduser arm) | S3 |
| `gateway/run.py` | 10 | 6 | upstream | PR candidates: the approvals heads-up replaces 5 upstream lines with `gateway.downstream_extensions._needs_risk_assessor_warning` so `TIRITH_ENABLED` is honoured through `hermes_cli.tirith_config` (the tirith fix PR, with `cli.py`); `start_gateway` calls `restore_durable_completions` (P2). The `DownstreamGatewayMixin` recorded parallel is GONE (lane DOORS-A 2026-09-24): `/queue-status` answers through `pre_gateway_dispatch`, the PM blocked-hook watcher rides `on_kanban_dispatch_tick`, the BG helpers were deleted by the BG ruling | S3 |
| `gateway/run_notifications.py` | 2 | 1 | upstream | PR candidate: `reply_to=watcher message_id` on watcher sends, its own one-line PR (no hook site inside the watcher loop). The BG-PR compact-send branch is deleted (owner ruling 2026-09-24, lane DOORS-A: BG on = notify AND an agent turn) | S3 |
| `gateway/shutdown_watchdog.py` | 12 | 3 | upstream | PR candidate: `_process_hermes_home` must not fall back to the override-honouring `get_hermes_home()` (issue #56986 class); upstream/main still falls back | S3 |
| `hermes_cli/auth.py` | 13 | 1 | hook | RECORDED PARALLEL of upstream `hermes_cli.auth._auth_file_path` (`get_hermes_home() / "auth.json"`): when the runtime has bound `HERMES_AUTH_HOME` (ContextVar, then env; written only by `agent_runtime.profile_context.persona_profile_context`) the head's store IS the active store, so a persona turn cannot fork a single-use refresh chain; unbound it is upstream's path. Lane AUTH-MOVE 2026-09-26 moved `persist_provider_login` to the fork-only transport. Lane CARRY3 2026-09-24 moved the rest out: the tail re-export of `agent_runtime.auth_extensions` (callers import it directly), the duplicate top-of-module `hermes_cli.config` import (upstream imports it below `PROVIDER_REGISTRY` on purpose) and the `hermes_constants` re-wrap; the resolver import is function-local. Cannot move: `_auth_file_path` is the one resolver and no store-home hook exists. Retires via HOME-PR (a pluggable per-store home resolver, auth store + background-work store), this plugin its consumer | S3 |
| `hermes_cli/auth_codex.py` | 3 | 1 | carry | Owner-requested 2026-09-24 (`98f8a8caf9` / `38f9345633` / `09d722bb30`). Only the PR-1 `on_verification` kwarg + fire is left (lane AUTH-MOVE 2026-09-26 moved the `login_*` caller into the fork-only `hermes_cli/provider_browser_login.py`); retires when PR-1 `up/auth-on-verification` merges (`docs/agent-runtime-harness/planned/auth-transport-plugin-2026-09-26.md` §2) | S3 |
| `hermes_cli/auth_codex_browser.py` | 4 | 1 | carry | Owner-requested 2026-09-24 (`98f8a8caf9` / `38f9345633` / `09d722bb30`). Only the PR-1 `on_verification` kwarg + fire is left (lane AUTH-MOVE 2026-09-26 moved the `login_*` caller into the fork-only `hermes_cli/provider_browser_login.py`); retires when PR-1 `up/auth-on-verification` merges (`docs/agent-runtime-harness/planned/auth-transport-plugin-2026-09-26.md` §2) | S3 |
| `hermes_cli/auth_commands.py` | 7 | 0 | hook | `auth set-key` / `auth login` (`hermes_cli/auth_noninteractive.py`): the launcher's argv builds these spellings (`provider_connect_controller.dart`), so they stay (lane S2). Waits on the widening PR for plugin sub-verbs on a builtin parser (`register_cli_command` is top-level only), or a launcher move to a harness spelling | S3 |
| `hermes_cli/auth_minimax.py` | 7 | 2 | carry | Owner-requested 2026-09-24 (`98f8a8caf9` / `38f9345633` / `09d722bb30`). Only the PR-1 `on_verification` kwarg + fire is left (lane AUTH-MOVE 2026-09-26 moved the `login_*` caller into the fork-only `hermes_cli/provider_browser_login.py`); retires when PR-1 `up/auth-on-verification` merges (`docs/agent-runtime-harness/planned/auth-transport-plugin-2026-09-26.md` §2) (with the `persist=False` kwarg: upstream's save activates) | S3 |
| `hermes_cli/auth_nous.py` | 2 | 1 | upstream | PR candidate: validate the portal-provided `inference_base_url` through upstream's own `_validate_nous_inference_url_from_network` (class G6 security hardening); `login_nous_account` moved to the fork-only transport (lane AUTH-MOVE 2026-09-26) | S3 |
| `hermes_cli/auth_xai.py` | 6 | 1 | carry | Owner-requested 2026-09-24 (`98f8a8caf9` / `38f9345633` / `09d722bb30`). Only the PR-1 `on_verification` kwarg + fire is left (lane AUTH-MOVE 2026-09-26 moved the `login_*` caller into the fork-only `hermes_cli/provider_browser_login.py`); retires when PR-1 `up/auth-on-verification` merges (`docs/agent-runtime-harness/planned/auth-transport-plugin-2026-09-26.md` §2) | S3 |
| `hermes_cli/commands_platforms.py` | 21 | 4 | upstream | `discord_skill_commands` byte copy deleted (lane MECH; upstream hermes_cli.commands 8ffd44a6f9). Left: PR candidate G7 - the Slack 50-command clamp named in the log and manifest output | S3 |
| `hermes_cli/config.py` | 10 | 6 | hook | fork-only: `load_config_readonly` applies the `local-llama-hermes` read projection (`hermes_cli/config_read_scope.project_readonly_config`, a ContextVar) - not a mutation refusal and not an upstream PR (re-dispositioned from G8 2026-09-24, lane LEDGER-DOCS, per the UPPR2 finding); here: `load_config_readonly` wraps the cache read in the projection and passes `ensure_home=False`. Retires via a config-read projection hook (widening PR), this plugin its consumer | S3 |
| `hermes_cli/doctor.py` | 40 | 7 | upstream | PR candidate: doctor resolves `HERMES_HOME`/`_DHH` at call time (module `__getattr__`) and loads dotenv per run (class G10 call-time home); `check_gateway_launcher` from `agent_runtime.doctor_extensions` is ours (`browser_probe_scope` / the `agent_browser_runnable_override` kwarg ADOPTED away, lane ADOPT 2026-09-24: tests patch `hermes_cli.doctor_tools.agent_browser_runnable`) — a doctor-check registration needs a widening PR | S3 |
| `hermes_cli/doctor_config.py` | 8 | 3 | upstream | PR candidate: call-time home instead of importing `doctor.HERMES_HOME` (G10) | S3 |
| `hermes_cli/doctor_platform.py` | 5 | 1 | upstream | PR candidate: call-time home (G10) | S3 |
| `hermes_cli/doctor_state.py` | 13 | 5 | upstream | PR candidate: call-time home (G10) | S3 |
| `hermes_cli/gateway.py` | 169 | 19 | upstream | PR candidate: POSIX-only `getuid`/`pwd` guards, the gateway home-resolution receipt (`hermes_cli/gateway_home_receipt.py`) (class G11); `_command_matches_profile` is upstream's own matcher extracted, a named parallel of `_scan_gateway_pids._matches_current_profile` until the PR. RECORDED PARALLEL since the 2026-09-25 merge (`067fa1a257`): `resolve_managed_python` / `ManagedPythonUnavailable` + the carried `_detect_venv_dir` (deleted upstream) beside upstream `_pm_runtime_venv_dir` (`pm.environments.selected_venv`) - pm's committed environment does not find the launcher's out-of-checkout venv and `hermes update` was not exercised on the merged tree (Q4); retire when the launcher's install moves onto pm bundles | S3 |
| `hermes_cli/gateway_windows.py` | 123 | 2 | upstream | PR candidate: launcher interpreter via `resolve_managed_python`, refuse a single-pinned named-profile wrapper, console-less task detection + status warning (G11) | S3 |
| `hermes_cli/kanban_db_dispatch.py` | 13 | 0 | upstream | PR candidate: crash-evidence capture on dead-worker reclaim (class G9 kanban); the `run_daemon(ttl_seconds)` half is gone (lane DOORS-A 2026-09-24: `HERMES_KANBAN_CLAIM_TTL_SECONDS`, plugin default) | S3 |
| `hermes_cli/main.py` | 60 | 195 | hook | §0.3, hunk by hunk: parser seam `build_downstream_parsers`, `harness`/`postinstall` in `cmd_console`, `dispatch_command` wrapper → hook (S1); `mark_main_entered` → hook `pre_command`; the two import-time boot-clock marks → upstream P4 or carry as one-liners; profile-bootstrap replacement (−187, `_profile_bootstrap.py` + `is_hermes_cli_entrypoint` gate) → upstream P1; `restore_durable_completions()` → MOVED (lane PF-2, 2026-09-26) to the plugin's `on_session_start` hook, `platform == "cli"`, once per process; the dead `cmd_postinstall` tail import → DELETED (PF-2); `_warn_legacy_console_gateway_task` tail import → confirm dead and delete (S1). After S1 + P1: ≤ 3 additive one-liners | S1, S3 |
| `hermes_cli/main_web_build.py` | 2 | 47 | upstream | PR candidate: single-winner lock for the stale-bytecode launch sweep (`hermes_cli/_bytecode_sweep.py`, class G12); until merged `_bytecode_sweep` shadows upstream `_sweep_stale_bytecode_if_checkout_changed`/`_record_bytecode_fingerprint`, a named parallel. The loud build-stamp carry left at the 2026-09-25 merge (`067fa1a257`) (upstream deleted the stamp helpers) | S3 |
| `hermes_cli/mcp_config.py` | 18 | 2 | upstream | PR candidate: `hermes mcp test --env` one-shot runtime env (class G13 MCP env); ours: machine-root path tokens (`agent_runtime.machine_roots`); `_ENV_VAR_NAME_RE` re-homed to `agent_runtime.mcp_environment` replaces upstream's constant (a parallel, restore it); the `cmd_args` line is the flag-binding REVERT | S3 |
| `hermes_cli/plugin_compat.py` | 4 | 0 | upstream | PR candidate: skip the compat AST scan for bundled plugins (class G14 plugin-loader cost) | S3 |
| `hermes_cli/plugins.py` | 50 | 2 | hook | Stage 1 seam: `discover_declared_cli_commands` / `_materialize_declared_cli_command` (manifest-declared CLI commands without discovery) — needs the widening PR, harness as consumer; the discovery `elapsed_ms` log is G14 | S1 |
| `hermes_cli/plugins_discovery.py` | 6 | 6 | upstream | PR candidate: enable/disable lists read through `load_config_readonly`, one shared config read (G14) | S3 |
| `hermes_cli/plugins_manifest.py` | 20 | 0 | hook | Stage 1 widening PR: the manifest `cli_commands` field (with `hermes_cli/plugins.py`) | S1 |
| `hermes_cli/provider_catalog.py` | 196 | 0 | upstream | PR candidate: hoist the OAuth login-flow rows (`OAUTH_FLOW_OVERRIDES`, `disconnect_command_for`) out of the dashboard into the catalog whose docstring says it unifies them (class G15, with `web_server_oauth.py`/`web_routers/oauth.py`); `provider_login_catalog`/`MODELS_DEV_LANE_IDS`/`models_dev_id_for`/`_default_flow_for` are ours (launcher roster), movable → a fork module beside `hermes_cli/model_picker_policy.py`; the `browser_login*` row keys moved to `harness_parts/provider_visibility.py` (lane AUTH-MOVE 2026-09-26) | S3 |
| `hermes_cli/service_manager.py` | 1 | 1 | upstream | PR candidate: comment names `tests/docker/test_uid_remap.py`, which upstream does not have; its file is `test_puid_pgid_remap.py` (class G16 doc accuracy) | S3 |
| `hermes_cli/slack_cli.py` | 17 | 0 | upstream | PR candidate: `slack manifest` names the slash commands Slack's 50-cap dropped (class G7 no silent caps) | S3 |
| `hermes_cli/subcommands/auth.py` | 37 | 0 | hook | `auth set-key` / `auth login` parsers (with `auth_commands.py`): launcher spellings kept (lane S2); waits on the sub-verbs-on-a-builtin-parser widening PR | S3 |
| `hermes_cli/subcommands/mcp.py` | 10 | 0 | upstream | PR candidate: `hermes mcp test --env KEY=VALUE` (G13) | S3 |
| `hermes_cli/uninstall.py` | 3 | 1 | upstream | PR candidate: warn that the checkout's git history is deleted (G16). `_comparable_path` and the PLUGIN-COMPAT `find_shell_configs` block left at the 2026-09-25 merge (`067fa1a257`) (upstream's `link.resolve()` normalizes extended-path prefixes) | S3 |
| `hermes_cli/update_cmd.py` | 11 | 0 | upstream | PR candidate: fork-history guard — a fork's failed fast-forward never resets (`hermes_cli/update_history.py`) (class G17 updater fork safety); the tail import of `_warn_legacy_console_gateway_task` is dead (no reader by grep), REVERT that line | S3 |
| `hermes_cli/update_cmd_git.py` | 5 | 2 | upstream | PR candidate: fork sync pushes without `--force-with-lease`; diverged-fork message (G17) | S3 |
| `hermes_cli/update_cmd_windows.py` | 30 | 1 | upstream | PR candidate: launcher refresh tolerates `ManagedPythonUnavailable` and warns about a legacy visible-console task (G11). The venv-holder clearing family left at the 2026-09-25 merge (`067fa1a257`) (upstream `5e4a2a3d24`) | S3 |
| `hermes_cli/update_inventory.py` | 15 | 0 | upstream | PR candidate: cached fork-history assessment in the update plan (G17) | S3 |
| `hermes_cli/web_routers/oauth.py` | 4 | 26 | upstream | PR candidate (G15): disconnect command read from `provider_catalog.disconnect_command_for` - replaces upstream's inline helper until merged; upstream's Windows PowerShell command and `platform` parameter ported into that authority at the 2026-09-25 merge (`067fa1a257`) | S3 |
| `hermes_cli/web_server_oauth.py` | 8 | 31 | upstream | PR candidate (G15): `_OAUTH_PROVIDER_CATALOG` built from `provider_catalog.OAUTH_FLOW_OVERRIDES` + status fns — replaces upstream's table until merged | S3 |
| `hermes_cli/worktree_ops.py` | 1 | 1 | upstream | PR candidate: the docstring names a pre-push stale-base gate neither tree has (G16 doc accuracy) | S3 |
| `hermes_state_messages.py` | 3 | 1 | upstream | PR candidate: surface `finish_reason` on every role, not only assistant rows (upstream still gates it on assistant) | S3 |
| `hermes_state_sessions.py` | 2 | 2 | upstream | PR candidate: deterministic `ORDER BY started_at DESC, id DESC` tie-break in session listing (2 queries) | S3 |
| `model_tools.py` | 44 | 3 | hook | seam: per-run tool-name filter (`blocked_tool_names`) + tool-defs memo hit/miss counters + `ensure_tool_describe_present` injection; needs a tool-filter hook PR (surface lacks it). The `get_registered_toolset_names` wrapper is gone (callers use `tools.registry.registry`) | S3 |
| `nix/checks.nix` | 1 | 1 | upstream | PR candidate: the `mcp catalog` check no longer swallows the command's failure (`2>/dev/null` + `true` fallback) | S3 |
| `plugins/dashboard_auth/_shared.py` | 4 | 3 | upstream | PR candidate: read-only config load (upstream `load_config_readonly`) + deepcopy of the returned section | S3 |
| `plugins/memory/__init__.py` | 69 | 1 | upstream | PR candidate: `_publish_module` binds the loaded provider on its parent package as real imports do (and unbinds on failure) | S3 |
| `plugins/platforms/feishu/adapter.py` | 63 | 5 | upstream | PR candidate: dedup-state path resolved per call, cache hydrated on first use — no `HERMES_HOME` frozen at construction (the `test_no_frozen_hermes_home` class) | S3 |
| `providers/__init__.py` | 1 | 1 | upstream | PR candidate (upstream boot import cost, one line): import through upstream's own `hermes_cli.plugins_discovery` so `hermes_cli.plugins` stays off the boot import path. KEPT (lane MECH 2026-09-24): the revert re-reds tests/agent_runtime/test_tool_visibility_import_deferral.py (3 failed). Nothing to move (lane CARRY3 2026-09-24): it is a generic fix to an upstream module's import | S3 |
| `pyproject.toml` | 64 | 1 | carry-permanent | RULED 2026-09-24 (lane META): the `agent_runtime` include in `packages.find` has no override point while `agent_runtime` ships in this distribution; retires at Stage 7 (its own package). Left beside it, not ruled: the ruff `F821` select + per-file ignores (PR candidate). Moved out: fork markers + `--timeout=30 --timeout-method=thread` → `tests/_downstream/conftest_plugin.py` `pytest_configure`; coverage/pytest-timeout → `requirements-fork-dev.txt` (installed by `scripts/ensure_fork_dev_deps.py` from `scripts/run_tests.sh`) | - |
| `run_agent.py` | 1 | 0 | hook | `blocked_tool_names` kwarg (the `model_tools.py` tool-filter hook PR); the `session_usage_ledger` seed left with the ledger (lane MOVE-A: `agent_runtime/usage_ledger.py` fed by the plugin's `post_api_request` hook) | S3 |
| `scripts/audit_pr_attribution.py` | 4 | 1 | upstream | PR candidate: exact-name contributor lookup incl. `case-variants/` (Windows case-insensitive FS); with `contributor-check.yml` + `release.py` | S3 |
| `scripts/check_subprocess_stdin.py` | 2 | 2 | upstream | PR candidate: `as_posix()` path compares so the allowlist and `/tests/` skip work on Windows | S3 |
| `scripts/releases/authors.py` | 3 | 1 | upstream | PR candidate: read `contributors/emails/` recursively for `case-variants/` (moved from the `scripts/release.py` row at the 2026-09-25 merge (`067fa1a257`): upstream moved the reader here); with `contributor-check.yml` + `audit_pr_attribution.py` | S3 |
| `scripts/run_tests.sh` | 116 | 1 | upstream | §0.4: runner improvements (P5); carry the hermetic-env rows. The shared-test-venv probe RETIRED at the 2026-09-25 merge (`067fa1a257`) for upstream's `HERMES_PYTHON` door (Q6); the PATHEXT line left (upstream forwards it) | S3 |
| `scripts/run_tests_parallel.py` | 181 | 42 | upstream | §0.4: runner improvements (P5); carry the hermetic-env rows | S3 |
| `tests/_fixtures/env_filter.py` | 10 | 1 | upstream | PR candidate: one compiled credential-suffix alternation in `_looks_like_credential` (hermes-suite-perf Stage 2); moved from the `tests/conftest.py` row with upstream's carve-out at the 2026-09-25 merge (`067fa1a257`) | S3 |
| `tests/_fixtures/live_system_guard.py` | 159 | 8 | upstream | PR candidates, moved from the `tests/conftest.py` row with upstream's carve-out at the 2026-09-25 merge (`067fa1a257`): the backend-spawn arm (`hermes gateway | serve |
| `tests/agent/test_coding_context.py` | 13 | 2 | upstream | lifted: up/win-line-endings | up/win-line-endings |
| `tests/agent/test_compression_adoption_preserves_live_tail.py` | 1 | 1 | upstream | PR candidate: stale doc pointers (docstring/comment names a moved or never-existing test path) | S3 |
| `tests/agent/test_image_routing.py` | 1 | 1 | upstream | lifted: up/win-tilde-home | up/win-tilde-home |
| `tests/agent/test_provider_fallback.py` | 5 | 3 | upstream | PR candidate: stale doc pointers (docstring/comment names a moved or never-existing test path) - names test_fallback_model.py, deleted upstream in e2fd462ebe | S3 |
| `tests/agent/test_shell_hooks_consent.py` | 6 | 1 | upstream | lifted: up/win-tilde-home | up/win-tilde-home |
| `tests/agent/test_skill_commands.py` | 10 | 2 | upstream | lifted: up/win-shell-invocation; rest superseded by upstream b2ecd3518f: pwd -W lifted; supporting-files hunk targets a test upstream deleted | up/win-shell-invocation |
| `tests/agent/test_skill_utils.py` | 7 | 5 | upstream | PR candidate: win-path-spelling extension (Path() compare, not in up/win-path-spelling); REVERT orphan S5 banner; 1 hunk already upstream (b2ecd3518f) | S3 |
| `tests/cron/test_cron_memory_contract.py` | 1 | 1 | upstream | PR candidate: stale doc pointers (docstring/comment names a moved or never-existing test path) | S3 |
| `tests/docker/test_dashboard.py` | 3 | 1 | upstream | PR candidate: stale doc pointers (docstring/comment names a moved or never-existing test path) | S3 |
| `tests/gateway/test_feishu.py` | 13 | 6 | upstream | PR candidate: hermetic home under a clear=True env (Path.home falls back to the real home) | S3 |
| `tests/gateway/test_media_spaced_paths_and_history_dedupe.py` | 1 | 0 | upstream | lifted: up/win-tilde-home | up/win-tilde-home |
| `tests/hermes_cli/test_apply_profile_override.py` | 24 | 2 | upstream | PR candidate: win platform-default root pin (_get_platform_default_hermes_home) + encoding not in up/win-text-encoding | S3 |
| `tests/hermes_cli/test_auth_nous_provider.py` | 33 | 4 | upstream | lifted: up/win-posix-only-apis | up/win-posix-only-apis |
| `tests/hermes_cli/test_backup.py` | 33 | 7 | upstream | lifted: up/win-path-spelling, up/win-posix-only-apis; rest PR candidate: held, test stays red on Windows for another reason (R10): .bat wrapper hunk | up/win-path-spelling+up/win-posix-only-apis |
| `tests/hermes_cli/test_deleted_profile_tombstone.py` | 6 | 1 | upstream | lifted: up/win-path-spelling | up/win-path-spelling |
| `tests/hermes_cli/test_doctor.py` | 3 | 3 | upstream | test half of the G10 call-time-home PR (`hermes_cli/doctor.py` row): 3 `monkeypatch.setenv("HERMES_HOME", …)` in place of `setattr(doctor_mod, "HERMES_HOME", …)` — the fork's doctor resolves the home at call time, so upstream's module-constant patch is inert. Stays with that PR (lane CARRY3 2026-09-24): the first site is `TestDoctorMemoryProviderSection._run_doctor_and_capture`, shared by 11 tests, so a rule-4 move would copy that class into the sibling | S3 |
| `tests/hermes_cli/test_doctor_journal_modes.py` | 4 | 3 | upstream | lifted: up/win-posix-only-apis; rest PR candidate: linux-only markers (@pytest.mark.linux_only; needs per-test proof the behaviour is Linux-specific) | up/win-posix-only-apis |
| `tests/hermes_cli/test_early_recovery.py` | 127 | 0 | upstream | lifted: up/import-guard-relative-imports | up/import-guard-relative-imports |
| `tests/hermes_cli/test_gateway_migrate_multiplex.py` | 3 | 0 | upstream | PR candidate: linux-only markers (@pytest.mark.linux_only; needs per-test proof the behaviour is Linux-specific) | S3 |
| `tests/hermes_cli/test_kanban_worktree_teardown.py` | 1 | 0 | upstream | PR candidate: linux-only markers (@pytest.mark.linux_only; needs per-test proof the behaviour is Linux-specific) | S3 |
| `tests/hermes_cli/test_linux_desktop_entry.py` | 20 | 0 | upstream | PR candidate: linux-only markers (@pytest.mark.linux_only; needs per-test proof the behaviour is Linux-specific) | S3 |
| `tests/hermes_cli/test_local_runtime.py` | 5 | 0 | upstream | PR candidate (lane CARRY2A): the stub's POST /models/unload marks the model unloaded so `unload_model`'s 15 s confirmation loop exits; that loop is upstream's own (prod identical), the fork's repo-wide --timeout=30 only turns upstream's wait into a red (3 tests time out on upstream's bytes) | S3 |
| `tests/hermes_cli/test_node_runtime_npm_resolution.py` | 3 | 0 | upstream | PR candidate: linux-only markers (@pytest.mark.linux_only; needs per-test proof the behaviour is Linux-specific) | S3 |
| `tests/hermes_cli/test_orphan_desktop_serve_reap.py` | 5 | 0 | upstream | PR candidate: linux-only markers (@pytest.mark.linux_only; needs per-test proof the behaviour is Linux-specific) | S3 |
| `tests/hermes_cli/test_projects_db.py` | 19 | 7 | upstream | lifted: up/win-path-spelling | up/win-path-spelling |
| `tests/hermes_cli/test_prompt_compose_command.py` | 25 | 11 | upstream | lifted: up/win-shell-invocation | up/win-shell-invocation |
| `tests/hermes_cli/test_setup_hermes_script.py` | 19 | 1 | upstream | lifted: up/win-shell-invocation | up/win-shell-invocation |
| `tests/hermes_cli/test_win_pty_bridge.py` | 41 | 3 | upstream | lifted: up/win-conpty-line-wrap | up/win-conpty-line-wrap |
| `tests/scripts/test_run_tests_parallel.py` | 100 | 3 | upstream | PR candidate: probe rootdir anchor (pytest.ini) + utf-8 decode not in up/win-text-encoding; REVERT orphan S5 banners; 1 hunk already upstream (524c38a98a) | S3 |
| `tests/test_hermes_constants.py` | 9 | 1 | upstream | PR candidate: win platform-default root pin (sys.platform, mirrors upstream's own win32 sibling) | S3 |
| `tests/tools/test_approved_command_clean_slate.py` | 20 | 2 | upstream | lifted: up/win-shell-invocation | up/win-shell-invocation |
| `tests/tools/test_base_environment.py` | 6 | 2 | upstream | PR candidate: held, test stays red on Windows for another reason (R10): bash resolution (win-shell-invocation class), then the 0600 test fails on NTFS mode bits; its concurrency hunk targets tests upstream deleted (524c38a98a) | S3 |
| `tests/tools/test_delegate.py` | 2 | 2 | upstream | PR candidate: stale doc pointers (docstring/comment names a moved or never-existing test path) | S3 |
| `tests/tools/test_execution_flag_detection.py` | 11 | 2 | upstream | lifted: up/win-shell-invocation; rest superseded by upstream 5f6b1d251f: second hunk annotates a test upstream deleted | up/win-shell-invocation |
| `tests/tools/test_file_ops_cwd_tracking.py` | 12 | 4 | upstream | PR candidate: held, test stays red on Windows for another reason (R10): _find_bash resolution (win-shell-invocation class); test still red at a later line | S3 |
| `tests/tools/test_file_tools.py` | 1 | 0 | upstream | PR candidate: win-path-spelling extension (os.path.normpath, not in up/win-path-spelling) | S3 |
| `tests/tools/test_file_tools_live.py` | 35 | 4 | upstream | lifted: up/win-line-endings, up/win-shell-invocation | up/win-line-endings+up/win-shell-invocation |
| `tests/tools/test_file_tools_tilde_profile.py` | 9 | 2 | upstream | lifted: up/win-path-spelling | up/win-path-spelling |
| `tests/tools/test_interrupt.py` | 1 | 1 | upstream | PR candidate: stale doc pointers (docstring/comment names a moved or never-existing test path) | S3 |
| `tests/tools/test_local_background_child_hang.py` | 10 | 3 | upstream | lifted: up/win-shell-invocation | up/win-shell-invocation |
| `tests/tools/test_local_env_cwd_recovery.py` | 10 | 1 | upstream | lifted: up/win-path-spelling | up/win-path-spelling |
| `tests/tools/test_local_env_relative_cwd.py` | 25 | 2 | upstream | lifted: up/win-shell-invocation | up/win-shell-invocation |
| `tests/tools/test_local_env_windows_msys.py` | 15 | 3 | upstream | PR candidate: win-path-spelling extension (os.path.join); REVERT duplicated S5 banner | S3 |
| `tests/tools/test_modal_sandbox_fixes.py` | 28 | 4 | hook | expects `tool_describe` injected by `model_tools.py` (waits on its tool-filter/injection hook PR); `_native_host_cwd` is G2 (Windows paths PR) | S3 |
| `tests/tools/test_skills_hub.py` | 15 | 3 | upstream | lifted: up/win-line-endings; rest PR candidate: held, test stays red on Windows for another reason (R10): jo.txt write_bytes does not fix the KeyError | up/win-line-endings |
| `tests/tools/test_subprocess_home_isolation.py` | 9 | 2 | upstream | lifted: up/win-path-spelling | up/win-path-spelling |
| `tests/tools/test_terminal_output_transform_hook.py` | 6 | 1 | upstream | lifted: up/win-shell-invocation | up/win-shell-invocation |
| `tools/approval_context.py` | 3 | 9 | upstream | PR candidate: `_tirith_fail_open` reads the one tirith flag authority with env overrides (`hermes_cli/tirith_config.py`) (class G18 tirith config authority) | S3 |
| `tools/approval_detection.py` | 43 | 7 | upstream | PR candidate: the verification-artifact cleanup exemption accepts the MSYS spelling and asks identity through `tools/path_identity` (G2 Windows paths) | S3 |
| `tools/async_delegation.py` | 15 | 1 | hook | RECORDED PARALLEL of upstream `tools.async_delegation._db_path` (`get_hermes_home() / "state.db"`): the delegation store resolves through `agent_runtime.profile_home.get_hermes_background_work_home`, so a dispatch inside a persona turn (HERMES_HOME flipped to the persona profile) persists where the operator's Activity projection reads. Cannot move (lane CARRY3 2026-09-24): a private upstream function and no per-store home hook on the plugin surface (rule 3 forbids patching it from the plugin). Retires via HOME-PR: a pluggable per-store home resolver (auth store, background-work store), this plugin its consumer | S3 |
| `tools/browser_tool_lifecycle.py` | 2 | 1 | upstream | PR candidate: daemon socket-dir binding compared by file identity (`tools/path_identity.denotes_same_file`) (G2) | S3 |
| `tools/code_execution_env.py` | 3 | 2 | upstream | PR candidate: interpreter / prefix identity via `denotes_same_file` (G2) | S3 |
| `tools/credential_files.py` | 1 | 1 | upstream | PR candidate: container-side paths via `as_posix()` on Windows hosts (G2) | S3 |
| `tools/environments/local.py` | 107 | 1 | upstream | PR candidate (G2): MSYS spellings, `_shell_arg_safe_path`. `_windows_bash_candidates` RETIRED at the 2026-09-25 merge (`067fa1a257`) (bash discovery is upstream `pm.shell`); `_bash_from_git` / `_is_windows_system_shim` deleted with it (tombstoned) | S3 |
| `tools/file_tools.py` | 24 | 2 | upstream | wire briefs for write_file/patch/search_files moved to the eternia-harness `llm_request` middleware (lane MOVE-A; registry keeps upstream's text). Left: PR candidate (G2) - the `_posix_match_forms` device-path guard over `tools/path_identity.py` | S3 |
| `tools/file_tools_write_guards.py` | 5 | 4 | upstream | PR candidate: sensitive / config / instruction path guards compare by identity and POSIX match forms (G2) | S3 |
| `tools/image_generation_tool.py` | 2 | 2 | carry | fork-only: `load_config_readonly` applies the `local-llama-hermes` read projection (`hermes_cli/config_read_scope.project_readonly_config`, a ContextVar) - not a mutation refusal and not an upstream PR (re-dispositioned from G8 2026-09-24, lane LEDGER-DOCS, per the UPPR2 finding); here: the FAL config read routed through it. Leaves with the config.py hook | S3 |
| `tools/mcp_tool_config.py` | 41 | 1 | hook | RECORDED PARALLEL inside upstream `_build_safe_env` / `_load_mcp_config`: child `HERMES_HOME` injection, per-server process env overrides, `runtime_env`, and the machine-root token + platform gate (`agent_runtime.mcp_environment`, `agent_runtime.machine_roots.resolve_mcp_servers`); replaces the `_build_safe_env` signature. Cannot move (lane CARRY3 2026-09-24): no MCP server-config or child-env hook exists; not an ADOPT of upstream `_interpolate_env_vars` (`${VAR}`), because `resolve_mcp_servers` also DROPS servers by platform. Retires via G13: the `HERMES_HOME` injection as an upstream fix plus a widening PR (an mcp server-config transform + child-env hook), this plugin its consumer | S3 |
| `tools/mcp_tool_transport.py` | 1 | 1 | hook | call-site half of `tools/mcp_tool_config.py` (passes `server_name` / `runtime_env` into `_build_safe_env`, one replaced upstream line); retires with that row's G13 widening PR | S3 |
| `tools/process_registry.py` | 9 | 13 | upstream | lane DOORS-A 2026-09-24 (owner ruling): the late `process notify` second door is DELETED — `_move_to_finished`'s completion block, the drain-skip clause and the `notify` action/enum/schema line are at upstream bytes, `tools/process_notify_store.py` is gone; completion is upstream's spawn-time `notify`, defaulted on by the plugin's `tool_request` middleware (`agent_runtime.background_completion`), and the persona-instance-gone drop lives in the serve drain (`dispatch_delivery._orphaned_persona_root`). Left, not a door: `ProcessNotificationMixin` in the bases (durable restore at startup, P2), `checkpoint_path` via the background-work home (HOME-PR), `wait_ceiling_seconds` (the 600 s mission-chat floor; wait-ceiling config PR), Windows PTY EOF `0x1A` (G2) | S3 |
| `tools/process_registry_notifications.py` | 29 | 1 | upstream | PR candidate: redact, ANSI-strip and bound every field of a process notification before it re-enters the conversation (class G6 security hardening) | S3 |
| `tools/registry.py` | 194 | 36 | upstream | §0.4: `check_fn` TTL cache + probe accounting — generic perf the fork measured (P3); tool-registration edits go with S2 | S3 |
| `tools/skills_hub_official.py` | 1 | 1 | upstream | PR candidate: bundle keys via `as_posix()` (G2) | S3 |
| `tools/skills_tool.py` | 53 | 68 | hook | moved (lane S2): `skill_search` -> plugin `register_tool` (toolset `skills`), `skill_matches_environment` -> upstream's delegate. Moved (lane PF-3): the `skill_view` runtime-compat refusal + `resolution_status`/`source_kind`/`content_hash` -> the plugin's `transform_tool_result` (`agent_runtime/skill_view_result.py`; model path only, §4 Q3); `skill_inspection_reader` -> `agent_runtime/skill_inspection.py` (private reads recorded below). Left: shared-root dirs (PLUGIN-AFTER #124191), `resolve_skill` vs upstream's walk (ADOPT, §4 Q6), the `skills_list` runtime filter (skill-visibility widening); `as_posix` is G2 | S3|
| `tools/terminal_tool.py` | 88 | 1 | hook | DESIGN (lane S2): `pre_tool_call` misses the direct `terminal_tool()` callers (`tui_gateway/methods_complete.py`, `tools/bot_mode_dm.py`, evals) and turns the typed block (`status: blocked`, `blocked_by`, `block_reason`) into `tool_error`; a safety envelope must not become bypassable. Waits on a terminal command-guard hook PR (a plugin guard consulted by `_check_all_guards`, typed result). Carry: persona chat container scope, `brief_schema('terminal')` | S3 |
| `tools/terminal_tool_result.py` | 2 | 1 | upstream | PR candidate: cwd change detected by file identity (G2) | S3 |
| `tools/tirith_security.py` | 61 | 11 | upstream | PR candidate: tirith flags through one authority (`hermes_cli/tirith_config.py`) + an unsupported-platform fail-closed warning instead of a silent allow (G18) | S3 |
| `tools/tool_search.py` | 117 | 16 | hook | RECORDED PARALLEL of upstream tool-search classify/describe: hardcoded `never_defer` (`agent_chat_*`) + the `never_defer` config key, `parameters` inlined in top search hits, an always-present standalone `tool_describe` (single `name`) serving full docs over every tool, legacy single `query`/`name` args. Cannot move (lane CARRY3 2026-09-24): in-module policy with no hook; the legacy args cannot ride `tool_request` middleware because the fork's single-name describe returns a different result shape. Retires via the G19 widening PR (`never_defer` key + describe always on + top-hit parameters); the legacy shape is dropped with it | S3 |
| `tools/tts_tool.py` | 5 | 4 | carry | fork-only: `load_config_readonly` applies the `local-llama-hermes` read projection (`hermes_cli/config_read_scope.project_readonly_config`, a ContextVar) - not a mutation refusal and not an upstream PR (re-dispositioned from G8 2026-09-24, lane LEDGER-DOCS, per the UPPR2 finding); here: the tts config read (deep-copied). The `denotes_same_file` artifact identity hunk stays a G2 PR candidate | S3 |
| `tools/tts_tool_delivery.py` | 3 | 2 | upstream | PR candidate: audio paths compared by identity (G2) | S3 |
| `tools/vision_tools.py` | 5 | 5 | carry | fork-only: `load_config_readonly` applies the `local-llama-hermes` read projection (`hermes_cli/config_read_scope.project_readonly_config`, a ContextVar) - not a mutation refusal and not an upstream PR (re-dispositioned from G8 2026-09-24, lane LEDGER-DOCS, per the UPPR2 finding); here: the two config reads routed through it, plus `_lookup_supports_vision`. Leaves with the config.py hook | S3 |
| `toolsets.py` | 25 | 2 | hook | `skill_search` is a plugin tool since lane S2, but its names stay in the core and `skills` lists: the fork pins it on every bundle that ships `skills_list` (`tests/tools/test_toolsets_downstream.py`) and bundles are static name lists. Waits on a register-toolset PR (bundle membership + the `harness_core` composite), PLUGIN-AFTER #123979. `expand_toolset_names` moved to `agent_runtime/toolset_names.py` (lane PF-3) | S3|
| `tui_gateway/entry.py` | 14 | 0 | upstream | P2: durable-completion restore at TUI startup (fallback: `on_session_start` hook) | S3 |
| `tui_gateway/hosted_room_driver.py` | 15 | 2 | hook | seam: Group Chat host-surface PR — `request_reconciliation`; its three settlement fixes (`stop_unresolved`, active inspection, terminal `error`) go in the same PR as plain fixes | S3 |
| `tui_gateway/server.py` | 1 | 1 | upstream | KEPT (lane MECH 2026-09-24): the REVERT re-reds tests/test_coverage_claims_resolve.py - base's docstring names `test_generated.py::test_every_method_has_a_contract`, which upstream deleted too (NAMED arm). PR candidate: upstream's own stale test name | S3 |
| `utils.py` | 4 | 4 | upstream | PR candidate: `newline=` passthrough on `atomic_write_text`/`_atomic_write` (3 fork callers). `_replace_with_windows_contention_retry` retired (lane MECH; upstream dcbe175423) | S3 |
| `website/docs/developer-guide/billing-lifecycle.md` | 7 | 5 | upstream | PR candidate (website docs): poll loop moved to `apps/shared/src/charge-settlement.ts` upstream; the upstream page is stale | S3 |
| `website/docs/developer-guide/chronos-managed-cron-contract.md` | 1 | 1 | upstream | PR candidate (website docs): verifier path is `plugins/cron_providers/chronos/verify.py` upstream; the upstream page is stale | S3 |
| `website/docs/developer-guide/gateway-session-lifecycle.md` | 3 | 2 | upstream | PR candidate (website docs): stuck-loop file is `.restart_failure_counts` and `session_store_max_age_days` defaults to 90 in upstream code; the upstream page is stale | S3 |
| `website/docs/developer-guide/relay-connector-contract.md` | 9 | 1 | upstream | PR candidate (website docs): `prospective_thread_id`, `profile`, `auto_thread_*` wire fields and the local-only trust signals exist in upstream code but not in its table | S3 |
| `website/docs/user-guide/egress/network-isolation.md` | 1 | 1 | upstream | PR candidate (website docs): the dashboard health route is `/api/health` (`hermes_cli/web_routers/status.py`) | S3 |

## Stock files a fork module reads by private name (no diff row, recorded here)

A file that is byte-identical to upstream has no row above; these fork modules
reach into it by a module-private name, so an upstream rename breaks them
without touching the ratchet. Each carries the PR that retires the import.

| upstream file | private names | fork reader | retires with |
|---|---|---|---|
| `gateway/session_context.py` | `_SESSION_ASYNC_DELIVERY`, `_UNSET` | `agent_runtime/_upstream_doors.py::session_async_delivery_var` / `::session_async_delivery_unset` (call-time, lane W3-B), read by `agent_runtime/delivery_capability.py` (`declare_async_delivery_channel`, `async_delivery_declared`; lane MOVE-A) | a two-function upstream PR adding those two readers beside `declare_stateless_channel` / `async_delivery_supported` |
| `agent/auxiliary_client.py` | `_compression_threshold_for_model` | `agent_runtime/_upstream_doors.py::compression_threshold_for_model`, read by `agent_runtime/prompt_observability/context_budget.py::_compaction_ratio` (lane R2, ruling Q7) | held widening PR: expose `compression_threshold_for_model` publicly |
| `tools/skills_tool.py` (also a diff row above) | `_find_all_skills` | `agent_runtime/_upstream_doors.py::skills_walker`, read by `agent_runtime/prompt_observability/skills_resolver.py::_resolve_skill_walker` (lane R2, ruling Q7) | held widening PR: publish `find_all_skills`. The public `agent.skill_utils.get_all_skills_dirs` returns the roots, not parsed skill rows; walking them in the fork would re-implement `_find_all_skills` (a parallel, ruled against 2026-09-23), so the door stays |
| `tools/skills_tool.py` (also a diff row above) | `_find_all_skills`, `_sort_skills`, `_get_disabled_skill_names`, `_skill_lookup_path_error`, `_skill_search_dirs`, `_locate_skill` | `agent_runtime/_upstream_doors.py::skills_tool_inspection_doors`, read by `agent_runtime/skill_inspection.py::skill_inspection_reader` (lane PF-3 moved the reader out of `skills_tool.py`; the plugin-fit sheet named four names, the tree reads six) | held widening PR: a read-only inspection port (catalog rows + locate) on upstream's skills tool; copying the walk and locate into the fork would be a second resolver (rule 9) |
| `agent/message_sanitization.py` | `_sanitize_surrogates` | `agent_runtime/_upstream_doors.py::sanitize_surrogates`, read by `agent_runtime/profile_runner/resident_actor.py::_sanitized_user_message_text` (lane R3, ruling Q7) | held widening PR: publish `sanitize_surrogates` |
| `tools/process_registry.py` (also a diff row above) | `ProcessRegistry._terminate_host_pid` | `agent_runtime/_upstream_doors.py::terminate_host_pid`, read by `tools/agent_chat_dispatch/child.py::_kill_child` (lane 2B-C, ruling Q7; a classmethod reached by attribute, so W0-G6's private-import arm cannot see it — and it does not walk `tools/` either) | held widening PR: publish `ProcessRegistry.terminate_host_pid` (the identity-verified tree-kill) |
| `agent/pet/generate/atlas.py` | `_fit_to_cell`, `_clear_transparent_rgb` | `agent/charsheet/_upstream_doors.py::fit_to_cell` / `::clear_transparent_rgb` (call-time), read by `agent/charsheet/pipeline/compose.py::compose_sheet` (lane B1, ruling Q7) | held widening PR: publish `fit_to_cell` and `clear_transparent_rgb` |
| `cron/scheduler.py` | `_parallel_pool`, `_sequential_pool` (module attributes, read by `getattr`) | `agent_runtime/_upstream_doors.py::cron_pools_present`, read by `agent_runtime/running_work/lanes_process.py::_cron_owned_here` (lane B4, ruling Q7; an attribute read by string, so W0-G6's private-import arm cannot see it) | held widening PR: publish `scheduler.owns_running_jobs()` |
| `tools/mcp_tool.py` | `_MCP_AVAILABLE` | `agent_runtime/_upstream_doors.py::mcp_sdk_available_flag`, read by `agent_runtime/mcp_admission/transport.py::mcp_sdk_available` (lane B4, ruling Q7) | held widening PR: publish `mcp_sdk_available()` |
| `tools/mcp_tool.py` | `_servers` | `agent_runtime/_upstream_doors.py::mcp_server_map`, read by `agent_runtime/mcp_admission/transport.py::_current_mcp_servers` (lane B4, ruling Q7) | held widening PR: publish `current_servers()` (the cache read under its lock, one call) |
| `tools/mcp_tool.py` | `_lock` | `agent_runtime/_upstream_doors.py::mcp_server_map`, read by `agent_runtime/mcp_admission/transport.py::_current_mcp_servers` (lane B4, ruling Q7) | held widening PR: the same `current_servers()` — the lock is only ever taken to read `_servers` |
| `tools/mcp_tool_scope.py` | `_key_name` | `agent_runtime/_upstream_doors.py::mcp_key_name`, read by `agent_runtime/mcp_admission/transport.py::_current_mcp_servers` (lane B4, ruling Q7) | held widening PR: publish `key_name` |
| `tools/mcp_tool_scope.py` | `_resolve_server_key` | `agent_runtime/_upstream_doors.py::mcp_resolve_server_key`, read by `agent_runtime/mcp_admission/transport.py::_current_mcp_servers` (lane B4, ruling Q7) | held widening PR: publish `resolve_server_key` |
| `tools/mcp_tool_loop.py` | `_signal_reconnect` | `agent_runtime/_upstream_doors.py::mcp_signal_reconnect`, read by `agent_runtime/mcp_admission/transport.py::_wake_parked_servers` (lane B4, ruling Q7) | held widening PR: publish `signal_reconnect` |
| `tools/mcp_tool_loop.py` | `_wait_for_server_session_ready` | `agent_runtime/_upstream_doors.py::mcp_wait_for_session`, read by `agent_runtime/mcp_admission/transport.py::_wake_parked_servers` (lane B4, ruling Q7) | held widening PR: publish `wait_for_server_session_ready` |
| `tools/mcp_tool_registration.py` | `_register_server_tools` | `agent_runtime/_upstream_doors.py::mcp_register_server_tools`, read by `agent_runtime/mcp_admission/transport.py::_reregister_warm_server` (lane B4, ruling Q7) | held widening PR: publish `register_server_tools` |
| `agent/pet/generate/prompts.py` | `_spacing_spec`, `_ASSUMED_STRIP_WIDTH`, `_BACKGROUND` | `agent/charsheet/_upstream_doors.py::prompt_spacing_spec` / `::prompt_assumed_strip_width` / `::prompt_background` (call-time), read by `agent/charsheet/prompts.py` (lane W3-B, ruling Q7) | held widening PR: publish `spacing_spec`, `ASSUMED_STRIP_WIDTH` and `BACKGROUND` |
| `agent/transports/codex.py` | `_bounded_prompt_cache_key`, `_cache_scope_from_session_id`, `_content_cache_key` | `agent_runtime/_upstream_doors.py::codex_bounded_prompt_cache_key` / `::codex_cache_scope_from_session_id` / `::codex_content_cache_key`, read by `agent_runtime/cache_routing.py` (lane W3-B, ruling Q7) | held widening PR: publish the three cache-key helpers |
| `agent/turn_api_call.py` | `_should_stream` | `agent_runtime/_upstream_doors.py::dispatch_streams`, read by `agent_runtime/conversation_observability.py::_dispatch_streams` (lane W3-B, ruling Q7) | held widening PR: publish `should_stream` |
| `hermes_cli/doctor_report.py` | `_section` | `agent_runtime/_upstream_doors.py::doctor_section`, read by `agent_runtime/doctor_extensions.py` (lane W3-B, ruling Q7) | held widening PR: publish `section` (or a doctor-check registration, the diff row's widening) |
| `gateway/run.py` | `_AGENT_PENDING_SENTINEL` | `agent_runtime/_upstream_doors.py::gateway_agent_pending_sentinel`, read by `agent_runtime/gateway_queue_status.py::queue_status_report` (lane W3-B, ruling Q7) | held widening PR: publish the sentinel (or an `is_agent_pending` reader) |
| `hermes_cli/profiles.py` | `_PROFILE_ID_RE`, `_iter_named_profile_dirs` | `agent_runtime/_upstream_doors.py::profile_id_pattern` / `::iter_named_profile_dirs`, read by `agent_runtime/profile_home.py::available_profile_template_summaries` (lane W3-B, ruling Q7) | held widening PR: publish both |
| `tools/skills_sync.py` | `_dir_hash`, `_read_skill_name` | `agent_runtime/_upstream_doors.py::skills_sync_primitives`, read by `agent_runtime/skill_publishability.py::_sync_primitives` (fail-closed; lane W3-B, ruling Q7) | held widening PR: publish both |
| `tools/terminal_tool_guards.py` | `_looks_like_help_or_version_command`, `_strip_quotes` | `agent_runtime/_upstream_doors.py::looks_like_help_or_version_command` / `::strip_quotes`, read by `agent_runtime/terminal_policy.py` (lane W3-B, ruling Q7) | held widening PR: publish both |

## Fork modules that shadow an upstream symbol (recorded parallels, no diff row)

The rule of record (owner 2026-09-23): adopt upstream's, or record the parallel with the
symbol it shadows, why it cannot be adopted, and what retires it. These live in fork-owned
files, so no row above counts them.

| upstream symbol | fork parallel | why | retires with |
|---|---|---|---|
| `hermes_cli/local_runtime/presets.py::generate_presets` (the router `--models-preset` INI) | `agent_runtime/local_llama_adapter/engine.py::write_preset` | per-model load knobs (context, GPU layers, flash-attn, KV cache types, chat template) are owner-ruled KEEP (2026-09-24, feature-diff row 9); upstream derives the same keys from its context policy and exposes no per-model override | an upstream per-model preset override; not filed (upstream PRs paused 2026-09-24) |
| `hermes_cli/local_runtime/bootstrap.py::ensure_local_runtime`'s binary pick (`installed_tags()` → `ensure_runtime_installed`) | `agent_runtime/local_llama_adapter/engine.py::Engine.start` serves the configured `executable_path` under upstream's `LlamaServerSupervisor` | GAP-PR-1: a user-supplied `llama-server` (bring-your-own build) has no upstream knob | GAP-PR-1 `local_runtime.executable_path`, held |
| `hermes_cli/local_runtime/bootstrap.py::models_dir` / `staged_models` | `agent_runtime/local_llama_adapter/engine.py::scan` over the adapter config's `model_roots` | GAP-PR-2: extra read-only model roots; upstream serves `models_dir()` plus per-file sideload | GAP-PR-2 `local_runtime.model_dirs`, held |
| `tools/environments/base_output.py::_drain_fd_select` / `_drain_fd_windows` (poll `select` / `PeekNamedPipe` before `os.read`) | `agent_runtime/subprocess_pumps.py::_pump_fd` | upstream's are private, drain stdout only, and stop on a bound tied to the process exiting; the dispatch pumps drain both pipes, hand the sink one line per append, and stop on an explicit release (lane Q-RUNTIME 2026-09-25, the Windows forced-release hang) | an upstream public poll-before-read pipe reader that takes a stop event and a per-line sink |
| `hermes_yaml.safe_load` (upstream's YAML 1.2 reader: bare `y`/`n` → bool, `1e3` → number) | `agent_runtime/yaml_io.load` (ruamel pure safe loader tuned to read as pyyaml did; `dump`/`YAMLError` ARE upstream's) | every realm artifact published before 2026-09-26 was written by pyyaml with flow-graph node keys `y:` unquoted; upstream's reader raises `found duplicate key "False"` on them (lane YAML, 37-value control) | a one-time migration that quotes `y`/`n`/`1e3` in published artifacts, after which `load` becomes `hermes_yaml.safe_load` and this row deletes (lane YAML 2026-09-26) |
