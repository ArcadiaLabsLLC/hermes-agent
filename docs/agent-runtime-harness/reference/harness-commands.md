# Harness command reference (generated)

[Task-oriented command directory](../10-command-directory.md)

Generated by `python scripts/emit_harness_command_directory.py --write`
from `hermes_cli.harness.build_parser`. Do not edit this file by hand.
Run `python scripts/emit_harness_command_directory.py --check` to detect drift.
No command handlers are executed during generation.

This lists parser syntax, NOT a safety classification or a promise that a
command is available on the connected runtime. Read the task directory
before changing state. Alias paths share the canonical command's flags.

## Command index

- [hermes harness](#hermes-harness)
- [hermes harness init](#hermes-harness-init)
- [hermes harness roots](#hermes-harness-roots)
- [hermes harness roots list](#hermes-harness-roots-list)
- [hermes harness roots set](#hermes-harness-roots-set)
- [hermes harness roots unset](#hermes-harness-roots-unset)
- [hermes harness roots migrate](#hermes-harness-roots-migrate)
- [hermes harness gateway](#hermes-harness-gateway)
- [hermes harness gateway id](#hermes-harness-gateway-id)
- [hermes harness gateway rename](#hermes-harness-gateway-rename)
- [hermes harness gateway pair](#hermes-harness-gateway-pair)
- [hermes harness gateway introduce](#hermes-harness-gateway-introduce)
- [hermes harness gateway devices](#hermes-harness-gateway-devices)
- [hermes harness gateway devices list](#hermes-harness-gateway-devices-list)
- [hermes harness gateway devices revoke](#hermes-harness-gateway-devices-revoke)
- [hermes harness gateway peers](#hermes-harness-gateway-peers)
- [hermes harness gateway peers pair](#hermes-harness-gateway-peers-pair)
- [hermes harness gateway peers join](#hermes-harness-gateway-peers-join)
- [hermes harness gateway peers list](#hermes-harness-gateway-peers-list)
- [hermes harness gateway peers revoke](#hermes-harness-gateway-peers-revoke)
- [hermes harness workspace](#hermes-harness-workspace)
- [hermes harness workspace list](#hermes-harness-workspace-list)
- [hermes harness workspace show](#hermes-harness-workspace-show)
- [hermes harness workspace create](#hermes-harness-workspace-create)
- [hermes harness workspace use](#hermes-harness-workspace-use)
- [hermes harness workspace add-agent](#hermes-harness-workspace-add-agent)
- [hermes harness workspace remove-agent](#hermes-harness-workspace-remove-agent)
- [hermes harness workspace rename](#hermes-harness-workspace-rename)
- [hermes harness workspace archive](#hermes-harness-workspace-archive)
- [hermes harness workspace delete](#hermes-harness-workspace-delete)
- [hermes harness realm](#hermes-harness-realm)
- [hermes harness realm list](#hermes-harness-realm-list)
- [hermes harness realm show](#hermes-harness-realm-show)
- [hermes harness realm create](#hermes-harness-realm-create)
- [hermes harness realm adopt](#hermes-harness-realm-adopt)
- [hermes harness realm bind-server](#hermes-harness-realm-bind-server)
- [hermes harness realm use](#hermes-harness-realm-use)
- [hermes harness realm default-scope](#hermes-harness-realm-default-scope)
- [hermes harness realm sync](#hermes-harness-realm-sync)
- [hermes harness realm sync status](#hermes-harness-realm-sync-status)
- [hermes harness realm sync pull](#hermes-harness-realm-sync-pull)
- [hermes harness realm sync publish](#hermes-harness-realm-sync-publish)
- [hermes harness realm sync held](#hermes-harness-realm-sync-held)
- [hermes harness realm sync resolve](#hermes-harness-realm-sync-resolve)
- [hermes harness realm sync revert](#hermes-harness-realm-sync-revert)
- [hermes harness realm skills](#hermes-harness-realm-skills)
- [hermes harness realm skills show](#hermes-harness-realm-skills-show)
- [hermes harness realm skills set](#hermes-harness-realm-skills-set)
- [hermes harness realm agents](#hermes-harness-realm-agents)
- [hermes harness realm agents show](#hermes-harness-realm-agents-show)
- [hermes harness realm agents set](#hermes-harness-realm-agents-set)
- [hermes harness flow](#hermes-harness-flow)
- [hermes harness flow set](#hermes-harness-flow-set)
- [hermes harness flow show](#hermes-harness-flow-show)
- [hermes harness flow list](#hermes-harness-flow-list)
- [hermes harness checkpoint](#hermes-harness-checkpoint)
- [hermes harness checkpoint fetch](#hermes-harness-checkpoint-fetch)
- [hermes harness checkpoint classes](#hermes-harness-checkpoint-classes)
- [hermes harness skills](#hermes-harness-skills)
- [hermes harness skills inventory](#hermes-harness-skills-inventory)
- [hermes harness skills link-external](#hermes-harness-skills-link-external)
- [hermes harness skills catalog](#hermes-harness-skills-catalog)
- [hermes harness skills publishable](#hermes-harness-skills-publishable)
- [hermes harness skills inbox](#hermes-harness-skills-inbox)
- [hermes harness skills promote](#hermes-harness-skills-promote)
- [hermes harness skills delete](#hermes-harness-skills-delete)
- [hermes harness skills restore](#hermes-harness-skills-restore)
- [hermes harness prompt-context](#hermes-harness-prompt-context)
- [hermes harness prompt-context show](#hermes-harness-prompt-context-show)
- [hermes harness board](#hermes-harness-board)
- [hermes harness board list](#hermes-harness-board-list)
- [hermes harness board show](#hermes-harness-board-show)
- [hermes harness board create](#hermes-harness-board-create)
- [hermes harness board update](#hermes-harness-board-update)
- [hermes harness board card](#hermes-harness-board-card)
- [hermes harness board card add](#hermes-harness-board-card-add)
- [hermes harness board card edit](#hermes-harness-board-card-edit)
- [hermes harness board card move](#hermes-harness-board-card-move)
- [hermes harness board card archive](#hermes-harness-board-card-archive)
- [hermes harness board card restore](#hermes-harness-board-card-restore)
- [hermes harness board resolve-conflict](#hermes-harness-board-resolve-conflict)
- [hermes harness office](#hermes-harness-office)
- [hermes harness office show](#hermes-harness-office-show)
- [hermes harness office actor-upsert](#hermes-harness-office-actor-upsert)
- [hermes harness office actor-remove](#hermes-harness-office-actor-remove)
- [hermes harness office actor-restore](#hermes-harness-office-actor-restore)
- [hermes harness office set-folders](#hermes-harness-office-set-folders)
- [hermes harness office resolve-conflict](#hermes-harness-office-resolve-conflict)
- [hermes harness office archive-surface](#hermes-harness-office-archive-surface)
- [hermes harness level](#hermes-harness-level)
- [hermes harness level show](#hermes-harness-level-show)
- [hermes harness level set](#hermes-harness-level-set)
- [hermes harness level clear](#hermes-harness-level-clear)
- [hermes harness map](#hermes-harness-map)
- [hermes harness map list](#hermes-harness-map-list)
- [hermes harness map show](#hermes-harness-map-show)
- [hermes harness map set](#hermes-harness-map-set)
- [hermes harness map clear](#hermes-harness-map-clear)
- [hermes harness persona](#hermes-harness-persona)
- [hermes harness persona list](#hermes-harness-persona-list)
- [hermes harness persona show](#hermes-harness-persona-show)
- [hermes harness persona tool-diff](#hermes-harness-persona-tool-diff)
- [hermes harness persona permission](#hermes-harness-persona-permission)
- [hermes harness persona permission set](#hermes-harness-persona-permission-set)
- [hermes harness persona assignments](#hermes-harness-persona-assignments)
- [hermes harness persona migrate-assignment-task-ids](#hermes-harness-persona-migrate-assignment-task-ids)
- [hermes harness persona chat](#hermes-harness-persona-chat)
- [hermes harness persona chat delete](#hermes-harness-persona-chat-delete)
- [hermes harness persona chat history](#hermes-harness-persona-chat-history)
- [hermes harness persona set-model](#hermes-harness-persona-set-model)
- [hermes harness persona set-skills](#hermes-harness-persona-set-skills)
- [hermes harness persona instance](#hermes-harness-persona-instance)
- [hermes harness persona instance create](#hermes-harness-persona-instance-create)
- [hermes harness persona instance open-chat](#hermes-harness-persona-instance-open-chat)
- [hermes harness persona instance close](#hermes-harness-persona-instance-close)
- [hermes harness persona instance archive](#hermes-harness-persona-instance-archive)
- [hermes harness persona instance retire](#hermes-harness-persona-instance-retire) — aliases: `delete`
- [hermes harness persona instance steer](#hermes-harness-persona-instance-steer)
- [hermes harness persona instance repair-steering](#hermes-harness-persona-instance-repair-steering)
- [hermes harness persona instance return-summary](#hermes-harness-persona-instance-return-summary)
- [hermes harness persona instance update-profile](#hermes-harness-persona-instance-update-profile)
- [hermes harness persona instance set-model](#hermes-harness-persona-instance-set-model)
- [hermes harness mission-chat](#hermes-harness-mission-chat)
- [hermes harness mission-chat message](#hermes-harness-mission-chat-message)
- [hermes harness mission-chat queue-skill](#hermes-harness-mission-chat-queue-skill)
- [hermes harness mission-chat steer](#hermes-harness-mission-chat-steer)
- [hermes harness mission-chat turn-resolve](#hermes-harness-mission-chat-turn-resolve)
- [hermes harness mission-chat clarify-tickets](#hermes-harness-mission-chat-clarify-tickets)
- [hermes harness mission-chat dispatch](#hermes-harness-mission-chat-dispatch)
- [hermes harness mission-chat dispatch redeliver](#hermes-harness-mission-chat-dispatch-redeliver)
- [hermes harness status](#hermes-harness-status)
- [hermes harness providers](#hermes-harness-providers)
- [hermes harness usage](#hermes-harness-usage)
- [hermes harness doctor](#hermes-harness-doctor)
- [hermes harness health](#hermes-harness-health)
- [hermes harness verify](#hermes-harness-verify)
- [hermes harness config](#hermes-harness-config)
- [hermes harness config show](#hermes-harness-config-show)
- [hermes harness migrate](#hermes-harness-migrate)
- [hermes harness observe](#hermes-harness-observe)
- [hermes harness contracts](#hermes-harness-contracts)
- [hermes harness contracts dump](#hermes-harness-contracts-dump)
- [hermes harness worktree](#hermes-harness-worktree)
- [hermes harness worktree reap](#hermes-harness-worktree-reap)
- [hermes harness persona-instance](#hermes-harness-persona-instance)
- [hermes harness persona-instance reconcile](#hermes-harness-persona-instance-reconcile)
- [hermes harness persona-instance chat-bindings](#hermes-harness-persona-instance-chat-bindings)
- [hermes harness persona-instance detail](#hermes-harness-persona-instance-detail)
- [hermes harness agent](#hermes-harness-agent)
- [hermes harness agent list](#hermes-harness-agent-list)
- [hermes harness agent create](#hermes-harness-agent-create)
- [hermes harness agent retire](#hermes-harness-agent-retire)
- [hermes harness agent set-profile](#hermes-harness-agent-set-profile)
- [hermes harness install-harness-skills](#hermes-harness-install-harness-skills)
- [hermes harness snapshot](#hermes-harness-snapshot)
- [hermes harness stream](#hermes-harness-stream)
- [hermes harness serve](#hermes-harness-serve)
- [hermes harness serve connect](#hermes-harness-serve-connect)
- [hermes harness work](#hermes-harness-work)
- [hermes harness work list](#hermes-harness-work-list)
- [hermes harness work peek](#hermes-harness-work-peek)
- [hermes harness work cancel](#hermes-harness-work-cancel)
- [hermes harness pets](#hermes-harness-pets)
- [hermes harness pets gallery](#hermes-harness-pets-gallery)
- [hermes harness pets install](#hermes-harness-pets-install)
- [hermes harness pets sprite](#hermes-harness-pets-sprite)
- [hermes harness pets thumb](#hermes-harness-pets-thumb)
- [hermes harness characters](#hermes-harness-characters)
- [hermes harness characters start](#hermes-harness-characters-start)
- [hermes harness characters list](#hermes-harness-characters-list)
- [hermes harness characters backfill-home](#hermes-harness-characters-backfill-home)
- [hermes harness characters migrate-home](#hermes-harness-characters-migrate-home)
- [hermes harness characters status](#hermes-harness-characters-status)
- [hermes harness characters thumb](#hermes-harness-characters-thumb)
- [hermes harness characters base](#hermes-harness-characters-base)
- [hermes harness characters turnaround](#hermes-harness-characters-turnaround)
- [hermes harness characters reroll-direction](#hermes-harness-characters-reroll-direction)
- [hermes harness characters approve-direction](#hermes-harness-characters-approve-direction)
- [hermes harness characters rows](#hermes-harness-characters-rows)
- [hermes harness characters reroll-row](#hermes-harness-characters-reroll-row)
- [hermes harness characters compose](#hermes-harness-characters-compose)
- [hermes harness characters auto](#hermes-harness-characters-auto)
- [hermes harness characters reopen](#hermes-harness-characters-reopen)
- [hermes harness characters add-state](#hermes-harness-characters-add-state)
- [hermes harness characters sprite](#hermes-harness-characters-sprite)
- [hermes harness characters payload-contract](#hermes-harness-characters-payload-contract)

## hermes harness

```text
usage: hermes harness [-h] [-o {json,table,yaml,wide}] [--json] [-q] [--no-color] [--fields FIELDS]
                      {init,roots,gateway,workspace,realm,flow,checkpoint,skills,prompt-context,board,office,level,map,persona,mission-chat,status,providers,usage,doctor,health,verify,config,migrate,observe,contracts,worktree,persona-instance,agent,install-harness-skills,snapshot,stream,serve,work,pets,characters}
                      ...

positional arguments:
  {init,roots,gateway,workspace,realm,flow,checkpoint,skills,prompt-context,board,office,level,map,persona,mission-chat,status,providers,usage,doctor,health,verify,config,migrate,observe,contracts,worktree,persona-instance,agent,install-harness-skills,snapshot,stream,serve,work,pets,characters}
    init                Initialize the harness store
    roots               Machine-local logical roots that make ${roots.<name>} config paths portable
    gateway             This runtime root's remote-gateway install identity — the name and id a
                        phone's install picker shows
    workspace           Manage Harness workspaces
    realm               Manage Harness realms
    flow                Operator flow-graph documents: ingest the Launcher's authored agent map
                        whole and set the referenced instances' steering relations
    checkpoint          Per-actor read-model checkpoint: bundle the on-disk entity-class stores into
                        a keyed transport envelope (the store IS the checkpoint)
    skills              Inspect the shared skills substrate the Launcher's Skills console consumes
    prompt-context      S8: on-demand prompt-observability contexts (the frame ships only LIVE
                        persona instances' current-session rows; historical rows are fetched here)
    board               Manage Mission Board planning boards + cards (planning only — cards never
                        drive runtime execution)
    office              Manage the Mission Office layout (one file per actor placement; realm-synced
                        like boards)
    level               Read or set a workspace's LEVEL document (the environment it stands in;
                        realm-synced whole-document)
    map                 Map CATALOGUE: the named scenes this install knows about, carried by the
                        realm
    persona             Run bounded live-token diagnostics for one persona
    mission-chat        Canonical Mission Control chat path
    status              Show harness status
    providers           List credential pools with typed auth health (machine-readable via --json)
    usage               Per-provider account usage/limit windows (machine-readable via --json)
    doctor              Show Harness runtime diagnostics: orphan worktrees, snapshot ids, event-log
                        health, model authority, persona/profile binding
    health              Check Harness runtime/provider dependencies are reachable and configured
    verify              Run runtime smoke verification: read-only harness CLI commands plus the
                        focused store/snapshot/status test modules
    config              Inspect Harness runtime config
    migrate             Inspect Harness runtime migrations
    observe             Show redaction-safe Mission Control observability
    contracts           Inspect canonical Mission Control event contracts
    worktree            Manage harness-managed git worktrees
    persona-instance    Manage durable persona-instance store rows
    agent               Inspect and rebind harness agent definitions
    install-harness-skills
                        Install versioned Harness skills into configured persona profiles
    snapshot            Build and print a redaction-safe runtime snapshot frame
    stream              Emit Mission Control hydrate/delta frames as NDJSON
    serve               Persistent NDJSON bridge: dispatch harness argv requests in one warm process
                        (Mission Control serve lane), on stdio and on the per-root localhost socket
    work                Background work running right now (list / peek / cancel)
    pets                Mission Control Petdex bridge
    characters          Mission Control character-sheet bridge (8-way sheets + QA)

options:
  -h, --help            show this help message and exit
  -o {json,table,yaml,wide}, --output {json,table,yaml,wide}
  --json                Alias for -o json
  -q, --quiet
  --no-color
  --fields FIELDS
```

## hermes harness init

```text
usage: hermes harness init [-h] [--json]

options:
  -h, --help  show this help message and exit
  --json
```

## hermes harness roots

```text
usage: hermes harness roots [-h] {list,set,unset,migrate} ...

positional arguments:
  {list,set,unset,migrate}
    list                Show this machine's logical-root bindings (read-only)
    set                 Bind a logical root to an absolute local path
    unset               Remove a logical-root binding
    migrate             Rewrite machine-local absolute paths in profile configs into ${roots.<name>}
                        token form

options:
  -h, --help            show this help message and exit
```

## hermes harness roots list

```text
usage: hermes harness roots list [-h] [-o {json,table,yaml,wide}] [--json] [-q] [--no-color]
                                 [--fields FIELDS]

options:
  -h, --help            show this help message and exit
  -o {json,table,yaml,wide}, --output {json,table,yaml,wide}
  --json                Alias for -o json
  -q, --quiet
  --no-color
  --fields FIELDS
```

## hermes harness roots set

```text
usage: hermes harness roots set [-h] [--allow-missing] [-o {json,table,yaml,wide}] [--json] [-q]
                                [--no-color] [--fields FIELDS] [--dry-run]
                                name path

positional arguments:
  name                  Logical root name, e.g. eternia_launcher
  path                  Absolute path to the checkout on THIS machine

options:
  -h, --help            show this help message and exit
  --allow-missing       Bind even when the path does not exist yet
  -o {json,table,yaml,wide}, --output {json,table,yaml,wide}
  --json                Alias for -o json
  -q, --quiet
  --no-color
  --fields FIELDS
  --dry-run
```

## hermes harness roots unset

```text
usage: hermes harness roots unset [-h] [-o {json,table,yaml,wide}] [--json] [-q] [--no-color]
                                  [--fields FIELDS] [--dry-run]
                                  name

positional arguments:
  name

options:
  -h, --help            show this help message and exit
  -o {json,table,yaml,wide}, --output {json,table,yaml,wide}
  --json                Alias for -o json
  -q, --quiet
  --no-color
  --fields FIELDS
  --dry-run
```

## hermes harness roots migrate

```text
usage: hermes harness roots migrate [-h] [--root NAME=PATH] [--no-platform-gates]
                                    [-o {json,table,yaml,wide}] [--json] [-q] [--no-color]
                                    [--fields FIELDS] [--dry-run] [--yes]
                                    [configs ...]

positional arguments:
  configs               config.yaml paths to migrate (default: every Hermes profile config)

options:
  -h, --help            show this help message and exit
  --root NAME=PATH      Explicit root binding; repeatable. Omit to auto-derive from .git ancestors.
  --no-platform-gates   Do not add platforms:[windows] to PowerShell/.ps1-only MCP entries
  -o {json,table,yaml,wide}, --output {json,table,yaml,wide}
  --json                Alias for -o json
  -q, --quiet
  --no-color
  --fields FIELDS
  --dry-run
  --yes, -y
```

## hermes harness gateway

```text
usage: hermes harness gateway [-h] {id,rename,pair,introduce,devices,peers} ...

positional arguments:
  {id,rename,pair,introduce,devices,peers}
    id                  Show this root's install identity (read-only — never mints; a root that has
                        never served has none)
    rename              Set the operator-facing display name for this root's install (the install_id
                        never changes)
    pair                Mint a short-TTL pairing code plus the QR payload a device scans to reach
                        this install
    introduce           Mint one envelope a launcher posts as a backend pair-grant: a peer code, a
                        device code, and where to dial this install
    devices             Devices paired with this install's gateway — list them, or revoke one
    peers               Installs paired with this one (operator-approved on BOTH sides) — pair,
                        join, list, revoke

options:
  -h, --help            show this help message and exit
```

## hermes harness gateway id

```text
usage: hermes harness gateway id [-h] [-o {json,table,yaml,wide}] [--json] [-q] [--no-color]
                                 [--fields FIELDS]

options:
  -h, --help            show this help message and exit
  -o {json,table,yaml,wide}, --output {json,table,yaml,wide}
  --json                Alias for -o json
  -q, --quiet
  --no-color
  --fields FIELDS
```

## hermes harness gateway rename

```text
usage: hermes harness gateway rename [-h] [-o {json,table,yaml,wide}] [--json] [-q] [--no-color]
                                     [--fields FIELDS] [--dry-run]
                                     name

positional arguments:
  name                  What a human should call this install, e.g. workstation

options:
  -h, --help            show this help message and exit
  -o {json,table,yaml,wide}, --output {json,table,yaml,wide}
  --json                Alias for -o json
  -q, --quiet
  --no-color
  --fields FIELDS
  --dry-run
```

## hermes harness gateway pair

```text
usage: hermes harness gateway pair [-h] [--name NAME] [--tier {console,read}]
                                   [-o {json,table,yaml,wide}] [--json] [-q] [--no-color]
                                   [--fields FIELDS]

options:
  -h, --help            show this help message and exit
  --name NAME           What to call the device that redeems this code, e.g. "the phone"
  --tier {console,read}
                        What the paired device may do. console: run console verbs (create/retire
                        agents). read: view only.
  -o {json,table,yaml,wide}, --output {json,table,yaml,wide}
  --json                Alias for -o json
  -q, --quiet
  --no-color
  --fields FIELDS
```

## hermes harness gateway introduce

```text
usage: hermes harness gateway introduce [-h] [--for-install FOR_INSTALL] [--for-device FOR_DEVICE]
                                        [--correlation CORRELATION] [--note NOTE]
                                        [-o {json,table,yaml,wide}] [--json] [-q] [--no-color]
                                        [--fields FIELDS]

options:
  -h, --help            show this help message and exit
  --for-install FOR_INSTALL
                        The hermes install id this introduction is FOR — the peer half is minted
                        scoped to it and no other install can spend it
  --for-device FOR_DEVICE
                        The ACCOUNT device id this introduction is for — copied onto the device row
                        as a join key (a label, not a check)
  --correlation CORRELATION
                        The backend grant id, stamped through both mints and every event so all
                        three parties name one errand
  --note NOTE           What this introduction is for, e.g. "the laptop" — shown while the codes are
                        pending
  -o {json,table,yaml,wide}, --output {json,table,yaml,wide}
  --json                Alias for -o json
  -q, --quiet
  --no-color
  --fields FIELDS
```

## hermes harness gateway devices

```text
usage: hermes harness gateway devices [-h] {list,revoke} ...

positional arguments:
  {list,revoke}
    list         Every paired device, oldest first, revoked ones included (never the credential —
                 there is no field for it)
    revoke       Refuse a paired device from its next handshake on (the row is kept, so an audit can
                 tell it from never-paired)

options:
  -h, --help     show this help message and exit
```

## hermes harness gateway devices list

```text
usage: hermes harness gateway devices list [-h] [-o {json,table,yaml,wide}] [--json] [-q]
                                           [--no-color] [--fields FIELDS]

options:
  -h, --help            show this help message and exit
  -o {json,table,yaml,wide}, --output {json,table,yaml,wide}
  --json                Alias for -o json
  -q, --quiet
  --no-color
  --fields FIELDS
```

## hermes harness gateway devices revoke

```text
usage: hermes harness gateway devices revoke [-h] [-o {json,table,yaml,wide}] [--json] [-q]
                                             [--no-color] [--fields FIELDS] [--dry-run]
                                             device_id

positional arguments:
  device_id             The device id from `harness gateway devices list`

options:
  -h, --help            show this help message and exit
  -o {json,table,yaml,wide}, --output {json,table,yaml,wide}
  --json                Alias for -o json
  -q, --quiet
  --no-color
  --fields FIELDS
  --dry-run
```

## hermes harness gateway peers

```text
usage: hermes harness gateway peers [-h] {pair,join,list,revoke} ...

positional arguments:
  {pair,join,list,revoke}
    pair                Mint a short-TTL PEER code plus the payload another install's operator runs
                        `peers join` with
    join                Redeem a peer code from ANOTHER install: dials it, and records the edge in
                        both stores
    list                Every paired install, oldest first, revoked ones included (never the
                        credential — there is no field for it)
    revoke              Refuse a paired install from its next handshake on — ONE-SIDED: the other
                        install keeps its own row

options:
  -h, --help            show this help message and exit
```

## hermes harness gateway peers pair

```text
usage: hermes harness gateway peers pair [-h] [--note NOTE] [-o {json,table,yaml,wide}] [--json]
                                         [-q] [--no-color] [--fields FIELDS]

options:
  -h, --help            show this help message and exit
  --note NOTE           What this edge is for, e.g. "laptop" — shown while the code is pending
  -o {json,table,yaml,wide}, --output {json,table,yaml,wide}
  --json                Alias for -o json
  -q, --quiet
  --no-color
  --fields FIELDS
```

## hermes harness gateway peers join

```text
usage: hermes harness gateway peers join [-h] [--host HOST] [--port PORT]
                                         [--fingerprint FINGERPRINT]
                                         [--expect-fingerprint EXPECT_FINGERPRINT]
                                         [--correlation CORRELATION] [--timeout TIMEOUT]
                                         [-o {json,table,yaml,wide}] [--json] [-q] [--no-color]
                                         [--fields FIELDS]
                                         payload

positional arguments:
  payload               The join_payload string from `harness gateway peers pair` over there, or the
                        bare 8-character code with --host/--port

options:
  -h, --help            show this help message and exit
  --host HOST           Override the address in the payload (a second interface, a NAT, a machine
                        that moved)
  --port PORT           Override the port in the payload
  --fingerprint FINGERPRINT
                        Override the certificate fingerprint to pin; omitting it pins NOTHING, which
                        is weaker
  --expect-fingerprint EXPECT_FINGERPRINT
                        The certificate fingerprint the ACCOUNT attests for that install; a payload
                        that disagrees is refused before anything is dialled
  --correlation CORRELATION
                        The backend grant id this join fulfils; echoed on the receipt so all three
                        parties name one errand
  --timeout TIMEOUT     Seconds to wait for the other install's handshake
  -o {json,table,yaml,wide}, --output {json,table,yaml,wide}
  --json                Alias for -o json
  -q, --quiet
  --no-color
  --fields FIELDS
```

## hermes harness gateway peers list

```text
usage: hermes harness gateway peers list [-h] [-o {json,table,yaml,wide}] [--json] [-q] [--no-color]
                                         [--fields FIELDS]

options:
  -h, --help            show this help message and exit
  -o {json,table,yaml,wide}, --output {json,table,yaml,wide}
  --json                Alias for -o json
  -q, --quiet
  --no-color
  --fields FIELDS
```

## hermes harness gateway peers revoke

```text
usage: hermes harness gateway peers revoke [-h] [--no-announce] [-o {json,table,yaml,wide}] [--json]
                                           [-q] [--no-color] [--fields FIELDS] [--dry-run]
                                           peer_install_id

positional arguments:
  peer_install_id       The install id from `harness gateway peers list`

options:
  -h, --help            show this help message and exit
  --no-announce         Skip telling the other install it was revoked (offline, or when it must not
                        be contacted); it learns at its next call
  -o {json,table,yaml,wide}, --output {json,table,yaml,wide}
  --json                Alias for -o json
  -q, --quiet
  --no-color
  --fields FIELDS
  --dry-run
```

## hermes harness workspace

```text
usage: hermes harness workspace [-h]
                                {list,show,create,use,add-agent,remove-agent,rename,archive,delete}
                                ...

positional arguments:
  {list,show,create,use,add-agent,remove-agent,rename,archive,delete}
    list                List workspaces
    show                Show one workspace
    create              Create a workspace
    use                 Set active workspace
    add-agent           Add a persona to a workspace roster
    remove-agent        Remove a persona from a workspace roster
    rename              Rename a workspace
    archive             Archive a workspace
    delete              Permanently delete a workspace and its office/board content (archive is the
                        reversible path)

options:
  -h, --help            show this help message and exit
```

## hermes harness workspace list

```text
usage: hermes harness workspace list [-h] [-o {json,table,yaml,wide}] [--json] [-q] [--no-color]
                                     [--fields FIELDS] [--sort SORT]

options:
  -h, --help            show this help message and exit
  -o {json,table,yaml,wide}, --output {json,table,yaml,wide}
  --json                Alias for -o json
  -q, --quiet
  --no-color
  --fields FIELDS
  --sort SORT
```

## hermes harness workspace show

```text
usage: hermes harness workspace show [-h] [-o {json,table,yaml,wide}] [--json] [-q] [--no-color]
                                     [--fields FIELDS]
                                     workspace_id

positional arguments:
  workspace_id

options:
  -h, --help            show this help message and exit
  -o {json,table,yaml,wide}, --output {json,table,yaml,wide}
  --json                Alias for -o json
  -q, --quiet
  --no-color
  --fields FIELDS
```

## hermes harness workspace create

```text
usage: hermes harness workspace create [-h] --name NAME [--realm REALM] [--agent AGENT]
                                       [--blueprint BLUEPRINT] [--isolation {soft,hard}]
                                       [--max-lanes MAX_LANES] [--from-workspace FROM_WORKSPACE]
                                       [--copy {office,board,agents,settings}]
                                       [-o {json,table,yaml,wide}] [--json] [-q] [--no-color]
                                       [--fields FIELDS] [--dry-run]

options:
  -h, --help            show this help message and exit
  --name NAME
  --realm REALM
  --agent AGENT
  --blueprint BLUEPRINT
  --isolation {soft,hard}
  --max-lanes MAX_LANES
  --from-workspace FROM_WORKSPACE
                        Use this workspace (any realm) as the template: copy the scopes below into
                        the new workspace
  --copy {office,board,agents,settings}
                        Template scope to copy (repeatable). Default with --from-workspace: every
                        scope. Requires --from-workspace.
  -o {json,table,yaml,wide}, --output {json,table,yaml,wide}
  --json                Alias for -o json
  -q, --quiet
  --no-color
  --fields FIELDS
  --dry-run
```

## hermes harness workspace use

```text
usage: hermes harness workspace use [-h] [--issued-at ISSUED_AT] [-o {json,table,yaml,wide}]
                                    [--json] [-q] [--no-color] [--fields FIELDS]
                                    workspace_id

positional arguments:
  workspace_id

options:
  -h, --help            show this help message and exit
  --issued-at ISSUED_AT
                        ISO-8601 UTC instant the operator issued this switch; a pointer already
                        owned by a strictly newer intent rejects this one as superseded (transport
                        replay guard)
  -o {json,table,yaml,wide}, --output {json,table,yaml,wide}
  --json                Alias for -o json
  -q, --quiet
  --no-color
  --fields FIELDS
```

## hermes harness workspace add-agent

```text
usage: hermes harness workspace add-agent [-h] [-o {json,table,yaml,wide}] [--json] [-q]
                                          [--no-color] [--fields FIELDS] [--dry-run]
                                          workspace_id persona_id

positional arguments:
  workspace_id
  persona_id

options:
  -h, --help            show this help message and exit
  -o {json,table,yaml,wide}, --output {json,table,yaml,wide}
  --json                Alias for -o json
  -q, --quiet
  --no-color
  --fields FIELDS
  --dry-run
```

## hermes harness workspace remove-agent

```text
usage: hermes harness workspace remove-agent [-h] [-o {json,table,yaml,wide}] [--json] [-q]
                                             [--no-color] [--fields FIELDS] [--dry-run] [--yes]
                                             workspace_id persona_id

positional arguments:
  workspace_id
  persona_id

options:
  -h, --help            show this help message and exit
  -o {json,table,yaml,wide}, --output {json,table,yaml,wide}
  --json                Alias for -o json
  -q, --quiet
  --no-color
  --fields FIELDS
  --dry-run
  --yes, -y
```

## hermes harness workspace rename

```text
usage: hermes harness workspace rename [-h] [-o {json,table,yaml,wide}] [--json] [-q] [--no-color]
                                       [--fields FIELDS] [--dry-run]
                                       workspace_id name

positional arguments:
  workspace_id
  name

options:
  -h, --help            show this help message and exit
  -o {json,table,yaml,wide}, --output {json,table,yaml,wide}
  --json                Alias for -o json
  -q, --quiet
  --no-color
  --fields FIELDS
  --dry-run
```

## hermes harness workspace archive

```text
usage: hermes harness workspace archive [-h] [-o {json,table,yaml,wide}] [--json] [-q] [--no-color]
                                        [--fields FIELDS] [--dry-run] [--yes]
                                        workspace_id

positional arguments:
  workspace_id

options:
  -h, --help            show this help message and exit
  -o {json,table,yaml,wide}, --output {json,table,yaml,wide}
  --json                Alias for -o json
  -q, --quiet
  --no-color
  --fields FIELDS
  --dry-run
  --yes, -y
```

## hermes harness workspace delete

```text
usage: hermes harness workspace delete [-h] [-o {json,table,yaml,wide}] [--json] [-q] [--no-color]
                                       [--fields FIELDS] [--dry-run] [--yes]
                                       workspace_id

positional arguments:
  workspace_id

options:
  -h, --help            show this help message and exit
  -o {json,table,yaml,wide}, --output {json,table,yaml,wide}
  --json                Alias for -o json
  -q, --quiet
  --no-color
  --fields FIELDS
  --dry-run
  --yes, -y
```

## hermes harness realm

```text
usage: hermes harness realm [-h]
                            {list,show,create,adopt,bind-server,use,default-scope,sync,skills,agents}
                            ...

positional arguments:
  {list,show,create,adopt,bind-server,use,default-scope,sync,skills,agents}
    list                List realms
    show                Show one realm
    create              Create a realm
    adopt               Adopt server-granted realms from the Eternia backend
    bind-server         Bind a realm to an Eternia server id
    use                 Set active realm
    default-scope       Preview default-scope adoption/reconciliation without mutating persisted
                        state
    sync                Git-backed selective sync for non-source-controlled realm artifacts
    skills              Per-realm selection of which shared skills publish to a realm
    agents              Per-realm selection of which persona definitions publish to a realm

options:
  -h, --help            show this help message and exit
```

## hermes harness realm list

```text
usage: hermes harness realm list [-h] [-o {json,table,yaml,wide}] [--json] [-q] [--no-color]
                                 [--fields FIELDS] [--sort SORT]

options:
  -h, --help            show this help message and exit
  -o {json,table,yaml,wide}, --output {json,table,yaml,wide}
  --json                Alias for -o json
  -q, --quiet
  --no-color
  --fields FIELDS
  --sort SORT
```

## hermes harness realm show

```text
usage: hermes harness realm show [-h] [-o {json,table,yaml,wide}] [--json] [-q] [--no-color]
                                 [--fields FIELDS]
                                 realm_id

positional arguments:
  realm_id

options:
  -h, --help            show this help message and exit
  -o {json,table,yaml,wide}, --output {json,table,yaml,wide}
  --json                Alias for -o json
  -q, --quiet
  --no-color
  --fields FIELDS
```

## hermes harness realm create

```text
usage: hermes harness realm create [-h] --name NAME [--server SERVER] [-o {json,table,yaml,wide}]
                                   [--json] [-q] [--no-color] [--fields FIELDS] [--dry-run]

options:
  -h, --help            show this help message and exit
  --name NAME
  --server SERVER
  -o {json,table,yaml,wide}, --output {json,table,yaml,wide}
  --json                Alias for -o json
  -q, --quiet
  --no-color
  --fields FIELDS
  --dry-run
```

## hermes harness realm adopt

```text
usage: hermes harness realm adopt [-h] [--server SERVER] [--credential-file CREDENTIAL_FILE]
                                  [-o {json,table,yaml,wide}] [--json] [-q] [--no-color]
                                  [--fields FIELDS] [--sort SORT] [--dry-run]

options:
  -h, --help            show this help message and exit
  --server SERVER       Only adopt realms bound to this Eternia server id
  --credential-file CREDENTIAL_FILE
                        Launcher-brokered realm sync credential JSON (fallback:
                        HERMES_REALM_SYNC_CREDENTIAL)
  -o {json,table,yaml,wide}, --output {json,table,yaml,wide}
  --json                Alias for -o json
  -q, --quiet
  --no-color
  --fields FIELDS
  --sort SORT
  --dry-run
```

## hermes harness realm bind-server

```text
usage: hermes harness realm bind-server [-h] [-o {json,table,yaml,wide}] [--json] [-q] [--no-color]
                                        [--fields FIELDS] [--dry-run]
                                        realm_id server_id

positional arguments:
  realm_id
  server_id

options:
  -h, --help            show this help message and exit
  -o {json,table,yaml,wide}, --output {json,table,yaml,wide}
  --json                Alias for -o json
  -q, --quiet
  --no-color
  --fields FIELDS
  --dry-run
```

## hermes harness realm use

```text
usage: hermes harness realm use [-h] [--issued-at ISSUED_AT] [-o {json,table,yaml,wide}] [--json]
                                [-q] [--no-color] [--fields FIELDS]
                                realm_id

positional arguments:
  realm_id

options:
  -h, --help            show this help message and exit
  --issued-at ISSUED_AT
                        ISO-8601 UTC instant the operator issued this switch; a pointer already
                        owned by a strictly newer intent rejects this one as superseded (transport
                        replay guard)
  -o {json,table,yaml,wide}, --output {json,table,yaml,wide}
  --json                Alias for -o json
  -q, --quiet
  --no-color
  --fields FIELDS
```

## hermes harness realm default-scope

```text
usage: hermes harness realm default-scope [-h] [--dry-run] [--winner-realm WINNER_REALM]
                                          [--winner-workspace WINNER_WORKSPACE] [--yes]
                                          [-o {json,table,yaml,wide}] [--json] [-q] [--no-color]
                                          [--fields FIELDS]

options:
  -h, --help            show this help message and exit
  --dry-run             Inventory only; never mutates persisted state
  --winner-realm WINNER_REALM
  --winner-workspace WINNER_WORKSPACE
  --yes, -y             Apply the explicitly selected recoverable reconciliation
  -o {json,table,yaml,wide}, --output {json,table,yaml,wide}
  --json                Alias for -o json
  -q, --quiet
  --no-color
  --fields FIELDS
```

## hermes harness realm sync

```text
usage: hermes harness realm sync [-h] {status,pull,publish,held,resolve,revert} ...

positional arguments:
  {status,pull,publish,held,resolve,revert}
    status              Show realm sync state
    pull                Pull and materialize realm sync artifacts
    publish             Publish allowlisted realm sync artifacts
    held                List what a pull HELD because BOTH sides changed: profile files (MEMORY.md /
                        core context / persona prompts) and skill packages
    resolve             Resolve one held profile file or skill package: --take local keeps the
                        member's content (and leaves it to publish), --take remote adopts the
                        realm's
    revert              Revert drifted local store rows to the last-pulled upstream (local-only: no
                        git, no network, no credential — and never mints a realm-visible tombstone)

options:
  -h, --help            show this help message and exit
```

## hermes harness realm sync status

```text
usage: hermes harness realm sync status [-h] [--credential-file CREDENTIAL_FILE]
                                        [-o {json,table,yaml,wide}] [--json] [-q] [--no-color]
                                        [--fields FIELDS]
                                        realm_id

positional arguments:
  realm_id

options:
  -h, --help            show this help message and exit
  --credential-file CREDENTIAL_FILE
                        Launcher-brokered realm sync credential JSON (fallback:
                        HERMES_REALM_SYNC_CREDENTIAL)
  -o {json,table,yaml,wide}, --output {json,table,yaml,wide}
  --json                Alias for -o json
  -q, --quiet
  --no-color
  --fields FIELDS
```

## hermes harness realm sync pull

```text
usage: hermes harness realm sync pull [-h] [--credential-file CREDENTIAL_FILE]
                                      [-o {json,table,yaml,wide}] [--json] [-q] [--no-color]
                                      [--fields FIELDS] [--dry-run]
                                      realm_id

positional arguments:
  realm_id

options:
  -h, --help            show this help message and exit
  --credential-file CREDENTIAL_FILE
                        Launcher-brokered realm sync credential JSON (fallback:
                        HERMES_REALM_SYNC_CREDENTIAL)
  -o {json,table,yaml,wide}, --output {json,table,yaml,wide}
  --json                Alias for -o json
  -q, --quiet
  --no-color
  --fields FIELDS
  --dry-run
```

## hermes harness realm sync publish

```text
usage: hermes harness realm sync publish [-h] [--credential-file CREDENTIAL_FILE]
                                         [-o {json,table,yaml,wide}] [--json] [-q] [--no-color]
                                         [--fields FIELDS] [--dry-run] [--yes]
                                         realm_id

positional arguments:
  realm_id

options:
  -h, --help            show this help message and exit
  --credential-file CREDENTIAL_FILE
                        Launcher-brokered realm sync credential JSON (fallback:
                        HERMES_REALM_SYNC_CREDENTIAL)
  -o {json,table,yaml,wide}, --output {json,table,yaml,wide}
  --json                Alias for -o json
  -q, --quiet
  --no-color
  --fields FIELDS
  --dry-run
  --yes, -y
```

## hermes harness realm sync held

```text
usage: hermes harness realm sync held [-h] [-o {json,table,yaml,wide}] [--json] [-q] [--no-color]
                                      [--fields FIELDS]
                                      realm_id

positional arguments:
  realm_id

options:
  -h, --help            show this help message and exit
  -o {json,table,yaml,wide}, --output {json,table,yaml,wide}
  --json                Alias for -o json
  -q, --quiet
  --no-color
  --fields FIELDS
```

## hermes harness realm sync resolve

```text
usage: hermes harness realm sync resolve [-h] --key KEY --take {local,remote}
                                         [-o {json,table,yaml,wide}] [--json] [-q] [--no-color]
                                         [--fields FIELDS] [--dry-run] [--yes]
                                         realm_id

positional arguments:
  realm_id

options:
  -h, --help            show this help message and exit
  --key KEY             Entity key from `realm sync held` — a profile file
                        (alice:memories/MEMORY.md) or a skill package (skill::launcher-mcp-
                        operations)
  --take {local,remote}
  -o {json,table,yaml,wide}, --output {json,table,yaml,wide}
  --json                Alias for -o json
  -q, --quiet
  --no-color
  --fields FIELDS
  --dry-run
  --yes, -y
```

## hermes harness realm sync revert

```text
usage: hermes harness realm sync revert [-h] [--item ITEMS] [--all] [-o {json,table,yaml,wide}]
                                        [--json] [-q] [--no-color] [--fields FIELDS] [--dry-run]
                                        [--yes]
                                        realm_id

positional arguments:
  realm_id

options:
  -h, --help            show this help message and exit
  --item ITEMS          FAMILY:CONTAINER:KEY from `realm sync status` store_drift.items (e.g.
                        office_actor:ws_x:dev_agent_1234); the container is empty when the row's
                        holder is gone, and is passed back empty
                        (persona_instance::personainst_1234) — the whole skill family is spelled
                        that way (skill::launcher-mcp-operations), since a skill package has no
                        realm-scoped container; repeatable
  --all                 Revert every drifted item in this realm
  -o {json,table,yaml,wide}, --output {json,table,yaml,wide}
  --json                Alias for -o json
  -q, --quiet
  --no-color
  --fields FIELDS
  --dry-run
  --yes, -y
```

## hermes harness realm skills

```text
usage: hermes harness realm skills [-h] {show,set} ...

positional arguments:
  {show,set}
    show      Show a realm's shared-skill publish selection (read-only, local store)
    set       Set a realm's shared-skill publish selection (local, reversible store edit — no --yes
              gate, like `realm use`)

options:
  -h, --help  show this help message and exit
```

## hermes harness realm skills show

```text
usage: hermes harness realm skills show [-h] [-o {json,table,yaml,wide}] [--json] [-q] [--no-color]
                                        [--fields FIELDS]
                                        realm_id

positional arguments:
  realm_id

options:
  -h, --help            show this help message and exit
  -o {json,table,yaml,wide}, --output {json,table,yaml,wide}
  --json                Alias for -o json
  -q, --quiet
  --no-color
  --fields FIELDS
```

## hermes harness realm skills set

```text
usage: hermes harness realm skills set [-h] [--all] [--skills SKILLS] [--none]
                                       [-o {json,table,yaml,wide}] [--json] [-q] [--no-color]
                                       [--fields FIELDS] [--dry-run]
                                       realm_id

positional arguments:
  realm_id

options:
  -h, --help            show this help message and exit
  --all                 Publish all shared skills (mode=all; the stored selection list is preserved)
  --skills SKILLS       Comma-separated skill slugs to publish (mode=selected)
  --none                Publish no skills (mode=selected, empty selection)
  -o {json,table,yaml,wide}, --output {json,table,yaml,wide}
  --json                Alias for -o json
  -q, --quiet
  --no-color
  --fields FIELDS
  --dry-run
```

## hermes harness realm agents

```text
usage: hermes harness realm agents [-h] {show,set} ...

positional arguments:
  {show,set}
    show      Show a realm's persona-definition publish selection
    set       Set a realm's persona-definition selection (required workspace/Office references
              remain pinned)

options:
  -h, --help  show this help message and exit
```

## hermes harness realm agents show

```text
usage: hermes harness realm agents show [-h] [-o {json,table,yaml,wide}] [--json] [-q] [--no-color]
                                        [--fields FIELDS]
                                        realm_id

positional arguments:
  realm_id

options:
  -h, --help            show this help message and exit
  -o {json,table,yaml,wide}, --output {json,table,yaml,wide}
  --json                Alias for -o json
  -q, --quiet
  --no-color
  --fields FIELDS
```

## hermes harness realm agents set

```text
usage: hermes harness realm agents set [-h] [--workspace] [--agents AGENTS] [--none]
                                       [-o {json,table,yaml,wide}] [--json] [-q] [--no-color]
                                       [--fields FIELDS] [--dry-run]
                                       realm_id

positional arguments:
  realm_id

options:
  -h, --help            show this help message and exit
  --workspace           Publish only definitions required by workspace rosters and Office placements
  --agents AGENTS       Comma-separated persona ids to publish in addition to required references
  --none                Clear the explicit selection; required references remain pinned
  -o {json,table,yaml,wide}, --output {json,table,yaml,wide}
  --json                Alias for -o json
  -q, --quiet
  --no-color
  --fields FIELDS
  --dry-run
```

## hermes harness flow

```text
usage: hermes harness flow [-h] {set,show,list} ...

positional arguments:
  {set,show,list}
    set            Store one flow-graph JSON doc and reconcile steering for the EXISTING instances
                   it references (never creates instances)
    show           Show the runtime's stored copy of one flow-graph doc
    list           List stored flow-graph doc ids

options:
  -h, --help       show this help message and exit
```

## hermes harness flow set

```text
usage: hermes harness flow set [-h] [--graph GRAPH] [--graph-file GRAPH_FILE]
                               [--requested-by REQUESTED_BY] [--json]

options:
  -h, --help            show this help message and exit
  --graph GRAPH         The flow-graph JSON document, inline
  --graph-file GRAPH_FILE
                        Path to a file holding the flow-graph JSON document
  --requested-by REQUESTED_BY
  --json
```

## hermes harness flow show

```text
usage: hermes harness flow show [-h] [--json] graph_id

positional arguments:
  graph_id

options:
  -h, --help  show this help message and exit
  --json
```

## hermes harness flow list

```text
usage: hermes harness flow list [-h] [--json]

options:
  -h, --help  show this help message and exit
  --json
```

## hermes harness checkpoint

```text
usage: hermes harness checkpoint [-h] {fetch,classes} ...

positional arguments:
  {fetch,classes}
    fetch          Bundle the per-actor store files into a keyed checkpoint envelope (entity class
                   -> actor id -> row read verbatim); read-only, writes nothing
    classes        List discovered entity classes with per-class actor counts and byte sizes (stat
                   only; contents not read)

options:
  -h, --help       show this help message and exit
```

## hermes harness checkpoint fetch

```text
usage: hermes harness checkpoint fetch [-h] [--classes CLASSES] [--max-rows MAX_ROWS] [--json]

options:
  -h, --help           show this help message and exit
  --classes CLASSES    Comma-separated entity-class filter (default: all discovered classes);
                       absent/unknown names are accounted in requested_absent
  --max-rows MAX_ROWS  Optional per-class row cap; truncation is accounted
                       ({truncated,total,returned}), never silent
  --json
```

## hermes harness checkpoint classes

```text
usage: hermes harness checkpoint classes [-h] [--json]

options:
  -h, --help  show this help message and exit
  --json
```

## hermes harness skills

```text
usage: hermes harness skills [-h]
                             {inventory,link-external,catalog,publishable,inbox,promote,delete,restore}
                             ...

positional arguments:
  {inventory,link-external,catalog,publishable,inbox,promote,delete,restore}
    inventory           Typed snapshot of the shared skill catalog, per-persona grants, and per-
                        realm publish/drift state
    link-external       Link the shared skills root into external harnesses (~/.claude, ~/.codex)
    catalog             S8: resolve ONE content-addressed skills catalog by its hash (the frame
                        ships only *_ref hashes; bodies are fetched once and cached forever)
    publishable         List every resolvable skill with whether it can reach a realm, and if not,
                        the typed reason (read-only)
    inbox               List quarantined per-realm inbox skill packages and how each would reconcile
                        (read-only)
    promote             Promote a held / authored / profile-local skill package into the canonical
                        shared root (hash-guarded, never-delete)
    delete              Delete a shared skill realm-wide: archive the local canonical package and
                        record a tombstone so a member's surviving copy cannot republish it
    restore             Lift ONE realm's skill tombstone (un-tombstone only — re-admitting the BYTES
                        is `skills promote`, and the receipt names the archived copy)

options:
  -h, --help            show this help message and exit
```

## hermes harness skills inventory

```text
usage: hermes harness skills inventory [-h] [--json]

options:
  -h, --help  show this help message and exit
  --json      Emit the skills_inventory/v1 contract as JSON
```

## hermes harness skills link-external

```text
usage: hermes harness skills link-external [-h] [--json]

options:
  -h, --help  show this help message and exit
  --json      Emit the link report as JSON
```

## hermes harness skills catalog

```text
usage: hermes harness skills catalog [-h] --hash CONTENT_HASH [--json]

options:
  -h, --help           show this help message and exit
  --hash CONTENT_HASH  The content hash carried by a chat_contexts row's available_skills_ref /
                       accessible_skills_ref
  --json
```

## hermes harness skills publishable

```text
usage: hermes harness skills publishable [-h] [--source-kind {profile_local,shared_core,external}]
                                         [--unpublishable-only] [-o {json,table,yaml,wide}] [--json]
                                         [-q] [--no-color] [--fields FIELDS]

options:
  -h, --help            show this help message and exit
  --source-kind {profile_local,shared_core,external}
                        Restrict to one resolver tier (default: all three)
  --unpublishable-only  Show only packages that cannot reach a realm as they stand
  -o {json,table,yaml,wide}, --output {json,table,yaml,wide}
  --json                Alias for -o json
  -q, --quiet
  --no-color
  --fields FIELDS
```

## hermes harness skills inbox

```text
usage: hermes harness skills inbox [-h] [--realm REALM] [-o {json,table,yaml,wide}] [--json] [-q]
                                   [--no-color] [--fields FIELDS]

options:
  -h, --help            show this help message and exit
  --realm REALM         Restrict to one realm id (default: all realms)
  -o {json,table,yaml,wide}, --output {json,table,yaml,wide}
  --json                Alias for -o json
  -q, --quiet
  --no-color
  --fields FIELDS
```

## hermes harness skills promote

```text
usage: hermes harness skills promote [-h] [--from-realm FROM_REALM] [--from-profile FROM_PROFILE]
                                     [--from-path FROM_PATH] [--adopt-divergent] [--move-source]
                                     [-o {json,table,yaml,wide}] [--json] [-q] [--no-color]
                                     [--fields FIELDS] [--dry-run]
                                     skill

positional arguments:
  skill                 Canonical skill slug (bare name or <category>/<name>)

options:
  -h, --help            show this help message and exit
  --from-realm FROM_REALM
                        Promote from this realm's inbox mirror
  --from-profile FROM_PROFILE
                        Promote from a profile's skills/ (implies --move-source)
  --from-path FROM_PATH
                        Promote from an explicit package directory
  --adopt-divergent     Adopt over a divergent canonical (archives the previous copy)
  --move-source         Archive the source package after a successful promotion (retire the
                        duplicate)
  -o {json,table,yaml,wide}, --output {json,table,yaml,wide}
  --json                Alias for -o json
  -q, --quiet
  --no-color
  --fields FIELDS
  --dry-run
```

## hermes harness skills delete

```text
usage: hermes harness skills delete [-h] [--realm REALMS] [-o {json,table,yaml,wide}] [--json] [-q]
                                    [--no-color] [--fields FIELDS] [--dry-run]
                                    skill

positional arguments:
  skill                 Canonical skill slug (bare name or <category>/<name>)

options:
  -h, --help            show this help message and exit
  --realm REALMS        Narrow the delete to this realm id (repeatable). Default (R-E): every non-
                        archived realm that currently publishes the slug — one canonical root serves
                        all realms, so leaving one un-tombstoned resurrects the copy on that realm's
                        next pull
  -o {json,table,yaml,wide}, --output {json,table,yaml,wide}
  --json                Alias for -o json
  -q, --quiet
  --no-color
  --fields FIELDS
  --dry-run
```

## hermes harness skills restore

```text
usage: hermes harness skills restore [-h] --realm REALM [-o {json,table,yaml,wide}] [--json] [-q]
                                     [--no-color] [--fields FIELDS]
                                     skill

positional arguments:
  skill                 Canonical skill slug named by the ledger entry to lift

options:
  -h, --help            show this help message and exit
  --realm REALM         Realm id whose ledger entry is lifted (a tombstone is per-realm truth; there
                        is no all-realms restore)
  -o {json,table,yaml,wide}, --output {json,table,yaml,wide}
  --json                Alias for -o json
  -q, --quiet
  --no-color
  --fields FIELDS
```

## hermes harness prompt-context

```text
usage: hermes harness prompt-context [-h] {show} ...

positional arguments:
  {show}
    show      Show one persisted prompt-observability context by id (read-only; the persisted files
              stay on disk after frame eviction)

options:
  -h, --help  show this help message and exit
```

## hermes harness prompt-context show

```text
usage: hermes harness prompt-context show [-h] --context-id CONTEXT_ID [--json]

options:
  -h, --help            show this help message and exit
  --context-id CONTEXT_ID
  --json
```

## hermes harness board

```text
usage: hermes harness board [-h] {list,show,create,update,card,resolve-conflict} ...

positional arguments:
  {list,show,create,update,card,resolve-conflict}
    list                List boards
    show                Show one board
    create              Create a board
    update              Update a board title/columns
    card                Manage board cards
    resolve-conflict    Resolve a realm-sync conflict on a card

options:
  -h, --help            show this help message and exit
```

## hermes harness board list

```text
usage: hermes harness board list [-h] [--workspace WORKSPACE] [-o {json,table,yaml,wide}] [--json]
                                 [-q] [--no-color] [--fields FIELDS] [--sort SORT]

options:
  -h, --help            show this help message and exit
  --workspace WORKSPACE, --workspace-id WORKSPACE
  -o {json,table,yaml,wide}, --output {json,table,yaml,wide}
  --json                Alias for -o json
  -q, --quiet
  --no-color
  --fields FIELDS
  --sort SORT
```

## hermes harness board show

```text
usage: hermes harness board show [-h] [--full] [-o {json,table,yaml,wide}] [--json] [-q]
                                 [--no-color] [--fields FIELDS]
                                 board_id

positional arguments:
  board_id

options:
  -h, --help            show this help message and exit
  --full                Include card bodies
  -o {json,table,yaml,wide}, --output {json,table,yaml,wide}
  --json                Alias for -o json
  -q, --quiet
  --no-color
  --fields FIELDS
```

## hermes harness board create

```text
usage: hermes harness board create [-h] --workspace WORKSPACE [--title TITLE]
                                   [-o {json,table,yaml,wide}] [--json] [-q] [--no-color]
                                   [--fields FIELDS] [--dry-run]

options:
  -h, --help            show this help message and exit
  --workspace WORKSPACE, --workspace-id WORKSPACE
  --title TITLE
  -o {json,table,yaml,wide}, --output {json,table,yaml,wide}
  --json                Alias for -o json
  -q, --quiet
  --no-color
  --fields FIELDS
  --dry-run
```

## hermes harness board update

```text
usage: hermes harness board update [-h] [--title TITLE] [--columns-json COLUMNS_JSON]
                                   [--expect-revision EXPECT_REVISION] [-o {json,table,yaml,wide}]
                                   [--json] [-q] [--no-color] [--fields FIELDS]
                                   board_id

positional arguments:
  board_id

options:
  -h, --help            show this help message and exit
  --title TITLE
  --columns-json COLUMNS_JSON
                        JSON array of {column_id,title,kind,wip_limit}
  --expect-revision EXPECT_REVISION
  -o {json,table,yaml,wide}, --output {json,table,yaml,wide}
  --json                Alias for -o json
  -q, --quiet
  --no-color
  --fields FIELDS
```

## hermes harness board card

```text
usage: hermes harness board card [-h] {add,edit,move,archive,restore} ...

positional arguments:
  {add,edit,move,archive,restore}
    add                 Add a card
    edit                Edit a card
    move                Move a card to a column / position
    archive             Archive a card (archive-never-delete)
    restore             Restore an archived card

options:
  -h, --help            show this help message and exit
```

## hermes harness board card add

```text
usage: hermes harness board card add [-h] [--board BOARD] [--workspace WORKSPACE] --title TITLE
                                     [--description DESCRIPTION] [--column COLUMN]
                                     [--priority {p0,p1,p2,p3}] [--labels LABELS]
                                     [--assignee ASSIGNEE] [--created-by CREATED_BY]
                                     [-o {json,table,yaml,wide}] [--json] [-q] [--no-color]
                                     [--fields FIELDS] [--dry-run]
                                     [--idempotency-key IDEMPOTENCY_KEY]

options:
  -h, --help            show this help message and exit
  --board BOARD         Board id (default: active workspace's default board)
  --workspace WORKSPACE, --workspace-id WORKSPACE
  --title TITLE
  --description DESCRIPTION
  --column COLUMN       Column id or kind (default: first queued column)
  --priority {p0,p1,p2,p3}
  --labels LABELS       Comma-separated labels
  --assignee ASSIGNEE
  --created-by CREATED_BY
                        operator (default) or a persona id
  -o {json,table,yaml,wide}, --output {json,table,yaml,wide}
  --json                Alias for -o json
  -q, --quiet
  --no-color
  --fields FIELDS
  --dry-run
  --idempotency-key IDEMPOTENCY_KEY
```

## hermes harness board card edit

```text
usage: hermes harness board card edit [-h] [--title TITLE] [--description DESCRIPTION]
                                      [--priority {p0,p1,p2,p3}] [--labels LABELS]
                                      [--assignee ASSIGNEE] [--clear-assignee]
                                      [--expect-revision EXPECT_REVISION]
                                      [-o {json,table,yaml,wide}] [--json] [-q] [--no-color]
                                      [--fields FIELDS] [--idempotency-key IDEMPOTENCY_KEY]
                                      card_id

positional arguments:
  card_id

options:
  -h, --help            show this help message and exit
  --title TITLE
  --description DESCRIPTION
  --priority {p0,p1,p2,p3}
  --labels LABELS       Comma-separated labels (replaces)
  --assignee ASSIGNEE
  --clear-assignee
  --expect-revision EXPECT_REVISION
  -o {json,table,yaml,wide}, --output {json,table,yaml,wide}
  --json                Alias for -o json
  -q, --quiet
  --no-color
  --fields FIELDS
  --idempotency-key IDEMPOTENCY_KEY
```

## hermes harness board card move

```text
usage: hermes harness board card move [-h] --column COLUMN [--before BEFORE] [--after AFTER]
                                      [--expect-revision EXPECT_REVISION]
                                      [-o {json,table,yaml,wide}] [--json] [-q] [--no-color]
                                      [--fields FIELDS] [--idempotency-key IDEMPOTENCY_KEY]
                                      card_id

positional arguments:
  card_id

options:
  -h, --help            show this help message and exit
  --column COLUMN       Target column id or kind
  --before BEFORE       Place before this card id
  --after AFTER         Place after this card id
  --expect-revision EXPECT_REVISION
  -o {json,table,yaml,wide}, --output {json,table,yaml,wide}
  --json                Alias for -o json
  -q, --quiet
  --no-color
  --fields FIELDS
  --idempotency-key IDEMPOTENCY_KEY
```

## hermes harness board card archive

```text
usage: hermes harness board card archive [-h] [-o {json,table,yaml,wide}] [--json] [-q] [--no-color]
                                         [--fields FIELDS]
                                         card_id

positional arguments:
  card_id

options:
  -h, --help            show this help message and exit
  -o {json,table,yaml,wide}, --output {json,table,yaml,wide}
  --json                Alias for -o json
  -q, --quiet
  --no-color
  --fields FIELDS
```

## hermes harness board card restore

```text
usage: hermes harness board card restore [-h] [-o {json,table,yaml,wide}] [--json] [-q] [--no-color]
                                         [--fields FIELDS]
                                         card_id

positional arguments:
  card_id

options:
  -h, --help            show this help message and exit
  -o {json,table,yaml,wide}, --output {json,table,yaml,wide}
  --json                Alias for -o json
  -q, --quiet
  --no-color
  --fields FIELDS
```

## hermes harness board resolve-conflict

```text
usage: hermes harness board resolve-conflict [-h] --take {local,remote} [-o {json,table,yaml,wide}]
                                             [--json] [-q] [--no-color] [--fields FIELDS]
                                             card_id

positional arguments:
  card_id

options:
  -h, --help            show this help message and exit
  --take {local,remote}
  -o {json,table,yaml,wide}, --output {json,table,yaml,wide}
  --json                Alias for -o json
  -q, --quiet
  --no-color
  --fields FIELDS
```

## hermes harness office

```text
usage: hermes harness office [-h]
                             {show,actor-upsert,actor-remove,actor-restore,set-folders,resolve-conflict,archive-surface}
                             ...

positional arguments:
  {show,actor-upsert,actor-remove,actor-restore,set-folders,resolve-conflict,archive-surface}
    show                Show a workspace's office surface + actors
    actor-upsert        Create or update one actor placement (keys are minted store-side)
    actor-remove        Archive an actor placement (archive-never-delete); tombstones and propagates
                        realm-wide unless --local-only
    actor-restore       Restore an archived actor placement
    set-folders         Replace the surface's shared folder taxonomy
    resolve-conflict    Resolve a realm-sync conflict on an actor placement
    archive-surface     Archive an ORPHANED office surface (a surface whose workspace no longer
                        resolves); clears its orphaned_office parity warning

options:
  -h, --help            show this help message and exit
```

## hermes harness office show

```text
usage: hermes harness office show [-h] [--workspace WORKSPACE] [--full] [-o {json,table,yaml,wide}]
                                  [--json] [-q] [--no-color] [--fields FIELDS]

options:
  -h, --help            show this help message and exit
  --workspace WORKSPACE, --workspace-id WORKSPACE
  --full                Include actor item bodies
  -o {json,table,yaml,wide}, --output {json,table,yaml,wide}
  --json                Alias for -o json
  -q, --quiet
  --no-color
  --fields FIELDS
```

## hermes harness office actor-upsert

```text
usage: hermes harness office actor-upsert [-h] [--workspace WORKSPACE] --actor-json ACTOR_JSON
                                          [--persona-instance-id PERSONA_INSTANCE_ID]
                                          [--allow-class-key] [--resurrect]
                                          [--expect-revision EXPECT_REVISION]
                                          [--updated-by UPDATED_BY] [-o {json,table,yaml,wide}]
                                          [--json] [-q] [--no-color] [--fields FIELDS] [--dry-run]

options:
  -h, --help            show this help message and exit
  --workspace WORKSPACE, --workspace-id WORKSPACE
  --actor-json ACTOR_JSON
                        Actor object (path or inline JSON): {persona_id, persona_instance_id?,
                        backing_profile?, items:[...]}
  --persona-instance-id PERSONA_INSTANCE_ID
                        Bind the placement to this persona instance (overrides --actor-json's
                        persona_instance_id); the store still mints the key
  --allow-class-key     Escape hatch: force a class-keyed write that would otherwise be refused for
                        re-creating an archived or duplicated placement
  --resurrect           Escape hatch: re-add an actor key that was deleted, clearing its tombstone
                        (default: such a write is refused)
  --expect-revision EXPECT_REVISION
  --updated-by UPDATED_BY
  -o {json,table,yaml,wide}, --output {json,table,yaml,wide}
  --json                Alias for -o json
  -q, --quiet
  --no-color
  --fields FIELDS
  --dry-run
```

## hermes harness office actor-remove

```text
usage: hermes harness office actor-remove [-h] [--workspace WORKSPACE] --actor ACTOR
                                          [--reason REASON] [--local-only]
                                          [--expect-revision EXPECT_REVISION]
                                          [-o {json,table,yaml,wide}] [--json] [-q] [--no-color]
                                          [--fields FIELDS] [--dry-run]

options:
  -h, --help            show this help message and exit
  --workspace WORKSPACE, --workspace-id WORKSPACE
  --actor ACTOR         Actor key
  --reason REASON
  --local-only          Diagnostic repair: archive on THIS install only — no tombstone, nothing
                        propagates, and a realm pull may legitimately bring the actor back. Use for
                        doctor/dispatch/census repairs of local projection; omit when the operator
                        means to delete the placement everywhere
  --expect-revision EXPECT_REVISION
  -o {json,table,yaml,wide}, --output {json,table,yaml,wide}
  --json                Alias for -o json
  -q, --quiet
  --no-color
  --fields FIELDS
  --dry-run
```

## hermes harness office actor-restore

```text
usage: hermes harness office actor-restore [-h] [--workspace WORKSPACE] --actor ACTOR
                                           [-o {json,table,yaml,wide}] [--json] [-q] [--no-color]
                                           [--fields FIELDS] [--dry-run]

options:
  -h, --help            show this help message and exit
  --workspace WORKSPACE, --workspace-id WORKSPACE
  --actor ACTOR         Actor key
  -o {json,table,yaml,wide}, --output {json,table,yaml,wide}
  --json                Alias for -o json
  -q, --quiet
  --no-color
  --fields FIELDS
  --dry-run
```

## hermes harness office set-folders

```text
usage: hermes harness office set-folders [-h] [--workspace WORKSPACE] --folders FOLDERS
                                         [--expect-revision EXPECT_REVISION]
                                         [-o {json,table,yaml,wide}] [--json] [-q] [--no-color]
                                         [--fields FIELDS] [--dry-run]

options:
  -h, --help            show this help message and exit
  --workspace WORKSPACE, --workspace-id WORKSPACE
  --folders FOLDERS     Comma-separated folder names (structural defaults always kept)
  --expect-revision EXPECT_REVISION
  -o {json,table,yaml,wide}, --output {json,table,yaml,wide}
  --json                Alias for -o json
  -q, --quiet
  --no-color
  --fields FIELDS
  --dry-run
```

## hermes harness office resolve-conflict

```text
usage: hermes harness office resolve-conflict [-h] [--workspace WORKSPACE] --actor ACTOR --take
                                              {local,remote} [--allow-class-key]
                                              [-o {json,table,yaml,wide}] [--json] [-q] [--no-color]
                                              [--fields FIELDS] [--dry-run]

options:
  -h, --help            show this help message and exit
  --workspace WORKSPACE, --workspace-id WORKSPACE
  --actor ACTOR         Actor key
  --take {local,remote}
  --allow-class-key     Escape hatch: adopt a remote actor on a persona CLASS key the re-key
                        migration archived (re-creates the class-keyed placement beside its
                        instance-keyed sibling)
  -o {json,table,yaml,wide}, --output {json,table,yaml,wide}
  --json                Alias for -o json
  -q, --quiet
  --no-color
  --fields FIELDS
  --dry-run
```

## hermes harness office archive-surface

```text
usage: hermes harness office archive-surface [-h] [--workspace WORKSPACE]
                                             [-o {json,table,yaml,wide}] [--json] [-q] [--no-color]
                                             [--fields FIELDS] [--dry-run]

options:
  -h, --help            show this help message and exit
  --workspace WORKSPACE, --workspace-id WORKSPACE
  -o {json,table,yaml,wide}, --output {json,table,yaml,wide}
  --json                Alias for -o json
  -q, --quiet
  --no-color
  --fields FIELDS
  --dry-run
```

## hermes harness level

```text
usage: hermes harness level [-h] {show,set,clear} ...

positional arguments:
  {show,set,clear}
    show            Show a workspace's level (metadata; --full carries the document)
    set             Store a workspace's level document VERBATIM (hermes validates that it is JSON
                    with a version and reformats nothing)
    clear           Remove a workspace's level (local only — a level the realm still publishes
                    returns on the next pull)

options:
  -h, --help        show this help message and exit
```

## hermes harness level show

```text
usage: hermes harness level show [-h] [--workspace WORKSPACE] [--full] [-o {json,table,yaml,wide}]
                                 [--json] [-q] [--no-color] [--fields FIELDS]

options:
  -h, --help            show this help message and exit
  --workspace WORKSPACE, --workspace-id WORKSPACE
  --full                Include the level document itself, byte for byte as stored
  -o {json,table,yaml,wide}, --output {json,table,yaml,wide}
  --json                Alias for -o json
  -q, --quiet
  --no-color
  --fields FIELDS
```

## hermes harness level set

```text
usage: hermes harness level set [-h] [--workspace WORKSPACE] --document DOCUMENT
                                [--expect-sha256 EXPECT_SHA256] [-o {json,table,yaml,wide}] [--json]
                                [-q] [--no-color] [--fields FIELDS] [--dry-run]

options:
  -h, --help            show this help message and exit
  --workspace WORKSPACE, --workspace-id WORKSPACE
  --document DOCUMENT   Level document: a PATH to a JSON file (use this — a level can be 1 MB and a
                        Windows command line caps at ~32 KB), or inline JSON
  --expect-sha256 EXPECT_SHA256
                        Compare-and-set: the sha256 of the stored bytes this write is based on, or
                        'none' if the workspace must have no level yet
  -o {json,table,yaml,wide}, --output {json,table,yaml,wide}
  --json                Alias for -o json
  -q, --quiet
  --no-color
  --fields FIELDS
  --dry-run
```

## hermes harness level clear

```text
usage: hermes harness level clear [-h] [--workspace WORKSPACE] [--expect-sha256 EXPECT_SHA256]
                                  [-o {json,table,yaml,wide}] [--json] [-q] [--no-color]
                                  [--fields FIELDS] [--dry-run]

options:
  -h, --help            show this help message and exit
  --workspace WORKSPACE, --workspace-id WORKSPACE
  --expect-sha256 EXPECT_SHA256
                        Compare-and-set: the sha256 of the stored bytes this clear is based on, or
                        'none' if the workspace must have no level
  -o {json,table,yaml,wide}, --output {json,table,yaml,wide}
  --json                Alias for -o json
  -q, --quiet
  --no-color
  --fields FIELDS
  --dry-run
```

## hermes harness map

```text
usage: hermes harness map [-h] {list,show,set,clear} ...

positional arguments:
  {list,show,set,clear}
    list                List the catalogue (names and hashes; never the documents)
    show                Show one catalogue map (metadata; --full carries the document)
    set                 Store a catalogue map VERBATIM (hermes validates that it is JSON with a
                        version and a name, and reformats nothing)
    clear               Remove a catalogue map (local only — a map the realm still publishes returns
                        on the next pull)

options:
  -h, --help            show this help message and exit
```

## hermes harness map list

```text
usage: hermes harness map list [-h] [-o {json,table,yaml,wide}] [--json] [-q] [--no-color]
                               [--fields FIELDS]

options:
  -h, --help            show this help message and exit
  -o {json,table,yaml,wide}, --output {json,table,yaml,wide}
  --json                Alias for -o json
  -q, --quiet
  --no-color
  --fields FIELDS
```

## hermes harness map show

```text
usage: hermes harness map show [-h] [--map MAP] [--full] [-o {json,table,yaml,wide}] [--json] [-q]
                               [--no-color] [--fields FIELDS]

options:
  -h, --help            show this help message and exit
  --map MAP, --map-id MAP
  --full                Include the map document itself, byte for byte as stored
  -o {json,table,yaml,wide}, --output {json,table,yaml,wide}
  --json                Alias for -o json
  -q, --quiet
  --no-color
  --fields FIELDS
```

## hermes harness map set

```text
usage: hermes harness map set [-h] [--map MAP] --document DOCUMENT [--expect-sha256 EXPECT_SHA256]
                              [-o {json,table,yaml,wide}] [--json] [-q] [--no-color]
                              [--fields FIELDS] [--dry-run]

options:
  -h, --help            show this help message and exit
  --map MAP, --map-id MAP
  --document DOCUMENT   Map document: a PATH to a JSON file (use this — a map carries a scene and a
                        Windows command line caps at ~32 KB), or inline JSON
  --expect-sha256 EXPECT_SHA256
                        Compare-and-set: the sha256 of the stored bytes this write is based on, or
                        'none' if the catalogue must not hold this map yet
  -o {json,table,yaml,wide}, --output {json,table,yaml,wide}
  --json                Alias for -o json
  -q, --quiet
  --no-color
  --fields FIELDS
  --dry-run
```

## hermes harness map clear

```text
usage: hermes harness map clear [-h] [--map MAP] [--expect-sha256 EXPECT_SHA256]
                                [-o {json,table,yaml,wide}] [--json] [-q] [--no-color]
                                [--fields FIELDS] [--dry-run]

options:
  -h, --help            show this help message and exit
  --map MAP, --map-id MAP
  --expect-sha256 EXPECT_SHA256
                        Compare-and-set: the sha256 of the stored bytes this clear is based on, or
                        'none' if the catalogue must not hold this map
  -o {json,table,yaml,wide}, --output {json,table,yaml,wide}
  --json                Alias for -o json
  -q, --quiet
  --no-color
  --fields FIELDS
  --dry-run
```

## hermes harness persona

```text
usage: hermes harness persona [-h]
                              {list,show,tool-diff,permission,assignments,migrate-assignment-task-ids,chat,set-model,set-skills,instance}
                              ...

positional arguments:
  {list,show,tool-diff,permission,assignments,migrate-assignment-task-ids,chat,set-model,set-skills,instance}
    list                List durable persona instances
    show                Show one durable persona instance
    tool-diff           Show resolved model tools and blocked tools for one persona
    permission          Restrict (or clear a restriction on) one chat session's tool permissions.
                        The runtime default is the standing posture — this is the temporary
                        narrowing lane, not an escalation ritual.
    assignments         List persona assignments
    migrate-assignment-task-ids
                        Archive pre-retirement persona assignments whose task_id is non-null
    chat                Manage durable persona chat sessions
    set-model           Persist a persona's default provider/model (profile-default lane; future
                        instances inherit it)
    set-skills          Persist a persona's default skill set (profile-default lane; future
                        instances inherit it)
    instance            Create, open, steer, retire, and maintain persona instances (chat is the
                        only messaging lane)

options:
  -h, --help            show this help message and exit
```

## hermes harness persona list

```text
usage: hermes harness persona list [-h] [--json]

options:
  -h, --help  show this help message and exit
  --json
```

## hermes harness persona show

```text
usage: hermes harness persona show [-h] [--json] persona_id_or_instance_id

positional arguments:
  persona_id_or_instance_id

options:
  -h, --help            show this help message and exit
  --json
```

## hermes harness persona tool-diff

```text
usage: hermes harness persona tool-diff [-h] [--session-id SESSION_ID]
                                        [--permission-mode PERMISSION_MODE]
                                        [--repo-scope REPO_SCOPE] [--workdir WORKDIR]
                                        [--explain-mcp] [--explain-envelope] [--json]
                                        persona_id

positional arguments:
  persona_id            Persona id

options:
  -h, --help            show this help message and exit
  --session-id SESSION_ID
  --permission-mode PERMISSION_MODE
                        Preview under this permission mode (profile_default | bounded | read_only |
                        unbounded). Default: the runtime default from the ROOT config.
  --repo-scope REPO_SCOPE
  --workdir WORKDIR
  --explain-mcp         Explain MCP admission for this persona (requested / admitted / denied, with
                        typed reasons). Inspection only — resolves policy without connecting to or
                        registering any MCP server.
  --explain-envelope    Explain the terminal safety envelope for this persona's mission-chat lane:
                        whether the lane binds an envelope scope, which command classes are
                        operator-grantable vs a hard floor no config lifts, which grants are active
                        from the ROOT config, and any typed grant-config issues. Inspection only —
                        resolves policy without running a command.
  --json
```

## hermes harness persona permission

```text
usage: hermes harness persona permission [-h] {set} ...

positional arguments:
  {set}
    set       Set this session's permission mode. 'bounded' / 'read_only' RESTRICT it below the
              runtime default; 'profile_default' CLEARS the restriction (the session falls back to
              the runtime default); 'unbounded' is normally redundant with the default and only
              needed when the default is narrower.

options:
  -h, --help  show this help message and exit
```

## hermes harness persona permission set

```text
usage: hermes harness persona permission set [-h] --session-id SESSION_ID --mode
                                             {profile_default,bounded,read_only,unbounded} --reason
                                             REASON [--turns TURNS] [--ttl-seconds TTL_SECONDS]
                                             [--expires-at EXPIRES_AT] [--json]
                                             persona_id

positional arguments:
  persona_id            Persona id

options:
  -h, --help            show this help message and exit
  --session-id SESSION_ID
  --mode {profile_default,bounded,read_only,unbounded}
                        bounded = the historical bounded tier (persona-safety blocks + chat-lane
                        cost cuts + envelope grants table only); read_only = bounded plus the
                        mutating-tool block and the reviewer-shaped MCP subset; profile_default = no
                        opinion, defer to the runtime default; unbounded = full access.
  --reason REASON
  --turns TURNS         Expire the restriction after this many turns (any restricting mode
                        decrements).
  --ttl-seconds TTL_SECONDS
  --expires-at EXPIRES_AT
  --json
```

## hermes harness persona assignments

```text
usage: hermes harness persona assignments [-h] [--persona PERSONA_ID] [--json]

options:
  -h, --help            show this help message and exit
  --persona PERSONA_ID
  --json
```

## hermes harness persona migrate-assignment-task-ids

```text
usage: hermes harness persona migrate-assignment-task-ids [-h] [--dry-run] [--json]

options:
  -h, --help  show this help message and exit
  --dry-run
  --json
```

## hermes harness persona chat

```text
usage: hermes harness persona chat [-h] {delete,history} ...

positional arguments:
  {delete,history}
    delete          Delete a persona chat session and clear active persona bindings
    history         Page a persona chat session's complete redaction-safe transcript

options:
  -h, --help        show this help message and exit
```

## hermes harness persona chat delete

```text
usage: hermes harness persona chat delete [-h] --session-id SESSION_ID [--persona PERSONA_ID]
                                          [--persona-instance-id PERSONA_INSTANCE_ID]
                                          [--requested-by REQUESTED_BY] [--json]

options:
  -h, --help            show this help message and exit
  --session-id SESSION_ID
  --persona PERSONA_ID
  --persona-instance-id PERSONA_INSTANCE_ID
  --requested-by REQUESTED_BY
  --json
```

## hermes harness persona chat history

```text
usage: hermes harness persona chat history [-h] --session-id SESSION_ID [--limit LIMIT]
                                           [--before BEFORE] [--json]

options:
  -h, --help            show this help message and exit
  --session-id SESSION_ID
  --limit LIMIT         Page size (clamped 1..40)
  --before BEFORE       Opaque cursor returned by the previous page
  --json
```

## hermes harness persona set-model

```text
usage: hermes harness persona set-model [-h] [--provider PROVIDER] [--model MODEL] [--use-default]
                                        [--issued-at ISSUED_AT] [--requested-by REQUESTED_BY]
                                        [--json]
                                        persona_id

positional arguments:
  persona_id            Persona id or profile:<name>

options:
  -h, --help            show this help message and exit
  --provider PROVIDER   Provider lane (canonical name or alias; api_mode is derived from it)
  --model MODEL         Model id for the provider lane
  --use-default         Clear the persona's model/provider/api_mode so the runtime default cascade
                        applies
  --issued-at ISSUED_AT
                        ISO-8601 issue timestamp; stale writes are superseded instead of applied
  --requested-by REQUESTED_BY
  --json
```

## hermes harness persona set-skills

```text
usage: hermes harness persona set-skills [-h] [--skill SKILLS] [--clear-skills]
                                         [--issued-at ISSUED_AT] [--requested-by REQUESTED_BY]
                                         [--json]
                                         persona_id

positional arguments:
  persona_id            Persona id or profile:<name>

options:
  -h, --help            show this help message and exit
  --skill SKILLS        Skill id for the persona default set (repeatable); the flags given REPLACE
                        the stored set
  --clear-skills        Write an empty default set: every future inheriting placement starts with no
                        skills
  --issued-at ISSUED_AT
                        ISO-8601 issue timestamp; stale writes are superseded instead of applied
  --requested-by REQUESTED_BY
  --json
```

## hermes harness persona instance

```text
usage: hermes harness persona instance [-h]
                                       {create,open-chat,close,archive,retire,delete,steer,repair-steering,return-summary,update-profile,set-model}
                                       ...

positional arguments:
  {create,open-chat,close,archive,retire,delete,steer,repair-steering,return-summary,update-profile,set-model}
    create              Create an Agent Profile (operator chat channel) or an additional placement-
                        backed instance (--add-instance); requires --display-name
    open-chat           Bind a persona instance to a durable chat session without ticking
    close               Cancel residual free-floating assignment rows for one persona instance
                        (maintenance; the lane that minted them is retired)
    archive             Complete residual free-floating assignment rows for one persona instance
                        (maintenance; the lane that minted them is retired)
    retire (delete)     Delete (end-of-life) a placement-backed persona instance: archive its row
                        (chat history preserved)
    steer               Re-route a persona instance's living-graph wiring (Stage 77 steering edge;
                        supports multi-parent fan-in)
    repair-steering     Strip non-instance principals (e.g. the operator) out of a persona
                        instance's steering fields; --dry-run previews without writing or emitting
    return-summary      Post a bounded child summary back into a parent chat session
    update-profile      Update runtime persona-instance profile overrides without editing the
                        backing Hermes profile
    set-model           Persist an instance-level provider/model override (this agent only;
                        duplicates keep theirs)

options:
  -h, --help            show this help message and exit
```

## hermes harness persona instance create

```text
usage: hermes harness persona instance create [-h] --persona PERSONA_ID --title TITLE
                                              [--requested-by REQUESTED_BY]
                                              [--coordinator-id COORDINATOR_ID]
                                              [--coordinator-max-spawns COORDINATOR_MAX_SPAWNS]
                                              [--coordinator-spawns-used COORDINATOR_SPAWNS_USED]
                                              [--coordinator-may-kill-own]
                                              [--coordinator-no-kill-own]
                                              [--coordinator-may-kill-others]
                                              [--client-message-id CLIENT_MESSAGE_ID]
                                              [--display-name DISPLAY_NAME]
                                              [--session-id SESSION_ID] [--kill-active]
                                              [--add-instance] [--placement-id PLACEMENT_ID]
                                              [--workspace-id WORKSPACE_ID] [--realm-id REALM_ID]
                                              [--json]

options:
  -h, --help            show this help message and exit
  --persona PERSONA_ID
  --title TITLE         Fallback display name when --display-name is empty (launcher wire-compat)
  --requested-by REQUESTED_BY
  --coordinator-id COORDINATOR_ID
                        Coordinator persona id when --requested-by is coordinator; coordinator:<id>
                        carries it inline
  --coordinator-max-spawns COORDINATOR_MAX_SPAWNS
                        In-scope create/spawn grant for this coordinator action
  --coordinator-spawns-used COORDINATOR_SPAWNS_USED
                        Create/spawn actions already used in this coordinator scope
  --coordinator-may-kill-own
                        Allow killing instances spawned by this coordinator
  --coordinator-no-kill-own
                        Require confirmation even for own-spawned instances
  --coordinator-may-kill-others
                        Allow killing non-operator instances spawned by another coordinator
  --client-message-id CLIENT_MESSAGE_ID
  --display-name DISPLAY_NAME
  --session-id SESSION_ID
  --kill-active         Cancel the current run/worker before replacing the active chat
  --add-instance        Create an additional placement-backed instance instead of targeting the
                        primary placement
  --placement-id PLACEMENT_ID
                        Scene itemId for an additional placement-backed instance; must end in the
                        deliberate-placement shape <persona-token>_agent_<hex8>
  --workspace-id WORKSPACE_ID, --workspace WORKSPACE_ID
                        Mission Control workspace the placement belongs to (scope-provenance
                        pointer; only meaningful with --add-instance)
  --realm-id REALM_ID   Mission Control realm the placement belongs to (scope-provenance pointer;
                        only meaningful with --add-instance)
  --json
```

## hermes harness persona instance open-chat

```text
usage: hermes harness persona instance open-chat [-h] --persona PERSONA_ID
                                                 [--persona-instance-id PERSONA_INSTANCE_ID]
                                                 [--session-id SESSION_ID] [--new-session]
                                                 [--idempotency-key IDEMPOTENCY_KEY] [--kill-active]
                                                 [--add-instance] [--placement-id PLACEMENT_ID]
                                                 [--workspace-id WORKSPACE_ID] [--realm-id REALM_ID]
                                                 [--display-name DISPLAY_NAME]
                                                 [--requested-by REQUESTED_BY]
                                                 [--coordinator-id COORDINATOR_ID]
                                                 [--coordinator-max-spawns COORDINATOR_MAX_SPAWNS]
                                                 [--coordinator-spawns-used COORDINATOR_SPAWNS_USED]
                                                 [--coordinator-may-kill-own]
                                                 [--coordinator-no-kill-own]
                                                 [--coordinator-may-kill-others] [--json]

options:
  -h, --help            show this help message and exit
  --persona PERSONA_ID
  --persona-instance-id PERSONA_INSTANCE_ID
                        Exact existing persona instance to bind when minting a new chat
  --session-id SESSION_ID
  --new-session         Mint and select a fresh server-owned chat session for the target instance
  --idempotency-key IDEMPOTENCY_KEY
                        Stable retry key required with --new-session
  --kill-active         Cancel the current run/worker before replacing the active chat
  --add-instance        Open the chat on an additional placement-backed instance
  --placement-id PLACEMENT_ID
                        Scene itemId for an additional placement-backed instance; must end in the
                        deliberate-placement shape <persona-token>_agent_<hex8>
  --workspace-id WORKSPACE_ID, --workspace WORKSPACE_ID
                        Mission Control workspace the placement belongs to (scope-provenance
                        pointer; only meaningful with --add-instance)
  --realm-id REALM_ID   Mission Control realm the placement belongs to (scope-provenance pointer;
                        only meaningful with --add-instance)
  --display-name DISPLAY_NAME
                        Authoritative name for a deliberately placed additional instance; ignored
                        unless --add-instance
  --requested-by REQUESTED_BY
  --coordinator-id COORDINATOR_ID
                        Coordinator persona id when --requested-by is coordinator; coordinator:<id>
                        carries it inline
  --coordinator-max-spawns COORDINATOR_MAX_SPAWNS
                        In-scope create/spawn grant for this coordinator action
  --coordinator-spawns-used COORDINATOR_SPAWNS_USED
                        Create/spawn actions already used in this coordinator scope
  --coordinator-may-kill-own
                        Allow killing instances spawned by this coordinator
  --coordinator-no-kill-own
                        Require confirmation even for own-spawned instances
  --coordinator-may-kill-others
                        Allow killing non-operator instances spawned by another coordinator
  --json
```

## hermes harness persona instance close

```text
usage: hermes harness persona instance close [-h] --reason REASON [--requested-by REQUESTED_BY]
                                             [--coordinator-id COORDINATOR_ID]
                                             [--coordinator-max-spawns COORDINATOR_MAX_SPAWNS]
                                             [--coordinator-spawns-used COORDINATOR_SPAWNS_USED]
                                             [--coordinator-may-kill-own]
                                             [--coordinator-no-kill-own]
                                             [--coordinator-may-kill-others] [--json]
                                             persona_instance_id

positional arguments:
  persona_instance_id

options:
  -h, --help            show this help message and exit
  --reason REASON
  --requested-by REQUESTED_BY
  --coordinator-id COORDINATOR_ID
                        Coordinator persona id when --requested-by is coordinator; coordinator:<id>
                        carries it inline
  --coordinator-max-spawns COORDINATOR_MAX_SPAWNS
                        In-scope create/spawn grant for this coordinator action
  --coordinator-spawns-used COORDINATOR_SPAWNS_USED
                        Create/spawn actions already used in this coordinator scope
  --coordinator-may-kill-own
                        Allow killing instances spawned by this coordinator
  --coordinator-no-kill-own
                        Require confirmation even for own-spawned instances
  --coordinator-may-kill-others
                        Allow killing non-operator instances spawned by another coordinator
  --json
```

## hermes harness persona instance archive

```text
usage: hermes harness persona instance archive [-h] [--reason REASON] [--requested-by REQUESTED_BY]
                                               [--json]
                                               persona_instance_id

positional arguments:
  persona_instance_id

options:
  -h, --help            show this help message and exit
  --reason REASON
  --requested-by REQUESTED_BY
  --json
```

## hermes harness persona instance retire

Aliases: `delete`.

```text
usage: hermes harness persona instance retire [-h] [--reason REASON] [--requested-by REQUESTED_BY]
                                              [--correlation-id CORRELATION_ID]
                                              [--coordinator-id COORDINATOR_ID]
                                              [--coordinator-max-spawns COORDINATOR_MAX_SPAWNS]
                                              [--coordinator-spawns-used COORDINATOR_SPAWNS_USED]
                                              [--coordinator-may-kill-own]
                                              [--coordinator-no-kill-own]
                                              [--coordinator-may-kill-others] [--json]
                                              persona_instance_id

positional arguments:
  persona_instance_id

options:
  -h, --help            show this help message and exit
  --reason REASON
  --requested-by REQUESTED_BY
  --correlation-id CORRELATION_ID
  --coordinator-id COORDINATOR_ID
                        Coordinator persona id when --requested-by is coordinator; coordinator:<id>
                        carries it inline
  --coordinator-max-spawns COORDINATOR_MAX_SPAWNS
                        In-scope create/spawn grant for this coordinator action
  --coordinator-spawns-used COORDINATOR_SPAWNS_USED
                        Create/spawn actions already used in this coordinator scope
  --coordinator-may-kill-own
                        Allow killing instances spawned by this coordinator
  --coordinator-no-kill-own
                        Require confirmation even for own-spawned instances
  --coordinator-may-kill-others
                        Allow killing non-operator instances spawned by another coordinator
  --json
```

## hermes harness persona instance steer

```text
usage: hermes harness persona instance steer [-h] [--parent PARENT_INSTANCE_ID]
                                             [--add-parent ADD_PARENT]
                                             [--remove-parent REMOVE_PARENT]
                                             [--set-parents PARENT [PARENT ...]] [--goal GOAL_ID]
                                             [--detach] [--requested-by REQUESTED_BY]
                                             [--coordinator-id COORDINATOR_ID]
                                             [--coordinator-max-spawns COORDINATOR_MAX_SPAWNS]
                                             [--coordinator-spawns-used COORDINATOR_SPAWNS_USED]
                                             [--coordinator-may-kill-own]
                                             [--coordinator-no-kill-own]
                                             [--coordinator-may-kill-others] [--json]
                                             persona_instance_id

positional arguments:
  persona_instance_id

options:
  -h, --help            show this help message and exit
  --parent PARENT_INSTANCE_ID
                        Back-compat: REPLACE the steering set with this single parent (== --set-
                        parents <p>)
  --add-parent ADD_PARENT
                        Additively ADD one parent to the steering set (fan-in; idempotent)
  --remove-parent REMOVE_PARENT
                        Remove ONE parent from the steering set (detach-one; last one detaches)
  --set-parents PARENT [PARENT ...]
                        Declaratively REPLACE the whole steering set with these parents (fan-in)
  --goal GOAL_ID        Correlation id this sub-agent inherits from its parent; rides the snapshot
                        as persona_instance.goal_id, which the Launcher groups its agent rooms by
  --detach              Detach from ALL parents and clear the inherited correlation id (becomes a
                        standalone owner)
  --requested-by REQUESTED_BY
  --coordinator-id COORDINATOR_ID
                        Coordinator persona id when --requested-by is coordinator; coordinator:<id>
                        carries it inline
  --coordinator-max-spawns COORDINATOR_MAX_SPAWNS
                        In-scope create/spawn grant for this coordinator action
  --coordinator-spawns-used COORDINATOR_SPAWNS_USED
                        Create/spawn actions already used in this coordinator scope
  --coordinator-may-kill-own
                        Allow killing instances spawned by this coordinator
  --coordinator-no-kill-own
                        Require confirmation even for own-spawned instances
  --coordinator-may-kill-others
                        Allow killing non-operator instances spawned by another coordinator
  --json
```

## hermes harness persona instance repair-steering

```text
usage: hermes harness persona instance repair-steering [-h] [--all] [--json] [--no-color]
                                                       [--dry-run]
                                                       [persona_instance_id]

positional arguments:
  persona_instance_id  Target one row; omit and pass --all to scan every row

options:
  -h, --help           show this help message and exit
  --all                Scan and repair every persona-instance row
  --json               Alias for -o json
  --no-color
  --dry-run
```

## hermes harness persona instance return-summary

```text
usage: hermes harness persona instance return-summary [-h] --parent-session-id PARENT_SESSION_ID
                                                      --summary SUMMARY [--proof-id PROOF_IDS]
                                                      [--artifact-ref ARTIFACT_REFS] [--json]
                                                      persona_instance_id

positional arguments:
  persona_instance_id

options:
  -h, --help            show this help message and exit
  --parent-session-id PARENT_SESSION_ID
  --summary SUMMARY
  --proof-id PROOF_IDS
  --artifact-ref ARTIFACT_REFS
  --json
```

## hermes harness persona instance update-profile

```text
usage: hermes harness persona instance update-profile [-h] [--display-name DISPLAY_NAME]
                                                      [--current-chat-goal CURRENT_CHAT_GOAL]
                                                      [--goal GOAL_ID] [--skill SKILLS]
                                                      [--clear-skills] [--inherit-skills]
                                                      [--requested-by REQUESTED_BY]
                                                      [--coordinator-id COORDINATOR_ID]
                                                      [--coordinator-max-spawns COORDINATOR_MAX_SPAWNS]
                                                      [--coordinator-spawns-used COORDINATOR_SPAWNS_USED]
                                                      [--coordinator-may-kill-own]
                                                      [--coordinator-no-kill-own]
                                                      [--coordinator-may-kill-others] [--json]
                                                      persona_instance_id

positional arguments:
  persona_instance_id

options:
  -h, --help            show this help message and exit
  --display-name DISPLAY_NAME
  --current-chat-goal CURRENT_CHAT_GOAL
  --goal GOAL_ID
  --skill SKILLS
  --clear-skills        Pin this agent to NO skills — an explicit empty set, not the template's
  --inherit-skills      Drop this agent's own skill set so it follows its persona template again,
                        live
  --requested-by REQUESTED_BY
  --coordinator-id COORDINATOR_ID
                        Coordinator persona id when --requested-by is coordinator; coordinator:<id>
                        carries it inline
  --coordinator-max-spawns COORDINATOR_MAX_SPAWNS
                        In-scope create/spawn grant for this coordinator action
  --coordinator-spawns-used COORDINATOR_SPAWNS_USED
                        Create/spawn actions already used in this coordinator scope
  --coordinator-may-kill-own
                        Allow killing instances spawned by this coordinator
  --coordinator-no-kill-own
                        Require confirmation even for own-spawned instances
  --coordinator-may-kill-others
                        Allow killing non-operator instances spawned by another coordinator
  --json
```

## hermes harness persona instance set-model

```text
usage: hermes harness persona instance set-model [-h] [--provider PROVIDER] [--model MODEL]
                                                 [--reasoning-effort REASONING_EFFORT]
                                                 [--use-profile-default] [--issued-at ISSUED_AT]
                                                 [--requested-by REQUESTED_BY]
                                                 [--coordinator-id COORDINATOR_ID]
                                                 [--coordinator-max-spawns COORDINATOR_MAX_SPAWNS]
                                                 [--coordinator-spawns-used COORDINATOR_SPAWNS_USED]
                                                 [--coordinator-may-kill-own]
                                                 [--coordinator-no-kill-own]
                                                 [--coordinator-may-kill-others] [--json]
                                                 persona_instance_id

positional arguments:
  persona_instance_id

options:
  -h, --help            show this help message and exit
  --provider PROVIDER   Provider lane (canonical name or alias; api_mode is derived from it)
  --model MODEL         Model id for the provider lane
  --reasoning-effort REASONING_EFFORT
                        Per-instance reasoning effort for reasoning-capable models (none, minimal,
                        low, medium, high, xhigh); empty string clears it
  --use-profile-default
                        Clear the instance override so the backing profile default applies live
  --issued-at ISSUED_AT
                        ISO-8601 issue timestamp; stale writes are superseded instead of applied
  --requested-by REQUESTED_BY
  --coordinator-id COORDINATOR_ID
                        Coordinator persona id when --requested-by is coordinator; coordinator:<id>
                        carries it inline
  --coordinator-max-spawns COORDINATOR_MAX_SPAWNS
                        In-scope create/spawn grant for this coordinator action
  --coordinator-spawns-used COORDINATOR_SPAWNS_USED
                        Create/spawn actions already used in this coordinator scope
  --coordinator-may-kill-own
                        Allow killing instances spawned by this coordinator
  --coordinator-no-kill-own
                        Require confirmation even for own-spawned instances
  --coordinator-may-kill-others
                        Allow killing non-operator instances spawned by another coordinator
  --json
```

## hermes harness mission-chat

```text
usage: hermes harness mission-chat [-h]
                                   {message,queue-skill,steer,turn-resolve,clarify-tickets,dispatch}
                                   ...

positional arguments:
  {message,queue-skill,steer,turn-resolve,clarify-tickets,dispatch}
    message             Send one Mission Control chat turn through the normal Hermes profile context
    queue-skill         Load a skill on the next Mission Control chat turn
    steer               Steer an active streamed Mission Control chat turn
    turn-resolve        Resolve one outcome_unknown chat turn
    clarify-tickets     Clarify-token adoption readout: live tickets with state/age/session binding,
                        and the bound_via histogram
    dispatch            Operate the agent-to-agent dispatch delivery queue

options:
  -h, --help            show this help message and exit
```

## hermes harness mission-chat message

```text
usage: hermes harness mission-chat message [-h] --persona PERSONA_ID
                                           [--persona-instance-id PERSONA_INSTANCE_ID]
                                           [--session-id SESSION_ID] [--new-session]
                                           [--clarify-token CLARIFY_TOKEN] [--title TITLE] --message
                                           MESSAGE [--provider PROVIDER] [--model MODEL]
                                           [--use-agent-default] [--surface-prompt SURFACE_PROMPT]
                                           [--agents-file AGENTS_FILE] [--workspace-id WORKSPACE_ID]
                                           [--workspace-name WORKSPACE_NAME]
                                           [--intent-hint INTENT_HINT] [--requested-by REQUESTED_BY]
                                           [--client-message-id CLIENT_MESSAGE_ID]
                                           [--idempotency-key IDEMPOTENCY_KEY] [--stream]
                                           [--max-seconds MAX_SECONDS]
                                           [--compression-threshold-tokens COMPRESSION_THRESHOLD_TOKENS]
                                           [--compression-protect-first-n COMPRESSION_PROTECT_FIRST_N]
                                           [--compression-protect-last-n COMPRESSION_PROTECT_LAST_N]
                                           [--relay-chain RELAY_CHAIN]
                                           [--relay-deadline-epoch RELAY_DEADLINE_EPOCH]
                                           [--requested-by-session REQUESTED_BY_SESSION]
                                           [--defer-thread-policy] [--json]

options:
  -h, --help            show this help message and exit
  --persona PERSONA_ID
  --persona-instance-id PERSONA_INSTANCE_ID
  --session-id SESSION_ID
  --new-session         Force a fresh canonical chat session for the target instead of continuing
                        the default thread (agent_chat_send new_session lane); ignored when
                        --session-id is given
  --clarify-token CLARIFY_TOKEN
                        Answer a clarify question by its token (clarify_request.clarify_token from
                        the asking turn); binds this reply to the thread the question was asked in
  --title TITLE         Title for a chat session this send MINTS (a fresh --new-session thread, or a
                        dispatch under the new_per_dispatch policy); ignored when continuing an
                        existing thread
  --message MESSAGE
  --provider PROVIDER   Provider override for this persona chat session only
  --model MODEL         Model override for this persona chat session only
  --use-agent-default   Clear the chat-scoped provider/model override before sending
  --surface-prompt SURFACE_PROMPT
  --agents-file AGENTS_FILE
                        Absolute path to one operator-selected workspace AGENTS.md to inject for
                        this turn
  --workspace-id WORKSPACE_ID, --workspace WORKSPACE_ID
  --workspace-name WORKSPACE_NAME
  --intent-hint INTENT_HINT
  --requested-by REQUESTED_BY
  --client-message-id CLIENT_MESSAGE_ID
  --idempotency-key IDEMPOTENCY_KEY
  --stream              Emit operator-chat deltas and the final payload as NDJSON
  --max-seconds MAX_SECONDS
                        Wall budget for this turn (default:
                        agent_runtime.mission_chat.default_max_seconds, or 240s when unset). The
                        agent is told how much remains, and the last max(60s, 15%) is reserved for a
                        final checkpoint reply; the turn then settles as budget_exhausted (terminal,
                        no turn-resolve)
  --compression-threshold-tokens COMPRESSION_THRESHOLD_TOKENS
                        One-turn native-compression proof seam; overrides the compressor token
                        threshold without changing profile config
  --compression-protect-first-n COMPRESSION_PROTECT_FIRST_N
                        One-turn native-compression proof seam; override protected head messages
  --compression-protect-last-n COMPRESSION_PROTECT_LAST_N
                        One-turn native-compression proof seam; override protected tail messages
  --relay-chain RELAY_CHAIN
                        Comma-separated canonical persona ids already on the agent-relay chain
                        (envelope provenance for chained agent_chat_send hops)
  --relay-deadline-epoch RELAY_DEADLINE_EPOCH
                        Absolute unix-epoch deadline shared by every hop on the relay chain
  --requested-by-session REQUESTED_BY_SESSION
                        Chat-root session id of the sender (envelope provenance; scopes bare-persona
                        target resolution to the sender's workspace)
  --defer-thread-policy
                        State NO opinion about the thread: let
                        agent_runtime.mission_chat.dispatch_session_policy decide (the tri-state
                        'unset' the in-process dispatch lane forwards). Overrides --new-session
  --json
```

## hermes harness mission-chat queue-skill

```text
usage: hermes harness mission-chat queue-skill [-h] --persona PERSONA_ID
                                               [--persona-instance-id PERSONA_INSTANCE_ID]
                                               --session-id SESSION_ID [--skill SKILL]
                                               [--skills SKILLS [SKILLS ...]] [--json]

options:
  -h, --help            show this help message and exit
  --persona PERSONA_ID
  --persona-instance-id PERSONA_INSTANCE_ID
  --session-id SESSION_ID
  --skill SKILL
  --skills SKILLS [SKILLS ...]
  --json
```

## hermes harness mission-chat steer

```text
usage: hermes harness mission-chat steer [-h] --session-id SESSION_ID --message MESSAGE
                                         --client-message-id CLIENT_MESSAGE_ID
                                         [--persona PERSONA_ID]
                                         [--persona-instance-id PERSONA_INSTANCE_ID] [--json]

options:
  -h, --help            show this help message and exit
  --session-id SESSION_ID
  --message MESSAGE
  --client-message-id CLIENT_MESSAGE_ID
  --persona PERSONA_ID
  --persona-instance-id PERSONA_INSTANCE_ID
  --json
```

## hermes harness mission-chat turn-resolve

```text
usage: hermes harness mission-chat turn-resolve [-h] --session-id SESSION_ID --client-message-id
                                                CLIENT_MESSAGE_ID --turn-id TURN_ID
                                                [--persona-instance-id PERSONA_INSTANCE_ID] --action
                                                {abandon} [--json]

options:
  -h, --help            show this help message and exit
  --session-id SESSION_ID
  --client-message-id CLIENT_MESSAGE_ID
  --turn-id TURN_ID
  --persona-instance-id PERSONA_INSTANCE_ID
  --action {abandon}
  --json
```

## hermes harness mission-chat clarify-tickets

```text
usage: hermes harness mission-chat clarify-tickets [-h] [-o {json,table,yaml,wide}] [--json] [-q]
                                                   [--no-color] [--fields FIELDS] [--sort SORT]
                                                   [--limit LIMIT] [--session-id SESSION_ID]
                                                   [--state {open,answered,rebound}]

options:
  -h, --help            show this help message and exit
  -o {json,table,yaml,wide}, --output {json,table,yaml,wide}
  --json                Alias for -o json
  -q, --quiet
  --no-color
  --fields FIELDS
  --sort SORT
  --limit LIMIT
  --session-id SESSION_ID
                        Only list tickets bound to this chat root (counts still cover the whole
                        store)
  --state {open,answered,rebound}
                        Only list tickets in this lifecycle state (counts still cover the whole
                        store)
```

## hermes harness mission-chat dispatch

```text
usage: hermes harness mission-chat dispatch [-h] {redeliver} ...

positional arguments:
  {redeliver}
    redeliver  Re-arm a DROPPED dispatch reply for another delivery pass (dropped -> pending,
               attempts 0, previous drop reason cleared)

options:
  -h, --help   show this help message and exit
```

## hermes harness mission-chat dispatch redeliver

```text
usage: hermes harness mission-chat dispatch redeliver [-h] [--json] dispatch_id

positional arguments:
  dispatch_id  The dispatch handle, e.g. dispatch-2540634d5cf3

options:
  -h, --help   show this help message and exit
  --json
```

## hermes harness status

```text
usage: hermes harness status [-h] [--json] [--prune-stale]

options:
  -h, --help     show this help message and exit
  --json
  --prune-stale  Delete serve_instances entries whose PID is PROVABLY dead, and report exactly which
                 (recycled-PID and unclassifiable entries are always kept)
```

## hermes harness providers

```text
usage: hermes harness providers [-h] [--json]

options:
  -h, --help  show this help message and exit
  --json
```

## hermes harness usage

```text
usage: hermes harness usage [-h] [--json] [--provider PROVIDER] [--timeout TIMEOUT]

options:
  -h, --help           show this help message and exit
  --json
  --provider PROVIDER  Restrict to a single provider lane (still emits the full envelope)
  --timeout TIMEOUT    Overall wall-clock bound (seconds) for the concurrent lane fetches
```

## hermes harness doctor

```text
usage: hermes harness doctor [-h] [--json] [--fix] [--dry-run] [--yes]
                             [--worktree-min-age-seconds WORKTREE_MIN_AGE_SECONDS]

options:
  -h, --help            show this help message and exit
  --json
  --fix                 Capture-then-reap the orphan worktrees the scan reports
  --dry-run             Preview --fix repairs without mutating runtime state
  --yes, -y             Confirm --fix repairs
  --worktree-min-age-seconds WORKTREE_MIN_AGE_SECONDS
```

## hermes harness health

```text
usage: hermes harness health [-h] [--json]

options:
  -h, --help  show this help message and exit
  --json
```

## hermes harness verify

```text
usage: hermes harness verify [-h] [--json] [--mode {live-tony,ci,temp-root}] [--skip-tests]

options:
  -h, --help            show this help message and exit
  --json
  --mode {live-tony,ci,temp-root}
  --skip-tests
```

## hermes harness config

```text
usage: hermes harness config [-h] {show} ...

positional arguments:
  {show}
    show      Show effective Harness runtime config

options:
  -h, --help  show this help message and exit
```

## hermes harness config show

```text
usage: hermes harness config show [-h] [--json]

options:
  -h, --help  show this help message and exit
  --json
```

## hermes harness migrate

```text
usage: hermes harness migrate [-h] [--check] [--json]

options:
  -h, --help  show this help message and exit
  --check     Report pending migrations without modifying data
  --json
```

## hermes harness observe

```text
usage: hermes harness observe [-h] [--json]

options:
  -h, --help  show this help message and exit
  --json
```

## hermes harness contracts

```text
usage: hermes harness contracts [-h] {dump} ...

positional arguments:
  {dump}
    dump      Dump redaction-safe contract registry

options:
  -h, --help  show this help message and exit
```

## hermes harness contracts dump

```text
usage: hermes harness contracts dump [-h] [--json]

options:
  -h, --help  show this help message and exit
  --json
```

## hermes harness worktree

```text
usage: hermes harness worktree [-h] {reap} ...

positional arguments:
  {reap}
    reap      Capture-then-reap orphan harness worktrees with no live owner

options:
  -h, --help  show this help message and exit
```

## hermes harness worktree reap

```text
usage: hermes harness worktree reap [-h] [--min-age-seconds MIN_AGE_SECONDS] [--dry-run]
                                    [--include-legacy-temp] [--json]

options:
  -h, --help            show this help message and exit
  --min-age-seconds MIN_AGE_SECONDS
  --dry-run             Preview typed reap/keep decisions without removing worktrees, capturing
                        patches, or emitting events
  --include-legacy-temp
                        Also inventory the canonical legacy system-temp hermes-agent-wt base
  --json
```

## hermes harness persona-instance

```text
usage: hermes harness persona-instance [-h] {reconcile,chat-bindings,detail} ...

positional arguments:
  {reconcile,chat-bindings,detail}
    reconcile           Archive-and-fold legacy-id persona-instance rows onto their canonical
                        channel (duplicate agent cards repair); records identity_map aliases; prunes
                        orphan rows; repairs missing steering parents and chat-session bindings
                        whose session SessionDB no longer has
    chat-bindings       READ-ONLY: name every persona instance whose bound chat session SessionDB no
                        longer holds — the producer of the snapshot's `session_not_in_db` parity
                        drop. Never writes; `persona-instance reconcile` is the repair
    detail              Serve the tool-detail payloads (tool_resolution / turn_tool_context /
                        permission_state / blocked_tools) evicted from the steady-state frame behind
                        the visibility_ref pointer

options:
  -h, --help            show this help message and exit
```

## hermes harness persona-instance reconcile

```text
usage: hermes harness persona-instance reconcile [-h] [--dry-run] [--json]

options:
  -h, --help  show this help message and exit
  --dry-run   Report actions without mutating the store. To ONLY ask which chat bindings are stale,
              prefer `persona-instance chat-bindings`, which has no write mode at all
  --json
```

## hermes harness persona-instance chat-bindings

```text
usage: hermes harness persona-instance chat-bindings [-h] [--json]

options:
  -h, --help  show this help message and exit
  --json
```

## hermes harness persona-instance detail

```text
usage: hermes harness persona-instance detail [-h] [--json] instance_id

positional arguments:
  instance_id  Persona-instance id (or persona id) whose tool detail to fetch

options:
  -h, --help   show this help message and exit
  --json
```

## hermes harness agent

```text
usage: hermes harness agent [-h] {list,create,retire,set-profile} ...

positional arguments:
  {list,create,retire,set-profile}
    list                List persisted/configured agent definitions
    create              Place an agent: roster row, chat root and office placement in ONE atomic
                        call
    retire              Retire a placed agent: archive its roster row AND every office actor bound
                        to it in ONE call
    set-profile         Rebind an agent to a different Hermes profile (the ONE door; cascades every
                        instance projection)

options:
  -h, --help            show this help message and exit
```

## hermes harness agent list

```text
usage: hermes harness agent list [-h] [--all-profiles] [-o {json,table,yaml,wide}] [--json] [-q]
                                 [--no-color] [--fields FIELDS] [--sort SORT]

options:
  -h, --help            show this help message and exit
  --all-profiles
  -o {json,table,yaml,wide}, --output {json,table,yaml,wide}
  --json                Alias for -o json
  -q, --quiet
  --no-color
  --fields FIELDS
  --sort SORT
```

## hermes harness agent create

```text
usage: hermes harness agent create [-h] --persona PERSONA_ID --workspace WORKSPACE_ID [--pos X Y]
                                   [--skill SKILLS] [--display-name DISPLAY_NAME]
                                   [--placement-id PLACEMENT_ID] [--realm-id REALM_ID]
                                   [--folder FOLDER] [--idempotency-key IDEMPOTENCY_KEY]
                                   [--correlation-id CORRELATION_ID] [--json]

options:
  -h, --help            show this help message and exit
  --persona PERSONA_ID  Roster persona id (or profile:<token>); an unknown id is refused before any
                        write
  --workspace WORKSPACE_ID, --workspace-id WORKSPACE_ID
                        Mission Control workspace the placement lands in; must already exist
  --pos X Y             Canvas position for the placement; omitted, the layout policy picks the
                        first free slot in the folder
  --skill SKILLS        Assign a skill to the new instance (repeatable); a canonical harness skill
                        is installed and hash-verified first
  --display-name DISPLAY_NAME
                        Authoritative name; omitted falls back to the persona's configured display
                        name
  --placement-id PLACEMENT_ID
                        Scene itemId to predict the actor key from; must end in <persona-
                        token>_agent_<hex8>, and omitted mints one server-side
  --realm-id REALM_ID
  --folder FOLDER       Office folder for the placement (default: Agents)
  --idempotency-key IDEMPOTENCY_KEY
                        Stable retry key; omitted mints a fresh cli-<uuid4> so a re-run is a new
                        gesture
  --correlation-id CORRELATION_ID
  --json
```

## hermes harness agent retire

```text
usage: hermes harness agent retire [-h] [--reason REASON] [--requested-by REQUESTED_BY]
                                   [--correlation-id CORRELATION_ID] [--json]
                                   persona_instance_id

positional arguments:
  persona_instance_id   Persona-instance id of the placement to retire

options:
  -h, --help            show this help message and exit
  --reason REASON
  --requested-by REQUESTED_BY
  --correlation-id CORRELATION_ID
  --json
```

## hermes harness agent set-profile

```text
usage: hermes harness agent set-profile [-h] --profile PROFILE [--requested-by REQUESTED_BY]
                                        [-o {json,table,yaml,wide}] [--json] [-q] [--no-color]
                                        [--fields FIELDS] [--dry-run]
                                        persona_id

positional arguments:
  persona_id            Store-persisted agent id

options:
  -h, --help            show this help message and exit
  --profile PROFILE     Target Hermes profile name; must exist and resolve ready
  --requested-by REQUESTED_BY
  -o {json,table,yaml,wide}, --output {json,table,yaml,wide}
  --json                Alias for -o json
  -q, --quiet
  --no-color
  --fields FIELDS
  --dry-run
```

## hermes harness install-harness-skills

```text
usage: hermes harness install-harness-skills [-h] [--active-profile-only] [--json]

options:
  -h, --help            show this help message and exit
  --active-profile-only
                        Install all Harness skills only into the active Hermes profile
  --json
```

## hermes harness snapshot

```text
usage: hermes harness snapshot [-h] [--json]

options:
  -h, --help  show this help message and exit
  --json
```

## hermes harness stream

```text
usage: hermes harness stream [-h] [--poll-interval POLL_INTERVAL]
                             [--heartbeat-interval HEARTBEAT_INTERVAL]
                             [--delta-debounce-ms DELTA_DEBOUNCE_MS] [--resync]
                             [--fold-entities persona_instance,incident]

options:
  -h, --help            show this help message and exit
  --poll-interval POLL_INTERVAL
  --heartbeat-interval HEARTBEAT_INTERVAL
  --delta-debounce-ms DELTA_DEBOUNCE_MS
                        Settle window for coalescing an event burst into one delta frame (0
                        disables)
  --resync              Force the first post-hydrate batch to a full core (S6: a reconnecting fold
                        client re-baselining before it folds patches)
  --fold-entities persona_instance,incident
                        Comma-separated entity classes THIS client can fold in place. A batch naming
                        any other entity is demoted to a full core rather than shipping a patch the
                        client would have to re-hydrate from. Omit the flag for the historical set
                        (persona_instance,incident) — exactly today's wire; pass an empty value to
                        declare that you fold nothing.
```

## hermes harness serve

```text
usage: hermes harness serve [-h] [--ndjson] [--no-socket] [--service] {connect} ...

positional arguments:
  {connect}
    connect    Connect to this root's live serve socket, perform the hello handshake, and print the
               reply as JSON

options:
  -h, --help   show this help message and exit
  --ndjson     NDJSON frame transport over stdio (the only v1 transport)
  --no-socket  Run stdio-only: do not race for the per-root socket ownership lock and do not listen
               (the ready frame reports socket.outcome=disabled)
  --service    Run as a durable service: stdin EOF means the starter DETACHED, not stop. The runtime
               keeps serving both socket lanes and ends only on `serve connect --drain`, SIGTERM, or
               a stdio `shutdown` sent before EOF. A starter that loses the per-root ownership lock
               exits 0 naming the winner instead of becoming a second executor. Incompatible with
               --no-socket (a drain would have no lane to arrive on).
```

## hermes harness serve connect

```text
usage: hermes harness serve connect [-h] [--probe] [--drain] [--deadline-seconds DEADLINE_SECONDS]
                                    [--client CLIENT] [--timeout TIMEOUT]

options:
  -h, --help            show this help message and exit
  --probe               Also ask the service for its version block (build, boot_id, connections)
  --drain               Ask the service to drain and read to its terminal frame (the durable-service
                        restart verb, from the outside)
  --deadline-seconds DEADLINE_SECONDS
                        Drain deadline handed to the service (the service floors it at its own
                        minimum, 30s by default, and caps it at 3600s)
  --client CLIENT       Client name recorded on the connection and in the service's logs (default:
                        harness-serve-connect)
  --timeout TIMEOUT     Socket connect/read timeout in seconds
```

## hermes harness work

```text
usage: hermes harness work [-h] {list,peek,cancel} ...

positional arguments:
  {list,peek,cancel}
    list              Every piece of running background work, with per-source health
    peek              Bounded read-only look at one item's recent output/progress
    cancel            Interrupt one piece of running work through its owning subsystem

options:
  -h, --help          show this help message and exit
```

## hermes harness work list

```text
usage: hermes harness work list [-h] [--kind KIND] [-o {json,table,yaml,wide}] [--json] [-q]
                                [--no-color] [--fields FIELDS] [--sort SORT] [--limit LIMIT]

options:
  -h, --help            show this help message and exit
  --kind KIND           Only rows of this kind (terminal, delegation, chat_turn, mcp_server,
                        cron_job, dispatch)
  -o {json,table,yaml,wide}, --output {json,table,yaml,wide}
  --json                Alias for -o json
  -q, --quiet
  --no-color
  --fields FIELDS
  --sort SORT
  --limit LIMIT
```

## hermes harness work peek

```text
usage: hermes harness work peek [-h] [-o {json,table,yaml,wide}] [--json] [-q] [--no-color]
                                [--fields FIELDS]
                                work_id

positional arguments:
  work_id               Work id from `harness work list`, e.g. terminal:sess-1

options:
  -h, --help            show this help message and exit
  -o {json,table,yaml,wide}, --output {json,table,yaml,wide}
  --json                Alias for -o json
  -q, --quiet
  --no-color
  --fields FIELDS
```

## hermes harness work cancel

```text
usage: hermes harness work cancel [-h] [--reason REASON] [--issued-at ISSUED_AT]
                                  [-o {json,table,yaml,wide}] [--json] [-q] [--no-color]
                                  [--fields FIELDS] [--dry-run] [--yes]
                                  work_id

positional arguments:
  work_id               Work id from `harness work list`

options:
  -h, --help            show this help message and exit
  --reason REASON       Recorded interrupt reason
  --issued-at ISSUED_AT
                        ISO-8601 issue timestamp; a cancel issued before the work started is
                        superseded instead of applied
  -o {json,table,yaml,wide}, --output {json,table,yaml,wide}
  --json                Alias for -o json
  -q, --quiet
  --no-color
  --fields FIELDS
  --dry-run
  --yes, -y
```

## hermes harness pets

```text
usage: hermes harness pets [-h] {gallery,install,sprite,thumb} ...

positional arguments:
  {gallery,install,sprite,thumb}
    gallery             List Petdex pets for Launcher
    install             Install a Petdex pet by slug
    sprite              Return an installed pet spritesheet payload
    thumb               Return a Petdex pet thumbnail

options:
  -h, --help            show this help message and exit
```

## hermes harness pets gallery

```text
usage: hermes harness pets gallery [-h] [--local-only] [--limit LIMIT] [--query QUERY] [--json]

options:
  -h, --help     show this help message and exit
  --local-only   Only include installed pets; skip the remote manifest
  --limit LIMIT  Maximum remote rows; 0 = all
  --query QUERY  Filter by slug/display name substring
  --json
```

## hermes harness pets install

```text
usage: hermes harness pets install [-h] [--force] [--json] slug

positional arguments:
  slug

options:
  -h, --help  show this help message and exit
  --force
  --json
```

## hermes harness pets sprite

```text
usage: hermes harness pets sprite [-h] [--no-sheet] [--json] slug

positional arguments:
  slug

options:
  -h, --help  show this help message and exit
  --no-sheet  Metadata only: drop `spritesheetBase64` and carry `sheet`, the absolute path, in its
              place. `spritesheetRevision` and every geometry/taxonomy key are unchanged. Mirrors
              `characters sprite --no-sheet`. The default is byte-identical to what it always was
  --json
```

## hermes harness pets thumb

```text
usage: hermes harness pets thumb [-h] [--url URL] [--json] slug

positional arguments:
  slug

options:
  -h, --help  show this help message and exit
  --url URL   Optional Petdex spritesheet URL for non-installed gallery pets
  --json
```

## hermes harness characters

```text
usage: hermes harness characters [-h]
                                 {start,list,backfill-home,migrate-home,status,thumb,base,turnaround,reroll-direction,approve-direction,rows,reroll-row,compose,auto,reopen,add-state,sprite,payload-contract}
                                 ...

positional arguments:
  {start,list,backfill-home,migrate-home,status,thumb,base,turnaround,reroll-direction,approve-direction,rows,reroll-row,compose,auto,reopen,add-state,sprite,payload-contract}
    start               Create a character draft at stage 'turnaround' (offline; generates nothing)
    list                List character drafts and installed characters
    backfill-home       Record `hermes_home` on library drafts that carry no home, and on no others.
                        The field is provenance of the authoring RUN, not an address — the library
                        is install-wide, so this stamps the home THIS run resolved onto a draft that
                        arrived without one (restored from quarantine, hand-copied in); `migrate-
                        home` stamps the legacy source home instead, which is the case that had a
                        better answer available. Explicit and receipted on purpose: a draft that
                        already states a home keeps it, and the write leaves `updated` and every
                        other key exactly as it found them, because the drafts this reaches are
                        dormant exhibits whose timeline is evidence. Idempotent — a second run
                        stamps nothing
    migrate-home        Move THIS home's legacy `<HERMES_HOME>/characters` store into the install-
                        wide library at `<hermes_root>/shared/characters`. Run once per profile that
                        has one. Drafts keep their directory leaf names and installed characters
                        keep their slugs, so a stored draft id still resolves; a draft carrying no
                        `hermes_home` is stamped with the SOURCE home BEFORE it moves, because
                        afterwards the directory no longer witnesses where it lived. A destination
                        that already holds the leaf or slug is a per-entry REFUSAL, never a merge
                        and never an overwrite, and nothing is deleted — the emptied source tree is
                        left standing as its own tombstone. Idempotent: a second run moves nothing
    status              Full draft state: stage, spec, per-item QA history
    thumb               Write a card-size QA crop of ONE FRAME of one row attempt (chroma keyed out,
                        NEAREST upscale on a flat dark backdrop) and return its path
    base                Set or replace the draft's base identity image
    turnaround          Generate the authored direction references (stage 'turnaround')
    reroll-direction    Re-generate ONE direction reference, with an optional operator note
    approve-direction   Approve direction references; advances to stage 'rows' once all are approved
    rows                Generate the animation row strips (stage 'rows')
    reroll-row          Re-generate ONE row strip, with an optional operator note
    compose             Compose, validate and install the sheet (stage 'rows' → 'composed')
    auto                Drive the whole pipeline in ONE process — turnaround, approve every
                        direction, generate the missing rows, compose and install — printing a
                        receipt line as each stage lands. For an operator's EXPLICIT 'drive it all
                        the way' ask and nothing else: it auto-approves the turnaround, which is the
                        last moment a reference can change. It never overrides a handedness refusal
                        (there is no --accept-handedness here) and it writes the same per-attempt
                        history the interactive verbs write, so `reopen` repair and every QA crop
                        work exactly as they do after a hand-driven run. It resumes rather than
                        restarts: a stage whose work already exists is skipped, with the reason on
                        the summary line, so running it after `reopen` regenerates the missing rows
                        instead of discarding the approved ones. Output is newline-delimited — with
                        --json every line is ONE compact object, and the LAST line is always the
                        summary
    reopen              Reopen a composed draft for fixes (stage 'composed' → 'rows'); the installed
                        sheet stays until the next compose
    add-state           Add ONE animation state to a draft at stage 'rows' (reopen a composed draft
                        first); the new rows start un-generated and no approved row is touched
    sprite              Return an installed character spritesheet payload
    payload-contract    Publish the KEY SET every `characters` READ payload can carry — the cross-
                        repo contract the launcher commits and diffs, so the two sides disagree in a
                        file instead of at runtime. Derived by RUNNING the verbs against a throwaway
                        library in a temp directory (the real library is never touched), never from
                        a hand-written list, so a key a producer grows or drops is in the dump the
                        day it moves. Key PATHS only and never a value, so the dump is byte-stable.
                        Conditional keys are marked with the modes that carry them —
                        `spritesheetBase64` and `sheet` are one slot spelled two ways, which a flat
                        key list cannot express

options:
  -h, --help            show this help message and exit
```

## hermes harness characters start

```text
usage: hermes harness characters start [-h] --concept CONCEPT [--slug SLUG]
                                       [--display-name DISPLAY_NAME] [--style STYLE]
                                       [--states STATES] [--directions DIRECTIONS]
                                       [--base-image BASE_IMAGE] [--authored-by AUTHORED_BY]
                                       [--json]

options:
  -h, --help            show this help message and exit
  --concept CONCEPT     What to draw, e.g. 'a tall knight in green enamel armour'
  --slug SLUG           Install slug; defaults to a slugified display name
  --display-name DISPLAY_NAME
                        Human name; defaults to the concept
  --style STYLE         Art-style hint passed to the prompts
  --states STATES       Animation states as 'idle:6,walk:8[,cheer:5:fixed]'; default = the CHAR8
                        states
  --directions DIRECTIONS
                        Direction scheme: 8 (five authored, three mirrored) or 4
  --base-image BASE_IMAGE
                        Identity-anchor image; copied into the draft
  --authored-by AUTHORED_BY
                        Persona driving this authoring run, recorded as provenance; nothing scopes
                        where the draft lives — the character library is install-wide at
                        <hermes_root>/shared/characters, one directory for every persona and profile
                        — but it is what lets a later reader check a resume is opening under the
                        profile that authored it
  --json
```

## hermes harness characters list

```text
usage: hermes harness characters list [-h] [--json]

options:
  -h, --help  show this help message and exit
  --json
```

## hermes harness characters backfill-home

```text
usage: hermes harness characters backfill-home [-h] [--json]

options:
  -h, --help  show this help message and exit
  --json
```

## hermes harness characters migrate-home

```text
usage: hermes harness characters migrate-home [-h] [--json]

options:
  -h, --help  show this help message and exit
  --json
```

## hermes harness characters status

```text
usage: hermes harness characters status [-h] --draft DRAFT [--json]

options:
  -h, --help     show this help message and exit
  --draft DRAFT  Draft id from `harness characters list`
  --json
```

## hermes harness characters thumb

```text
usage: hermes harness characters thumb [-h] --draft DRAFT (--row ROW | --direction DIRECTION)
                                       [--attempt ATTEMPT] [--frame FRAME] [--scale SCALE]
                                       [--square] [--json]

options:
  -h, --help            show this help message and exit
  --draft DRAFT
  --row ROW             An authored row key, e.g. walk-n
  --direction DIRECTION
                        An authored direction, e.g. e — crops that direction's turnaround REFERENCE
                        instead of a row strip. Mirrored directions are never drawn and are refused.
                        A reference holds one pose, so --frame does not apply to it
  --attempt ATTEMPT     Which attempt to crop, 0-based as in `status --json` history; -1 = latest
  --frame FRAME         Which frame cell of the strip to crop, 0-based (default 0); the crop is the
                        half that removes pixels, so there is always one
  --scale SCALE         NEAREST upscale factor (default 2); refused, never clamped. At or below the
                        default the OUTPUT must fit the console's fixed decode ceiling; above it the
                        crop is a fullscreen-viewer artifact, bounded by the write ceiling and
                        reported as withinConsoleBudget=false. The payload carries a SECOND bound,
                        withinOwnSheet — is the crop no larger than the sheet THIS draft composes —
                        which refuses nothing and is reported at every scale. Draw a crop inline
                        only when BOTH are true; otherwise open it in the viewer
  --square              Pad the finished crop onto a square flat-dark backdrop (side = the longer
                        edge, cell centred) so the console's 1:1 centre-cover hero card shows the
                        WHOLE frame instead of a torso zoom. The filename gains -sq and the payload
                        says square: true. Both budget booleans are weighed on the padded output.
                        Use it for a hero card; take the bare crop for a compare pair, whose panes
                        align on today's shapes
  --json
```

## hermes harness characters base

```text
usage: hermes harness characters base [-h] --draft DRAFT --image IMAGE [--json]

options:
  -h, --help     show this help message and exit
  --draft DRAFT
  --image IMAGE  Path to the identity-anchor image; copied into the draft
  --json
```

## hermes harness characters turnaround

```text
usage: hermes harness characters turnaround [-h] --draft DRAFT [--json]

options:
  -h, --help     show this help message and exit
  --draft DRAFT
  --json
```

## hermes harness characters reroll-direction

```text
usage: hermes harness characters reroll-direction [-h] --draft DRAFT --direction DIRECTION
                                                  [--note NOTE] [--json]

options:
  -h, --help            show this help message and exit
  --draft DRAFT
  --direction DIRECTION
                        An authored direction, e.g. ne (mirrored directions are never generated)
  --note NOTE           Operator note appended to the prompt and stored with the attempt
  --json
```

## hermes harness characters approve-direction

```text
usage: hermes harness characters approve-direction [-h] --draft DRAFT
                                                   (--direction DIRECTION | --all)
                                                   [--attempt ATTEMPT] [--json]

options:
  -h, --help            show this help message and exit
  --draft DRAFT
  --direction DIRECTION
                        Approve this one direction
  --all                 Approve the latest attempt of every authored direction
  --attempt ATTEMPT     Which attempt to approve; -1 = latest (single-direction only)
  --json
```

## hermes harness characters rows

```text
usage: hermes harness characters rows [-h] --draft DRAFT [--only ONLY] [--json]

options:
  -h, --help     show this help message and exit
  --draft DRAFT
  --only ONLY    Restrict the run to these row keys, e.g. 'walk-e,walk-ne'
  --json
```

## hermes harness characters reroll-row

```text
usage: hermes harness characters reroll-row [-h] --draft DRAFT --row ROW [--note NOTE] [--json]

options:
  -h, --help     show this help message and exit
  --draft DRAFT
  --row ROW      An authored row key, e.g. walk-e
  --note NOTE    Operator note appended to the prompt and stored with the attempt
  --json
```

## hermes harness characters compose

```text
usage: hermes harness characters compose [-h] --draft DRAFT [--accept-handedness ACCEPT_HANDEDNESS]
                                         [--json]

options:
  -h, --help            show this help message and exit
  --draft DRAFT
  --accept-handedness ACCEPT_HANDEDNESS
                        Mirrored-art REFUSALS you have looked at and are overriding, spelled
                        '<row>:<basis>' — take the spelling from the refusal. Per row, never
                        blanket. TWO shapes refuse and both can be accepted: a row BOTH passes agree
                        about ('idle-e:rotation+states'), and a row carried by a whole mirrored
                        STATE, where every judged row of that state reads as a mirror
                        ('jumping-e:states') — that one is accepted row by row like any other. A
                        single-basis finding about a single row is a warning and there is nothing to
                        accept. The basis is named on purpose: a bare row key waived a second,
                        independent body of evidence at once. Naming a row that was not flagged is
                        itself refused, and the honoured list rides on the installed manifest,
                        'characters list' and the sprite payload as {row, gain, basis}
  --json
```

## hermes harness characters auto

```text
usage: hermes harness characters auto [-h] --draft DRAFT
                                      [--through {turnaround,approve-direction,rows,compose}]
                                      [--json]

options:
  -h, --help            show this help message and exit
  --draft DRAFT
  --through {turnaround,approve-direction,rows,compose}
                        Last step to run (default: compose, the whole pipeline). Steps before it
                        that the draft already carries are skipped and reported
  --json
```

## hermes harness characters reopen

```text
usage: hermes harness characters reopen [-h] --draft DRAFT [--json]

options:
  -h, --help     show this help message and exit
  --draft DRAFT
  --json
```

## hermes harness characters add-state

```text
usage: hermes harness characters add-state [-h] --draft DRAFT --state STATE [--json]

options:
  -h, --help     show this help message and exit
  --draft DRAFT
  --state STATE  One state in the --states grammar: 'jumping:6' or 'cheer:4:fixed'. Frames 2..8 — a
                 one-frame row is refused HERE rather than several generations later at 'rows'
  --json
```

## hermes harness characters sprite

```text
usage: hermes harness characters sprite [-h] [--no-sheet] [--json] slug

positional arguments:
  slug

options:
  -h, --help  show this help message and exit
  --no-sheet  Metadata only: drop `spritesheetBase64` (468.8 KiB of it on the live 3-state
              character, and the sheet bytes are not read at all) and carry `sheet`, the absolute
              path, in its place. `spritesheetRevision` and every geometry/taxonomy key are
              unchanged, so a consumer that wants framesByRow/states/rows and reads the file itself
              pays kilobytes instead of half a megabyte. The default is byte-identical to what it
              always was
  --json
```

## hermes harness characters payload-contract

```text
usage: hermes harness characters payload-contract [-h] [--json]

options:
  -h, --help  show this help message and exit
  --json
```
