{
  "measured_at": "2026-09-24",
  "base": "f24a1d7f92",
  "files": 215,
  "deleted_lines": 1036,
  "heavy": 4,
  "reasons": [
    {
      "change": "seam Stage 1 (seam/s1-proof)",
      "files": "+2 (459 -> 461)",
      "deleted_lines": "flat (2834)",
      "reason": "hook-pending: hermes_cli/plugins_manifest.py (cli_commands manifest field, +20/-0) and hermes_cli/plugin_compat.py (bundled skip, +4/-0, upstream-lift candidate) join the list; plugins_discovery.py's readers take an optional config (2 def lines). Stage 1 itself returned 2 deleted lines (builtin list, dispatch line). All leave with the upstream lift."
    },
    {
      "change": "S4+S5 landing (seam/s45-landing)",
      "files": "448 = S5's 446 + the 2 S1 hook-pending carries above (S5 was cut before S1 landed and dropped this reason)",
      "deleted_lines": "2816 -> 2800 (S4's hermes_constants.py 16 -> 1, profiles.py 30 -> 29, main.py 193 -> 191 from S1)",
      "reason": "merge of S1 (main), S5 and S4; no merge re-added footprint"
    },
    {
      "change": "lane MECH (seam/mech-apply-2026-09-24)",
      "files": "448 -> 408: 40 files byte-identical to d337b736aa again (16 of them to upstream/main too) - 15 REVERT rows, 9 win-text-encoding tests, 7 website pages, the P6/P7 set (hermes_constants, profile_cmd, subcommands/profile, test_profile_delete_log_handlers), modal/singularity, ci.yaml, kanban_db, test_startup_latency_regressions",
      "deleted_lines": "2800 -> 2612 (profiles.py 29 -> 20, auxiliary_client 42 -> 2, turn_api_call 26 -> 1, the reverts and moves)",
      "reason": "applied the ledger's decided dispositions: REVERTs, adopt-upstream duplicates, the three § Seams defects, the four dropped-PR rulings, movable carries; heavy 12 -> 11"
    },
    {
      "change": "lane S2 (seam/s2-plugin-surface-2026-09-24)",
      "files": "408 -> 404: hermes_cli/skills_hub.py and hermes_cli/subcommands/skills.py (link-external rehomed as `hermes harness skills link-external`), tests/agent/test_system_prompt.py and tests/tools/test_terminal_tool.py byte-identical to d337b736aa again",
      "deleted_lines": "2612 -> 2602 (the four restored files' 8, prompt_builder 11 -> 10, skills_tool 70 -> 69)",
      "reason": "seam Stage 2: the T6b guidance and the skill-confirm sentence ride the eternia-harness plugin's system-prompt section; skill_search is a plugin register_tool; link-external is a harness verb; prompt_builder and skills_tool adopt upstream's skill_matches_environment; heavy 11 -> 10 (tools/skills_tool.py 207 -> 136)"
    },
    {
      "change": "weekly merge upstream/main f24a1d7f92 (seam/upstream-merge-2026-09-24)",
      "files": "404 at base d337b736aa (after lane S2, merged in) -> 373 at base f24a1d7f92: the base moved, rows left the ledger (upstream deleted 9 fork-edited tests in its purge lanes, 5 gpt-6 superseded prod rows, adopted-upstream test carries), 1 row joined (uninstall-section.test.tsx, add/add), the two relocated tests stay (gateway fence), 5 re-check carries stay",
      "deleted_lines": "2602 -> 2404 (model_metadata 17, the purged tests, the adopted test carries); 509 of the 2404 are the two tests/gateway relocations, unchanged since before the merge",
      "reason": "history-preserving merge of 1,663 upstream commits and the supersession pass, then origin/main (lane S2) merged in; no number rose against the pre-merge line; heavy 10 -> 10"
    },
    {
      "change": "lane CARRY (seam/test-carries-out-2026-09-24)",
      "files": "373 -> 355",
      "deleted_lines": "2404 -> 2309",
      "reason": "upstream test files restored to upstream's bytes; the fork's marks ride tests/_downstream/id_markers.py by test id, fork tests ride *_downstream.py siblings with upstream fixtures imported by name. Class 1 host-OS fakes dropped (3 files); class 2 shared-fixture carries moved (8 files leave, test_nous_inference_url_validation leaves; prompt_builder, kanban_db, file_operations, tui_gateway_server keep only the in-place hunks that replace upstream behaviour, named in their ledger rows); class 3 shared-process carries leave (dashboard_auth_gate: subsumed by _web_server_app_is_pristine; file_tools_cwd_resolution: Windows spelling, id-table xfails); class 4 fork-prod carries: test_approval and test_doctor_command_install leave, background_process_notifications / test_doctor / tool_search_multiquery keep only the replacing hunks; class 5 marker carries leave (test_anthropic_adapter, test_commands, test_update_concurrent_quarantine) plus test_gateway_service's dead merge-residue helper"
    },
    {
      "change": "lane ADOPT group 3 (seam/carry-adopt-2026-09-24)",
      "files": "355 -> 351",
      "deleted_lines": "2309 -> 2305",
      "reason": "scripts/desktop-update/{posix.sh,retry-policy.ps1,windows.ps1} and tests/scripts/desktop_update/test_desktop_update_windows_retry_policy.py back to merge-base bytes: the fork history guard (hermes_cli/update_history.py, fork-owned) exits 2, which upstream's updaters already treat as a non-retryable refusal; the test's timeout(45) rides tests/_downstream/id_markers.py"
    },
    {
      "change": "lane ADOPT group 5 (seam/carry-adopt-2026-09-24)",
      "files": "351 -> 350",
      "deleted_lines": "2305 -> 2304",
      "reason": "gateway/run_turn.py back to merge-base bytes: upstream's inline heartbeat text replaces the fork's _format_long_running_heartbeat (deleted from gateway/downstream_extensions.py with its fork test); the /stop hint is dropped by owner ruling"
    },
    {
      "change": "lane ADOPT group 15 (seam/carry-adopt-2026-09-24)",
      "files": "350 -> 349",
      "deleted_lines": "2304 -> 2225",
      "reason": "hermes_cli/main_desktop.py back to merge-base bytes (upstream _stop_desktop_processes_locking_build; hermes_cli/_desktop_processes.py deleted); hermes_cli/profiles.py 14/20 -> 2/0 (upstream psutil loop, agent_runtime/profile_processes.py deleted; the orphan hook stays); tests/hermes_cli/test_profiles.py 308/1 -> 58/0; the hermetic fence patches psutil.process_iter; heavy 9 -> 8"
    },
    {
      "change": "lane ADOPT group 16 (seam/carry-adopt-2026-09-24)",
      "files": "349 -> 348",
      "deleted_lines": "2225 -> 2222",
      "reason": "hermes_cli/doctor_tools.py back to merge-base bytes (upstream agent_browser_runnable call); browser_probe_scope/browser_runnable deleted from agent_runtime/doctor_extensions.py and the agent_browser_runnable_override kwarg from run_doctor; tests/hermes_cli/test_doctor.py 43/5 -> 3/3"
    },
    {
      "change": "lane META (seam/carry-meta-2026-09-24)",
      "files": "348 (after lane ADOPT) -> 346",
      "deleted_lines": "2222 -> 2131",
      "reason": "groups 1+2 of the carry census: .gitignore back to stock (every fork entry's producer now writes a self-ignoring dir; /profiles/ was upstream's own line; .claude/ is the up/gitignore-claude-dir PR); .gitattributes keeps only the root eol rule, ruled carry-permanent; uv.lock back to stock (coverage/pytest-timeout -> requirements-fork-dev.txt, installed by scripts/ensure_fork_dev_deps.py from run_tests.sh), pyproject markers + timeout addopts -> tests/_downstream/conftest_plugin.py, pyproject ruled carry-permanent (agent_runtime include, Stage 7); heavy -1 (uv.lock); AGENTS.md trimmed to a one-line fork pointer (landing-gate bullet -> docs/downstream-development.md), ruled carry-permanent; README.md ruled carry-permanent (licence notice)",
      "heavy": "8 -> 7"
    },
    {
      "change": "lane CARRY2A (seam/test-carries-cli-2026-09-24)",
      "files": "346 -> 313",
      "deleted_lines": "2131 -> 1918",
      "reason": "tests/hermes_cli carries restored to upstream's bytes (the fork's marks ride tests/_downstream/id_markers.py, fork tests ride *_downstream.py siblings). Class A1: 11 files whose fork hunks were dead on this runner — comments, reload removals and sys.modules restores already done by the directory's _sys_modules_identity_is_restored, the AWS scrub tests/conftest.py already does, doctor HERMES_HOME re-spellings the fork's PEP 562 _LIVE_HOME_NAMES already answers, a call re-routed to _bytecode_sweep that main still re-exports. Class A2: 9 files whose hardening upstream's bytes no longer need on this host — hang-bound widenings (active_sessions, relay_shared_metrics), host-OS fakes (managed_uv, update_stale_dashboard, owner 2026-09-24: drop the fakes), stubs for walks that fit the cap (update_autostash, update_fleet_restart_pending, local_quickstart), a Git-on-PATH respelling (kanban_reclaim_claim_lock_guard), and worktree_selfheal's pre-2.32-git hedge plus a second-pass test upstream's TestRepackStampede now owns. Class B/C: 13 files — marks, timeouts and platform treatments by test id (codex_cli_model_picker, lazy_command_exports, gateway_job_teardown_live, relaunch, kanban_boards), a marker-gated conftest fixture (background_agent_turns for the two notification-display witnesses; profiles' psutil-lister tests need nothing since lane ADOPT's fence patches psutil.process_iter), and rule-4 strict xfails naming the fork symbol with the opposite assertion in a sibling (config_read_guard, dashboard_tui_backcompat, dashboard_unified_launch, gateway_windows); mcp_config and profiles' fork tests moved to siblings importing the upstream fixture"
    },
    {
      "change": "lane MOVE-A group 9 (seam/carry-move-a-2026-09-24)",
      "files": "313 -> 304",
      "deleted_lines": "1918 -> 1882",
      "reason": "tool wire briefs: the eternia-harness plugin registers llm_request middleware (tools/downstream_schema.brief_request_tools) that swaps tools[].description by name for the chat/Responses/Anthropic shapes; the registry keeps upstream's text, so the FULL_* copies and dead mirror rows are deleted; tools/{browser,clarify,close_terminal,code_execution,read_terminal,session_search,todo,web}_tool(s).py and tests/tools/test_execute_helper_contract.py are byte-identical to f24a1d7f92; persona scratch sessions adopt upstream's hidden 'tool' source"
    },
    {
      "change": "lane MOVE-A group 12 (seam/carry-move-a-2026-09-24)",
      "files": "304 -> 300",
      "deleted_lines": "1882 -> 1881",
      "reason": "usage ledger: agent_runtime/usage_ledger.py (ContextVar list bound by the persona runner around run_conversation) fed by the eternia-harness post_api_request hook; the Codex app-server path, which never fires that hook, calls record_usage itself; the session_usage_ledger seeds and the turn-result key are gone; agent/usage_pricing.py, turn_usage.py, turn_finalizer.py, auxiliary_client.py byte-identical to f24a1d7f92 (the construction counter moved into its only consumer, the test; agent_runtime/auxiliary_probe.py deleted)"
    },
    {
      "change": "lane MOVE-A group 16 (seam/carry-move-a-2026-09-24)",
      "files": "300 -> 296",
      "deleted_lines": "1881 -> 1876",
      "reason": "harness keys remote_gateway/charsheet/display.busy_text_mode leave DEFAULT_CONFIG (readers default in code) and the comment rewraps revert; hermes_cli/web_server_config.py, status.py, status_auth.py, hermes_state.py byte-identical to f24a1d7f92: the provider report in hermes_cli/harness.py reads status_auth._API_KEYS / status._first_env_value directly and appends its own Anthropic row; RuntimeSessionMixin becomes agent_runtime.session_extensions.delete_compression_lineage(db, ...), called from persona_commands.py"
    },
    {
      "change": "lane MOVE-A group 4 (seam/carry-move-a-2026-09-24)",
      "files": "296 -> 295",
      "deleted_lines": "1876 -> 1876",
      "reason": "the two async-delivery readers moved to agent_runtime/delivery_capability.py, which imports upstream's module-private _SESSION_ASYNC_DELIVERY/_UNSET (recorded in the ledger's private-name table with the two-function PR that retires it); gateway/session_context.py byte-identical to f24a1d7f92; gateway/run_config_loaders.py stays - no profile config seed exists to carry background_process_notifications: result"
    },
    {
      "change": "lane CARRY2B (seam/test-carries-rest-2026-09-24)",
      "files": "295 -> 230",
      "deleted_lines": "1876 -> 1635",
      "reason": "upstream test files outside tests/hermes_cli restored to the merge base's bytes: tests/tools (20 files). Fork tests ride *_downstream.py siblings with upstream fixtures imported by name; POSIX-only reds are id-table skips reading 'POSIX-only; upstream fix = @pytest.mark.linux_only'; fork behaviour that makes an upstream assertion false is a strict xfail row naming the fork symbol; readers the fork moved to load_config_readonly get a by-id read-through to upstream's patched load_config; tests/gateway (11 files + conftest) and tui_gateway's kanban poller: the agent-turn lane is opted in by id (background_agent_turns, now one root fixture for the whole tree), mid-test monkeypatch.undo() tests are skip rows with scoped-context siblings, conftest env-gap skips are id rows; the rest (agent 10, hermes_state 3, plugins 4, providers, cron, e2e conftest, scripts 5, tui_gateway 2, tests/*.py 2) and tests/tools' two remaining rule-4 carries plus test_code_execution_modes (whose T6b tests went red when lane MOVE-A moved the brief to middleware): file-level id rows carry the pytest-timeout budgets and the MCF-66 opt-in with claude_home_is_tmp_path (the credentials gate treats that mark as the redirect); rule-4 carries become strict xfail rows with the fork half in the sibling; heavy 7 -> 6"
    },
    {
      "change": "lane CARRY3 tests/ (seam/carry-pass-2026-09-24)",
      "files": "230 -> 219",
      "deleted_lines": "1635 -> 1072",
      "reason": "tests/{agent,tools,hermes_cli}/conftest.py byte-identical: the fork-only root conftest.py registers each tests/_downstream/<dir>_conftest module under a <dir>/_downstream_conftest.py name when pytest registers that directory's conftest (directory-scoped fixtures, as the star-import gave), and imports tests._downstream.conftest_plugin after tests/conftest.py (its pytest_plugins line leaves); four monkeypatch.undo() carries (kanban_worker_pid_fingerprint, macos_tcc_anchor, plugins, update_zip_two_phase) run upstream's bytes under the scoped_monkeypatch_undo id mark (undo narrowed to the body's own patches); test_cross_profile_kill_refusal and test_stderr_timestamp back at upstream's path (the tests/hermes_cli gateway fence honours spawns_gateway_lookalike; the tests/gateway relocations deleted, -509); kanban_core_functionality and kanban_db byte-identical (the hermes_cli conftest empties kanban_db_dispatch._live_worker_procs after each test); heavy 6 -> 4",
      "heavy": "6 -> 4"
    },
    {
      "change": "lane CARRY3 tools/ (seam/carry-pass-2026-09-24)",
      "files": "flat (219)",
      "deleted_lines": "1072 -> 1058",
      "reason": "tools/process_registry.py 67/50 -> 55/36: the process_manage wire brief rides tools/downstream_schema.BRIEF_DESCRIPTIONS (llm_request middleware), FULL_PROCESS_DESCRIPTION and its tool_full_descriptions mirror are deleted (the registry keeps upstream's text plus one notify line), the #17327 comment block is upstream's again. The five tools/ carry rows are recorded parallels flipped to hook (HOME-PR, G13 widening, on_process_exit + wait-ceiling, G19)"
    },
    {
      "change": "lane CARRY3 hermes_cli/ (seam/carry-pass-2026-09-24)",
      "files": "219 -> 216",
      "deleted_lines": "1058 -> 1044",
      "reason": "hermes_cli/runtime_provider.py and runtime_provider_custom.py byte-identical: the non-persisting selection scope moved into agent_runtime.pool_rotation (a ContextVar PoolRotationMixin._select_with_rotation reads, select_without_persisting_rotation = select under pool_rotation_scope(False)), so the ladder calls upstream's pool.select(); probe_runtime_provider opens the scope around resolve_runtime_provider instead of a persist_pool_rotation kwarg (agent/credential_pool.py 10/18 -> 9/18: _select_unlocked's persist_rotation parameter is gone). hermes_cli/web_routers/profiles.py byte-identical: the promote route is the eternia-harness dashboard plugin API route /api/plugins/eternia-harness/profiles/{name}/promote (no caller outside the fork's own test). hermes_cli/auth.py 23/2 -> 13/1: the auth_extensions tail re-export (callers import it), the duplicate hermes_cli.config top import and the hermes_constants re-wrap leave"
    },
    {
      "change": "lane REDS3 t6b briefs (fork/main-reds-2026-09-24)",
      "files": "flat (216)",
      "deleted_lines": "1044 -> 1043",
      "reason": "tools/vision_tools.py registers upstream's VISION_ANALYZE_SCHEMA again: upstream's own diet (#97339) made its text shorter than the fork brief, so the brief and its brief_schema wrap leave; read_file's brief joins BRIEF_DESCRIPTIONS (llm_request middleware) and its stale tool_full_descriptions mirror row is deleted"
    },
    {
      "change": "lane CLOSE (seam/close-2026-09-24)",
      "files": "216 -> 215",
      "deleted_lines": "1043 -> 1036",
      "reason": "owner ruling 2026-09-24: adopt upstream's `concise` background_process_notifications default. gateway/run_config_loaders.py byte-identical to f24a1d7f92 (-4); cli-config.yaml.example and hermes_cli/config_defaults.py lose the concise->result line (-2, -1); only the BG-PR background_process_agent_turns lines remain in them"
    }
  ]
}
